'use strict';
/* ============================================================
   МИНИМУМ — трекер ежедневного минимума
   Без зависимостей. Данные — localStorage, схема версионируется.
   ============================================================ */

/* ── ТЕКСТЫ РАЗДЕЛА «СИСТЕМА» ────────────────────────────────
   Правятся только здесь. kind: 'leads' — абзацы с жирным
   зачином, 'rules' — нумерованный канон, 'note' — врезка. */
const SYSTEM_TEXTS = [
  {
    kind: 'leads',
    title: 'Архитектура — три решения',
    items: [
      { lead: 'Минимум отделён от нормы.', text: 'Минимум — неснижаемая планка, которая выполняется даже в худший день. Норма — обычный объём. Приложение видит только минимум.' },
      { lead: 'Единица самооценки — неделя.', text: 'День фиксируется, но не оценивается; картина складывается раз в неделю на разборе.' },
      { lead: 'Инициатива у человека.', text: 'Система не напоминает, не повышает планку сама и ничего не решает за владельца.' }
    ]
  },
  {
    kind: 'rules',
    title: 'Пять правил',
    items: [
      'Минимум выполняется даже в худший день.',
      'Не пропускай дважды: пропуск — событие, два подряд — начало новой привычки.',
      // Правило держится механикой в одном месте из двух: повышение планки
      // разбор предлагает одному пункту за раз. Второе — «одно изменение» на
      // неделю — свободное поле, и держит его сам владелец. Прежде здесь
      // называлась и лестница («лестница одна»); она снята задачей 28.D, и
      // текст не вправе обещать механику, которой в приложении нет
      // (задача 26, п. 7.2: «Система» описывает то, что приложение делает).
      'Одно изменение за раз: повышение планки одно за разбор; «одно изменение» недели держит сам владелец.',
      'Самооценка раз в неделю, не ежедневно.',
      'Планка повышается только вручную и только после устойчивых трёх недель.'
    ]
  },
  {
    // Прежний текст перечислял Тело · Движение · Сон · Развитие — набор,
    // которого у владельца нет: посев заводит Утро · Подряд · Движение
    // (задача 26, п. 7.3). Названо стартовым, чтобы не разойтись снова:
    // блоки переименовываются и убираются.
    //
    // Глагол стирания здесь больше не пишется («Расписание 1/3», п. 4.6): у
    // блока появились дни, от которых зависят прошлые числа его действий, и
    // по словарю конституции блок не стирается, а уводится из виду. Прежний
    // текст обещал операцию, которой в приложении нет, а «Система»
    // описывает то, что приложение делает. Корень слова в литерале — вместе
    // с этим комментарием — сторожит тест Р1/4.6.
    kind: 'leads',
    title: 'Блоки',
    items: [
      { lead: 'Блок — связка пунктов.', text: 'На дневном экране они соединены линией и идут подряд. Линия показывает принадлежность, а не очередь: порядок не принудителен. У блока есть дни недели; действие появляется в дни своего блока, а свои дни действия могут их только сузить.' },
      { lead: 'Стартовая программа:', text: 'Утро — умыться, душ. Подряд — подтягивания с отжиманиями, английский, развитие. Движение — пешком. Тренировка, телефон вне кровати и отбой идут без блока.' },
      { lead: 'Всё правится.', text: 'Блоки заводятся, переименовываются и убираются в Настройках → Расписание; убранный блок уводит из виду свои действия и привычки, отметки остаются.' }
    ]
  },
  {
    kind: 'note',
    title: 'Заметка',
    text: 'Привычка держится минимум 2–3 недели, пока не покажется лёгкой. Начинай всегда с маленьких шагов: +0,01% всё равно лучше, чем ничего.'
  }
];

/* ── СТРОКА ДНЯ (задача 28.E/B) ───────────────────────────────
   Единственное исключение из правила «в списке пунктов и на дневных
   экранах — только слова владельца». Границы жёсткие и записаны в
   CLAUDE.md: одна строка, один экран, закрытый набор в коде, выбор —
   чистая функция от ключа логического дня.

   Плоский массив строк без источников: форма данных держит запрет на
   цитаты сама — приписать строке автора здесь просто негде.

   ДЛИНА НАБОРА — 91, и это не опечатка отчёта: промпт задачи называл
   90, но перечислял 91 строку, а редактировать набор задача запрещает
   («возьми дословно… не подгоняй»). 91 = 7 × 13 делится на семь, и
   простой остаток от деления числа дней намертво привязал бы каждую
   строку к одному дню недели — ровно то, от чего сторожил счёт. Набор
   оставлен как есть, привязку снимает выбор (см. dayLine). */
const DAY_LINES = [
  'Минимум выполняется даже в худший день.',
  'Не спеши — доверься накопительному эффекту.',
  'Плюс один процент сегодня — это не мало, это направление.',
  'Малое, повторённое, становится опорой.',
  'Ровный шаг доносит дальше быстрого.',
  'Ноль целых одна сотая процента всё равно больше нуля.',
  'Привычка держится две-три недели, пока не покажется лёгкой.',
  'Начинай с маленьких шагов: они единственные, что не срываются.',
  'Планка опускается до того уровня, на котором ты её выполнишь.',
  'Сегодня достаточно сделать минимум.',
  'Практика — это то, что остаётся, когда настроение ушло.',
  'День без сил тоже считается днём.',
  'Мелкий шаг вперёд отменяет большой шаг назад.',
  'Постоянство складывается медленнее, чем хочется, и держится дольше, чем кажется.',
  'Тот, кто делает мало и каждый день, обгоняет того, кто делает много и редко.',
  'Цепь рвётся не в трудный день, а в тот, когда решил, что можно.',
  'Не пропускай дважды.',
  'Один пропуск — случайность. Два — новая привычка.',
  'Возвращаться важнее, чем не уходить.',
  'Ты не начинаешь заново — ты продолжаешь.',
  'Сложное складывается из простого, повторённого много раз.',
  'Каждый раз, когда делаешь, ты голосуешь за того, кем становишься.',
  'Сначала действие, потом настроение.',
  'Не жди подходящего дня — подходящих не бывает.',
  'Уменьшай до тех пор, пока отговорки не кончатся.',
  'Две минуты — уже практика.',
  'Действие меньше сопротивления всегда побеждает.',
  'Убери одно препятствие — и делать станет легче, чем не делать.',
  'Сила воли кончается, устройство жизни остаётся.',
  'Проще изменить обстановку, чем себя.',
  'Дисциплина — это когда решение принято заранее.',
  'Ты не поднимешься до цели, ты опустишься до своей системы.',
  'Система важнее цели.',
  'Результат — побочный эффект практики.',
  'Считай не итог, а количество повторений.',
  'Числа растут потом. Сначала растёт привычка.',
  'Терпение — это скорость, которую не видно.',
  'Лучше меньше, да чаще.',
  'Тише едешь — дальше будешь.',
  'Капля камень точит.',
  'Вода камень точит не силой, а постоянством.',
  'Дорога в тысячу ли начинается с одного шага.',
  'Кто идёт медленно, идёт далеко.',
  'Не бойся медленно идти, бойся остановиться.',
  'Терпение и труд всё перетрут.',
  'Мало-помалу птичка гнездо вьёт.',
  'По зёрнышку — ворох, по капельке — море.',
  'Пока дышу, надеюсь.',
  'Начало — половина дела.',
  'Хорошее начало — половина успеха.',
  'Что посеешь, то и пожнёшь.',
  'Всякое дело начинается с малого.',
  'Дело мастера боится.',
  'Не откладывай на завтра то, что можно сделать сегодня.',
  'Всякому овощу своё время.',
  'Семь раз отмерь, один раз отрежь.',
  'Утро вечера мудренее.',
  'Кто рано встаёт, тому Бог подаёт.',
  'Без труда не выловишь и рыбку из пруда.',
  'Глаза боятся, а руки делают.',
  'Лиха беда начало.',
  'Куй железо, пока горячо.',
  'Один в поле не воин, но один шаг в день — уже путь.',
  'Тише сам, крепче дело.',
  'Сегодня сделанное завтра не сделается само.',
  'Долгий путь одолевает идущий.',
  'Идущий дорогу осилит.',
  'Постоянство лучше порыва.',
  'Человек привыкает — и это его главная сила.',
  'Мы есть то, что делаем изо дня в день.',
  'Совершенство — не действие, а привычка.',
  'Мы становимся тем, что повторяем.',
  'Привычка — вторая натура.',
  'Что делаешь часто, то делаешь легко.',
  'Трудное становится привычным, привычное — лёгким, лёгкое — приятным.',
  'Нет ничего сильнее привычки.',
  'Привычка сильнее принуждения.',
  'Кто хочет — ищет способ, кто не хочет — причину.',
  'Начать всегда труднее, чем продолжить.',
  'Труден только первый шаг.',
  'Делай, что должен, и будь что будет.',
  'Спеши медленно.',
  'Пока откладываешь, жизнь проходит.',
  'Сделанное сегодня освобождает завтра.',
  'Порядок в малом рождает порядок в большом.',
  'Кто верен в малом, тот верен и в большом.',
  'Довольно для каждого дня своей заботы.',
  'Сегодня — единственный день, в котором можно что-то сделать.',
  'Лучшее время начать было вчера. Второе лучшее — сейчас.',
  'Не важно, как медленно ты идёшь, пока ты не останавливаешься.',
  'Маленькие дела, сделанные, лучше больших, задуманных.',
];

/* Якорь — неподвижная дата в коде, а не начало отсчёта владельца:
   calendarSince он правит сам, и строка дня скакала бы от правки
   настройки. Дата произвольна и значения не несёт — важно лишь то,
   что она неподвижна. */
const DAY_LINE_EPOCH = '2026-01-01';

/* Строка дня: чистая функция от ключа логического дня и только от него.
   Ни days{}, ни items[], ни reviews[], ни daysInSystem она не читает —
   от тапа по кругу не меняется, потому что принадлежит дате, а не
   результату; а число о практике на «Сегодня» запрещено конституцией.

   Индекс — не голый остаток. К номеру дня прибавляется номер пройденного
   круга: длина набора кратна семи, и без сдвига строка i выпадала бы
   всегда на один и тот же день недели. Сдвиг на круг поворачивает набор
   на одну позицию каждые 91 день, поэтому за семь кругов (637 дней)
   каждая строка обходит все дни недели. Внутри круга порядок прежний и
   каждая строка встречается ровно один раз. */
function dayLine(key) {
  const n = diffDays(key, DAY_LINE_EPOCH);
  const len = DAY_LINES.length;
  const i = ((n + Math.floor(n / len)) % len + len) % len;
  return DAY_LINES[i];
}

/* ── Константы времени (задача 23) ───────────────────────────
   Каждая объявлена там, где работает, но проходит через timing():
   первый аргумент — имя, второй — значение по умолчанию, оно же и есть
   рантайм приложения. Ни одна не становится настройкой владельца и в
   store не попадает.

   Тест подменяет константу ДО загрузки app.js, положив объект в
   globalThis.MINIMUM_TIMING ({ MOTION_MS: 20, … }); правка самого
   app.js для этого не нужна. Значения читаются один раз при загрузке —
   после неё поведение неизменно. Нечисловое, бесконечное и
   отрицательное значение игнорируется: подмена не должна уметь то,
   чего не умеет рантайм.

   Блок стоит перед «Хранилищем», а не внутри: timing() зовут разделы
   от зеркала до перетаскивания, и таблицы должны быть инициализированы
   раньше первого вызова (const, не var — TDZ обязывает). */
const TIMING_DEFAULTS = {}; // имя → значение по умолчанию (рантайм)
const TIMING = {};          // имя → значение, с которым работает эта загрузка

function timing(name, def) {
  TIMING_DEFAULTS[name] = def;
  const o = (typeof globalThis !== 'undefined' && globalThis.MINIMUM_TIMING) || null;
  const v = o ? o[name] : undefined;
  TIMING[name] = (typeof v === 'number' && isFinite(v) && v >= 0) ? v : def;
  return TIMING[name];
}

/* ── Хранилище ─────────────────────────────────────────────── */

const NS = 'minimum:data';
const SCHEMA_VERSION = 20;

/* Основной режим (задача Р2): запись, которая есть всегда. До первого
   отрезка журнала режимов и при пустом журнале действует он — ровно
   прежнее поведение, выраженное данными. id постоянен и не переводится:
   переименовать режим владелец может, сменить id — нет. */
const MAIN_MODE = 'main';
const MAIN_MODE_NAME = 'Основной';

let store = null;
let saveFailed = false; // хранилище недоступно — постоянный баннер над экраном
const MAX_TIME = 8640000000000000; // предел представимой даты: за ним new Date() — Invalid Date

const uid = () => (typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function')
  ? crypto.randomUUID()
  : Math.random().toString(36).slice(2, 10) + Date.now().toString(36).slice(-4);

/* Блок по умолчанию для стартовых пунктов (и для миграции v1-данных) */
const DEFAULT_GROUPS = {
  'Умыться': 'Тело',
  'Принять душ': 'Тело',
  'Подтягивания + отжимания': 'Тело',
  'Пешком': 'Движение',
  'Телефон вне кровати': 'Сон',
  'Развитие': 'Развитие',
  'Тренировка': 'Тело'
};
const TRAIN_NOTE = 'Полноценная тренировка, 40–50 минут';

/* Понедельник дня k, если k — понедельник, иначе ближайший следующий */
function nextCalendarMonday(k) {
  const mon = weekStartOf(k);
  return mon === k ? k : addDays(mon, 7);
}

/* Стартовая программа привычек — тот же посев идёт миграцией v5 */
function seedHabits(today) {
  const habit = (name) => ({
    id: uid(), name, value: null, unit: '', type: 'daily', area: 'habit', normPerWeek: 7,
    goal: null, note: '', group: '', removedAt: null, addedAt: today, schedule: [{ from: today, mask: WEEK_ALL }], at: '', raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null, history: [],
    formula: null, ladder: null, ladderLog: []
  });
  return [
    { id: uid(), name: 'Отбой', value: null, unit: '', type: 'param', area: 'habit',
      pkind: 'time', pvalue: 0, pstep: -15, goal: null, note: '', group: '',
      removedAt: null, addedAt: today, at: '', raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null, history: [{ date: today, value: 0 }],
      formula: null, ladder: null, ladderLog: [] },
    habit('Перестать грызть ногти'),
    habit('Ловить импульс трат → алгоритм')
  ];
}

/* ── Посев программы (задача 17) ───────────────────────────────
   Одноразовый и идемпотентный: migrate заводит программу владельца
   только на пустом store (items[] пуст) и только пока не стоит флаг
   settings.seed17. Непустые данные посев не трогает и флага не ставит.

   Чистка ставит флаг сама (emptyStore): «Начать с чистого листа»
   обязано оставаться чистым и после перезапуска — иначе следующий
   старт прогнал бы стёртый store через migrate и вернул программу
   (инвариант 18, «ни одного стартового пункта»). */

const SEED_GROUPS = ['Утро', 'Подряд', 'Движение'];

/* [название, значение, единица, блок, тип, цель, подпись, добавка] */
const SEED_ITEMS = [
  ['Умыться', null, '', 'Утро', 'daily', null, ''],
  ['Принять душ', null, '', 'Утро', 'daily', null, ''],
  ['Подтягивания + отжимания', 5, 'повт.', 'Подряд', 'daily', null, ''],
  ['Английский', 5, 'мин', 'Подряд', 'daily', null, ''],
  ['Развитие', 10, 'мин', 'Подряд', 'daily', null, ''],
  ['Пешком', 500, 'м', 'Движение', 'daily', null, ''],
  ['Тренировка', null, '', '', 'weekly', 3, TRAIN_NOTE],
  ['Телефон вне кровати', null, '', '', 'daily', null, '', { area: 'habit', normPerWeek: 7 }],
  ['Отбой', null, '', '', 'param', null, '',
    { area: 'habit', pkind: 'time', pvalue: 0, pstep: -15 }]
];

/* Пункты программы. Единственная фабрика на два пути — посев в migrate
   и defaultStore: наборы обязаны совпадать, поэтому собираются здесь. */
function programItems(today) {
  return SEED_ITEMS.map(([name, value, unit, group, type, goal, note, extra]) => {
    const it = {
      id: uid(), name, value, unit, type, area: 'min', goal, note, group,
      removedAt: null, addedAt: today, at: '', raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null,
      // числовому пункту — стартовая запись истории планки (инвариант 5)
      history: (typeof value === 'number') ? [{ date: today, value }] : [],
      formula: null, ladder: null, ladderLog: []
    };
    Object.assign(it, extra);
    // порог параметра — такая же планка: своя стартовая запись истории
    if (it.type === 'param') it.history = [{ date: today, value: it.pvalue }];
    // расписание — только у ежедневных (инвариант 10); defaultStore не проходит
    // через migrate, поэтому каноническую форму задаёт эта фабрика (инвариант 19)
    if (it.type === 'daily') it.schedule = [{ from: today, mask: WEEK_ALL }];
    // журнал принадлежности блоку — только у действия (задача «Расписание 1/3»):
    // пустой значит «item.group на всю жизнь», и фабрика ставит его явно —
    // «поля нет» каноническим не считается, а второго прогона migrate у
    // defaultStore нет
    if (it.type === 'daily' && it.area === 'min') it.groupLog = [];
    // режим — тоже только у действия (задача Р2): программа принадлежит
    // основному режиму; привычки, параметры и счётчик глобальны
    if (it.type === 'daily' && it.area === 'min') it.mode = MAIN_MODE;
    return it;
  });
}

/* Блоки программы — в канонической форме v20: без подписи, дни «все семь»
   (пустой список отрезков), не убраны, в основном режиме. Фабрика одна на
   посев и defaultStore. */
const programGroups = () => SEED_GROUPS.map(name => ({ name, caption: '', days: [], removedAt: null, mode: MAIN_MODE }));

/* Список режимов свежего store — один основной (задача Р2) */
const programModes = () => [{ id: MAIN_MODE, name: MAIN_MODE_NAME, removedAt: null }];

/* День посева — по нему подсказка «одна новая привычка за раз» отличает
   программу от пунктов владельца (задача 22, п. 7.2). Пометить посевные
   пункты полем нельзя, не меняя схему, но у всех девяти один addedAt:
   день первого запуска, самый ранний в store.

   Одиночный пункт с минимальным addedAt посевным не считается никогда:
   после чистки store пуст, а settings.seed17 стоит намеренно (инвариант
   18) — первый же пункт владельца оказался бы самым ранним и выпал бы из
   счёта, и подсказка не показалась бы при добавлении второй привычки.
   Посев кладёт девять пунктов одной датой, владелец добавляет по одному —
   количество в группе и есть различитель (решение архитектора). */
function seedDayKey() {
  if (store.settings.seed17 !== true || !store.items.length) return null;
  let min = null, n = 0;
  for (const it of store.items) {
    if (min === null || it.addedAt < min) { min = it.addedAt; n = 1; }
    else if (it.addedAt === min) n++;
  }
  return n > 1 ? min : null;
}

/* Позже всех заведённый пункт владельца; посев в счёт не идёт */
function ownerNewestItem() {
  const seedDay = seedDayKey();
  let newest = null;
  for (const it of store.items) {
    if (seedDay !== null && it.addedAt === seedDay) continue;
    if (!newest || it.addedAt > newest.addedAt) newest = it;
  }
  return newest;
}

function seedProgram(s, today) {
  s.groups = programGroups();
  s.items = programItems(today);
  s.settings.seed17 = true;
  s.settings.habitSeeded = true; // привычки программы уже здесь — посев v5 не нужен
}

function defaultStore() {
  const today = dateKeyShift(new Date(), 4);
  return {
    schemaVersion: SCHEMA_VERSION,
    items: programItems(today),
    // порядок блоков на экранах (инвариант 13); блок из двух и более
    // активных пунктов сам по себе рисует линию — отдельного признака нет
    groups: programGroups(),
    // режимы расписания (задача Р2): список и журнал активного режима
    // отрезками. Пустой журнал — основной режим на всю жизнь
    modes: programModes(),
    modeLog: [],
    days: {},          // "YYYY-MM-DD" -> { itemId: true }
    weekLog: [],       // инкременты недельных счётчиков текущей календарной недели
    reviews: [],       // закрытые недели
    pendingRaises: [], // принятые повышения, ещё не записанные в разбор
    pendingLowers: [], // принятые понижения, ещё не записанные в разбор
    exercises: [],     // упражнения тренировки: рабочая нагрузка и её история
    sessions: [],      // записанные тренировки: день, значения упражнений, заметка
    // заметки и выписки владельца. Экран снят задачей 28.C, но поле живёт:
    // данные никуда не делись, идут в экспорт и проходят нормализацию
    notes: [],
    paramDecided: {},  // itemId -> {week, from, to|null}: решения по параметрам, привязанные к разбираемой неделе
    draftOneChange: '',
    weekStart: today,  // историческая отсечка скользящей эпохи
    settings: {
      dayBoundary: 4,
      dayThreshold: 0.8,
      exportedAt: null,
      calendarSince: nextCalendarMonday(today),
      habitSeeded: true,
      seed17: true     // программа уже здесь: migrate не станет сеять её повторно
    }
  };
}

/* Числовое поле из внешних данных: число или числовая строка
   (запятая как точка); всё остальное — fallback. */
function numOr(v, fallback) {
  if (typeof v === 'number') return isFinite(v) ? v : fallback;
  if (typeof v === 'string' && v.trim() !== '') {
    const n = Number(v.replace(',', '.'));
    return isFinite(n) ? n : fallback;
  }
  return fallback;
}

/* ── Формула и лестница: нормализация полей ────────────────────
   Механики сняты задачей 28.D — экрана, форм и решений больше нет.
   НОРМАЛИЗАЦИЯ ОСТАЁТСЯ ЦЕЛИКОМ, и это решение, а не остаток: поля
   formula, ladder и ladderLog живут в store владельца, уходят в экспорт
   и в зеркало, а снятие самих полей — отдельная задача. Пока их
   нормализует migrate, ни один байт не теряется — ни при обновлении,
   ни при импорте. Снятие самих полей — отдельное решение владельца
   (инвариант 16).

   Убрать normLadder, не убрав строку `it.ladder = … normLadder(…)` в
   цикле по пунктам, — ReferenceError при первой же миграции. Функции и
   строки цикла снимаются только вместе с полями. */

/* Семь полей формулы; на логику не влияют — только текст владельца */
const FORMULA_KEYS = ['anchor', 'when', 'pair', 'identity', 'twoMin', 'friction', 'proof'];

/* Формула из внешних данных: объект из семи строк либо null.
   Пустая (все поля пусты после trim) — это null, а не объект пустышек.

   v14 → v15 (задача 20): формула несёт режим. Отдельного шага миграции не
   нужно — mode достраивается этой же безусловной нормализацией, как в своё
   время normPerWeek (v5→v6) и сама formula (v6→v7); шаг аддитивен и
   идемпотентен, days{} и reviews[] не трогает. */
/* v17 → v18 (задача 29/B): расписание пункта — список отрезков.
   Нормализация приводит его к канонической форме и НИ ОДНОГО дня не
   двигает: пункту без расписания ставится один отрезок «все семь дней»
   с from = addedAt, то есть ровно прежнее поведение, выраженное данными.

   Канон: отрезки по возрастанию from, дубли одного дня схлопнуты (побеждает
   последний — так же, как в setSchedule), подряд идущие одинаковые маски
   схлопнуты (правило recordBar, инвариант 5), первый отрезок не раньше
   addedAt. Пустая маска — не расписание: пункт, не существующий ни в один
   день недели, не отличим от убранного, а убирать умеет «Убрать».
   Шаг идемпотентен: повторный прогон канонической формы её не меняет. */
function normSchedule(list, addedAt) {
  const src = (Array.isArray(list) ? list : [])
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) && isDayKey(x.from) && isMask(x.mask))
    .filter(x => maskDays(x.mask) > 0)
    .map(x => ({ from: x.from < addedAt ? addedAt : x.from, mask: x.mask }))
    .sort((a, b) => (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
  const out = [];
  for (const seg of src) {
    if (out.length && out[out.length - 1].from === seg.from) out.pop(); // один день — один отрезок
    if (out.length && out[out.length - 1].mask === seg.mask) continue;  // повтор маски не пишется
    out.push(seg);
  }
  // отрезка нет вовсе — «все семь» с первого дня жизни: прежнее поведение
  if (!out.length || out[0].from !== addedAt) out.unshift({ from: addedAt, mask: WEEK_ALL });
  return (out.length > 1 && out[0].mask === out[1].mask) ? out.slice(1) : out;
}

/* v18 → v19 («Расписание 1/3»): дни блока — те же отрезки, что у пункта,
   но без якоря на дне заведения: у блока нет addedAt, и «до первого
   отрезка» для него значит «все семь» — начальный период без нижней
   границы. Поэтому ВЕДУЩИЕ отрезки «все семь» отбрасываются: они ничего
   не говорят, а второй способ сказать одно и то же форма данных не заводит.
   Прочий канон — как у normSchedule: мусор и пустая маска роняются, один
   день — один отрезок (побеждает последний), подряд одинаковые маски
   схлопнуты. Повторный прогон канонической формы её не меняет. */
function normBlockDays(list) {
  const src = (Array.isArray(list) ? list : [])
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) && isDayKey(x.from) && isMask(x.mask))
    .filter(x => maskDays(x.mask) > 0)
    .map(x => ({ from: x.from, mask: x.mask }))
    .sort((a, b) => (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
  const out = [];
  for (const seg of src) {
    if (out.length && out[out.length - 1].from === seg.from) out.pop(); // один день — один отрезок
    if (out.length && out[out.length - 1].mask === seg.mask) continue;  // повтор маски не пишется
    out.push(seg);
  }
  // после схлопывания ведущий «все семь» может быть только один
  if (out.length && out[0].mask === WEEK_ALL) out.shift();
  return out;
}

/* Журнал принадлежности действия блоку: [{from, group}] по возрастанию from.
   Пункт хранит блок ИМЕНЕМ, и перенос «Утро (ежедневно)» → «Школа (будни)»
   без истории задним числом выбросил бы пункт из всех прошлых выходных —
   ровно класс дефекта, который задача 28.E/A лечила у active. Пустой
   журнал — «item.group на всю жизнь».

   Порядок шагов обязателен — от него зависит идемпотентность:
   (1) валидные записи, from не раньше addedAt, по возрастанию;
   (2) последняя запись расходится с item.group — ДОПИСАТЬ нынешнее
       состояние не раньше сегодняшнего дня (прошлое по записям не
       переписывается; импорт мог принести рассогласование). Дописывается
       ДО дедупликации: при совпадении дня нынешнее побеждает;
   (3) один день — одна запись (побеждает последняя);
   (4) подряд одинаковые блоки схлопнуты;
   (5) первая запись — с дня заведения;
   (6) одно состояние на всю жизнь — это не история: [].
   Имя берётся через groupNameOf, а не сырым item.group: пробелы вокруг
   имени второй прогон срезал бы шагом (1), и канон разошёлся бы с собой. */
function normGroupLog(list, item, today) {
  const addedAt = item.addedAt;
  const log = (Array.isArray(list) ? list : [])
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) && isDayKey(x.from) && typeof x.group === 'string')
    .map(x => ({ from: x.from < addedAt ? addedAt : x.from, group: x.group.trim() }))
    .sort((a, b) => (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
  const cur = groupNameOf(item);
  if (log.length && log[log.length - 1].group !== cur) {
    const lastFrom = log[log.length - 1].from;
    log.push({ from: today > lastFrom ? today : lastFrom, group: cur });
  }
  const out = [];
  for (const e of log) {
    if (out.length && out[out.length - 1].from === e.from) out.pop();   // один день — одна запись
    if (out.length && out[out.length - 1].group === e.group) continue;  // тот же блок продолжается
    out.push(e);
  }
  if (out.length && out[0].from > addedAt) out[0].from = addedAt;
  return out.length > 1 ? out : [];
}

/* v19 → v20 (задача Р2): список режимов из внешних данных.
   Канон: объекты {id, name, removedAt}; id — непустая строка, имя — непустое
   после trim, removedAt — ключ дня либо null. Основной режим есть всегда:
   нет в данных — дописывается первым, есть — остаётся на своём месте.
   Дубль id отбрасывается (ссылки и так ведут на первый); дубль ИМЕНИ —
   тоже, но ссылки на его id переводятся на режим с тем же именем, а не в
   основной: блоки и действия уходят туда, где владелец их и видел. Имя
   основного режима занимается первым, раньше прочих. Прочее мусорное —
   отбрасывается, как блок без имени; ссылки на него ведут в основной.
   → { modes, resolve }: resolve(id) — id режима, в котором запись окажется.
   Повторный прогон канона ничего не меняет. */
function normModes(list) {
  const valid = (Array.isArray(list) ? list : [])
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) &&
      typeof x.id === 'string' && x.id && typeof x.name === 'string' && x.name.trim())
    .map(x => ({ id: x.id, name: x.name.trim(), removedAt: isDayKey(x.removedAt) ? x.removedAt : null }));
  let main = valid.find(x => x.id === MAIN_MODE);
  if (!main) { main = { id: MAIN_MODE, name: MAIN_MODE_NAME, removedAt: null }; valid.unshift(main); }
  const ids = new Set([MAIN_MODE]);
  const names = new Map([[main.name, MAIN_MODE]]);
  const remap = new Map();
  const modes = [];
  for (const x of valid) {
    if (x === main) { modes.push(x); continue; }
    if (ids.has(x.id)) continue;
    if (names.has(x.name)) { if (!remap.has(x.id)) remap.set(x.id, names.get(x.name)); continue; }
    ids.add(x.id);
    names.set(x.name, x.id);
    modes.push(x);
  }
  // known — то же, но неизвестный id остаётся как есть: отрезку журнала с
  // неизвестным режимом место не в основном, а нигде (normModeLog его уронит)
  const known = id => (typeof id === 'string' && ids.has(id)) ? id : (remap.has(id) ? remap.get(id) : id);
  const resolve = id => {
    const k = known(id);
    return typeof k === 'string' && ids.has(k) ? k : MAIN_MODE;
  };
  return { modes, resolve, known };
}

/* Журнал активного режима: [{from, mode}] по возрастанию from — те же
   отрезки, что расписание пункта и дни блока, и те же правила канона:
   мусор и неизвестный режим роняются, один день — один отрезок (побеждает
   последний), подряд одинаковые схлопнуты. Ведущие отрезки основного
   режима отбрасываются: до первого отрезка и так действует основной, а
   второго способа сказать одно и то же форма данных не заводит.
   modes — список режимов или их id. Повторный прогон канона его не меняет. */
function normModeLog(list, modes) {
  const known = new Set((Array.isArray(modes) ? modes : [])
    .map(m => (typeof m === 'string' ? m : (m && typeof m === 'object' ? m.id : null))));
  const src = (Array.isArray(list) ? list : [])
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) && isDayKey(x.from) &&
      typeof x.mode === 'string' && known.has(x.mode))
    .map(x => ({ from: x.from, mode: x.mode }))
    .sort((a, b) => (a.from < b.from ? -1 : a.from > b.from ? 1 : 0));
  const out = [];
  for (const seg of src) {
    if (out.length && out[out.length - 1].from === seg.from) out.pop(); // один день — один отрезок
    if (out.length && out[out.length - 1].mode === seg.mode) continue;  // тот же режим продолжается
    if (!out.length && seg.mode === MAIN_MODE) continue;                // до первого и так основной
    out.push(seg);
  }
  return out;
}

/* Время у пункта (задача 29/B) — ПОДПИСЬ, не механика: ни уведомлений, ни
   просрочки, ни сортировки, ни одного расчёта. Строка «ЧЧ:ММ» либо пустая. */
const TIME_RE = /^([01]\d|2[0-3]):[0-5]\d$/;
const normTime = v => (typeof v === 'string' && TIME_RE.test(v.trim()) ? v.trim() : '');

function normFormula(f) {
  if (!f || typeof f !== 'object' || Array.isArray(f)) return null;
  const out = {};
  let any = false;
  for (const k of FORMULA_KEYS) {
    const v = typeof f[k] === 'string' ? f[k].trim() : '';
    out[k] = v;
    if (v) any = true;
  }
  // режим описывает, КАК читать те же семь полей, и сам формулы не создаёт:
  // пустая формула с одним лишь mode остаётся null. Неизвестное — 'build'.
  out.mode = f.mode === 'break' ? 'break' : 'build';
  return any ? out : null;
}

/* Лестница из внешних данных: непустой список ступеней, индекс в границах,
   неделя последнего шага — понедельник или null. Без ступеней лестницы нет. */
function normLadder(l, today) {
  if (!l || typeof l !== 'object' || Array.isArray(l)) return null;
  const steps = Array.isArray(l.steps)
    ? l.steps.filter(s => typeof s === 'string').map(s => s.trim()).filter(Boolean)
    : [];
  if (!steps.length) return null;
  let step = Math.round(numOr(l.step, 0));
  if (!(step > 0)) step = 0;
  if (step > steps.length - 1) step = steps.length - 1;
  // рукотворный не-понедельник приводится к своему понедельнику, а не
  // обнуляется: иначе правка данных открывала бы лишний шаг на неделе
  const wk = l.steppedWeek;
  return {
    steps,
    step,
    steppedWeek: isDayKey(wk) ? weekStartOf(wk) : null,
    startedAt: isDayKey(l.startedAt) ? l.startedAt : today,
    // v15 → v16 (задача 21): привычка закрыта владельцем. Отдельного шага
    // миграции не нужно — done достраивается этой же безусловной
    // нормализацией, как mode в v14→v15; шаг аддитивен и идемпотентен,
    // days{}, history и ladderLog не трогает. Ставился только действием
    // владельца; действия сняты задачей 28.D — поле сохраняется как есть.
    done: l.done === true
  };
}

/* Журнал шагов: записи с валидным днём, неотрицательной ступенью и текстом.
   Флаг start (стартовая запись создания лестницы) сохраняется, если он есть,
   и только в виде true — иначе поле не появляется вовсе. */
function normLadderLog(log) {
  if (!Array.isArray(log)) return [];
  return log
    .filter(e => e && typeof e === 'object' && !Array.isArray(e) && isDayKey(e.date))
    .map(e => {
      const out = {
        date: e.date,
        step: Math.max(0, Math.round(numOr(e.step, 0))),
        text: typeof e.text === 'string' ? e.text : ''
      };
      if (e.start === true) out.start = true;
      if (e.closed === true) out.closed = true;
      return out;
    });
}

/* Миграции схемы. При изменении структуры: поднять SCHEMA_VERSION
   и добавить шаг вида if (s.schemaVersion < N) { ...; }.
   Толерантна к мусору: не-объекты отбрасываются, обязательные поля
   достраиваются, числовые приводятся или обнуляются — импортированный
   или повреждённый store не должен ронять ни migrate, ни рендер. */
/* opts.external — данные пришли извне (импорт файла, возврат стёртой
   копии). Стартовая программа принадлежит ПЕРВОМУ ЗАПУСКУ, а не всякому
   пустому store: файл, снятый версией без флага seed17, приносил пустой
   items[] и получал девять чужих пунктов (аудит, находка 3). */
function migrate(s, opts) {
  if (!s || typeof s !== 'object' || Array.isArray(s)) return defaultStore();
  s.schemaVersion = numOr(s.schemaVersion, 0) || 1; // мусорная версия = v1, шаги миграций не пропускаются

  // настройки — первыми: от dayBoundary зависит «сегодня» для достройки дат
  if (!s.settings || typeof s.settings !== 'object' || Array.isArray(s.settings)) s.settings = {};
  // граница дня — час суток: целое 0..23. Без рамки импортированное
  // значение вроде 1e6 уводило логический день на десятилетия назад и
  // currentWeekStart навсегда становился null (аудит, находка 28).
  s.settings.dayBoundary = (typeof s.settings.dayBoundary === 'number' && isFinite(s.settings.dayBoundary))
    ? Math.min(23, Math.max(0, Math.round(s.settings.dayBoundary)))
    : 4;
  // v13 → v14 (задача 17): порог зачёта дня — доля отмеченного, шаг 0,1
  s.settings.dayThreshold = clampThreshold(s.settings.dayThreshold);
  // отметка экспорта — момент времени, и она обязана быть представимой
  // датой. isFinite её пропускал: 8.64e15 + 1 даёт Invalid Date, оттуда
  // «NaN-NaN-NaN» и английская строка «Экспорт запускался: Invalid Date»
  // в русском интерфейсе (задача 27, Д8). Тот же класс враждебного числа
  // у dayBoundary зажат строкой выше — здесь зажимаем тем же приёмом.
  // Вне диапазона — не «починить наугад», а «экспорта не было»: подделывать
  // дату, которой не было, приложение не должно
  if (!(typeof s.settings.exportedAt === 'number' && isFinite(s.settings.exportedAt)
        && Math.abs(s.settings.exportedAt) <= MAX_TIME)) {
    if (s.settings.exportedAt !== undefined) s.settings.exportedAt = null;
  }
  const today = dateKeyShift(new Date(), s.settings.dayBoundary);

  // пункты: только объекты; id и addedAt достраиваются, id дедуплицируются
  if (!Array.isArray(s.items)) s.items = [];
  s.items = s.items.filter(it => it && typeof it === 'object' && !Array.isArray(it));
  // Пустота, пришедшая извне, — намеренная: посев ей не адресован
  // независимо от schemaVersion и наличия флагов (A.2.1). Флаги ставятся
  // сразу и переживают сохранение: иначе следующий старт прогнал бы этот
  // же store через migrate уже как «первый запуск» и всё-таки засеял.
  if (opts && opts.external && !s.items.length) {
    s.settings.seed17 = true;
    s.settings.habitSeeded = true; // и посев привычек шага v4→v5
  }
  // посев программы (задача 17): только пустой store и только один раз
  let seeded = false;
  if (!s.items.length && s.settings.seed17 !== true) { seedProgram(s, today); seeded = true; }
  // v19 → v20 (задача Р2): режимы. Список — ДО пунктов и блоков: их поле mode
  // проверяется по нему. Шаг аддитивен: файлу без режимов это «основной
  // режим на всю жизнь» — ровно прежнее поведение; days{}, reviews[],
  // schedule и groupLog не меняются ни на символ
  const modesNorm = normModes(s.modes);
  s.modes = modesNorm.modes;
  const modeOf = modesNorm.resolve;
  const ids = new Set();
  for (const it of s.items) {
    if (typeof it.id !== 'string' || !it.id || ids.has(it.id)) it.id = uid();
    ids.add(it.id);
    if (!isDayKey(it.addedAt)) it.addedAt = today;
    it.type = it.type === 'weekly' ? 'weekly' : (it.type === 'param' ? 'param' : 'daily');
    it.area = it.area === 'habit' ? 'habit' : 'min';
    if (it.type === 'weekly') it.area = 'min'; // недельный счётчик принадлежит только минимуму (инвариант 10)
    // Отрезок жизни пункта (задача 28.E/A): addedAt — день, с которого он
    // есть, removedAt — день, с которого его уже нет, либо null. Нормализация
    // безусловная и стоит рядом с addedAt: это вторая половина одной пары.
    it.removedAt = isDayKey(it.removedAt) ? it.removedAt : null;
    // v16 → v17: выключенный пункт становится убранным С ДНЯ ЗАВЕДЕНИЯ, поле
    // active снимается. Числа владельца при этом не сдвигаются ни на единицу:
    // прежнее правило применимости выбрасывало выключенный пункт из ВСЕХ дней,
    // и пустой отрезок [addedAt, addedAt) выбрасывает его ровно так же.
    // Шаг безусловный, как и прочие нормализации: экспорт версии v16,
    // импортированный позже, обязан пройти его тем же путём. Идемпотентен —
    // после первого прогона поля active в данных нет вовсе. Рукотворный
    // removedAt при active === false не переписывается: он точнее.
    if (it.active === false && it.removedAt === null) it.removedAt = it.addedAt;
    delete it.active;
    if (typeof it.name !== 'string') it.name = '';
    if (typeof it.unit !== 'string') it.unit = '';
    if (typeof it.note !== 'string') it.note = '';
    if (typeof it.group !== 'string') it.group = '';
    it.value = numOr(it.value, null);
    if (it.value !== null && it.value <= 0) it.value = null; // планка всегда > 0, как и в формах
    if (it.type === 'param') {
      it.area = 'habit'; // параметры существуют только в области привычек
      it.value = null;   // и не несут планку минимума
      it.pkind = it.pkind === 'number' ? 'number' : 'time';
      let pv = numOr(it.pvalue, 0);
      if (it.pkind === 'time') pv = ((Math.round(pv) % 1440) + 1440) % 1440; // минуты суток
      it.pvalue = pv; // числовой порог может быть дробным — формы его не округляют
      it.pstep = Math.round(numOr(it.pstep, 0));
    }
    // v17 → v18: расписание — только у ЕЖЕДНЕВНЫХ пунктов обеих областей.
    // Недельный счётчик и параметр дневных отметок не несут вовсе
    // (инвариант 10), и маска им нечего означать.
    if (it.type === 'daily') it.schedule = normSchedule(it.schedule, it.addedAt);
    else delete it.schedule;
    // v18 → v19: журнал принадлежности блоку — только у ДЕЙСТВИЯ (daily min):
    // дни блока ограничивают только его (у привычки маска собственная, у
    // счётчика и параметра дневных отметок нет). Прочим поле снимается.
    // Маска пункта не трогается ни на символ: она стала переопределением,
    // и «все семь» в ней — нейтральный элемент ∧, то есть «как блок».
    if (it.type === 'daily' && it.area === 'min') it.groupLog = normGroupLog(it.groupLog, it, today);
    else delete it.groupLog;
    // v19 → v20: режим — только у действия, и не меняется никогда. Без режима
    // или с неизвестным — основной; привычки, параметры и недельный счётчик
    // глобальны, поле им снимается (решение оркестратора Р2)
    if (it.type === 'daily' && it.area === 'min') it.mode = modeOf(it.mode);
    else delete it.mode;
    it.at = normTime(it.at); // подпись времени — у любого пункта
    if (it.type === 'daily' && it.area === 'habit') {
      // норма недели (инвариант 11): целое 1–7, невалид — к ближайшему
      // допустимому, и не больше числа дней НЫНЕШНЕГО расписания: норма 5
      // при двух днях в маске невыполнима с рождения (задача 29/B)
      const cap = maskDays(it.schedule[it.schedule.length - 1].mask);
      it.normPerWeek = Math.max(1, Math.min(cap, Math.round(numOr(it.normPerWeek, 7))));
    } else {
      delete it.normPerWeek; // норма — только у привычек
    }
    const g = numOr(it.goal, null);
    it.goal = g !== null && Math.round(g) >= 1 ? Math.round(g) : null;
    it.raiseAfter = Math.max(0, Math.round(numOr(it.raiseAfter, 0)));
    // v10 → v11 (задача 16C): якоря обеих механик планки — понедельник недели
    // последнего решения либо null. raiseAfter остаётся историческим полем:
    // счёт по массиву reviews пропущенные разборы блокировали навсегда.
    // Рукотворный не-понедельник приводится к своему понедельнику, как steppedWeek.
    it.raiseAfterWeek = isDayKey(it.raiseAfterWeek) ? weekStartOf(it.raiseAfterWeek) : null;
    it.lowerAfterWeek = isDayKey(it.lowerAfterWeek) ? weekStartOf(it.lowerAfterWeek) : null;
    if (!Array.isArray(it.history)) it.history = [];
    it.history = it.history
      .filter(h => h && typeof h === 'object' && !Array.isArray(h) && isDayKey(h.date))
      .map(h => ({ date: h.date, value: numOr(h.value, null) }))
      .filter(h => h.value !== null);
    // v6 → v7: формула, лестница и журнал шагов — аддитивно. Лестницу несёт
    // только ежедневный пункт: правило от той поры, когда лестница занимала
    // единственный слот, а снять её можно было лишь в листе детали, который
    // открывался только для daily (аудит, находка 4). Механики сняты
    // задачей 28.D, нормализация оставлена целиком — иначе данные владельца
    // пострадали бы при первом же запуске. Журнал не трогаем.
    it.formula = normFormula(it.formula);
    it.ladder = it.type === 'daily' ? normLadder(it.ladder, today) : null;
    it.ladderLog = normLadderLog(it.ladderLog);
  }
  // ЖИВАЯ лестница в данных одна: побеждает начатая позже (строгое сравнение —
  // при равенстве и отсутствии startedAt остаётся первая по порядку items[]),
  // у прочих снимается; журналы, отметки и история не трогаются. Закрытые
  // (done) в конфликте не участвуют и не снимаются (задача 21, 4.2).
  // Дедуп остаётся и после снятия механики (задача 28.D): интерфейс лестниц
  // больше не заводит, но принести две живых может импорт файла, снятого
  // прежней версией, — это последний рубеж канонической формы поля.
  let keeper = null;
  for (const it of s.items) {
    if (!it.ladder || it.ladder.done) continue;
    if (!keeper || it.ladder.startedAt > keeper.ladder.startedAt) keeper = it;
  }
  for (const it of s.items) if (it.ladder && !it.ladder.done && it !== keeper) it.ladder = null;

  // v7 → v8: живой лестнице с пустым журналом дописывается стартовая запись
  // от startedAt. Непустой журнал не трогается: исходная ступень пути в нём
  // неизвестна, домысливать её нельзя. Идёт после разрешения конфликта —
  // снятой лестнице старт не пишется.
  if (s.schemaVersion < 8) {
    for (const it of s.items) {
      if (!it.ladder || it.ladderLog.length) continue;
      it.ladderLog.push({
        date: isDayKey(it.ladder.startedAt) ? it.ladder.startedAt : today,
        step: it.ladder.step,
        text: it.ladder.steps[it.ladder.step] || '',
        start: true
      });
    }
  }

  // отметки: ключ — валидный день, значение — непустой объект с булевыми
  // полями (как их оставляет toggleMark). Посторонние значения отбрасываются
  // ПОИМЁННО, а не вместе с днём (задача 25, п. 4): прежнее правило «весь
  // день целиком» уносило валидные отметки — один не-булев флаг в дне из
  // четырёх отметок стоил всех четырёх, а посторонний ключ в каждом дне
  // (правка файла руками) обнулял days{} без единого слова. День, в котором
  // после фильтрации не осталось ничего, по-прежнему не существует.
  //
  // v20 (задача Р2, аддитивно, схема не поднимается): false — пропуск «Не
  // сегодня», и он законен только у ДЕЙСТВИЯ минимума. У известного пункта
  // другого рода (привычка, счётчик, параметр) false отбрасывается: интерфейс
  // его не создаёт, а оставшись, он запер бы круг привычки — пропущенному
  // тап отказывает. Счёт потерь его видит (категория «пропуск»). У id,
  // которого среди пунктов нет, false остаётся, как и отметка: чей он был,
  // не узнать, а стирать факт наугад нельзя.
  const notAction = new Set(s.items.filter(it => !(it.type === 'daily' && it.area === 'min')).map(it => it.id));
  if (!s.days || typeof s.days !== 'object' || Array.isArray(s.days)) s.days = {};
  for (const k of Object.keys(s.days)) {
    const day = s.days[k];
    if (!isDayKey(k) || !day || typeof day !== 'object' || Array.isArray(day)) { delete s.days[k]; continue; }
    for (const id of Object.keys(day)) {
      if (typeof day[id] !== 'boolean' || (day[id] === false && notAction.has(id))) delete day[id];
    }
    if (!Object.keys(day).length) delete s.days[k];
  }

  // блоки (инвариант 13): имена уникальны и непусты, дубликаты схлопываются
  // в первый; порядок массива — это порядок блоков на экранах. Пересборка
  // списка заодно снимает поле chain (шаг v9→v10) — блок и есть цепочка.
  // v18 → v19: блок несёт подпись, дни недели отрезками и день ухода.
  // Шаг аддитивен: старому блоку без полей это и есть «пустая подпись, все
  // семь дней, не убран» — ровно прежнее поведение, выраженное данными
  // v19 → v20: блок принадлежит режиму, и имя уникально В ПРЕДЕЛАХ режима —
  // одноимённые блоки разных режимов законны. Без режима или с неизвестным —
  // основной; дубль имени внутри режима схлопывается в первый, как прежде
  if (!Array.isArray(s.groups)) s.groups = [];
  {
    const out = [];
    const seen = new Map(); // режим → имена его блоков
    for (const g of s.groups) {
      if (!g || typeof g !== 'object' || Array.isArray(g)) continue;
      const name = typeof g.name === 'string' ? g.name.trim() : '';
      const mode = modeOf(g.mode);
      if (!seen.has(mode)) seen.set(mode, new Set());
      if (!name || seen.get(mode).has(name)) continue;
      seen.get(mode).add(name);
      out.push({
        name,
        caption: typeof g.caption === 'string' ? g.caption.trim() : '',
        days: normBlockDays(g.days),
        removedAt: isDayKey(g.removedAt) ? g.removedAt : null,
        mode
      });
    }
    s.groups = out;
  }

  // v19 → v20: журнал активного режима. Ссылка на отброшенный дубль имени
  // переводится туда же, куда блоки и действия (modeOf), и лишь затем канон.
  // Режим, действующий сегодня, убранным быть не может: убрать активный
  // интерфейс не даёт, принести такое может только импорт — и тогда режим
  // возвращается, как блок, на который ссылается живой пункт
  s.modeLog = normModeLog((Array.isArray(s.modeLog) ? s.modeLog : []).map(x =>
    (x && typeof x === 'object' && !Array.isArray(x) && typeof x.mode === 'string'
      ? { from: x.from, mode: modesNorm.known(x.mode) } : x)), s.modes);
  {
    const act = modeOn(today, s.modeLog);
    const am = s.modes.find(x => x.id === act);
    if (am && am.removedAt !== null) am.removedAt = null;
  }

  if (!Array.isArray(s.weekLog)) s.weekLog = [];
  s.weekLog = s.weekLog.filter(e => e && typeof e === 'object' && !Array.isArray(e));
  if (!Array.isArray(s.reviews)) s.reviews = [];
  s.reviews = s.reviews.filter(r => r && typeof r === 'object' && !Array.isArray(r));
  if (!Array.isArray(s.pendingRaises)) s.pendingRaises = [];
  s.pendingRaises = s.pendingRaises.filter(e => e && typeof e === 'object' && !Array.isArray(e));
  if (!Array.isArray(s.pendingLowers)) s.pendingLowers = [];
  s.pendingLowers = s.pendingLowers.filter(e => e && typeof e === 'object' && !Array.isArray(e));

  // v11 → v12 (задача 16D): упражнения и записанные тренировки. Шаг
  // аддитивен и идемпотентен: days{}, reviews[] и items[] не трогает.
  // Форма упражнения — как у пункта: имя, единица, рабочая нагрузка и
  // её история по тем же правилам (recordBar общий).
  if (!Array.isArray(s.exercises)) s.exercises = [];
  {
    const exIds = new Set();
    s.exercises = s.exercises.filter(e => e && typeof e === 'object' && !Array.isArray(e));
    for (const ex of s.exercises) {
      if (typeof ex.id !== 'string' || !ex.id || exIds.has(ex.id)) ex.id = uid();
      exIds.add(ex.id);
      if (typeof ex.name !== 'string') ex.name = '';
      if (typeof ex.unit !== 'string') ex.unit = '';
      if (!isDayKey(ex.addedAt)) ex.addedAt = today;
      // тот же отрезок жизни, что у пункта, и тот же шаг v16 → v17
      ex.removedAt = isDayKey(ex.removedAt) ? ex.removedAt : null;
      if (ex.active === false && ex.removedAt === null) ex.removedAt = ex.addedAt;
      delete ex.active;
      ex.value = numOr(ex.value, null);
      if (ex.value !== null && ex.value <= 0) ex.value = null; // нагрузка всегда > 0
      if (!Array.isArray(ex.history)) ex.history = [];
      ex.history = ex.history
        .filter(h => h && typeof h === 'object' && !Array.isArray(h) && isDayKey(h.date))
        .map(h => ({ date: h.date, value: numOr(h.value, null) }))
        .filter(h => h.value !== null);
    }
  }
  // v12 → v13 (задача 16E): свободные заметки. Пустая по тексту заметка
  // не существует — в интерфейсе пустое сохранение удаляет её, поэтому
  // и на входе такие записи отбрасываются: данных в них нет.
  // v13 → v14 (задача 17): вид записи и источник. Существующим —
  // 'note' и пустой source; источник живёт только у выписки.
  if (!Array.isArray(s.notes)) s.notes = [];
  {
    const nIds = new Set();
    s.notes = s.notes
      .filter(n => n && typeof n === 'object' && !Array.isArray(n) &&
        typeof n.text === 'string' && n.text.trim())
      .map(n => {
        let id = typeof n.id === 'string' && n.id && !nIds.has(n.id) ? n.id : uid();
        nIds.add(id);
        const kind = n.kind === 'quote' ? 'quote' : 'note';
        return {
          id,
          date: isDayKey(n.date) ? n.date : today,
          text: n.text.trim(),
          kind,
          source: kind === 'quote' && typeof n.source === 'string' ? n.source.trim() : '',
          updatedAt: Math.max(0, Math.round(numOr(n.updatedAt, 0)))
        };
      });
  }

  if (!Array.isArray(s.sessions)) s.sessions = [];
  s.sessions = s.sessions
    .filter(x => x && typeof x === 'object' && !Array.isArray(x) && isDayKey(x.date))
    .map(x => ({
      id: typeof x.id === 'string' && x.id ? x.id : uid(),
      date: x.date,
      entries: (Array.isArray(x.entries) ? x.entries : [])
        .filter(e => e && typeof e === 'object' && !Array.isArray(e) && typeof e.exId === 'string')
        .map(e => ({ exId: e.exId, value: numOr(e.value, null) }))
        .filter(e => e.value !== null),
      note: typeof x.note === 'string' ? x.note : ''
    }));
  if (!s.paramDecided || typeof s.paramDecided !== 'object' || Array.isArray(s.paramDecided)) s.paramDecided = {};
  for (const k of Object.keys(s.paramDecided)) {
    const d = s.paramDecided[k];
    const ok = d && typeof d === 'object' && !Array.isArray(d) && isDayKey(d.week) &&
      typeof d.from === 'number' && (d.to === null || typeof d.to === 'number');
    if (!ok) delete s.paramDecided[k];
  }
  if (typeof s.settings.habitSeeded !== 'boolean') s.settings.habitSeeded = false;
  if (typeof s.draftOneChange !== 'string') s.draftOneChange = '';
  if (!isDayKey(s.weekStart)) s.weekStart = today;

  // v1 → v2: «Принять душ», подпись тренировки, блоки, посев истории планки.
  // Только что засеянной программе шаг не адресован: он переписал бы блоки
  // по именам старого набора («Тренировка» → «Тело»).
  if (s.schemaVersion < 2 && !seeded) {
    for (const it of s.items) {
      if (!it.group && DEFAULT_GROUPS[it.name]) {
        it.group = DEFAULT_GROUPS[it.name];
        // журнал принадлежности нормализован выше — по ПРЕЖНЕМУ имени, и
        // шаг его рассинхронизировал: последняя запись назвала бы старый
        // блок, а второй прогон migrate дописал бы запись рассогласования —
        // migrate(migrate(x)) ≠ migrate(x). Сверка тем же правилом, что в
        // цикле пунктов; другого места, меняющего item.group, в migrate нет.
        if (it.type === 'daily' && it.area === 'min') it.groupLog = normGroupLog(it.groupLog, it, today);
      }
      if (it.name === 'Тренировка' && !it.note) it.note = TRAIN_NOTE;
      if (typeof it.value === 'number' && isFinite(it.value) && !it.history.length) {
        it.history.push({ date: it.addedAt || dateKeyShift(new Date(), s.settings.dayBoundary), value: it.value });
      }
    }
    // в ПУСТОЙ список душ не дописывается: пустота здесь либо намеренная
    // (внешний файл, A.2), либо уже засеяна выше — оба случая не про v1
    if (s.items.length && !s.items.some(i => i.name === 'Принять душ')) {
      const shower = {
        id: uid(), name: 'Принять душ', value: null, unit: '', type: 'daily', area: 'min',
        goal: null, note: '', group: 'Тело', removedAt: null,
        addedAt: dateKeyShift(new Date(), s.settings.dayBoundary), raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null, history: [],
        // вставка идёт после валидации пунктов — каноническая форма задаётся здесь
        schedule: [{ from: dateKeyShift(new Date(), s.settings.dayBoundary), mask: WEEK_ALL }], at: '',
        groupLog: [], // действие: журнал принадлежности блоку, пока пустой (v19)
        mode: MAIN_MODE, // и режим (v20): у v1-данных режимов не было
        formula: null, ladder: null, ladderLog: []
      };
      const at = s.items.findIndex(i => i.name === 'Умыться');
      s.items.splice(at >= 0 ? at + 1 : 0, 0, shower);
    }
  }

  // v2 → v3: срез недели получает weekStart — период счёта тренировок
  if (s.schemaVersion < 3) {
    for (const r of s.reviews) {
      if (!isDayKey(r.weekStart)) {
        r.weekStart = (Array.isArray(r.keys) && isDayKey(r.keys[0])) ? r.keys[0] : today;
      }
    }
  }

  // v3 → v4: отметка последнего экспорта — мягкий дефолт
  if (s.schemaVersion < 4) {
    if (!('exportedAt' in s.settings)) s.settings.exportedAt = null;
  }

  // v4 → v5: календарные недели и программа привычек; мёртвое поле подсказки вычищается
  if (s.schemaVersion < 5) {
    if (!isDayKey(s.settings.calendarSince)) {
      s.settings.calendarSince = nextCalendarMonday(dateKeyShift(new Date(), s.settings.dayBoundary));
    }
    if (!s.settings.habitSeeded) { // однократность посева — по флагу, не по именам
      s.items.push(...seedHabits(dateKeyShift(new Date(), s.settings.dayBoundary)));
      s.settings.habitSeeded = true;
    }
    delete s.settings.hintShownForItemId;
  }

  // v9 → v10: признак цепочки упразднён — линию рисует сам блок. Поле снято
  // безусловной пересборкой списка выше, отдельный шаг не нужен; items[],
  // days{} и reviews[] не изменяются

  // v8 → v9: список блоков собирается из item.group в порядке первого появления
  // в items[]. Существующий store.groups не перезаписывается; items[], days{}
  // и reviews[] не изменяются — группа остаётся именем в пункте (инвариант 13)
  if (s.schemaVersion < 9) {
    // v20: блок ищется в режиме. У действия — в его режиме; глобальному пункту
    // (привычке, счётчику, параметру) блок годится с этим именем в любом
    // режиме, а новый заводится в основном — у данных до v9 режимов не было
    const has = (mode, name) => s.groups.some(g => g.name === name && (mode === null || g.mode === mode));
    for (const it of s.items) {
      const name = (it.group || '').trim();
      const action = it.type === 'daily' && it.area === 'min';
      if (!name || has(action ? it.mode : null, name)) continue;
      // шаг идёт ПОСЛЕ пересборки списка — форма обязана быть канонической
      // сразу, второго прогона нормализации у досборки нет
      s.groups.push({ name, caption: '', days: [], removedAt: null, mode: action ? it.mode : MAIN_MODE });
    }
  }

  // v18 → v19: живой пункт в убранном блоке. Интерфейс такого не создаёт
  // (уход блока уводит его пункты), принести может только импорт. Блок,
  // на имя которого ссылается хоть один ЖИВОЙ пункт, возвращается: иначе
  // маска невидимого блока резала бы дни пункта, который интерфейс
  // показывает «без блока», — и владелец не нашёл бы, чем это снять.
  // Идемпотентно: после шага таких блоков нет.
  // v20: у действия — блок его режима. Глобальный пункт уходит вместе с
  // блоком, только когда живого блока с этим именем не остаётся ни в одном
  // режиме (removeGroup), поэтому и возвращает он блок, только когда живого
  // одноимённого нет нигде, — первый по порядку store.groups
  {
    const liveActions = new Set();
    const liveGlobal = new Set();
    for (const it of s.items) {
      if (it.removedAt) continue;
      const name = (it.group || '').trim();
      if (it.type === 'daily' && it.area === 'min') liveActions.add(JSON.stringify([it.mode, name]));
      else if (name) liveGlobal.add(name);
    }
    for (const g of s.groups) if (g.removedAt !== null && liveActions.has(JSON.stringify([g.mode, g.name]))) g.removedAt = null;
    for (const name of liveGlobal) {
      // живым здесь считается и блок УБРАННОГО режима — осознанно, не holdsName
      // (задача Р3, п. 0.3): привычка, чей единственный живой тёзка остался в
      // убранном режиме, стоит на экранах «без блока», но убранный блок другого
      // режима старт за владельца не оживляет — вернуть режим или блок решает он
      if (s.groups.some(g => g.name === name && g.removedAt === null)) continue;
      const g = s.groups.find(x => x.name === name);
      if (g) g.removedAt = null;
    }
  }

  // v5 → v6: недельная норма привычек (normPerWeek = 7) — достраивается
  // безусловной валидацией пунктов выше, отдельный шаг не нужен; миграция
  // аддитивна: days{} и reviews[] не изменяются (решение владельца 19.07.2026)
  // v6 → v7: formula/ladder/ladderLog — тем же способом; ничего не сеялось
  // и тогда, и сейчас (стартовый текст лестницы жил только в её форме)
  // рукотворный/битый calendarSince приводится тем же правилом; не-понедельник
  // нормализуется вперёд — недели существуют только целиком
  if (!isDayKey(s.settings.calendarSince)) {
    s.settings.calendarSince = nextCalendarMonday(dateKeyShift(new Date(), s.settings.dayBoundary));
  } else if (weekStartOf(s.settings.calendarSince) !== s.settings.calendarSince) {
    s.settings.calendarSince = nextCalendarMonday(s.settings.calendarSince);
  }

  s.schemaVersion = SCHEMA_VERSION;
  return s;
}

/* Чтение localStorage: store при валидной строке, null при пустой или битой —
   дальше решает стартовая проверка init() (зеркало или дефолт, инвариант 9).
   Битая строка сохраняется в corrupt-ключ до любого восстановления. */
function load() {
  let raw = null;
  try { raw = localStorage.getItem(NS); } catch (e) { return null; }
  if (!raw) return null;
  try {
    return migrate(JSON.parse(raw));
  } catch (e) {
    // нечитаемые данные не уничтожаются: сырая строка уходит в резервный
    // ключ вместе с датой — без неё владелец не понимает, что там лежит
    // (задача 25, п. 6). Формат-обёртка; прежняя голая строка читается тоже.
    try { localStorage.setItem(CORRUPT_KEY, JSON.stringify({ raw, at: Date.now() })); } catch (e2) { /* некуда сохранить */ }
    return null;
  }
}

/* true = записалось. Возвращать успех save() обязан по двум причинам
   (задача 27, Д5 и Д6): замещающие операции расходуют копию прежних данных
   и обязаны откатиться, если рабочий ключ записать не удалось; а тихое
   «Сохранено» не должно печататься там, где записи не было. Значение
   добавлено к прежнему поведению, не заменяет его: saveFailed по-прежнему
   взводится и гасится здесь же, прежние вызовы результат просто игнорируют. */
function save() {
  let ok = false;
  try {
    localStorage.setItem(NS, JSON.stringify(store));
    saveFailed = false; // первый успешный save снимает флаг
    scheduleMirror();   // успешное сохранение дублируется в зеркало (инвариант 9)
    ok = true;
  } catch (e) {
    saveFailed = true; // приватный режим / переполнение — баннер на текущем экране
  }
  storageNote(); // вне try: своей ошибкой она не должна выглядеть отказом записи
  return ok;
}

/* Успех ПОСЛЕДНЕЙ записи. Доменные функции зовут save() внутри и возвращают
   своё (сработала ли доменная операция), а обработчику нужно знать именно
   про запись — иначе подтверждение печатается там, где на диск ничего не
   легло (задача 27, Д6). */
const lastSaveOk = () => !saveFailed;

/* ── Зеркало в IndexedDB (инвариант 9) ─────────────────────────
   Тонкая обёртка: open/get/put, все ошибки глушатся — недоступность
   IndexedDB не меняет поведение приложения. */

const IDB_NAME = 'minimum';
const IDB_STORE = 'mirror';
const IDB_KEY = 'snapshot';
const MIRROR_PROBE_MS = timing('MIRROR_PROBE_MS', 1500); // сколько ждать зеркало на старте, не задерживая первый рендер

/* Нечитаемый снапшот зеркала откладывается СВОИМ ключом, а не общим с
   localStorage (задача 28.A, п. 1.2): оба источника могут оказаться
   нечитаемыми в одну сессию — load() пишет свой ключ первым, и один
   ключ на двоих затирал бы одно другим. Формат и показ общие. */
const MIRROR_CORRUPT_KEY = NS + ':mirror-corrupt';

let mirrorTimer = null;
let mirrorDirty = false; // есть изменения, не доехавшие до зеркала
let mirrorReady = false; // стартовая проверка init() завершена — писать можно
let mirrorUnverified = false; // чтение зеркала не завершилось: что там лежит — неизвестно
/* Снапшот несёт практику, которой нет в рабочей копии, и владелец решения
   ещё не принял: { store, savedAt, stats }. Пока предложение стоит, зеркало
   не пишется — иначе следующий save() затёр бы то, что предлагается. */
let mirrorOffer = null;

function idbOpen() {
  return new Promise(resolve => {
    if (typeof indexedDB === 'undefined') { resolve(null); return; }
    try {
      const req = indexedDB.open(IDB_NAME, 1);
      req.onupgradeneeded = () => {
        try { req.result.createObjectStore(IDB_STORE); } catch (e) { /* уже есть */ }
      };
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => resolve(null);
      req.onblocked = () => resolve(null);
    } catch (e) { resolve(null); }
  });
}

/* Чтение зеркала с РАЗЛИЧЕНИЕМ трёх исходов (задача 19, A.1):
     'read'   — снапшот прочитан;
     'empty'  — база открылась, ключа нет (зеркала ещё не было);
     'failed' — чтение не завершилось: ошибка открытия, ошибка запроса
                или таймаут.
   Различать обязательно. Раньше все три сводились к null, и init(),
   не дождавшись медленного IndexedDB, писал поверх снапшота дефолтный
   store — единственная страховка от исчезновения localStorage
   уничтожалась ровно в тот момент, когда была нужна (аудит, находка 1).
   timeoutMs = 0 — ждать сколько угодно (строка «Резервная копия»). */
function mirrorProbe(timeoutMs) {
  // IndexedDB нет вовсе — терять нечего, это не «не дочитали»
  if (typeof indexedDB === 'undefined') return Promise.resolve({ status: 'empty', snap: null });
  return new Promise(resolve => {
    let done = false;
    const finish = r => { if (!done) { done = true; resolve(r); } };
    const fail = () => finish({ status: 'failed', snap: null });
    if (timeoutMs > 0) setTimeout(fail, timeoutMs);
    idbOpen().then(db => {
      if (!db) { fail(); return; }
      try {
        const req = db.transaction(IDB_STORE, 'readonly').objectStore(IDB_STORE).get(IDB_KEY);
        req.onsuccess = () => {
          finish(req.result ? { status: 'read', snap: req.result } : { status: 'empty', snap: null });
          db.close();
        };
        req.onerror = () => { fail(); db.close(); };
      } catch (e) { fail(); try { db.close(); } catch (e2) {} }
    }).catch(fail);
  });
}

/* Снапшот или null — прежний контракт поверх mirrorProbe */
function mirrorRead() {
  return mirrorProbe(0).then(r => r.snap);
}

/* Снять снапшот целиком. Единственный вызывающий — «Убрать» у строки
   нечитаемой копии: пока снапшот лежит, каждый старт упирается в него и
   зеркало не ведётся вовсе. Решение владельца, не приложения. */
function mirrorClear() {
  return idbOpen().then(db => new Promise(resolve => {
    if (!db) { resolve(false); return; }
    try {
      const tx = db.transaction(IDB_STORE, 'readwrite');
      tx.objectStore(IDB_STORE).delete(IDB_KEY);
      tx.oncomplete = () => { resolve(true); db.close(); };
      tx.onerror = () => { resolve(false); db.close(); };
      tx.onabort = () => { resolve(false); db.close(); };
    } catch (e) { resolve(false); try { db.close(); } catch (e2) {} }
  })).catch(() => false);
}

function mirrorWrite(snapshot) {
  return idbOpen().then(db => new Promise(resolve => {
    if (!db) { resolve(false); return; }
    try {
      const tx = db.transaction(IDB_STORE, 'readwrite');
      tx.objectStore(IDB_STORE).put(snapshot, IDB_KEY);
      tx.oncomplete = () => { resolve(true); db.close(); };
      tx.onerror = () => { resolve(false); db.close(); };
      tx.onabort = () => { resolve(false); db.close(); };
    } catch (e) { resolve(false); try { db.close(); } catch (e2) {} }
  })).catch(() => false);
}

/* Непарсящийся снапшот — ОТКАЗ чтения, а не пустота (задача 28.A, п. 1.1).
   Прежде исключение разбора глушилось на месте, store оставался null, и
   исход 'read' продолжал считаться успехом: mirrorReady вставал (статус-то
   не 'failed'), дефолтный store уезжал в зеркало и затирал снапшот В ТОЙ ЖЕ
   сессии. Замер: 9 посевных пунктов легли поверх снапшота, из которого не
   удалось прочитать ни байта.

   Сырая строка откладывается ровно так же, как load() откладывает
   нечитаемый localStorage, и показывается той же строкой «Данных».
   Сам снапшот при этом НЕ трогается: отложенная строка лежит в
   localStorage — в том самом хранилище, от исчезновения которого зеркало
   и страхует. Перезаписать снапшот вправе только владелец, кнопкой
   «Убрать» (она же снимает снапшот, иначе следующий старт упёрся бы
   в него снова).

   Результат не возвращается намеренно: удалась запись или нет, поведение
   одно и то же — сессия объявляется непроверенной и зеркало не пишется.
   Отложить не вышло (квота) — владелец всё равно видит «Резервная копия
   не проверена», а снапшот цел; врать про сохранённую строку нечем. */
function keepMirrorCorrupt(snap) {
  let raw;
  try {
    raw = (snap && typeof snap.json === 'string') ? snap.json : JSON.stringify(snap);
  } catch (e) { return; } // снапшот не сериализуется — сохранять нечего
  if (typeof raw !== 'string') return;
  try {
    localStorage.setItem(MIRROR_CORRUPT_KEY, JSON.stringify({ raw, at: Date.now() }));
  } catch (e) { /* некуда сохранить: снапшот трогать нельзя, он цел */ }
}

/* Снапшот → store или null. Разбор и migrate — как при восстановлении:
   зеркало пишет само приложение, посев ему адресован на общих правах. */
function mirrorParse(snap) {
  if (!snap || typeof snap.json !== 'string') return null;
  try { return migrate(JSON.parse(snap.json)); } catch (e) { return null; }
}

/* Снапшот несёт то, чего в рабочей копии нет ВОВСЕ: дни отметок или пункты.
   Сравнение по содержимому, а не по возрасту — и это вынужденно: savedAt
   есть только у снапшота, у localStorage времени записи нет ни в одном поле
   (разведка 0.5), а завести его значило бы поднять схему. Направление
   выбрано так, чтобы отставание зеркала на одну операцию (дебаунс 500 мс)
   предложения не порождало: оно даёт снапшоту МЕНЬШЕ, а не больше. */
function mirrorHasMore(snapStore, cur) {
  if (!snapStore || typeof snapStore !== 'object') return false;
  const days = (snapStore.days && typeof snapStore.days === 'object') ? snapStore.days : {};
  for (const k of Object.keys(days)) if (!cur.days[k]) return true;
  const ids = new Set(cur.items.map(i => i.id));
  return (Array.isArray(snapStore.items) ? snapStore.items : []).some(i => i && !ids.has(i.id));
}

/* Стартовая сверка с зеркалом. Зовётся ПОСЛЕ первого рендера, поэтому
   старт не удлиняет (п. 2.4), и зовётся ВСЕГДА — в том числе при валидном
   localStorage (п. 2.1). Прежде зеркало в этом случае не читалось вовсе, и
   осторожность, взведённая неудачным чтением, жила ровно одну сессию:
   первая же отметка делала localStorage источником истины, а следующий
   старт затирал подлинный снапшот. Замер: практика в 1 пункт и 2 дня
   становилась 9 посевными пунктами и 1 днём.

   Осторожность не запоминается, а ВЫВОДИТСЯ ЗАНОВО на каждом старте —
   поэтому переживает перезапуск и не требует поля в settings. */
function verifyMirror() {
  return mirrorProbe(MIRROR_PROBE_MS).then(probe => {
    if (probe.status === 'failed') { mirrorUnverified = true; return; }
    if (probe.status === 'read') {
      const kept = mirrorParse(probe.snap);
      if (!kept) {
        keepMirrorCorrupt(probe.snap); // сырую строку — на виду, снапшот — не трогать
        mirrorUnverified = true;
        return;
      } else if (mirrorHasMore(kept, store)) {
        mirrorOffer = { store: kept, savedAt: probe.snap.savedAt, stats: wipeStats(kept) };
        return; // молча не затираем: решение за владельцем (п. 2.2)
      }
    }
    mirrorReady = true;
    scheduleMirror(); // догнать всё, что накопилось, пока сверка шла
  }).then(() => {
    // строка и блок «Данных» рождаются разметкой: экран перерисовывается,
    // если владелец на нём (форма при этом сохраняет черновик обычным путём)
    if (ui.tab === 'settings') renderSettings();
    else updateMirrorNote();
  });
}

/* Дебаунс ~500 мс: частые отметки не молотят IndexedDB */
const MIRROR_FLUSH_MS = timing('MIRROR_FLUSH_MS', 500);

function scheduleMirror() {
  if (!mirrorReady || typeof indexedDB === 'undefined') return;
  mirrorDirty = true;
  clearTimeout(mirrorTimer);
  mirrorTimer = setTimeout(flushMirror, MIRROR_FLUSH_MS);
}

/* Немедленный сброс незаписанного снапшота (pagehide, уход в фон, тесты) */
function flushMirror() {
  if (!mirrorReady || !mirrorDirty) return Promise.resolve(false);
  mirrorDirty = false;
  clearTimeout(mirrorTimer);
  return mirrorWrite({
    json: JSON.stringify(store),
    savedAt: Date.now(),
    schemaVersion: store.schemaVersion
  }).then(ok => {
    if (!ok) mirrorDirty = true; // сбой записи — изменения не потеряны, доедут со следующим flush
    return ok;
  });
}

/* ── Даты и граница дня ────────────────────────────────────── */

const pad2 = n => String(n).padStart(2, '0');

function dateKeyFromDate(d) {
  return d.getFullYear() + '-' + pad2(d.getMonth() + 1) + '-' + pad2(d.getDate());
}

/* Логическая дата: время до boundaryHours относится к предыдущему дню. */
function dateKeyShift(d, boundaryHours) {
  return dateKeyFromDate(new Date(d.getTime() - boundaryHours * 3600000));
}

function todayKey() {
  return dateKeyShift(new Date(), store.settings.dayBoundary);
}

function keyToDate(k) {
  const [y, m, d] = k.split('-').map(Number);
  return new Date(y, m - 1, d, 12, 0, 0); // полдень — вне зоны перевода часов
}

/* Валидный ключ логического дня: формат YYYY-MM-DD и существующая дата */
function isDayKey(k) {
  return typeof k === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(k) &&
    dateKeyFromDate(keyToDate(k)) === k;
}

/* Понедельник календарной недели, которой принадлежит логический день */
/* День недели логического дня: 0 — понедельник, 6 — воскресенье. Формула
   жила внутри weekStartOf; расписанию (задача 29/B) она нужна отдельно, и
   двух её копий быть не должно. Та же конвенция, что у трёх мест разметки
   (полоса недели привычки, точечный путь той же полосы, шапка сетки разбора). */
function weekdayOf(dayKey) {
  return (keyToDate(dayKey).getDay() + 6) % 7;
}

function weekStartOf(dayKey) {
  return addDays(dayKey, -weekdayOf(dayKey));
}

/* Миллисекунды до ближайшего момента границы дня */
function msToNextBoundary() {
  const now = new Date();
  const next = new Date(now.getFullYear(), now.getMonth(), now.getDate(),
    store.settings.dayBoundary, 0, 0, 0);
  while (next.getTime() <= now.getTime()) next.setDate(next.getDate() + 1);
  return next.getTime() - now.getTime();
}

function addDays(k, n) {
  const d = keyToDate(k);
  d.setDate(d.getDate() + n);
  return dateKeyFromDate(d);
}

function diffDays(a, b) {
  return Math.round((keyToDate(a) - keyToDate(b)) / 86400000);
}

function fmtWeekday(k) {
  return keyToDate(k).toLocaleDateString('ru-RU', { weekday: 'long' });
}

function fmtDay(k) {
  return keyToDate(k).toLocaleDateString('ru-RU', { day: 'numeric', month: 'long' });
}

function fmtShort(k) {
  return keyToDate(k).toLocaleDateString('ru-RU', { day: 'numeric', month: 'short' });
}

/* Дата и время из миллисекунд — для строки резервной копии */
function fmtStamp(ms) {
  return new Date(ms).toLocaleString('ru-RU', { day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit' });
}

function plural(n, one, few, many) {
  const m10 = n % 10, m100 = n % 100;
  if (m10 === 1 && m100 !== 11) return one;
  if (m10 >= 2 && m10 <= 4 && (m100 < 12 || m100 > 14)) return few;
  return many;
}

/* ── Домены: отметки, счётчики, недели ─────────────────────── */

/* ── Отрезок жизни пункта (инвариант 12, задача 28.E/A) ────────
   Пункт живёт от addedAt до removedAt: первый день — тот, в который он
   заведён, последний — накануне того, в который убран. Поле active
   упразднено вместе с тумблером: два контрола с одним последствием и были
   той перегруженностью, на которую жаловался владелец, а после правки
   правила применимости они бы ещё и разошлись — «Убрать» перестал бы
   двигать прошлое, тумблер продолжил бы.

   Две функции, и путать их нельзя. live() — «есть сейчас»: по ней
   строятся списки дневных экранов и механики, работающие с настоящим.
   livedOn() — «был в том дне»: по ней и только по ней считается прошлое. */
const live = x => !x.removedAt;

function livedOn(x, dayKey) {
  return x.addedAt <= dayKey && (!x.removedAt || dayKey < x.removedAt);
}

/* ── Расписание пункта (задача 29/B, инвариант 12) ─────────────
   `item.schedule` — список ОТРЕЗКОВ `[{from, mask}]` по возрастанию from.
   mask — семь символов '0'/'1', понедельник первым: '1000001' — Пн и Вс.

   Отрезки, а не одно поле, ровно по той причине, по которой в задаче
   28.E/A упразднён `active`: маска, положенная в пункт одним значением,
   применилась бы ко ВСЕЙ истории, и сегодняшняя правка переписала бы
   вчерашние числа. День читается маской, действовавшей В ТОТ ДЕНЬ, и
   прошлое не двигается никогда.

   Списка нет или он пуст — «все семь дней»: пункт без расписания ведёт
   себя ровно так, как вёл до задачи 29. */
const WEEK_ALL = '1111111';
const isMask = m => typeof m === 'string' && /^[01]{7}$/.test(m);
const maskDays = m => (isMask(m) ? m.split('').filter(c => c === '1').length : 7);

/* Маска, действовавшая в дне: последний отрезок с from ≤ dayKey. Отрезков
   нет, или все они позже этого дня — «все семь»: до первой записи
   расписания не существовало, и прошлое о нём не знает. */
function scheduleOn(item, dayKey) {
  const segs = Array.isArray(item && item.schedule) ? item.schedule : [];
  for (let i = segs.length - 1; i >= 0; i--) {
    if (segs[i].from <= dayKey) return segs[i].mask;
  }
  return WEEK_ALL;
}

/* Нынешняя маска — последний отрезок; её показывает и правит форма */
const scheduleNow = item => scheduleOn(item, todayKey());

/* ── Дни блока и эффективные дни («Расписание 1/3») ───────────
   Блок несёт дни недели теми же отрезками, что пункт: `block.days =
   [{from, mask}]`. Отрезки — по той же причине, что у пункта: маска блока,
   положенная одним значением, сегодняшней правкой переписала бы прошлые
   дни ВСЕХ его действий разом.

   Эффективные дни ДЕЙСТВИЯ (daily min) — посимвольное ∧ своей маски и
   маски блока, в котором оно состояло В ТОТ ДЕНЬ. Своя маска стала
   переопределением: «как блок» хранится маской «все семь» — нейтральным
   элементом ∧, и второго способа сказать одно и то же форма данных не
   заводит. Своими днями действие может дни блока только СУЗИТЬ.

   Привычку дни блока не ограничивают: её маска собственная (задача 29/B),
   а норма недели считается от неё — ∧ с блоком делал бы норму невыполнимой
   без единой правки самой привычки. */

/* Посимвольное ∧ двух масок. Невалидная маска читается «все семь» —
   нейтральным элементом, а не нулём: мусор не должен гасить дни. */
function andMask(a, b) {
  const x = isMask(a) ? a : WEEK_ALL;
  const y = isMask(b) ? b : WEEK_ALL;
  let out = '';
  for (let i = 0; i < 7; i++) out += (x[i] === '1' && y[i] === '1') ? '1' : '0';
  return out;
}

/* Своя маска действия после правки «Свои дни» в форме: (own ∧ ¬блок) ∨
   (выбор ∧ блок). Внутри дней блока — выбор владельца; вне их — прежние
   биты своей маски как есть. Там чипы недоступны: владелец их не выбирал и
   не снимал, и терять их нечем оправдать — при переносе действия в блок с
   другими днями они снова начнут что-то значить («Расписание 1/3», п. 2.5).
   Своя маска хранится КАК ЕСТЬ, ∧ с днями блока применяется только при
   чтении (effectiveMaskOn). Невалидная маска читается «все семь», как в andMask. */
function mergeOwnMask(own, pick, within) {
  const o = isMask(own) ? own : WEEK_ALL;
  const p = isMask(pick) ? pick : WEEK_ALL;
  const w = isMask(within) ? within : WEEK_ALL;
  let out = '';
  for (let i = 0; i < 7; i++) out += w[i] === '1' ? p[i] : o[i];
  return out;
}

/* Маска блока в дне: последний отрезок с from ≤ dayKey. До первого
   отрезка и без отрезков вовсе — «все семь»: у блока нет дня заведения,
   и прошлое до первой правки дней о них не знает. */
function blockMaskOn(block, dayKey) {
  const segs = Array.isArray(block && block.days) ? block.days : [];
  for (let i = segs.length - 1; i >= 0; i--) {
    if (segs[i].from <= dayKey) return segs[i].mask;
  }
  return WEEK_ALL;
}

const blockMaskNow = block => blockMaskOn(block, todayKey());

/* Смена дней блока — отрезок с сегодняшнего дня, правила setSchedule
   (инвариант 5): повторная смена в тот же день заменяет, возврат к прежней
   маске схлопывает. Снимаются ВСЕ хвостовые отрезки с from ≥ сегодня, а не
   только сегодняшний: отрезок «из будущего» (сбитые часы) иначе встал бы
   ПЕРЕД новым и нарушил порядок. Прежние отрезки не трогаются.
   Не сохраняет сам: смена дней — часть updateGroup, у которой одна запись
   и полный откат. */
function setBlockDays(block, mask) {
  if (!block || !isMask(mask) || maskDays(mask) === 0) return false; // пустая маска — не дни
  const t = todayKey();
  const segs = Array.isArray(block.days) ? block.days : (block.days = []);
  while (segs.length && segs[segs.length - 1].from >= t) segs.pop();
  const prev = segs.length ? segs[segs.length - 1].mask : WEEK_ALL;
  if (prev !== mask) segs.push({ from: t, mask });
  return true;
}

/* Блок, в котором действие состояло в этот день. Журнал пуст — item.group
   на всю жизнь. День раньше первой записи (правка часов, рукотворный файл)
   читается первой записью: журнал начинается с addedAt, и раньше пункта
   не было. */
function groupOn(item, dayKey) {
  const log = Array.isArray(item && item.groupLog) ? item.groupLog : [];
  if (!log.length) return groupNameOf(item);
  for (let i = log.length - 1; i >= 0; i--) {
    if (log[i].from <= dayKey) return log[i].group;
  }
  return log[0].group;
}

/* Смена блока у пункта. НЕ сохраняет; true — имя действительно сменилось.
   У действия пишется журнал: первая смена фиксирует прежний блок с дня
   заведения, повторная в тот же день заменяет сегодняшнюю запись, возврат
   к прежнему блоку её схлопывает; одно состояние на всю жизнь — это не
   история, журнал пуст. Прошлые записи не трогаются никогда. Пункт,
   заведённый сегодня, истории не получает: вчерашнего дня у него не было. */
function setItemGroup(item, name) {
  const to = String(name ?? '').trim();
  const was = groupNameOf(item);
  if (to === was) return false;
  if (item.type === 'daily' && item.area === 'min') {
    const t = todayKey();
    const log = Array.isArray(item.groupLog) ? item.groupLog : (item.groupLog = []);
    if (!log.length && item.addedAt < t) log.push({ from: item.addedAt, group: was });
    while (log.length && log[log.length - 1].from >= t) log.pop();
    const prev = log.length ? log[log.length - 1].group : null;
    if (prev !== to) log.push({ from: t, group: to });
    if (log.length <= 1) log.length = 0;
  }
  item.group = to;
  return true;
}

/* ── Тип действия («Расписание 1/3», п. 2.5) ──────────────────
   Действие заводится быстрым добавлением ежедневным, а недельным счётчиком
   становится правкой — но ТОЛЬКО В ДЕНЬ ЗАВЕДЕНИЯ. Пункт существует один
   сегодняшний день, и смена типа не двигает ни одного прошлого числа. Позже
   ежедневный пункт и счётчик — разные истории: отметки в days{} против
   записей weekLog, — и превращение одного в другое переписало бы прошлое
   задним числом, оставив отметки без пункта или записи без счётчика. */
function canChangeType(item) {
  return !!item && item.area === 'min' && (item.type === 'daily' || item.type === 'weekly') &&
    item.addedAt === todayKey();
}

/* Отказ по данным: и в день заведения у пункта может быть сегодняшний
   факт — отметка у ежедневного или запись у счётчика. Смена типа сделала
   бы его ничьим. null — менять можно. */
function typeChangeRefusal(item, to) {
  if (!item || to === item.type) return null;
  if (item.type === 'weekly' && store.weekLog.some(e => e && e.itemId === item.id)) {
    return 'Тип не меняется: по счётчику уже есть записи';
  }
  if (item.type === 'daily' && isMarked(todayKey(), item.id)) return 'Тип не меняется: пункт сегодня отмечен';
  // пропуск «Не сегодня» (задача Р2) — такой же сегодняшний факт: у счётчика
  // дневных значений нет, и пропуск остался бы ничьим
  if (item.type === 'daily' && isSkipped(todayKey(), item.id)) return 'Тип не меняется: у пункта сегодня «Не сегодня»';
  return null;
}

/* Смена типа. НЕ сохраняет (это часть записи формы с одной save);
   true — тип сменился. Поля — в канонической форме migrate: у счётчика нет
   ни расписания, ни журнала принадлежности, ни лестницы и есть цель ≥ 1; у
   ежедневного — расписание «все семь» с дня заведения, пустой журнал и
   цели нет. */
function setItemType(item, to, goal) {
  if (!canChangeType(item) || (to !== 'daily' && to !== 'weekly') || to === item.type) return false;
  if (typeChangeRefusal(item, to)) return false;
  if (to === 'weekly') {
    const g = Math.round(Number(goal));
    if (!(g >= 1)) return false; // счётчик без цели не существует
    item.type = 'weekly';
    item.goal = g;
    delete item.schedule;
    delete item.groupLog;
    delete item.mode; // счётчик глобален для режимов (задача Р2)
    item.ladder = null;
  } else {
    item.type = 'daily';
    item.goal = null;
    item.schedule = [{ from: item.addedAt, mask: WEEK_ALL }];
    item.groupLog = [];
    item.mode = activeMode(); // действие рождается в активном режиме (задача Р2)
  }
  return true;
}

/* Эффективная маска пункта в дне — ОДНО правило на всё приложение.
   Блок ищется среди ВСЕХ, включая убранные: прошлое действия, чей блок
   потом убран, считается днями того блока, какими они были. */
/* Задача Р2: действие существует в днях СВОЕГО режима. В день, когда
   действовал другой режим, эффективных дней у него нет вовсе — маска
   пуста, и dueOn, weekMaskDays, planWeekCount получают это правило даром,
   одним местом. Имя блока разрешается в блок режима самого действия. */
const WEEK_NONE = '0000000';

function effectiveMaskOn(item, dayKey) {
  const own = scheduleOn(item, dayKey);
  if (!(item.type === 'daily' && item.area === 'min')) return own;
  if (itemMode(item) !== modeOn(dayKey)) return WEEK_NONE;
  const g = findGroup(groupOn(item, dayKey), itemMode(item));
  return g ? andMask(own, blockMaskOn(g, dayKey)) : own;
}

/* День недели входит в эффективную маску. Та же логика, что
   effectiveMaskOn(...)[день недели], но без сборки строки: это самый
   горячий путь приложения — через него серия и рекорд проходят каждый
   пункт каждого дня эпохи. Результат обязан совпадать, это закреплено
   тестом. Режим дня — двоичным поиском по журналу (modeOn), не проходом. */
function inEffectiveDays(item, dayKey) {
  const wd = weekdayOf(dayKey);
  if (scheduleOn(item, dayKey)[wd] !== '1') return false;
  if (!(item.type === 'daily' && item.area === 'min')) return true;
  if (itemMode(item) !== modeOn(dayKey)) return false;
  const g = findGroup(groupOn(item, dayKey), itemMode(item));
  return !g || blockMaskOn(g, dayKey)[wd] === '1';
}

/* Применимость ДНЯ: пункт в этот день жил И день недели входит в его
   ЭФФЕКТИВНЫЕ дни того дня. Единственная содержательная точка прошлого —
   minDayItems, ровно как было с livedOn (инвариант 12). У действия в
   эффективные дни входит и режим (задача Р2): день чужого режима — не его
   день, и переключение сегодня вчерашнего не трогает — вчера читается
   режимом вчера. */
function dueOn(item, dayKey) {
  return livedOn(item, dayKey) && inEffectiveDays(item, dayKey);
}

/* «Есть сегодня»: живой и сегодняшний день недели в эффективных днях */
function dueNow(item, tKey) {
  const t = tKey || todayKey();
  return live(item) && inEffectiveDays(item, t);
}

/* Сколько дней расписания у пункта в календарной неделе mon — знаменатель
   недельных порогов (инвариант 4) и потолок нормы привычки (инвариант 11).
   Считается по эффективной маске КАЖДОГО дня недели: неделя, в которую
   расписание или дни блока сменились, честно даёт смешанное число. У
   привычки эффективная маска — её собственная. */
function weekMaskDays(item, mon) {
  let n = 0;
  for (let i = 0; i < 7; i++) {
    if (inEffectiveDays(item, addDays(mon, i))) n++;
  }
  return n;
}

/* Сколько раз действие стояло в плане дней keys и сколько из них отмечено.
   Числитель фильтруется тем же правилом, что знаменатель (инвариант 12,
   как у «Отметок»): отметка в дне вне плана в счёт не идёт. */
function weekPlan(item, keys) {
  let planned = 0, done = 0;
  for (const k of (Array.isArray(keys) ? keys : [])) {
    if (!dueOn(item, k)) continue;
    planned++;
    if (isMarked(k, item.id)) done++;
  }
  return { planned, done };
}

/* Пропуски «Не сегодня» (задача Р2) в днях плана keys — число «пропусков K»
   сетки разбора. Тем же фильтром, что weekPlan: пропуск в дне, который потом
   ушёл из плана, в счёт не идёт, и «пропусков» не бывает больше дней плана.
   Отдельной функцией, а не третьим полем weekPlan: пропуск не меняет ни
   плана, ни отметок — это третье, независимое число строки. */
function weekSkips(item, keys) {
  let n = 0;
  for (const k of (Array.isArray(keys) ? keys : [])) {
    if (isSkipped(k, item.id) && dueOn(item, k)) n++;
  }
  return n;
}

/* Смена расписания — отрезок с сегодняшнего дня. Повторная смена в тот же
   логический день ЗАМЕНЯЕТ последний отрезок, а не плодит записи; возврат
   к прежней маске схлопывает его. Правила те же, что у recordBar
   (инвариант 5) — второго словаря владельцу учить не нужно.
   Прежние отрезки не трогаются никогда: в этом весь смысл отрезков.
   Отрезок «из будущего» прежним не считается и снимается (scheduleCore).

   Список, опустевший после снятия сегодняшнего отрезка, получает отрезок
   ВСЕГДА, даже «все семь» (задача «Расписание 1/3»): у пункта,
   заведённого сегодня, единственный отрезок и есть сегодняшний, и возврат
   к «как блок» оставлял schedule = [] — форму, которую migrate от себя
   переписывает в [{addedAt, все семь}]. Потерь не было, но в localStorage,
   зеркале и экспорте жила неканоническая запись. У канонического пункта,
   заведённого раньше, первый отрезок начинается с addedAt < сегодня и не
   снимается — список там не пустеет. */
function setSchedule(item, mask) {
  if (!scheduleCore(item, mask)) return false;
  clampNorm(item);
  save();
  return true;
}

/* Ядро смены расписания — без записи и без зажатия нормы: возврат пункта
   (restoreItemCore) меняет маску внутри операции с одной записью и общим
   откатом, как всё, что делает возврат блока. */
function scheduleCore(item, mask) {
  if (!isMask(mask) || maskDays(mask) === 0) return false; // пустая маска — не расписание
  const t = todayKey();
  const segs = Array.isArray(item.schedule) ? item.schedule : (item.schedule = []);
  // сегодняшний переписывается, а отрезки «из будущего» (логический день
  // сдвинулся назад — граница дня, исправленные часы) снимаются вместе с
  // ним, как хвост дней блока (setBlockDays) и журнала (setItemGroup).
  // Снималось прежде только `from === today`, и новый отрезок вставал ЗА
  // будущим: список выходил из порядка, сессия читала его с конца, а
  // migrate при следующем старте сортировал — и последняя правка дней
  // проигрывала прежней, пересчитывая «N из M», серию и цепь задним числом
  while (segs.length && segs[segs.length - 1].from >= t) segs.pop();
  const prev = segs.length ? segs[segs.length - 1].mask : WEEK_ALL;
  if (prev !== mask || !segs.length) segs.push({ from: t, mask });
  return true;
}

/* Норма привычки не может превышать числа дней её расписания (инвариант
   11): норма 5 при двух днях в маске невыполнима с рождения. Зажатие —
   это СМЕНА нормы со всеми её последствиями (серия недель пересчитывается
   ретроактивно), поэтому оно возвращает прежнее значение: вызывающий
   обязан сказать об этом владельцу, а не смолчать (задача 29/B, B.2.4). */
function clampNorm(item) {
  if (item.type !== 'daily' || item.area !== 'habit') return null;
  const cap = maskDays(scheduleNow(item));
  const was = item.normPerWeek || 7;
  if (was <= cap) return null;
  item.normPerWeek = cap;
  return was;
}

const liveDaily = () => store.items.filter(i => live(i) && i.type === 'daily');
const liveWeekly = () => store.items.filter(i => live(i) && i.type === 'weekly');
/* Пункты области, применимые СЕГОДНЯ: дневные экраны и их горячие пути */
const dueDaily = (area, t) => liveDaily().filter(i => i.area === area && dueNow(i, t));

/* ── Уход и возврат ────────────────────────────────────────────
   Словарь задачи 28.E/A: «удалить» — стереть, «убрать» — увести из виду.
   Удаляется только то, от чего не зависит ни одно прошлое число. Блок
   воплощал это правило до «Расписания 1/3»; с днями недели от него зависят
   прошлые числа его действий, и он тоже убирается (removeGroup). Пункт,
   упражнение и блок убираются: факт остаётся, из виду уходит.

   Уход действует с СЕГОДНЯШНЕГО дня включительно: removedAt = todayKey(),
   и livedOn перестаёт видеть пункт начиная с этого дня. Вчера и раньше не
   двигается никогда.

   Отказ записи откатывает поле целиком (A.1.8) — как всякая замещающая
   операция: в памяти не должно остаться того, чего нет на диске. */
function removeItem(id) {
  const it = store.items.find(x => x.id === id);
  if (!it || !live(it)) return false;
  it.removedAt = todayKey();
  if (save()) return true;
  it.removedAt = null;
  return false;
}

/* Возврат В ТОТ ЖЕ ДЕНЬ — полная отмена: отрезок не разрывался ни на день,
   и разрывать его записью нечем. ПОЗЖЕ — новая запись с новым id и
   сегодняшним addedAt: иначе дни паузы задним числом вошли бы в
   знаменатель тех дней, в которые пункта не было. Отметки и история
   принадлежат прежнему отрезку и остаются при нём.

   Новая запись встаёт СРАЗУ ЗА прежней, а не в конец списка: порядок
   внутри блока — решение владельца, и возврат не повод его терять.
   Мёртвые поля формулы и лестницы (задача 28.D) в неё не копируются:
   вторая живая лестница противоречила бы канонической форме, которую
   стережёт дедуп migrate.

   Ядро возврата не пишет: оно меняет store и отдаёт вернувшуюся запись
   вместе с откатом. Возврат блока (restoreGroup) возвращает сразу
   несколько пунктов, и запись у него обязана быть ОДНА — с общим откатом
   всех изменений, если она не удалась. Обёртка restoreItem — прежнее
   поведение слово в слово: одна запись, откат при отказе.

   Действие, которое вернулось бы БЕЗ ЕДИНОГО ДНЯ (своя маска ∧ нынешние
   дни блока пусты), возвращается «как блок»: своя маска с сегодняшнего
   дня становится «все семь», и эффективные дни — это дни блока (задача
   «Расписание 1/3», п. 1.3: пустая эффективная маска неотличима от
   убранного пункта, а убирать умеет «Убрать»). Такое состояние возникает
   без единой ошибки владельца: пункт убран, дни блока сужены (zeroDaysIn
   убранных не видит — их нет на экране), пункт возвращён. Отказ здесь не
   годится: уводящая из виду операция обратима ВСЕГДА (инвариант 12), а
   отказ возврата блока из-за одного такого пункта — нуля, пришедшего
   импортом, например, — оставил бы убранный блок убранным навсегда:
   формы у убранного блока нет. Прошлое не двигается: у той же записи
   меняется только сегодняшний отрезок, у новой — её единственный. Смена
   маски — решение, принятое за владельца, поэтому ядро отдаёт признак
   asBlock, и вызывающий обязан назвать её строкой, как зажатие нормы. */
function restoreItemCore(id) {
  const at = store.items.findIndex(x => x.id === id);
  const it = at < 0 ? null : store.items[at];
  if (!it || live(it)) return null;
  if (laterSegmentOf(it)) return null; // отрезок уже продолжен — второй живой экземпляр был бы дублем
  const t = todayKey();
  const asBlock = returnsWithoutDays(it);
  if (it.removedAt === t) {
    const was = it.schedule;
    it.removedAt = null;
    if (asBlock) {
      it.schedule = it.schedule.map(x => Object.assign({}, x));
      scheduleCore(it, WEEK_ALL);
    }
    return { item: it, asBlock, undo: () => { it.removedAt = t; it.schedule = was; } };
  }
  const copy = {
    id: uid(), name: it.name, value: it.value, unit: it.unit,
    type: it.type, area: it.area, goal: it.goal, note: it.note, group: it.group,
    removedAt: null, addedAt: t, raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null,
    history: [], formula: null, ladder: null, ladderLog: []
  };
  // расписание и подпись времени переезжают вместе с пунктом: copy
  // собирается ПЕРЕЧИСЛЕНИЕМ, и забытое поле терялось бы молча. Отрезки
  // начинаются заново с нового addedAt — прежние принадлежат прежнему
  // отрезку жизни, как отметки и история (инвариант 12).
  if (it.type === 'daily') copy.schedule = [{ from: t, mask: asBlock ? WEEK_ALL : scheduleNow(it) }];
  // журнал принадлежности — тоже прежнему отрезку: новая запись начинает
  // жизнь в нынешнем блоке, и истории переноса у неё нет (канон — [])
  if (it.type === 'daily' && it.area === 'min') copy.groupLog = [];
  // режим — тот же: действие принадлежит режиму навсегда (задача Р2)
  if (it.type === 'daily' && it.area === 'min') copy.mode = itemMode(it);
  copy.at = it.at || '';
  if (it.type === 'daily' && it.area === 'habit') copy.normPerWeek = it.normPerWeek || 7;
  if (it.type === 'param') {
    copy.pkind = it.pkind; copy.pvalue = it.pvalue; copy.pstep = it.pstep;
    copy.history = [{ date: t, value: it.pvalue }]; // порог — та же планка (инвариант 5)
  } else if (typeof copy.value === 'number') {
    copy.history = [{ date: t, value: copy.value }];
  }
  store.items.splice(at + 1, 0, copy);
  // откат ищет копию по ссылке, а не по индексу: при возврате блока за
  // ней могли встать другие копии, и индексы сдвинулись
  return { item: copy, asBlock, undo: () => { const i = store.items.indexOf(copy); if (i >= 0) store.items.splice(i, 1); } };
}

/* Вернётся ли пункт без единого дня: действие, у которого нынешняя своя
   маска ∧ нынешние дни его блока пусты. Блок ищется среди всех — дни
   убранного блока вернутся вместе с ним. Неизвестный блок ничего не режет. */
function returnsWithoutDays(item) {
  if (!item || item.type !== 'daily' || item.area !== 'min') return false;
  const g = findGroup(groupNameOf(item), itemMode(item)); // блок режима самого действия
  return maskDays(andMask(scheduleNow(item), g ? blockMaskNow(g) : WEEK_ALL)) === 0;
}

/* Продолжение отрезка жизни: возврат ПОЗЖЕ дня ухода заводит новую запись,
   а прежняя остаётся убранной (инвариант 12). Её «Вернуть» после этого —
   не возврат, а второй живой экземпляр того же дела: дубль на «Сегодня» и
   лишний пункт в знаменателе «N из M». Возврат блока (restoreGroup) делает
   такие прежние записи массовыми: все действия сезонного блока разом
   встали бы в «Убранные» рядом со своими живыми преемниками.

   Поля-ссылки на преемника нет (это поле схемы и решение архитектора),
   поэтому преемник узнаётся по тому, что у пункта видит владелец: то же
   имя, тип, область и блок, а заведён ПОЗЖЕ дня ухода прежней записи —
   ровно так его заводит возврат. Строго позже: возврат в день ухода новой
   записи не заводит, а одноимённое дело, набранное в тот же день, —
   соседнее, а не продолжение. Цепочка уходов и возвратов прячет все
   прежние отрезки: у каждого есть запись позже. Переименованный преемник
   прежний отрезок не прячет — и возврат его честно заведёт дело со
   старым именем.

   Преемник прячет РОВНО ОДНУ прежнюю запись (Р1/ревью). Одноимённые дела
   в одном блоке законны — быстрое добавление повторы не склеивает, — и
   прежде любая поздняя запись прятала их ВСЕ: возврат блока с двумя «Вода»
   заводил копию первой, копия тут же становилась «преемником» второй, и
   вторая не возвращалась и пропадала из «Убранных» без дороги назад. Пары
   ставятся жадно по записям в порядке addedAt: каждой достаётся ещё не
   взятая убранная запись, ушедшая раньше её addedAt, — ближайшая ВЫШЕ по
   items[], а если выше нет — ближайшая ниже. Копия возврата встаёт сразу за
   своей прежней записью, поэтому в пару берёт именно её, и в «Убранных»
   остаются те двойники, которых не возвращали, с их днями ухода. Жадный
   проход на порогах «ушла раньше» даёт наибольшее число пар при любом
   выборе: из N убранных двойников после K возвратов в «Убранных» остаются
   N − K. Запись без пары стоит в «Убранных» и возвращается.
   → запись-преемник | null */
function laterSegmentOf(item) {
  if (!item || live(item) || !item.removedAt) return null;
  const g = groupNameOf(item);
  // у действия — ещё и тот же режим (задача Р2): одноимённое дело в том же
  // по имени блоке другого режима — соседнее, а не продолжение
  const m = isAction(item) ? itemMode(item) : null;
  return successorAmong(store.items.filter(y => y.name === item.name && y.type === item.type &&
    y.area === item.area && groupNameOf(y) === g && (m === null || itemMode(y) === m)), item);
}

/* Преемник упражнения (задача Р2, п. 5) — то же правило, что у пункта:
   возврат позже дня ухода заводит новую запись (restoreExercise), и
   «Вернуть» у прежней завело бы второй живой экземпляр — два одинаковых
   поля на листе «Тренировка». У упражнения нет ни типа, ни области, ни
   блока: преемника узнают по имени и по addedAt строго позже removedAt;
   пара одна на запись — одноимённые упражнения законны. */
function laterExerciseOf(ex) {
  if (!ex || live(ex) || !ex.removedAt) return null;
  return successorAmong(store.exercises.filter(y => y.name === ex.name), ex);
}

/* Общая часть правила преемника: same — записи одного дела в порядке
   своего списка (item входит в них), пары ставятся жадно — см. выше.
   Прошлое правило не двигает: оно только прячет прежнюю запись из
   «Убранных» и не даёт вернуть её вторым экземпляром. */
function successorAmong(same, item) {
  const at = new Map(same.map((y, i) => [y, i]));
  // ближе — выше по списку и вплотную; запись ниже преемника — после всех выше
  const far = (x, y) => (at.get(x) < at.get(y) ? at.get(y) - at.get(x) : same.length + at.get(x));
  const order = same.slice().sort((p, q) =>
    (p.addedAt < q.addedAt ? -1 : p.addedAt > q.addedAt ? 1 : at.get(p) - at.get(q)));
  const taken = new Set();
  for (const y of order) {
    let best = null;
    for (const x of same) {
      if (x === y || live(x) || !x.removedAt || taken.has(x) || !(y.addedAt > x.removedAt)) continue;
      if (!best || far(x, y) < far(best, y)) best = x;
    }
    if (!best) continue;
    if (best === item) return y;
    taken.add(best);
  }
  return null;
}

function restoreItem(id) {
  const r = restoreItemCore(id);
  if (!r) return null;
  if (save()) return r.item;
  r.undo();
  return null;
}

/* Упражнения: то же поле и тот же механизм (A.4.1). Правка тривиальна —
   в прошлых числах «Прогресса» упражнения не участвуют вовсе, у них
   только история нагрузки, а она принадлежит своей записи. */
function removeExercise(id) {
  const ex = findExercise(id);
  if (!ex || !live(ex)) return false;
  ex.removedAt = todayKey();
  if (save()) return true;
  ex.removedAt = null;
  return false;
}

function restoreExercise(id) {
  const at = store.exercises.findIndex(x => x.id === id);
  const ex = at < 0 ? null : store.exercises[at];
  if (!ex || live(ex)) return null;
  if (laterExerciseOf(ex)) return null; // уже продолжено — второй экземпляр был бы дублем (Р2, п. 5)
  const t = todayKey();
  if (ex.removedAt === t) {
    ex.removedAt = null;
    if (save()) return ex;
    ex.removedAt = t;
    return null;
  }
  const copy = {
    id: uid(), name: ex.name, unit: ex.unit, value: ex.value,
    history: typeof ex.value === 'number' ? [{ date: t, value: ex.value }] : [],
    removedAt: null, addedAt: t
  };
  store.exercises.splice(at + 1, 0, copy);
  if (save()) return copy;
  store.exercises.splice(at + 1, 1);
  return null;
}

/* Значение дня у пункта — ТРИ состояния одним ключом (задача Р2, «Не
   сегодня»): `true` — отметка, `false` — пропуск, ключа нет — не отмечено.
   Кодирование выбрано так, чтобы ни одна прежняя проверка истинности
   отметки не поменяла смысла: isMarked, everMarked, marksInSystem,
   itemWeekCount, planWeekCount, weekPlan, closeWeek и счёт отметок
   dataCounts (`=== true`) читают false как «не отмечено» без правки.
   Пропуск — не отметка: пороги планки его не знают (инвариант 4), «Отметки»
   «Прогресса» тоже. Отдельно его читают только знаменатель дня
   (minDayMarks) и сетка разбора. */
function isMarked(dayKey, itemId) {
  return !!(store.days[dayKey] && store.days[dayKey][itemId]);
}

function isSkipped(dayKey, itemId) {
  return !!store.days[dayKey] && store.days[dayKey][itemId] === false;
}

/* Круг пропущенного пункта неактивен: снятие пропуска — «Вернуть», а не тап
   по кругу. Отказ возвращает false и не заводит пустого дня. */
function toggleMark(dayKey, itemId) {
  if (isSkipped(dayKey, itemId)) return false;
  const day = store.days[dayKey] || (store.days[dayKey] = {});
  if (day[itemId]) {
    delete day[itemId];
    if (!Object.keys(day).length) delete store.days[dayKey];
  } else {
    day[itemId] = true;
  }
  save();
  return true;
}

/* «Не сегодня» (задача Р2): пропуск — решение владельца о СЕГОДНЯШНЕМ дне,
   а не отметка и не забытая отметка. Только живое действие минимума,
   запланированное сегодня и ещё не отмеченное: у привычки своя недельная
   норма с разрешёнными пропусками (инвариант 11), у счётчика и параметра
   дневных значений нет, а вчера и раньше из интерфейса правится только
   установкой отметки через точку (инвариант 7). Одна запись, откат при
   отказе хранилища — в памяти не остаётся того, чего нет на диске. */
function skipToday(itemId) {
  const item = store.items.find(i => i.id === itemId);
  const t = todayKey();
  if (!isAction(item) || !dueNow(item, t) || isMarked(t, itemId) || isSkipped(t, itemId)) return false;
  const fresh = !store.days[t];
  const day = store.days[t] || (store.days[t] = {});
  day[itemId] = false;
  if (save()) return true;
  delete day[itemId];
  if (fresh) delete store.days[t];
  return false;
}

/* «Вернуть»: снимает сегодняшний пропуск — пункт снова «не отмечен». Только
   сегодня: вчерашний пропуск из интерфейса не снимается (глубже сегодня
   данные правятся только установкой отметки за вчера, и пропуск ей
   отказывает). Не требует, чтобы пункт по-прежнему стоял в плане: снять
   собственное решение владелец вправе всегда. */
function unskipToday(itemId) {
  const t = todayKey();
  if (!isAction(store.items.find(i => i.id === itemId)) || !isSkipped(t, itemId)) return false;
  const day = store.days[t];
  delete day[itemId];
  const emptied = !Object.keys(day).length;
  if (emptied) delete store.days[t];
  if (save()) return true;
  store.days[t] = day;
  day[itemId] = false;
  return false;
}

/* Отмечался ли пункт хоть раз — не позже дня upto, если он задан.
   Чистая функция от days{}: reviews в счёт не идут (инвариант 4). */
function everMarked(item, upto) {
  for (const k of Object.keys(store.days)) {
    if ((upto === undefined || k <= upto) && store.days[k][item.id]) return true;
  }
  return false;
}

/* «Не пропускай дважды»: пункт существовал вчера и не был отмечен.
   Пропустить можно только начатое (задача 22, п. 2): до первой отметки
   точки нет вовсе — иначе владелец в первый же день видел бы у всей
   программы «вчера — пропуск» за день, в который её ещё не было.

   Начатость считается НЕ ПОЗЖЕ ВЧЕРА (задача 24, п. 7): сегодняшняя
   первая в жизни отметка не делает вчерашний пропуск «начатым» задним
   числом — точка появится назавтра обычным путём. Иначе она рождалась
   в момент тапа, но только полной перерисовкой, и точечный путь с ней
   расходился (находка сторожа задачи 23; решение архитектора — прав
   точечный путь: отметить пункт и тут же получить укор за вчера
   владелец не должен). */
function missedYesterday(item, tKey) {
  const y = addDays(tKey, -1);
  // Применимость вчерашнего дня — одним правилом (задача 29/B): пункт вчера
  // ЖИЛ и вчерашний день недели стоял в его тогдашней маске. Отдельная
  // проверка `addedAt <= y` отсюда ушла: livedOn внутри dueOn делает ровно
  // её, и две копии одного условия разошлись бы при первой же правке.
  // Без маски пункт «только по воскресеньям» показывал бы укор шесть дней
  // из семи, вечно.
  if (!dueOn(item, y) || isMarked(y, item.id)) return false;
  // вчерашний пропуск — решение владельца, а не забытая отметка (задача Р2):
  // укора за него нет, и «отметить» ему не предлагается
  if (isSkipped(y, item.id)) return false;
  return everMarked(item, y);
}

/* Единственная допустимая правка прошлого (инвариант 7): установить отметку
   за вчера через точку-маркер. Только вчера, только установка — не снятие. */
function markYesterday(itemId) {
  const item = store.items.find(i => i.id === itemId);
  if (!item || !live(item) || item.type !== 'daily') return false;
  const y = addDays(todayKey(), -1);
  if (!(item.addedAt <= y)) return false;
  // отметка в день вне расписания невозможна: её негде было бы показать,
  // а в days{} она осталась бы навсегда и считалась бы механиками
  if (!dueOn(item, y)) return false;
  if (isMarked(y, item.id)) return false;
  // пропуск вчера — решение, а не забытая отметка: переписать его отметкой
  // задним числом значило бы править прошлое глубже, чем разрешает инвариант 7
  if (isSkipped(y, item.id)) return false;
  const day = store.days[y] || (store.days[y] = {});
  day[item.id] = true;
  save();
  return true;
}

/* Начало текущего периода счёта тренировок: календарная неделя, а в
   переходные дни до calendarSince — прежняя скользящая отсечка weekStart */
function trainSince() {
  return currentWeekStart() || store.weekStart;
}

function trainCount(itemId) {
  const since = trainSince();
  return store.weekLog.reduce((n, e) => n + (e.itemId === itemId && e.date >= since ? 1 : 0), 0);
}

function incTrain(itemId) {
  store.weekLog.push({ itemId, date: todayKey(), ts: Date.now() });
  save();
}

/* Отмена — только записей текущей недели (прошлое неизменяемо). Вместе с
   записью счётчика уходит и сессия того же дня; рабочая нагрузка
   упражнений при этом НЕ откатывается — история планки правдива
   (задача 16D, «Принятые ограничения»). */
function undoTrain(itemId) {
  const since = trainSince();
  for (let i = store.weekLog.length - 1; i >= 0; i--) {
    const e = store.weekLog[i];
    if (e.itemId !== itemId || e.date < since) continue;
    store.weekLog.splice(i, 1);
    for (let j = store.sessions.length - 1; j >= 0; j--) {
      if (store.sessions[j].date === e.date) { store.sessions.splice(j, 1); break; }
    }
    save();
    return;
  }
}

/* ── Упражнения и тренировки (инвариант 15) ───────────────────
   Упражнение — имя, единица и рабочая нагрузка с историей по общим
   правилам планки (recordBar). Сессия — факт тренировки: день,
   значения упражнений и заметка. */

const liveExercises = () => store.exercises.filter(live);

function findExercise(id) {
  return store.exercises.find(e => e.id === id) || null;
}

function addExercise(name, unit, value) {
  const nm = (name || '').trim();
  if (!nm) return null;
  const v = (typeof value === 'number' && isFinite(value) && value > 0) ? value : null;
  const ex = {
    id: uid(), name: nm, unit: (unit || '').trim(), value: v,
    history: v === null ? [] : [{ date: todayKey(), value: v }],
    removedAt: null, addedAt: todayKey()
  };
  store.exercises.push(ex);
  save();
  return ex;
}

function updateExercise(id, name, unit) {
  const ex = findExercise(id);
  if (!ex) return false;
  const nm = (name || '').trim();
  if (!nm) return false; // безымянное упражнение не сохраняется — как у пунктов
  ex.name = nm;
  ex.unit = (unit || '').trim();
  save();
  return true;
}

/* Соседи упражнения по перестановке — только живые: убранное стоит в
   «Убранных», в порядке не участвует, но своё место в массиве сохраняет.
   Тот же приём, что у siblingIndexes для пунктов (инвариант 17). */
function exerciseIndexes() {
  const out = [];
  store.exercises.forEach((x, i) => { if (live(x)) out.push(i); });
  return out;
}

function moveExercise(id, dir) {
  const i = store.exercises.findIndex(e => e.id === id);
  if (i < 0) return false;
  const idxs = exerciseIndexes();
  const at = idxs.indexOf(i);
  if (at < 0) return false; // убранное не двигается: его строки в списке нет
  const j = idxs[at + (dir === 'up' ? -1 : 1)];
  if (j === undefined) return false;
  const t = store.exercises[i];
  store.exercises[i] = store.exercises[j];
  store.exercises[j] = t;
  save();
  return true;
}

/* Заметок и выписок в интерфейсе больше нет: экран снят задачей 28.C.
   Доменные функции (addNote, updateNote, deleteNote, notesByDate, quotes,
   quoteOfDay) ушли вместе с ним. ДАННЫЕ ОСТАЛИСЬ: `store.notes[]` живёт,
   нормализация в migrate его валидирует, счёт потерь импорта его считает,
   экспорт отдаёт целиком. Снятие поля — отдельное решение владельца, и до
   него ни один байт заметок не теряется. */

/* Запись тренировки: сессия дня, обновление нагрузок (история — только
   при изменении) и инкремент недельного счётчика, как раньше. */
function recordSession(weeklyId, entries, note) {
  const list = (Array.isArray(entries) ? entries : [])
    .map(e => ({ exId: e.exId, value: e.value }))
    .filter(e => findExercise(e.exId) && typeof e.value === 'number' && isFinite(e.value) && e.value > 0);
  store.sessions.push({ id: uid(), date: todayKey(), entries: list, note: (note || '').trim() });
  for (const e of list) {
    const ex = findExercise(e.exId);
    if (ex.value === e.value) continue; // нагрузка не менялась — истории нечего писать
    ex.value = e.value;
    recordBar(ex, e.value);
  }
  incTrain(weeklyId); // save() внутри
  return true;
}

/* Календарные недели (инвариант 2): понедельник–воскресенье в логических днях */
function currentWeekStart() {
  const t = todayKey();
  const since = store.settings.calendarSince;
  if (!isDayKey(since) || t < since) return null; // переходные дни до calendarSince
  return weekStartOf(t);
}

function previousWeekStart() {
  const cur = currentWeekStart();
  return cur ? addDays(cur, -7) : null;
}

/* Разбор предлагается только за последнюю завершённую календарную неделю.
   Пропущенные недели тихо проходят; скользящие записи reviews (без week)
   первый календарный разбор не блокируют. Уже разобранная неделя не
   разбирается повторно, где бы её запись ни стояла в reviews. */
function reviewDue() {
  const prev = previousWeekStart();
  if (!prev || prev < store.settings.calendarSince) return false;
  return !store.reviews.some(r => r.week === prev);
}

/* Окно разбора — ровно последняя завершённая неделя; сегодня не входит */
function windowKeys() {
  const prev = previousWeekStart();
  const keys = [];
  if (!prev) return keys;
  for (let i = 0; i < 7; i++) keys.push(addDays(prev, i));
  return keys;
}

/* Последнее записанное «одно изменение» — тихая строка на разборе (инвариант 3) */
function currentOneChange() {
  const last = store.reviews[store.reviews.length - 1];
  const s = (last && typeof last.oneChange === 'string') ? last.oneChange.trim() : '';
  return s || null;
}

/* ── Планка: повышение и понижение (инвариант 4) ─────────────
   Обе механики считают по КАЛЕНДАРНЫМ неделям и отметкам в days{},
   а не по массиву reviews: пропущенный разбор недели не съедает —
   она просто проходит тихо (инвариант 2), а механика продолжает
   работать. Якорь решения — понедельник недели, в которую оно
   принято; следующее предложение возможно, когда все нужные недели
   строго позже якоря. */

/* Пороги планки — ДОЛИ от числа дней расписания недели m (задача 29/B).
   При m = 7 обязаны дать ровно прежние 6 и 3: существующим пунктам
   механика не меняется ни на волос, и это закреплено тестом.
   Пункт «только по воскресеньям» (m = 1) прежде не мог набрать «6 из 7»
   НИКОГДА — молчаливый запрет по данным, которых владелец не видит, ровно
   тот, что задача 28.D сняла в этом же файле, — и одновременно проходил
   «≤3 из 7» при безупречном исполнении, получая «Сделать легче» каждую
   неделю. Доли лечат оба конца разом. */
const raiseNeed = m => Math.ceil(6 / 7 * m);
const lowerNeed = m => Math.floor(3 / 7 * m);

/* Числитель порогов — отметки в ДНЯХ ПЛАНА недели mon: тот же фильтр, что у
   знаменателя weekMaskDays (inEffectiveDays), по правилу инварианта 12
   («числитель фильтруется тем же правилом, что знаменатель», как у
   «Отметок» и сетки разбора weekPlan). Отметка вне плана возникает без
   ошибки владельца: отметил сегодня, потом в тот же день сузил дни блока
   или свои дни — отметка в days{} остаётся навсегда, а день из плана
   ушёл. Прежде такая отметка засчитывалась: повышение предлагалось при
   одной отметке из двух по плану, а сетка разбора показывала «1 из 2».
   При m = 7 результат прежний: отметок в днях вне эффективной маски
   интерфейс не создаёт, а дни до addedAt в знаменатель и так входят. */
function planWeekCount(item, mon) {
  let n = 0;
  for (let i = 0; i < 7; i++) {
    const k = addDays(mon, i);
    if (isMarked(k, item.id) && inEffectiveDays(item, k)) n++;
  }
  return n;
}

/* Знаменатель в тексте карточки понижения: у пункта без расписания обе
   недели дают 7, и «из 7 и 7» читалось бы канцелярией — печатается одно
   число. Расписание сменилось внутри окна — числа разные, и оба названы. */
function lowDen(item, weeks) {
  const ms = weeks.map(w => weekMaskDays(item, w));
  return ms.every(m => m === ms[0]) ? String(ms[0]) : ms.join(' и ');
}

/* Критерий повышения: 3 последние закрытые недели, в каждой ≥ ceil(6/7·m) */
function raiseEligible(item) {
  if (item.type !== 'daily' || !live(item) || item.area !== 'min') return false; // повышение — только минимум
  if (!(typeof item.value === 'number' && isFinite(item.value) && item.value > 0)) return false;
  // Здесь стоял guard «пункт с живой лестницей повышения не получает»: шаг
  // ступени и шаг планки в одну неделю были бы двумя изменениями за раз.
  // Лестница снята задачей 28.D — второго шага в приложении не осталось, и
  // guard стал бы запретом по данным, которых владелец не видит: пункт,
  // носивший лестницу, молча не получал бы предложения никогда. Критерий
  // теперь один — три закрытые недели по ≥6 из 7.
  const W = closedWeeks(3);
  if (W.length < 3) return false;
  const ms = W.map(w => weekMaskDays(item, w));
  // Неделя без единого дня в плане — не «держится» и не «не держится»:
  // её просто не было. raiseNeed(0) = lowerNeed(0) = 0 сделали бы такой
  // пункт готовым И к повышению, И к понижению в одном разборе — это
  // ломает взаимоисключаемость инварианта 4. Пустая эффективная маска
  // достижима с блоками (своя маска ∧ дни блока = ни одного дня). При
  // m ≥ 1 поведение прежнее ни на волос.
  if (ms.some(m => m === 0)) return false;
  if (!W.every((w, i) => planWeekCount(item, w) >= raiseNeed(ms[i]))) return false;
  return item.raiseAfterWeek === null || W[0] > item.raiseAfterWeek;
}

/* Критерий понижения: 2 последние закрытые недели, в каждой ≤ floor(3/7·m).
   Значения планки критерий не требует: пункт без числа тоже может
   не держаться — решение по нему сводится к «Оставить». */
function lowerEligible(item) {
  if (item.type !== 'daily' || !live(item) || item.area !== 'min') return false;
  const W = closedWeeks(2);
  if (W.length < 2) return false;
  // пункт должен существовать во ВСЕХ рассматриваемых неделях (A.4.1):
  // иначе «0 и 0 из 7» у заведённого сегодня пункта проходило как
  // «не держится» и разбор предлагал облегчить планку за недели, в
  // которые пункта не было (аудит, находка 6). Повышению такая защита
  // не нужна: там критерий требует ≥6 отметок, а у нового пункта их нет.
  if (!(item.addedAt <= W[0])) return false;
  // и пункт должен быть начат (задача 22, п. 1): ни одной отметки за всё
  // время до конца окна — облегчать нечего, планка ещё не проверялась.
  // Владелец, засеявший программу и не успевший отметиться, получал
  // предложение урезать её за недели, в которые не мог отмечаться.
  if (!everMarked(item, addDays(W[W.length - 1], 6))) return false;
  const ms = W.map(w => weekMaskDays(item, w));
  // тот же guard, что у повышения: в неделе без дней плана облегчать нечего
  if (ms.some(m => m === 0)) return false;
  if (!W.every((w, i) => planWeekCount(item, w) <= lowerNeed(ms[i]))) return false;
  return item.lowerAfterWeek === null || W[0] > item.lowerAfterWeek;
}

/* Все пункты, готовые к повышению, — по порядку items[] */
const raiseReady = () => store.items.filter(raiseEligible);

/* Пункт, которому в ЭТОМ разборе предлагается повышение: первый готовый
   по порядку items[], и только если решения по планке вверх на текущей
   неделе ещё не было. «Принять» и «Не сейчас» ставят один и тот же якорь
   raiseAfterWeek = currentWeekStart() (инвариант 4) — он и служит признаком
   принятого решения, ровно как steppedWeek у лестницы. Отдельного поля
   и схемы это не требует.

   Так правило «одно изменение за раз» становится механикой (задача 24,
   п. 6): на трёх идеальных неделях посева готовы были все четыре числовых
   пункта минимума, и разбор предлагал повысить программу целиком.
   Остальные не запрещаются — их предложение просто откладывается
   на следующую неделю прежним путём, якорей это не трогает. */
function raiseOffer() {
  const cur = currentWeekStart();
  if (cur && store.items.some(i => i.raiseAfterWeek === cur)) return null;
  return store.items.find(raiseEligible) || null;
}

function raiseSuggest(v) {
  return v <= 12 ? v + 1 : Math.round(v * 1.1);
}

/* Насколько легче: крупная планка — на четверть, мелкая — на единицу,
   единице и ниже облегчать нечего (null — кнопки шага нет) */
function lowerSuggest(v) {
  if (!(typeof v === 'number' && isFinite(v))) return null;
  if (v > 12) return Math.round(v * 0.75);
  if (v >= 2) return v - 1;
  return null;
}

/* Якорь недели решения: и «Принять», и «Не сейчас» ставят его одинаково —
   отсчёт трёх недель начинается заново, с недели строго после текущей */
function resetRaiseCount(item) {
  item.raiseAfterWeek = currentWeekStart();
  save();
}

/* Понижение: планка меняется немедленно и попадает в срез разбора */
function acceptLower(item, newValue) {
  const from = item.value;
  item.value = newValue;
  recordBar(item, newValue);
  store.pendingLowers.push({ itemId: item.id, name: item.name, from, to: newValue });
  item.lowerAfterWeek = currentWeekStart();
  save();
}

/* «Оставить»: планка не меняется, решение по неделе зафиксировано */
function keepBar(item) {
  item.lowerAfterWeek = currentWeekStart();
  save();
}

/* Запись изменения планки в историю пункта.
   Повторное изменение в тот же день заменяет последнюю запись,
   возврат к прежнему значению схлопывает её — в истории остаётся
   движение по неделям без правок и дублей («5 → 5» не бывает). */
function recordBar(item, newValue) {
  if (!Array.isArray(item.history)) item.history = [];
  const last = item.history[item.history.length - 1];
  if (last && last.date === todayKey()) {
    const prev = item.history[item.history.length - 2];
    if (prev && prev.value === newValue) item.history.pop();
    else last.value = newValue;
  } else if (!last || last.value !== newValue) {
    item.history.push({ date: todayKey(), value: newValue });
  }
}

function acceptRaise(item, newValue) {
  const from = item.value;
  item.value = newValue;
  recordBar(item, newValue);
  store.pendingRaises.push({ itemId: item.id, name: item.name, from, to: newValue });
  item.raiseAfterWeek = currentWeekStart();
  save();
}

/* ── Параметры недели и готовность к новой привычке (area habit) ── */

function fmtParam(item, v) {
  const val = (v === undefined) ? item.pvalue : v;
  if (item.pkind === 'time') {
    const m = ((Math.round(val) % 1440) + 1440) % 1440;
    return pad2(Math.floor(m / 60)) + ':' + pad2(m % 60);
  }
  return item.unit ? `${val} ${item.unit}` : String(val);
}

function paramStepTarget(item) {
  if (item.pkind === 'time') return (((item.pvalue + item.pstep) % 1440) + 1440) % 1440;
  return item.pvalue + item.pstep;
}

/* Решение по параметру, принадлежащее разбираемой неделе;
   решение чужой недели — как отсутствие решения */
function paramDecision(itemId) {
  const d = store.paramDecided[itemId];
  return (d && d.week === previousWeekStart()) ? d : null;
}

/* Параметры, ждущие решения в этом разборе: их карточки живут в видимой
   части, в «Решении 1» (задача 24, п. 2) — порог параметра меняется тем
   же recordBar и той же историей, что планка пункта. Решённые остаются
   под свёрткой тихой строкой итога: это уже read-only справка. */
const pendingParams = () => store.items.filter(i => i.type === 'param' && live(i) && !paramDecision(i.id));

/* Одно решение на параметр за разбор (инвариант 10); шаг применяется немедленно */
function applyParamStep(itemId) {
  const item = store.items.find(i => i.id === itemId);
  if (!item || item.type !== 'param' || !live(item)) return false;
  if (!reviewDue() || paramDecision(itemId)) return false;
  const from = item.pvalue;
  item.pvalue = paramStepTarget(item);
  recordBar(item, item.pvalue); // история порога — по общим правилам истории планки
  store.paramDecided[itemId] = { week: previousWeekStart(), from, to: item.pvalue };
  save();
  return true;
}

function keepParam(itemId) {
  const item = store.items.find(i => i.id === itemId);
  if (!item || item.type !== 'param' || !live(item)) return false;
  if (!reviewDue() || paramDecision(itemId)) return false;
  store.paramDecided[itemId] = { week: previousWeekStart(), from: item.pvalue, to: null };
  save();
  return true;
}

/* ── Норма и серия привычки (инвариант 11) ──────────────────
   Чистые функции от days{}, normPerWeek и календаря: разбор и
   closeWeek на них не влияют, смена нормы ретроактивна. */

/* Отметки ежедневного пункта в календарной неделе с понедельником mon —
   общая функция для обеих областей (инвариант 10) */
function itemWeekCount(item, mon) {
  let n = 0;
  for (let i = 0; i < 7; i++) if (isMarked(addDays(mon, i), item.id)) n++;
  return n;
}

/* Отметки привычки в той же неделе — тонкая обёртка (инвариант 11) */
function habitWeekCount(item, mon) {
  return itemWeekCount(item, mon);
}

/* Серия, считая назад от недели lastWeek включительно; недели существуют
   только с calendarSince (инвариант 2). Неделя без нормы обрывает счёт,
   поэтому цикл конечен: пустых недель норма (≥1) не набирает. */
function habitStreakFrom(item, lastWeek) {
  const since = store.settings.calendarSince;
  if (!isDayKey(since)) return 0;
  let n = 0;
  for (let w = lastWeek; w >= since; w = addDays(w, -7)) {
    if (habitWeekCount(item, w) < (item.normPerWeek || 7)) break;
    n++;
  }
  return n;
}

/* Серия на сегодня: от последней завершённой недели; текущая не входит —
   сегодняшние тапы серию не меняют */
function habitStreak(item) {
  const cur = currentWeekStart();
  return cur ? habitStreakFrom(item, addDays(cur, -7)) : 0;
}

/* Информационная готовность: 2 последние ЗАВЕРШЁННЫЕ календарные недели
   каждая активная привычка выполнила норму (≥ normPerWeek; норма
   ретроактивна — берётся текущая).
   Считает по days{} и календарю, как и всё остальное в программе роста.
   Раньше читала два последних элемента reviews — и тогда десять идеальных
   недель без единого разбора готовности не давали, а два разбора
   полугодовой давности давали её при пустых последних неделях
   (аудит, находка 5). reviews — архив, а не источник. */
function habitsSteady() {
  const habits = store.items.filter(i => i.type === 'daily' && i.area === 'habit' && live(i));
  if (!habits.length) return false;
  const W = closedWeeks(2);
  if (W.length < 2) return false;
  return W.every(w => habits.every(h => habitWeekCount(h, w) >= (h.normPerWeek || 7)));
}

/* ── Закрытые недели ───────────────────────────────────────────
   Общий счётчик календаря. Жил в разделе лестницы, но читателей у него
   всегда было больше: повышение и понижение планки, готовность привычек,
   консистентность в разборе. Лестница снята задачей 28.D — функция
   осталась и переехала под собственный заголовок. */

/* До n понедельников последних ЗАВЕРШЁННЫХ календарных недель, по
   возрастанию; текущая не входит, недели до calendarSince не существуют
   (инвариант 2) — при нехватке список короче n. */
function closedWeeks(n) {
  const out = [];
  const cur = currentWeekStart();
  const since = store.settings.calendarSince;
  if (!cur || !isDayKey(since)) return out;
  for (let i = 1; i <= n; i++) {
    const w = addDays(cur, -7 * i);
    if (w < since) break;
    out.unshift(w);
  }
  return out;
}

/* ── Блоки (инвариант 13) ──────────────────────────────────
   Блок — запись в store.groups: {name, caption, days, removedAt}. Пункт
   хранит имя блока строкой; неизвестное имя не ошибка — такой пункт просто
   идёт без заголовка последним. Блок из двух и более активных пунктов и
   есть цепочка.

   С задачи «Расписание 1/3» блок несёт дни недели, от которых зависят
   прошлые числа его действий. По словарю конституции («удаляется только
   то, от чего не зависит ни одно прошлое число») он переходит из
   «удаляется» в «убирается»: removeGroup/restoreGroup — тот же отрезок
   жизни, что у пункта (инвариант 12). */

const groupNameOf = it => (it.group || '').trim();

/* ── Режимы (задача Р2) ─────────────────────────────────────────
   Режим — набор блоков и действий: «Школа», «Каникулы». Активный режим
   хранится ОТРЕЗКАМИ журнала store.modeLog = [{from, mode}] — по той же
   причине, что расписание пункта и дни блока: режим, положенный одним
   значением, сегодняшним переключением переписал бы прошлые дни. День
   читается режимом, действовавшим В ТОТ ДЕНЬ; до первого отрезка и при
   пустом журнале — основной.

   Блок и действие (daily min, в блоке и без) принадлежат ровно одному
   режиму: поле mode ставится при создании и не меняется. Привычки,
   параметры и недельный счётчик глобальны — поля не несут и ссылаются на
   блок только именем. Имя блока уникально В ПРЕДЕЛАХ режима. */

const isAction = it => !!it && it.type === 'daily' && it.area === 'min';

/* Режим записи. «Поля нет» каноническим не считается (migrate его ставит),
   но читается основным — как пустой список отрезков читается «все семь»:
   запись, собранная руками без поля, не выпадает из всех дней. */
const itemMode = it => (it && typeof it.mode === 'string' && it.mode ? it.mode : MAIN_MODE);
const blockMode = g => (g && typeof g.mode === 'string' && g.mode ? g.mode : MAIN_MODE);

/* Режим, действовавший в дне: последний отрезок с from ≤ dayKey, двоичным
   поиском — через эту функцию серия и рекорд проходят каждый день эпохи,
   и проход по сотням отрезков на каждый день был бы квадратом. log — для
   migrate, у которой store ещё не назначен. */
function modeOn(dayKey, log) {
  const segs = Array.isArray(log) ? log : (store && Array.isArray(store.modeLog) ? store.modeLog : []);
  let lo = 0, hi = segs.length - 1, at = -1;
  while (lo <= hi) {
    const mid = (lo + hi) >> 1;
    if (segs[mid].from <= dayKey) { at = mid; lo = mid + 1; } else hi = mid - 1;
  }
  return at < 0 ? MAIN_MODE : segs[at].mode;
}

const activeMode = () => modeOn(todayKey());

const modeList = () => (Array.isArray(store.modes) ? store.modes : []);
const findMode = id => modeList().find(m => m.id === id) || null;
const liveModes = () => modeList().filter(live);

/* Запись в области режима: действие — только своего, глобальный пункт —
   любого (он ссылается на блок именем) */
const belongsToMode = (it, mode) => !isAction(it) || itemMode(it) === mode;

/* Имя режима свободно среди ВСЕХ режимов, включая убранные: убранный режим
   возвращается, и два одноимённых в выборе было бы нечем различить */
function modeNameTaken(name, exceptId) {
  const n = String(name ?? '').trim();
  return modeList().some(m => m.id !== exceptId && m.name === n);
}

/* Новый режим — в конец списка, не активным: выбор — отдельное решение
   («Выбрать»). copyOf — id режима-источника: копия берёт его ЖИВЫЕ блоки
   (подпись, дни на сегодня одним отрезком, тот же порядок) и ЖИВЫЕ
   действия — и в блоках, и без блока — новыми записями с сегодняшнего дня:
   новые id, своя маска на сегодня, журнал принадлежности пуст, история —
   одна стартовая запись при числе. Прошлого у копии нет. Привычки,
   параметры и недельные счётчики не копируются: они глобальны и в новом
   режиме и так есть. Копия сразу каноническая — migrate от неё ничего не
   меняет. Одна запись; отказ — откат всего.
   → {ok: true, id} | {ok: false, reason}: 'empty', 'taken', 'missing'
   (источника нет), 'storage'. */
function addMode(name, opts) {
  const n = String(name ?? '').trim();
  if (!n) return { ok: false, reason: 'empty' };
  if (modeNameTaken(n)) return { ok: false, reason: 'taken' };
  const copyOf = opts && opts.copyOf !== undefined && opts.copyOf !== null ? opts.copyOf : null;
  if (copyOf !== null && !findMode(copyOf)) return { ok: false, reason: 'missing' };
  if (!Array.isArray(store.modes)) store.modes = programModes();
  const id = uid();
  const t = todayKey();
  const mode = { id, name: n, removedAt: null };
  const blocks = [];
  const copies = [];
  if (copyOf !== null) {
    for (const g of store.groups) {
      if (!live(g) || blockMode(g) !== copyOf) continue;
      const m = blockMaskNow(g);
      blocks.push({
        name: g.name,
        caption: typeof g.caption === 'string' ? g.caption : '',
        days: m !== WEEK_ALL ? [{ from: t, mask: m }] : [],
        removedAt: null,
        mode: id
      });
    }
    for (const src of store.items) {
      if (!live(src) || !isAction(src) || itemMode(src) !== copyOf) continue;
      copies.push(actionCopy(src, src.group, id, t));
    }
  }
  store.modes.push(mode);
  store.groups.push(...blocks);
  store.items.push(...copies);
  if (save()) return { ok: true, id };
  store.modes.splice(store.modes.indexOf(mode), 1);
  store.groups.splice(store.groups.length - blocks.length, blocks.length);
  store.items.splice(store.items.length - copies.length, copies.length);
  return { ok: false, reason: 'storage' };
}

/* Копия действия новой записью с сегодняшнего дня — общая часть копии
   режима и копии блока в другой режим. Своя маска — на сегодня, прошлого
   нет: история — одна стартовая запись при числе, журнал принадлежности
   пуст. Поля перечислены: забытое терялось бы молча. */
function actionCopy(src, group, mode, t) {
  return {
    id: uid(), name: src.name, value: src.value, unit: src.unit, type: 'daily', area: 'min',
    goal: null, note: src.note, group, removedAt: null, addedAt: t, at: src.at || '',
    raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null,
    history: typeof src.value === 'number' ? [{ date: t, value: src.value }] : [],
    formula: null, ladder: null, ladderLog: [],
    schedule: [{ from: t, mask: scheduleNow(src) }], groupLog: [], mode
  };
}

/* Переименование режима одной записью. Пустое и занятое — отказ.
   → {ok: true} | {ok: false, reason}: 'missing', 'empty', 'taken', 'storage' */
function renameMode(id, name) {
  const m = findMode(id);
  if (!m) return { ok: false, reason: 'missing' };
  const n = String(name ?? '').trim();
  if (!n) return { ok: false, reason: 'empty' };
  if (modeNameTaken(n, id)) return { ok: false, reason: 'taken' };
  const was = m.name;
  m.name = n;
  if (save()) return { ok: true };
  m.name = was;
  return { ok: false, reason: 'storage' };
}

/* Уход режима — из выбора, с сегодняшнего дня. Режим с историей не
   удаляется, а убирается: прошлые дни читаются режимом того дня, и
   отрезки журнала, блоки и действия не трогаются вовсе — ни один прошлый
   день не сдвигается. Активный режим убрать нельзя: сегодняшний день
   остался бы без режима, который можно выбрать.
   → {ok: true} | {ok: false, reason}: 'missing', 'removed', 'active', 'storage' */
function removeMode(id) {
  const m = findMode(id);
  if (!m) return { ok: false, reason: 'missing' };
  if (!live(m)) return { ok: false, reason: 'removed' };
  if (activeMode() === id) return { ok: false, reason: 'active' };
  m.removedAt = todayKey();
  if (save()) return { ok: true };
  m.removedAt = null;
  return { ok: false, reason: 'storage' };
}

/* → {ok: true} | {ok: false, reason}: 'missing', 'live', 'storage' */
function restoreMode(id) {
  const m = findMode(id);
  if (!m) return { ok: false, reason: 'missing' };
  if (live(m)) return { ok: false, reason: 'live' };
  const was = m.removedAt;
  m.removedAt = null;
  if (save()) return { ok: true };
  m.removedAt = was;
  return { ok: false, reason: 'storage' };
}

/* Выбор активного режима — отрезок с сегодняшнего дня, правила setSchedule
   (инвариант 12): хвост from ≥ сегодня снимается (повторный выбор в тот же
   день заменяет, отрезок «из будущего» не встанет перед новым), возврат к
   прежнему режиму схлопывает. Прежние отрезки не трогаются никогда:
   переключение сегодня не меняет ни вчера, ни цепь, ни серию, ни рекорд.
   Выбрать можно только живой режим. Одна запись; отказ — откат журнала.
   → {ok: true} | {ok: false, reason}: 'missing', 'removed', 'storage' */
function setActiveMode(id) {
  const m = findMode(id);
  if (!m) return { ok: false, reason: 'missing' };
  if (!live(m)) return { ok: false, reason: 'removed' };
  if (!Array.isArray(store.modeLog)) store.modeLog = [];
  const log = store.modeLog;
  const was = log.slice();
  const t = todayKey();
  while (log.length && log[log.length - 1].from >= t) log.pop();
  const prev = log.length ? log[log.length - 1].mode : MAIN_MODE;
  if (prev !== id) log.push({ from: t, mode: id });
  if (save()) return { ok: true };
  log.length = 0;
  log.push(...was);
  return { ok: false, reason: 'storage' };
}

/* Поиск блока В РЕЖИМЕ, среди живых и убранных: имя уникально в пределах
   режима, и прошлое действия, чей блок убран, читает дни того блока. Режим
   не назван — активный: так ищут формы и списки, которые показывают его. */
function findGroup(name, mode) {
  const n = String(name ?? '').trim();
  const m = mode === undefined ? activeMode() : mode;
  return store.groups.find(g => g.name === n && blockMode(g) === m) || null;
}

/* Живые блоки режима (по умолчанию — активного) в порядке store.groups */
const liveGroups = mode => {
  const m = mode === undefined ? activeMode() : mode;
  return store.groups.filter(g => live(g) && blockMode(g) === m);
};

/* Занято ли имя В РЕЖИМЕ. Блок ищется ПО ИМЕНИ, поэтому занятым считается
   не только имя блока режима (живого или убранного), но и имя, оставшееся
   в действиях режима — в item.group или в журнале принадлежности, — если
   это не сам блок except. Переименование давнего блока в такое
   «осиротевшее» имя (импорт, «(нет в списке)») задним числом применило бы
   его дни к прошлому чужих действий. Глобальные пункты имя не занимают:
   дни блока их не режут (задача Р2). Пустое имя — не имя. */
function nameTaken(name, except, mode) {
  const n = String(name ?? '').trim();
  if (!n) return false;
  if (except !== undefined && except !== null && n === String(except).trim()) return false;
  const m = mode === undefined ? activeMode() : mode;
  if (store.groups.some(g => g.name === n && blockMode(g) === m)) return true;
  return store.items.some(it => isAction(it) && itemMode(it) === m && (groupNameOf(it) === n ||
    (Array.isArray(it.groupLog) && it.groupLog.some(e => e && e.group === n))));
}

/* Новый блок — в конец списка. Занятое среди всех блоков имя — отказ.
   Осиротевшее имя (есть в пунктах, нет в store.groups) РАЗРЕШЕНО: дни
   нового блока пусты или начинаются сегодня, и прошлое не двигается.
   Но у осиротевшего имени ЕСТЬ действия — они войдут в новый блок, и его
   дни с сегодняшнего дня лягут на их свои дни. Действие, у которого не
   останется ни одного дня, — та же пустая эффективная маска, которую
   updateGroup не создаёт (п. 1.3): отказ, как у правки (Р1/рецензия).
   Вызов addGroup(name) без подписи и дней остаётся прежним: «все семь» —
   нейтральный элемент ∧ и ни одного дня не отнимает. */
function addGroup(name, caption, mask, mode) {
  const n = String(name ?? '').trim();
  const md = mode === undefined ? activeMode() : mode; // новый блок — в активном режиме (Р2)
  if (!n || findGroup(n, md)) return false;
  const m = mask === undefined ? WEEK_ALL : mask;
  if (!isMask(m) || maskDays(m) === 0) return false; // пустая маска — не дни
  if (zeroDaysIn(n, m, md).length) return false;
  store.groups.push({
    name: n,
    caption: String(caption ?? '').trim(),
    days: m === WEEK_ALL ? [] : [{ from: todayKey(), mask: m }],
    removedAt: null,
    mode: md
  });
  save();
  return true;
}

/* Индексы живых блоков режима в store.groups — соседи по перестановке.
   Убранный блок и блок другого режима в списке не стоят и соседями не
   считаются, а своё место в массиве сохраняют: ровно как siblingIndexes у
   пунктов (инвариант 17). */
function liveGroupIndexes(mode) {
  const m = mode === undefined ? activeMode() : mode;
  const out = [];
  store.groups.forEach((g, i) => { if (live(g) && blockMode(g) === m) out.push(i); });
  return out;
}

function moveGroup(name, dir, mode) {
  const m = mode === undefined ? activeMode() : mode;
  const i = store.groups.findIndex(g => g.name === String(name ?? '').trim() && blockMode(g) === m);
  if (i < 0) return false;
  const idxs = liveGroupIndexes(m);
  const at = idxs.indexOf(i);
  if (at < 0) return false; // убранный блок не двигается: его строки в списке нет
  const j = idxs[at + (dir === 'up' ? -1 : 1)];
  if (j === undefined) return false;
  const t = store.groups[i];
  store.groups[i] = store.groups[j];
  store.groups[j] = t;
  save();
  return true;
}

/* Переименование без записи: имя в списке, item.group и журналы
   принадлежности ВСЕХ пунктов меняются вместе. Журнал переписывается
   потому, что имя в нём — ссылка на тот же блок: иначе прошлые дни
   действия искали бы блок по имени, которого больше нет, и теряли бы его
   дни задним числом. */
function renameGroupCore(g, from, to) {
  g.name = to;
  const m = blockMode(g);
  // Глобальные пункты (привычки, параметры, счётчики) ссылаются на блок
  // только именем, а одноимённый блок может жить и в другом режиме (Р2).
  // Имя остаётся прежним у всех глобальных пунктов, если их ДЕРЖИТ
  // одноимённый блок — живой блок живого режима (holdsName, задача Р3,
  // п. 0.3). Убранный блок не держит никого (Р2/рецензия: живую привычку
  // при имени без единого живого блока следующий старт оживил бы вместе с
  // блоком), и с задачи Р3 — даже пункт, ушедший с ним в один день: такой
  // пункт идёт за переименованием и встаёт в «Убранные» под новым именем
  // (goneBesideBlock), а возврат того блока его уже не уводит назад. Блок
  // убранного режима не держит тоже: владелец его не видит нигде.
  const held = store.groups.some(x => x !== g && x.name === from && holdsName(x));
  for (const it of store.items) {
    if (isAction(it)) {
      if (itemMode(it) !== m) continue; // действия чужого режима — при своём блоке
      if (groupNameOf(it) === from) it.group = to;
      if (Array.isArray(it.groupLog)) for (const e of it.groupLog) if (e.group === from) e.group = to;
    } else if (groupNameOf(it) === from && !held) {
      it.group = to;
    }
  }
}

/* Переименование атомарно. Пустое имя и занятое (nameTaken) не проходят. */
function renameGroup(oldName, newName, mode) {
  const from = String(oldName ?? '').trim();
  const to = String(newName ?? '').trim();
  const g = findGroup(from, mode);
  if (!g || !to) return false;
  if (to !== from && nameTaken(to, from, blockMode(g))) return false; // имена уникальны в режиме
  if (to !== from) renameGroupCore(g, from, to);
  save();
  return true;
}

/* Действия блока, у которых при маске блока mask не останется ни одного
   дня: живые daily min режима с пустым ∧ нынешней своей маски и mask.
   Такое состояние интерфейс создавать не даёт — пустая эффективная маска
   неотличима от убранного пункта, а убирать умеет «Убрать». */
function zeroDaysIn(groupName, mask, mode) {
  const n = String(groupName ?? '').trim();
  const m = mode === undefined ? activeMode() : mode;
  return store.items
    .filter(it => live(it) && isAction(it) && itemMode(it) === m && groupNameOf(it) === n &&
      maskDays(andMask(scheduleNow(it), mask)) === 0)
    .map(it => it.name);
}

/* Правка блока одной записью: имя, подпись, дни. Все проверки — ДО
   записи, частичной записи нет; отказ хранилища откатывает всё целиком.
   → {ok: true, name} | {ok: false, reason, names?}; reason: 'missing' (блока
   нет), 'empty', 'removed' (имя носит убранный блок), 'taken', 'nodays'
   (маска пуста или невалидна), 'zero' (у действий names не останется
   дней), 'storage'. Дни не менялись — нуль, пришедший импортом, не отказ:
   владелец правил не их. */
function updateGroup(oldName, patch, mode) {
  const from = String(oldName ?? '').trim();
  const g = findGroup(from, mode);
  if (!g) return { ok: false, reason: 'missing' };
  const md = blockMode(g); // все проверки — в режиме самого блока (Р2)
  const p = (patch && typeof patch === 'object') ? patch : {};
  const to = p.name === undefined ? from : String(p.name ?? '').trim();
  if (!to) return { ok: false, reason: 'empty' };
  if (to !== from) {
    const other = findGroup(to, md);
    if (other && !live(other)) return { ok: false, reason: 'removed' };
    if (nameTaken(to, from, md)) return { ok: false, reason: 'taken' };
  }
  const nowMask = blockMaskNow(g);
  const mask = p.mask === undefined ? nowMask : p.mask;
  if (!isMask(mask) || maskDays(mask) === 0) return { ok: false, reason: 'nodays' };
  if (mask !== nowMask) {
    const names = zeroDaysIn(from, mask, md);
    if (names.length) return { ok: false, reason: 'zero', names };
  }
  const caption = p.caption === undefined
    ? (typeof g.caption === 'string' ? g.caption : '')
    : String(p.caption ?? '').trim();

  // снимок всего, что может измениться, — для отката при отказе записи
  const was = { name: g.name, caption: g.caption, days: g.days };
  const itemsWas = store.items.map(it => ({
    it, group: it.group, hadLog: 'groupLog' in it,
    log: Array.isArray(it.groupLog) ? it.groupLog.map(e => Object.assign({}, e)) : it.groupLog
  }));
  g.days = Array.isArray(g.days) ? g.days.map(x => Object.assign({}, x)) : [];
  if (to !== from) renameGroupCore(g, from, to);
  g.caption = caption;
  if (mask !== nowMask) setBlockDays(g, mask);
  if (save()) return { ok: true, name: to };
  g.name = was.name;
  g.caption = was.caption;
  g.days = was.days;
  for (const x of itemsWas) {
    x.it.group = x.group;
    if (x.hadLog) x.it.groupLog = x.log; else delete x.it.groupLog;
  }
  return { ok: false, reason: 'storage' };
}

/* Пункт не может молча войти в УБРАННЫЙ блок: интерфейс показал бы его
   «без блока», а маска невидимого блока резала бы его дни. null — можно. */
/* Живой одноимённый блок в ДРУГОМ месте store — в другом режиме (Р2).
   Одно правило на уход блока (глобальные пункты уходят, только если такого
   нет) и на слова его последствия в форме: названное между тапами обязано
   совпадать с тем, что сделает второй тап (Р2/рецензия). «Живой» — в смысле
   holdsName (задача Р3, п. 0.3): живой блок ЖИВОГО режима. */
function hasLiveNamesake(g) {
  return store.groups.some(x => x !== g && x.name === g.name && holdsName(x));
}

/* Держит ли блок глобальные пункты своим именем (задача Р3, п. 0.3, решение
   архитектора): только ЖИВОЙ блок ЖИВОГО режима. Убранный блок не держит —
   в том числе ушедший в тот же день, что пункт; блок убранного режима не
   держит тоже: его режим нельзя выбрать, блока владелец не видит нигде, и
   держать под ним привычки значило бы прятать их за невидимым. Одно правило
   на уход блока (hasLiveNamesake), переименование (renameGroupCore),
   «Убранные» (goneBesideBlock) и заголовки привычек (habitSections).
   Режима нет в store (запись собрана руками без modes) — читается живым,
   как блок без поля читается основным. */
function holdsName(g) {
  if (!g || !live(g)) return false;
  const m = findMode(blockMode(g));
  return !m || live(m);
}

function groupJoinRefusal(name, mode) {
  const g = findGroup(name, mode);
  return g && !live(g) ? `Блок «${g.name}» убран — вернуть можно в «Убранных»` : null;
}

/* Уход блока — с сегодняшнего дня включительно, вместе со ВСЕМИ живыми
   пунктами блока (обеих областей, всех типов): блок, ушедший из виду,
   не оставляет на экранах пунктов без заголовка. Прошлое не двигается:
   дни блока и отрезки жизни пунктов читают вчера как вчера. Одна запись;
   отказ — откат всех полей.
   С режимами (Р2): действия — только режима блока. Глобальные пункты
   (привычки, параметры, счётчики) ссылаются на блок именем и уходят,
   только если живого блока с этим именем не остаётся НИ В ОДНОМ режиме:
   иначе уход блока одного режима увёл бы их из-под одноимённого другого.
   Живого — живого режима (holdsName, задача Р3, п. 0.3): блок убранного
   режима глобальных пунктов не держит, и они уходят вместе с блоком. */
function removeGroup(name, mode) {
  const g = findGroup(name, mode);
  if (!g || !live(g)) return false;
  const t = todayKey();
  const md = blockMode(g);
  const namesake = hasLiveNamesake(g);
  const touched = store.items.filter(it => live(it) && groupNameOf(it) === g.name &&
    (isAction(it) ? itemMode(it) === md : !namesake));
  g.removedAt = t;
  for (const it of touched) it.removedAt = t;
  if (save()) return true;
  g.removedAt = null;
  for (const it of touched) it.removedAt = null;
  return false;
}

/* Возврат блока. Возвращаются пункты, ушедшие вместе с ним, — те, чей день
   ухода совпадает с днём ухода блока; по правилам restoreItem: в тот же
   день — полная отмена, позже — новая запись сразу за прежней (инвариант
   12). Одна запись; отказ — полный откат и массива, и полей.

   Ограничение: пункт, убранный поштучно В ТОТ ЖЕ день до ухода блока,
   вернётся вместе с блоком — отличить его нечем без нового поля. Операция
   обратима: уход пункта повторяется одним действием.

   Пункты возвращаются по очереди, и копия, заведённая для одного, уже
   стоит в store, когда ядро проверяет преемника у следующего. Одноимённому
   соседу она не мешает (Р1/ревью): преемник прячет одну запись, копия
   заведена сегодня — позже всех — и встаёт сразу за своей прежней записью,
   поэтому в пару берёт именно её (laterSegmentOf). Судьба каждого пункта
   та же, что по store на входе операции: вернётся всякий, у кого на входе
   не было пары. */
/* Пункты, ушедшие вместе с блоком: день ухода совпал с днём ухода блока.
   Действия — только режима блока; глобальный пункт — если ушёл в тот же
   день (его уводит уход последнего живого одноимённого блока, Р2).
   Одно правило на возврат блока и на строку «Вернулись с днями блока». */
function restoreSetOf(g, when) {
  const md = blockMode(g);
  const day = when === undefined ? g.removedAt : when;
  return store.items.filter(it => groupNameOf(it) === g.name && it.removedAt === day && belongsToMode(it, md));
}

function restoreGroup(name, mode) {
  const g = findGroup(name, mode);
  if (!g || live(g)) return false;
  const when = g.removedAt;
  g.removedAt = null;
  const back = restoreSetOf(g, when).map(it => it.id);
  const undos = [];
  for (const id of back) {
    const r = restoreItemCore(id);
    if (r) undos.push(r.undo);
  }
  if (save()) return true;
  for (let i = undos.length - 1; i >= 0; i--) undos[i]();
  g.removedAt = when;
  return false;
}

/* Копия блока: имя «‹имя› (копия)», при занятости — «(копия 2)», «(копия 3)»…
   Встаёт СРАЗУ ЗА источником. Дни — маска источника на сегодня, одним
   отрезком с сегодняшнего дня: прошлого у копии нет. Копируются ЖИВЫЕ
   действия (пункты минимума, daily и weekly) в порядке items[] — новыми
   записями с сегодняшнего дня, без истории прошлого; привычки и параметры
   не копируются: «действия» — пункты минимума. Копия уже в канонической
   форме — migrate от неё ничего не меняет. → имя копии | null (отказ). */
function duplicateGroup(name, mode) {
  const g = findGroup(name, mode);
  if (!g) return null;
  const md = blockMode(g); // копия — в том же режиме, имя свободно в нём (Р2)
  let nm = `${g.name} (копия)`;
  for (let k = 2; nameTaken(nm, undefined, md); k++) nm = `${g.name} (копия ${k})`;
  const t = todayKey();
  const m = blockMaskNow(g);
  const block = {
    name: nm,
    caption: typeof g.caption === 'string' ? g.caption : '',
    days: m !== WEEK_ALL ? [{ from: t, mask: m }] : [],
    removedAt: null,
    mode: md
  };
  // действия режима блока и недельные счётчики с его именем (счётчик
  // глобален и стоит в карточке одноимённого блока активного режима)
  const copies = store.items
    .filter(it => live(it) && it.area === 'min' && (it.type === 'daily' || it.type === 'weekly') &&
      groupNameOf(it) === g.name && belongsToMode(it, md))
    .map(src => {
      const c = {
        id: uid(), name: src.name, value: src.value, unit: src.unit, type: src.type, area: 'min',
        goal: src.goal, note: src.note, group: nm, removedAt: null, addedAt: t, at: src.at || '',
        raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null,
        history: typeof src.value === 'number' ? [{ date: t, value: src.value }] : [],
        formula: null, ladder: null, ladderLog: []
      };
      // расписание, журнал и режим — только у ежедневного (инвариант 10, Р2)
      if (src.type === 'daily') { c.schedule = [{ from: t, mask: scheduleNow(src) }]; c.groupLog = []; c.mode = md; }
      return c;
    });
  store.groups.splice(store.groups.indexOf(g) + 1, 0, block);
  store.items.push(...copies);
  if (save()) return nm;
  store.groups.splice(store.groups.indexOf(block), 1);
  store.items.splice(store.items.length - copies.length, copies.length);
  return null;
}

/* Копия блока В ДРУГОЙ РЕЖИМ (задача Р2) — по правилам duplicateGroup:
   дни на сегодня одним отрезком, живые действия новыми записями без
   прошлого. Отличия: имя свободно в целевом режиме — остаётся тем же, занято
   — «‹имя› (копия)», «(копия 2)»… по nameTaken цели; блок встаёт В КОНЕЦ
   блоков целевого режима в store.groups; недельные счётчики не копируются —
   межрежимная копия берёт только действия, как копия режима. Источник —
   блок режима fromMode (по умолчанию активного); цель — живой режим, не
   тот же. Одна запись; отказ — откат. → имя копии | null (отказ). */
function duplicateGroupTo(name, targetMode, fromMode) {
  const g = findGroup(name, fromMode);
  const target = findMode(targetMode);
  if (!g || !target || !live(target) || target.id === blockMode(g)) return null;
  const md = target.id;
  let nm = g.name;
  if (nameTaken(nm, undefined, md)) {
    nm = `${g.name} (копия)`;
    for (let k = 2; nameTaken(nm, undefined, md); k++) nm = `${g.name} (копия ${k})`;
  }
  const t = todayKey();
  const m = blockMaskNow(g);
  const block = {
    name: nm,
    caption: typeof g.caption === 'string' ? g.caption : '',
    days: m !== WEEK_ALL ? [{ from: t, mask: m }] : [],
    removedAt: null,
    mode: md
  };
  const copies = store.items
    .filter(it => live(it) && isAction(it) && itemMode(it) === blockMode(g) && groupNameOf(it) === g.name)
    .map(src => actionCopy(src, nm, md, t));
  let at = -1;
  store.groups.forEach((x, i) => { if (blockMode(x) === md) at = i; });
  store.groups.splice(at < 0 ? store.groups.length : at + 1, 0, block);
  store.items.push(...copies);
  if (save()) return nm;
  store.groups.splice(store.groups.indexOf(block), 1);
  store.items.splice(store.items.length - copies.length, copies.length);
  return null;
}

/* Здесь жило удаление блока (deleteGroup) — единственная стирающая операция
   над списком. Снято интерфейсным этапом «Расписания 1/3» (п. 1.4) вместе с
   кнопкой «Удалить блок»: блок несёт дни, удаление (блок по имени больше не
   найдётся) задним числом вернуло бы его действиям все семь, и «N из M»
   прошлых дней сдвинулось бы. По словарю конституции такое не удаляется, а
   убирается — removeGroup/restoreGroup выше. */

/* Раскладка дневного экрана: сначала ЖИВЫЕ блоки в порядке store.groups,
   затем пункты без блока, с неизвестным или с убранным блоком — одной
   секцией без заголовка */
/* С режимами (Р2) — блоки режима mode (по умолчанию активного). Действие
   чужого режима под одноимённый блок не встаёт: у его режима свой блок, и
   идёт оно в секцию без заголовка; глобальный пункт встаёт под блок по
   имени. */
function groupedItems(items, mode) {
  const m = mode === undefined ? activeMode() : mode;
  const out = [];
  const taken = new Set();
  for (const g of store.groups) {
    if (!live(g) || blockMode(g) !== m) continue;
    const inGroup = items.filter(it => groupNameOf(it) === g.name && belongsToMode(it, m));
    for (const it of inGroup) taken.add(it);
    if (inGroup.length) out.push({ group: g, items: inGroup });
  }
  const loose = items.filter(it => !taken.has(it));
  if (loose.length) out.push({ group: null, items: loose });
  return out;
}

/* Раскладка привычек и параметров по блокам — ОДНА на дневной экран
   «Привычки» и секцию «Привычки» «Настроек» (задача Р3, п. 0.2, решение
   архитектора). Привычки глобальны, и блок у них — только имя (инвариант
   20): раскладка по блокам одного активного режима (groupedItems) уводила
   на дневном экране в секцию без заголовка привычку, чей блок живёт в
   другом режиме, хотя «Настройки» показывали её под этим именем.

   Секции по порядку: живые блоки АКТИВНОГО режима в порядке store.groups
   (group — сам блок: дневной экран печатает его подпись); затем прочие
   имена, у которых есть блок, держащий имя (holdsName: живой блок живого
   режима), в порядке store.groups, одно имя — одна секция (group: null —
   подпись у разных режимов своя, и печатать чужую нельзя); последней —
   пункты без блока (name: null): пустое имя и имя, которого не держит ни
   один блок. Смена режима меняет порядок секций, но привычку не прячет и
   в «Без блока» не переносит. Пустые секции не рождаются. Чистая функция
   от store.groups, режимов и переданных пунктов; какие пункты печатать
   (живые, убранные с коротким путём) — решает место вызова. */
function habitSections(items, mode) {
  const m = mode === undefined ? activeMode() : mode;
  const out = [];
  const known = new Set();
  const own = store.groups.filter(g => live(g) && blockMode(g) === m);
  for (const g of own.concat(store.groups.filter(holdsName))) {
    if (known.has(g.name)) continue;
    known.add(g.name);
    const list = items.filter(it => groupNameOf(it) === g.name);
    if (list.length) out.push({ name: g.name, group: own.includes(g) ? g : null, items: list });
  }
  const loose = items.filter(it => !known.has(groupNameOf(it)));
  if (loose.length) out.push({ name: null, group: null, items: loose });
  return out;
}

/* ── Быстрое добавление («Расписание 1/3») ─────────────────────
   Строки владельца → действия. Разделитель подписи — ПЕРВОЕ вхождение
   любого из четырёх знаков с пробельными символами по бокам: « · » (точка
   U+00B7), « — » (U+2014), « – » (U+2013) и « - » (дефис; задача Р2, п. 0:
   владелец набирает подпись тире, а iOS сама меняет «--» на «—»). Имя
   слева, подпись справа. Без пробельных по бокам знак — часть имени:
   «Кросс-фит», «10–15 минут», «a·b». «/» разделителем не является вовсе —
   в именах он встречается и с пробелами («Подтягивания / отжимания»).
   Пустые строки пропускаются, повторы не склеиваются: две одинаковые
   строки — два действия, так их и набрали. */
function parseQuickLines(text) {
  const out = [];
  for (const raw of String(text ?? '').split(/\r?\n/)) {
    const line = raw.trim();
    if (!line) continue;
    const m = /^(.*?)\s+[·—–-](?:\s+(.*))?$/.exec(line);
    if (m) out.push({ name: m[1].trim(), note: (m[2] || '').trim() });
    else out.push({ name: line, note: '' });
  }
  return out;
}

/* Действия в блок groupName ('' — «Без блока»), в конец items[], одной
   записью; отказ — откат и []. Каждое — ежедневное, «как блок», в
   канонической форме. */
function addActions(groupName, lines, mode) {
  const list = (Array.isArray(lines) ? lines : [])
    .filter(l => l && typeof l.name === 'string' && l.name.trim());
  if (!list.length) return [];
  const t = todayKey();
  const group = String(groupName ?? '').trim();
  const md = mode === undefined ? activeMode() : mode; // действие рождается в активном режиме (Р2)
  const made = list.map(l => ({
    id: uid(), name: l.name.trim(), value: null, unit: '', type: 'daily', area: 'min',
    goal: null, note: typeof l.note === 'string' ? l.note.trim() : '', group, removedAt: null,
    addedAt: t, at: '', schedule: [{ from: t, mask: WEEK_ALL }], groupLog: [], mode: md,
    raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null, history: [],
    formula: null, ladder: null, ladderLog: []
  }));
  store.items.push(...made);
  if (save()) return made;
  store.items.splice(store.items.length - made.length, made.length);
  return [];
}

/* Секция привычки или параметра в раскладке habitSections — той самой,
   что рисует секцию «Привычки» «Настроек» (Р3/рецензия). Соседи стрелок и
   перетаскивания у привычек берутся из неё, а не из сырого item.group: с
   задачи Р3 имя, которое держит только блок убранного режима, стоит в
   «Без блока» рядом с пунктами без блока, и соседство по имени оставляло
   такую строку посреди списка с обеими неактивными стрелками, а соседнюю
   уводило через её голову (инвариант 17: границы движения — список,
   который видит владелец). Раскладка и соседство — одна функция, разойтись
   им нечем. null — пункта нет среди привычек store. */
function habitSectionOf(item) {
  return habitSections(store.items.filter(x => x.area === 'habit')).find(x => x.items.includes(item)) || null;
}

/* Имя секции строки привычки для перетаскивания (data-dgroup): '' — «Без
   блока». */
function habitSectionName(item) {
  const sec = habitSectionOf(item);
  return sec && sec.name !== null ? sec.name : '';
}

/* Строки карточки «Без блока» «Расписания» режима mode (по умолчанию
   активного) — действия режима без его живого блока и глобальные недельные
   счётчики без одноимённого живого блока этого режима (Р2), в порядке
   items[], живые и убранные: какие печатать, решает место вызова. Одна
   функция на карточку (scheduleSection) и на её соседство
   (scheduleSectionOf, задача Р4). */
function looseRows(mode) {
  const m = mode === undefined ? activeMode() : mode;
  const names = new Set(liveGroups(m).map(g => g.name));
  return store.items.filter(it => it.area === 'min' && belongsToMode(it, m) && !names.has(groupNameOf(it)));
}

/* Секция строки минимума в «Расписании» — по образцу habitSectionOf
   (задача Р4, п. 1). Недельный счётчик глобален для режимов (Р2) и в
   режиме, где одноимённого живого блока нет, печатается в «Без блока»
   рядом с действиями без блока; соседство по сырому item.group оставляло
   его там с обеими неактивными стрелками и не давало перетащить, а соседей
   уводило через его голову (вопрос 1 задачи Р3). Соседи строки «Без блока»
   — все строки этой карточки (name: null), тем же правилом, что у привычек:
   секция = соседи, границы движения — список, который видит владелец
   (инвариант 17). Строки карточки блока — прежнее правило: действия
   активного режима и счётчики с именем блока. Действие ДРУГОГО режима на
   экране не стоит и соседей ищет, как прежде, — одноимённые действия
   своего режима, без счётчиков: разбиение на секции остаётся разбиением,
   и ни одна строка не попадает в чужие соседи. Раскладка и соседство —
   одна функция, разойтись им нечем. null — пункта нет среди пунктов
   минимума store. */
function scheduleSectionOf(item) {
  if (!item || item.area !== 'min' || !store.items.includes(item)) return null;
  const am = activeMode();
  const loose = looseRows(am);
  if (loose.includes(item)) return { name: null, items: loose };
  const g = groupNameOf(item);
  const scope = x => (isAction(x) ? itemMode(x) : am);
  const own = scope(item);
  return { name: g, items: store.items.filter(x => x.area === 'min' && groupNameOf(x) === g && scope(x) === own) };
}

/* Имя секции строки «Расписания» для перетаскивания (data-dgroup): '' —
   «Без блока» (у блока имя непустое всегда, спутать не с чем). */
function scheduleSectionName(item) {
  const sec = scheduleSectionOf(item);
  return sec && sec.name !== null ? sec.name : '';
}

/* Соседи пункта по перестановке — пункты того же блока и той же области
   (задача 16F). Блок пункт меняет полем «Блок» в форме правки, а не
   стрелками и не перетаскиванием: порядок и принадлежность — разные
   решения. Возвращает индексы в store.items по возрастанию. */
/* С режимами (Р2) соседи — ещё и одной области режима: действие — среди
   действий своего режима и недельных счётчиков карточки; счётчик, глобальный
   для режимов, стоит в карточке одноимённого блока АКТИВНОГО режима и
   соседствует с его действиями. Привычки и параметры — строки своей секции
   habitSections (Р3/рецензия, habitSectionOf выше); пункты минимума — строки
   своей секции «Расписания» (задача Р4, scheduleSectionOf выше): в «Без
   блока» соседи — вся карточка, в карточке блока — прежнее правило. */
function siblingIndexes(item) {
  // убранный пункт в списке не стоит и соседом не считается: стрелка
  // перепрыгивает его, а его собственное место в items[] не меняется
  const sec = item.area === 'habit' ? habitSectionOf(item) : scheduleSectionOf(item);
  const members = new Set(sec ? sec.items : []);
  const out = [];
  store.items.forEach((x, i) => { if (live(x) && members.has(x)) out.push(i); });
  return out;
}

/* Перестановка пункта в пределах своего блока: обмен с ближайшим
   соседом. Границы блока — первый и последний его пункт. */
function moveItem(id, dir) {
  const i = store.items.findIndex(x => x.id === id);
  if (i < 0) return false;
  const idxs = siblingIndexes(store.items[i]);
  const at = idxs.indexOf(i);
  if (at < 0) return false; // убранный пункт не двигается: его строки в списке нет
  const j = idxs[at + (dir === 'up' ? -1 : 1)];
  if (j === undefined) return false;
  const t = store.items[i];
  store.items[i] = store.items[j];
  store.items[j] = t;
  save();
  return true;
}

/* Есть ли куда двигать — тем же правилом рисуется disabled у стрелок */
function canMoveItem(id, dir) {
  const i = store.items.findIndex(x => x.id === id);
  if (i < 0) return false;
  const idxs = siblingIndexes(store.items[i]);
  const at = idxs.indexOf(i);
  return at >= 0 && idxs[at + (dir === 'up' ? -1 : 1)] !== undefined;
}

/* Перестановка перетаскиванием: пункт встаёт на позицию to среди своих
   соседей по блоку, порядок остальных не меняется. */
function reorderItem(id, to) {
  const item = store.items.find(x => x.id === id);
  if (!item || !live(item)) return false;
  const idxs = siblingIndexes(item);
  const list = idxs.map(i => store.items[i]);
  const from = list.indexOf(item);
  if (from < 0 || to < 0 || to >= list.length || to === from) return false;
  list.splice(from, 1);
  list.splice(to, 0, item);
  idxs.forEach((pos, k) => { store.items[pos] = list[k]; });
  save();
  return true;
}

/* Перетаскивание блока: to — позиция среди ЖИВЫХ блоков. Убранные своё
   место в store.groups сохраняют, как убранные пункты в items[]. */
function reorderGroup(name, to, mode) {
  const m = mode === undefined ? activeMode() : mode;
  const g = store.groups.find(x => x.name === name && blockMode(x) === m);
  if (!g || !live(g)) return false;
  const idxs = liveGroupIndexes(m);
  const list = idxs.map(i => store.groups[i]);
  const from = list.indexOf(g);
  if (from < 0 || to < 0 || to >= list.length || to === from) return false;
  list.splice(from, 1);
  list.splice(to, 0, g);
  idxs.forEach((pos, k) => { store.groups[pos] = list[k]; });
  save();
  return true;
}

function reorderExercise(id, to) {
  const ex = findExercise(id);
  if (!ex || !live(ex)) return false;
  const idxs = exerciseIndexes();
  const list = idxs.map(i => store.exercises[i]);
  const from = list.indexOf(ex);
  if (from < 0 || to < 0 || to >= list.length || to === from) return false;
  list.splice(from, 1);
  list.splice(to, 0, ex);
  idxs.forEach((pos, k) => { store.exercises[pos] = list[k]; });
  save();
  return true;
}

/* ── Прогресс (инвариант 14) ───────────────────────────────────
   Чистые функции от days{}, items[] и календаря. Ни разборы, ни
   закрытие недели на них не влияют: прошлое читается по фактам
   отметок, а не по срезам. Всё это живёт только на «Прогрессе» —
   экране, который открывают намеренно. */

/* Пункты минимума, ЖИВШИЕ в этот день (инвариант 12). Единственная
   содержательная правка задачи 28.E/A — здесь; всё остальное наследуется.

   Прежде фильтр читал i.active — НЫНЕШНЕЕ значение — и применял его ко
   всем дням истории: сегодняшний тумблер переписывал прошлое. Замер на
   фикстуре 28 дней (разведка A.0.2): выключение одного пункта из шести
   превращало серию из 9 дней в 26, рекорд из 9 в 26, а цепь из «22
   полных, 4 частичных» в «26 полных, 0 частичных» — без единой новой
   отметки. Правило применимости и было дефектом, а не отсутствие
   удаления, которого просил владелец.

   Теперь день читается по отрезку жизни: пункт заведён не позже этого
   дня и не убран раньше или в этот день (livedOn). Уход действует с
   сегодняшнего дня включительно — вчера и раньше не двигается никогда. */
function minDayItems(dayKey) {
  return store.items.filter(i =>
    i.type === 'daily' && i.area === 'min' && dueOn(i, dayKey));
}

/* Сколько таких пунктов отмечено: 0 — день пуст, меньше всех —
   частичный, все — закрыт. Один проход для серии и для цепи дней. */
/* С задачи Р2 чисел четыре. planned — запланированные в этот день (по
   расписанию, режиму и отрезку жизни), skipped — пропущенные среди них,
   total = planned − skipped — ЗНАМЕНАТЕЛЬ дня («N из M», «день закрыт»,
   доля), done — отмеченные. Пропуск выпадает из знаменателя, но
   нейтральным день не делает: нейтральность решает planned (dayScore). */
function minDayMarks(dayKey) {
  const items = minDayItems(dayKey);
  let done = 0, skipped = 0;
  for (const i of items) {
    if (isMarked(dayKey, i.id)) done++;
    else if (isSkipped(dayKey, i.id)) skipped++;
  }
  return { done, total: items.length - skipped, skipped, planned: items.length };
}

/* «День закрыт» — отмечены ВСЕ применимые пункты. Понятие принадлежит
   экрану «Сегодня» и планке дня; серия и цепь дней им не пользуются:
   у них своё понятие «день зачтён» (доля ≥ порога, задача 17).
   Пустой список пунктов днём закрытым не делает: закрывать было
   нечего. Иначе вакуумная истина дала бы серию до начала календаря. */
function minDayClosed(dayKey) {
  const m = minDayMarks(dayKey);
  return m.total > 0 && m.done === m.total;
}

/* Счёт блока «Сегодня» для свёртки (задача Р2, п. 4): items — действия
   блока, запланированные в этот день (секция groupedItems). Блок ВЫПОЛНЕН,
   когда каждое из них отмечено или пропущено и их хотя бы одно. Правило
   знаменателя то же, что у дня: пропущенное из «N из M» выпадает, поэтому
   отмеченные выполненного блока и есть его знаменатель — «N из N».

   В отличие от «день закрыт», сплошные пропуски блок выполненным делают:
   свёртка — не оценка, а уборка решённого с глаз, и решено здесь всё.
   Строка при этом говорит правду числом — «0 из 0 · пропусков K». Пустой
   список выполненным не бывает: сворачивать нечего, и такой секции на
   экране нет. Чистая функция от days{}: ui не читает, развёртку владельца
   решает интерфейс. */
function blockTally(items, dayKey) {
  let done = 0, skipped = 0;
  for (const i of items) {
    if (isMarked(dayKey, i.id)) done++;
    else if (isSkipped(dayKey, i.id)) skipped++;
  }
  return { done, skipped, full: items.length > 0 && done + skipped === items.length };
}

/* ── Доля дня и порог зачёта (задача 17) ───────────────────────
   «День зачтён» — не «день закрыт»: планке дня нужны все пункты,
   серии довольно доли. Порог живёт в настройках, шаг 0,1. */

const THRESHOLD_MIN = 0.3, THRESHOLD_MAX = 1, THRESHOLD_STEP = 0.1;

/* Порог из внешних данных: доля в [0,3..1,0], округлённая до десятой */
function clampThreshold(v) {
  const n = Math.round(numOr(v, 0.8) * 10) / 10;
  return Math.min(THRESHOLD_MAX, Math.max(THRESHOLD_MIN, n));
}

function dayThreshold() {
  return clampThreshold(store.settings.dayThreshold);
}

/* Доля отмеченного среди применимых пунктов дня. Пунктов не было —
   null: день выпадает из счёта и серию не обрывает. */
/* Пропуски (задача Р2) из доли выпадают, но НЕЙТРАЛЬНЫМ день делает только
   пустой план: запланировано ≥ 1 и всё пропущено — доля 0, день не зачтён
   и серию рвёт (или тратит амнистию) по общему правилу. Иначе «Не сегодня»
   у всех дел давало бы серию без единой отметки — сквозной день, который
   владелец назначает себе сам. */
function dayScore(dayKey) {
  const m = minDayMarks(dayKey);
  if (!m.planned) return null;
  return m.total > 0 ? m.done / m.total : 0;
}

/* Сравнение доли с порогом идёт с допуском: 0,1 и деления вроде 4/5
   не представимы в двоичной дроби точно, и 0,8 ≥ 0,8 могло бы не
   выполниться после нескольких шагов степпера. */
const EPS = 1e-9;

/* Сколько отметок из total даёт зачёт при текущем пороге — число для
   подписи степпера: минимальное done, при котором done/total ≥ порога */
function dayNeed(total) {
  return total > 0 ? Math.ceil(total * dayThreshold() - EPS) : 0;
}

/* Подпись под степпером порога. Отдельной функцией, потому что тумблер
   пункта меняет число применимых пунктов и обязан её обновить точечно
   (задача 22, п. 5): полная перерисовка убила бы переход тумблера.
   Пунктов нет — подписи нет, но узел остаётся: его нечем было бы
   создать, когда первый пункт включат обратно. */
function thresholdNote() {
  // знаменатель — тот же, что у планки дня: пропущенные сегодня не в счёте (Р2)
  const total = minDayMarks(todayKey()).total;
  return total ? `День зачтён, если отмечено не меньше ${dayNeed(total)} из ${total}.` : '';
}

/* «В системе N дней»: логические дни от calendarSince до сегодня
   включительно. Не прерывается никогда — пропуски на неё не влияют.
   Ноль до наступления calendarSince (миграция ставит его в ближайший
   понедельник, который может быть впереди). */
function daysInSystem() {
  const since = store.settings.calendarSince;
  const t = todayKey();
  if (!isDayKey(since) || t < since) return 0;
  return diffDays(t, since) + 1;
}

/* Амнистия одна на неделю: следующий незачтённый день прощается,
   только если предыдущий прощённый дальше этого срока (задача 17).
   Два незачтённых подряд — частный случай: расстояние 1 ≤ 7. */
const AMNESTY_GAP = 7;

/* Серия зачтённых дней, считая назад от дня end (инвариант 14).
   Сегодня незачтённым пропускается — день ещё не кончился, и амнистию
   он не тратит. Дальше назад: зачтённый +1; незачтённый прощается,
   если прошлая амнистия дальше AMNESTY_GAP, иначе обрыв; день без
   применимых пунктов проходит мимо счёта. Дно — calendarSince.

   score — как читать долю дня; отдельным параметром ради bestStreak,
   который прогоняет ту же логику по всей эпохе с готовой таблицей. */
function streakBack(end, score) {
  const t = todayKey();
  const since = store.settings.calendarSince;
  const floor = isDayKey(since) ? since : end;
  if (end < floor) return 0;
  const th = dayThreshold() - EPS;
  let n = 0;
  let k = end;
  if (k === t) { // сегодня: зачтённый идёт в счёт, незачтённый просто пропускается
    const s = score(k);
    if (s !== null && s >= th) n = 1;
    k = addDays(k, -1);
  }
  let amnesty = null; // день последней амнистии (он позже текущего k)
  for (; k >= floor; k = addDays(k, -1)) {
    const s = score(k);
    if (s === null) continue;
    if (s >= th) { n++; continue; }
    if (amnesty !== null && diffDays(amnesty, k) <= AMNESTY_GAP) break;
    amnesty = k; // прощён: серию не рвёт и в счёт не идёт
  }
  return n;
}

function dayStreak() {
  return streakBack(todayKey(), dayScore);
}

/* Рекорд — максимальная серия за всю историю по тем же правилам:
   в store не хранится, считается по days{} при каждом показе.

   Считается ОДНИМ проходом (задача 19, B.1). Раньше здесь стоял
   streakBack для каждого дня эпохи, и каждый уходил назад до
   calendarSince: квадратично по дням. Замер аудита — 100 мс на годе,
   940 мс на трёх годах, и «Прогресс» замирал вместе с ним.

   Требование к этому месту — наблюдаемое, а не предписывающее (задача
   23, п. 8.1): конституция спрашивает не «сколькими проходами», а
   «за сколько», и держит ответ тестом «bestStreak на трёх годах
   истории укладывается в 50 мс». Линейный проход даёт 1,6 мс,
   квадратичный возврат — 717 мс, то есть падение теста. Один проход —
   способ уложиться, и он описан ниже; способ можно сменить, порог —
   нет.

   Наблюдение, которое делает проход линейным: куда упрётся счёт,
   зависит не от дня, которым он кончается, а от ЦЕПОЧКИ незачтённых
   дней под ним. Счёт, начатый выше незачтённого дня u, прощает u,
   потом следующий незачтённый v — если v дальше AMNESTY_GAP, иначе
   обрывается на нём. Значит для каждого незачтённого дня можно один
   раз вычислить день обрыва (breakAt) по предыдущему такому же, а
   длину серии, кончающейся днём i, взять как разность префиксных
   сумм зачтённых дней. Дни без применимых пунктов (доля null) в
   обеих суммах прозрачны — как и в streakBack. */
function bestStreak() {
  const t = todayKey();
  const since = store.settings.calendarSince;
  if (!isDayKey(since) || t < since) return 0;
  const th = dayThreshold() - EPS;

  // проход первый: доли дней, префикс зачтённых, список незачтённых
  const days = [];       // дни эпохи по возрастанию
  const scored = [];     // scored[i] — зачтённых дней в days[0..i]
  const unscored = [];   // индексы дней с долей ниже порога (null — не сюда)
  let acc = 0;
  for (let k = since; k <= t; k = addDays(k, 1)) {
    const s = dayScore(k);
    if (s !== null) {
      if (s >= th) acc++;
      else unscored.push(days.length);
    }
    days.push(k);
    scored.push(acc);
  }

  // проход второй: цепочка амнистий. breakAt[j] — индекс дня, на котором
  // оборвётся счёт, спустившийся ниже j-го незачтённого дня; −1 — обрыва
  // нет, счёт доходит до начала эпохи.
  const breakAt = new Array(unscored.length);
  for (let j = 0; j < unscored.length; j++) {
    if (j === 0) { breakAt[j] = -1; continue; }
    const u = days[unscored[j]], v = days[unscored[j - 1]];
    breakAt[j] = diffDays(u, v) <= AMNESTY_GAP ? unscored[j - 1] : breakAt[j - 1];
  }

  // длина серии, кончающейся днём i (правило «сегодня» сюда не входит)
  const upto = (i, j) => {
    if (i < 0) return 0;
    if (j < 0) return scored[i];              // незачтённых под ним не было
    const b = breakAt[j];
    return scored[i] - (b < 0 ? 0 : scored[b]);
  };

  // проход третий: максимум. j идёт вместе с i — последний незачтённый ≤ i
  const last = days.length - 1;
  let best = 0, j = -1;
  for (let i = 0; i < last; i++) {
    if (j + 1 < unscored.length && unscored[j + 1] === i) j++;
    const n = upto(i, j);
    if (n > best) best = n;
  }
  // сегодня по особому правилу: незачтённый пропускается, а не амнистируется,
  // поэтому счёт за сегодня — это счёт за вчера плюс сам сегодняшний день
  const todayScored = scored[last] - (last > 0 ? scored[last - 1] : 0);
  const nToday = (last > 0 ? upto(last - 1, j) : 0) + todayScored;
  return nToday > best ? nToday : best;
}

/* Понедельники последних n календарных недель по возрастанию, включая
   текущую. Опирается на weekStartOf, а не на currentWeekStart: сетка
   рисуется и до наступления calendarSince. */
function chainWeeks(n) {
  const mon = weekStartOf(todayKey());
  const out = [];
  for (let i = n - 1; i >= 0; i--) out.push(addDays(mon, -7 * i));
  return out;
}

/* Знаменатель строки «Отметки»: дни от позднейшей из двух дат —
   когда пункт заведён и когда началась эпоха — до сегодня включительно.
   Пункт, заведённый вчера, показывает «1 из 2», а не «1 из 32»:
   иначе новый пункт выглядел бы провалом с первого дня. */
function marksWindow(item) {
  const since = store.settings.calendarSince;
  const t = todayKey();
  if (!isDayKey(since)) return 0;
  const from = (isDayKey(item.addedAt) && item.addedAt > since) ? item.addedAt : since;
  if (t < from) return 0;
  // дни РАСПИСАНИЯ, а не календарные (задача 29/B): пункт «только по
  // воскресеньям», не пропустивший ни одного занятия, читался «1 из 12»
  let n = 0;
  for (let k = from; k <= t; k = addDays(k, 1)) if (dueOn(item, k)) n++;
  return n;
}

/* Отметок пункта за время в системе — числитель строки «Отметки» */
function marksInSystem(item) {
  const since = store.settings.calendarSince;
  const t = todayKey();
  if (!isDayKey(since)) return 0;
  let n = 0;
  for (const k of Object.keys(store.days)) {
    // тем же правилом, что знаменатель: отметка в дне вне расписания
    // (сужение маски после отметки) в счёт не идёт — иначе «5 из 3»
    if (k >= since && k <= t && store.days[k][item.id] && dueOn(item, k)) n++;
  }
  return n;
}

/* Ряд «Подъёма» — точки {date, value}. Источник один — история планки
   (у упражнения — история нагрузки, инвариант 15); меньше двух записей —
   ряда нет. Вторым источником был ladderLog: пункт без истории значений
   получал ступеньку по журналу лестницы. Лестница снята задачей 28.D, и
   рисовать подъём по механике, которой нет, нечем — правило «не больше
   одного визуала на пункт» держится теперь само собой. */
function riseSeries(item) {
  const hist = Array.isArray(item.history) ? item.history : [];
  if (hist.length < 2) return null;
  return { kind: 'bar', points: hist.map(x => ({ date: x.date, value: x.value })) };
}

/* ── Закрытие недели ───────────────────────────────────────── */

/* Решения по планке, принятые В ТЕКУЩУЮ календарную неделю. Приём тот же,
   что у счётчика тренировок (инвариант 3): читаем только свою неделю, а не
   всё накопленное. Датой служит якорь недели у самого пункта —
   raiseAfterWeek / lowerAfterWeek, который ставят ОБЕ кнопки решения;
   отдельного поля в записи для этого не нужно, схема не меняется.

   Прежде пропущенное закрытие складывало решения двух разборов в ОДИН
   срез: разбор недели W1 поднял планку «А», разбор не закрыли, назавтра
   окно сменилось, разбор W2 поднял «Б» — и срез W2 приписывал себе оба
   (задача 27). Пункт, решённый дважды, отдаёт в срез последнее решение. */
function pendingThisWeek(list, field) {
  const cur = currentWeekStart();
  if (!cur) return [];
  const seen = new Set();
  const out = [];
  for (let i = list.length - 1; i >= 0; i--) {
    const r = list[i];
    const it = store.items.find(x => x.id === r.itemId);
    if (!it || it[field] !== cur || seen.has(r.itemId)) continue;
    seen.add(r.itemId);
    out.unshift(r);
  }
  return out;
}

function closeWeek() {
  if (!reviewDue()) return false; // guard: завершённой неразобранной недели нет
  const week = previousWeekStart();
  const keys = windowKeys();
  const perItem = {};
  for (const it of store.items) {
    if (it.type !== 'daily') continue;
    const marks = keys.map(k => isMarked(k, it.id));
    // пропуски (задача Р2) — отдельным рядом: срез неизменяем, и отметкой
    // пропуск в нём не читается; count остаётся числом отметок
    const skips = keys.map(k => isSkipped(k, it.id));
    // убранные без отметок и пропусков в окне не попадают в срез
    if (!live(it) && !marks.some(Boolean) && !skips.some(Boolean)) continue;
    perItem[it.id] = {
      name: it.name, marks, count: marks.filter(Boolean).length,
      skips, skipCount: skips.filter(Boolean).length
    };
  }
  const weekEnd = keys[6];
  const trainings = {};
  for (const w of store.items.filter(i => i.type === 'weekly')) {
    const count = store.weekLog.filter(e => e.itemId === w.id && e.date >= week && e.date <= weekEnd).length;
    if (!live(w) && !count) continue; // как и в perItem: убранные без счёта не попадают
    trainings[w.id] = { name: w.name, count, goal: w.goal };
  }
  store.reviews.push({
    closedAt: Date.now(),
    week, // понедельник разобранной недели
    keys,
    perItem,
    trainings,
    oneChange: (store.draftOneChange || '').trim(),
    raises: pendingThisWeek(store.pendingRaises, 'raiseAfterWeek'),
    lowers: pendingThisWeek(store.pendingLowers, 'lowerAfterWeek'),
    // в срез идут только решения разобранной недели; чистится paramDecided целиком
    params: Object.entries(store.paramDecided)
      .filter(([, d]) => d.week === week)
      .map(([id, d]) => ({ id, from: d.from, to: d.to }))
  });
  store.pendingRaises = [];
  store.pendingLowers = [];
  store.draftOneChange = '';
  store.paramDecided = {};
  // счётчик «Сегодня» обнуляется сменой недели, не закрытием: чистим только прошлое
  const cur = currentWeekStart();
  store.weekLog = store.weekLog.filter(e => e.date >= cur);
  save();
  return true;
}

/* ── Чистка и её отмена (задача 16.1) ──────────────────────────
   Стирание данных — не исключение из правила «необратимых операций
   нет», а его применение: прежний store целиком ложится в отдельный
   ключ и живёт там, пока владелец не уберёт копию вторым решением.
   Копия — не часть store: ни в экспорт, ни в импорт она не входит. */

const WIPE_KEY = NS + ':wiped';
const CORRUPT_KEY = NS + ':corrupt';

/* Числа для строки предупреждения и для строки возврата.

   Режимы — данные владельца (Р2/рецензия): store, где кроме заведённого
   режима и журнала выбора нет ничего, прежде считался пустым, и чистка,
   импорт и «Вернуть» стирали режимы без копии — необратимо. Считаются
   режимы СВЕРХ стартового основного (он есть в любом store, и пустой
   store от него содержательным не становится; переименованный основной —
   уже слово владельца) и отрезки журнала выбора. */
function wipeStats(s) {
  return {
    items: s.items.length,
    groups: s.groups.length,
    days: Object.keys(s.days).length,
    reviews: s.reviews.length,
    ladders: s.items.filter(i => i.ladder).length,
    exercises: s.exercises.length,
    sessions: s.sessions.length,
    notes: s.notes.length,
    modes: (Array.isArray(s.modes) ? s.modes : [])
      .filter(m => !(m && m.id === MAIN_MODE && m.name === MAIN_MODE_NAME)).length,
    modeLog: Array.isArray(s.modeLog) ? s.modeLog.length : 0
  };
}

function wipedCopy() {
  try {
    const raw = localStorage.getItem(WIPE_KEY);
    if (!raw) return null;
    const c = JSON.parse(raw);
    return (c && typeof c === 'object' && c.store && typeof c.store === 'object') ? c : null;
  } catch (e) { return null; } // нечитаемая копия — как её отсутствие
}

function dropWiped() {
  try { localStorage.removeItem(WIPE_KEY); } catch (e) { /* нечего убирать */ }
}

/* Сырое содержимое ключа копии и его восстановление — для ОТКАТА.
   Любое замещение трогает копию ДО записи рабочего ключа: иначе некуда
   было бы отступить, если копию положить не удалось. Но и запись рабочего
   ключа может не удаться, и тогда замещение остаётся совершённым
   наполовину — копия израсходована, а на диске прежнее состояние.
   Возврат при этом терял практику ЦЕЛИКОМ: dropWiped снимал копию,
   save() падал, и на диске оставался пустой store (задача 27, Д5).
   Пара функций возвращает ключ ровно в то состояние, в каком он был. */
function wipedRaw() {
  try { return localStorage.getItem(WIPE_KEY); } catch (e) { return null; }
}

function setWipedRaw(raw) {
  try {
    if (raw === null) localStorage.removeItem(WIPE_KEY);
    else localStorage.setItem(WIPE_KEY, raw);
  } catch (e) { /* откат копии не удался — сделать больше нечем */ }
}

/* Есть ли в store хоть что-нибудь владельца. Признак берётся из того же
   wipeStats, что показывает предупреждение чистки: заводить отдельное
   поле незачем — ни одного ненулевого числа значит терять нечего. */
function hasData(s) {
  return Object.values(wipeStats(s)).some(n => n > 0);
}

/* Копия прежнего состояния — одна механика на все поводы замещения:
   чистку, импорт и сам возврат (инвариант 18). Возвращает false, только
   если копию некуда положить, — тогда замещающая операция не выполняется.

   Пустой store копию НЕ подменяет: «одна, последняя» значит «последняя
   СОДЕРЖАТЕЛЬНАЯ». Вторая чистка подряд прежде клала в копию пустоту и
   уносила практику, которую первая чистка туда положила (задача 25, п. 7). */
function keepPrev(prev, kind) {
  if (!hasData(prev)) return true;
  try {
    localStorage.setItem(WIPE_KEY, JSON.stringify({
      store: prev, wipedAt: Date.now(), stats: wipeStats(prev), kind
    }));
    return true;
  } catch (e) {
    return false;
  }
}

/* Два источника нечитаемого — рабочий ключ и снапшот зеркала (задача 28.A,
   п. 1.3). Формат, показ и обе кнопки у них общие, различаются только ключ
   и слова: второй такой же механизм заводить незачем. */
const CORRUPT_SRC = {
  data: {
    key: CORRUPT_KEY,
    title: 'Найдены нечитаемые данные',
    why: 'Приложение не смогло их прочитать и отложило, ничего не стирая.',
    file: 'minimum-нечитаемое-'
  },
  mirror: {
    key: MIRROR_CORRUPT_KEY,
    title: 'Резервная копия оказалась нечитаемой',
    why: 'Приложение не смогло её прочитать и отложило содержимое сюда, ничего не стирая. Пока она лежит, новая копия не ведётся: «Стереть нечитаемое» освободит место под неё.',
    file: 'minimum-копия-нечитаемая-'
  }
};

/* Нечитаемые данные, отложенные load(): {raw, at}. Прежний формат — голая
   строка без даты — читается тоже, дата тогда неизвестна (задача 25, п. 6). */
function corruptCopy(src) {
  const key = (CORRUPT_SRC[src] || CORRUPT_SRC.data).key;
  let raw = null;
  try { raw = localStorage.getItem(key); } catch (e) { return null; }
  if (!raw) return null;
  try {
    const c = JSON.parse(raw);
    if (c && typeof c === 'object' && typeof c.raw === 'string' && typeof c.at === 'number') return c;
  } catch (e) { /* голая строка старого формата — ниже */ }
  return { raw, at: null };
}

function dropCorrupt(src) {
  const key = (CORRUPT_SRC[src] || CORRUPT_SRC.data).key;
  try { localStorage.removeItem(key); } catch (e) { /* нечего убирать */ }
}

/* Пустое хранилище: ни одного стартового пункта. Граница дня и порог
   зачёта — настройки того, как считается день, а не данные: обе
   переживают чистку (инвариант 18). */
function emptyStore(boundary, threshold) {
  const b = (typeof boundary === 'number' && isFinite(boundary)) ? boundary : 4;
  const today = dateKeyShift(new Date(), b);
  return {
    schemaVersion: SCHEMA_VERSION,
    items: [], groups: [], modes: programModes(), modeLog: [], days: {}, weekLog: [], reviews: [],
    pendingRaises: [], pendingLowers: [], exercises: [], sessions: [], notes: [],
    paramDecided: {},
    draftOneChange: '',
    weekStart: today,
    settings: {
      dayBoundary: b,
      dayThreshold: clampThreshold(threshold),
      exportedAt: null,
      calendarSince: nextCalendarMonday(today),
      habitSeeded: true, // посев привычек уже состоялся: чистый лист остаётся чистым
      seed17: true       // и посев программы: перезапуск не вернёт стёртое (инвариант 18)
    }
  };
}

/* Копия пишется ПЕРЕД заменой store: если её некуда положить, чистка
   не выполняется вовсе — необратимо стирать нельзя. Зеркало сбрасывается
   немедленно: с дебаунсом следующий запуск при пустом localStorage
   восстановил бы стёртое из старого снапшота. */
function wipeAll() {
  const prev = store;
  const wasCopy = wipedRaw();
  if (!keepPrev(prev, 'wipe')) return false;
  store = emptyStore(
    prev.settings && prev.settings.dayBoundary,
    prev.settings && prev.settings.dayThreshold);
  // запись пустого store не удалась — откат целиком: на диске осталось
  // прежнее, значит и в памяти, и в копии должно остаться прежнее. Иначе
  // экран показывал бы чистый лист, копия — «до чистки», а на диске лежала
  // бы нетронутая практика: три разных ответа на один вопрос (задача 27, Д5)
  if (!save()) {
    store = prev;
    setWipedRaw(wasCopy);
    return false;
  }
  flushMirror();
  return true;
}

/* Возврат: копия проходит migrate (как и импорт, инвариант 6 — она
   могла быть снята прежней версией).

   Возврат — такое же замещение, как чистка и импорт, и обратим он тем же
   путём (задача 26, п. 1.1): нынешнее состояние ложится в ТУ ЖЕ копию до
   подмены. Копия становится обменной, «Вернуть» — переключателем между
   двумя состояниями, и наработанное после чистки или импорта не теряется.
   Прежде возврат был единственной необратимой операцией в интерфейсе:
   неделя отметок, набранная после чистки, уходила одним тапом.

   Пустое нынешнее состояние менять не на что — копия просто убирается,
   как и прежде: keepPrev пустой store в копию не кладёт (иначе «Вернуть»
   подменяло бы практику пустотой), а оставить прежнюю копию нельзя — её
   содержимое только что стало живым, и строка врала бы про стёртое. */
function restoreWiped() {
  const c = wipedCopy();
  if (!c) return false;
  let restored;
  // external: копия — данные извне, посев ей не адресован (A.2.1)
  try { restored = migrate(c.store, { external: true }); } catch (e) { return false; }
  const prev = store;
  const wasCopy = wipedRaw();
  if (hasData(prev)) {
    if (!keepPrev(prev, 'restore')) return false; // копию некуда положить — возврат не выполняется
  } else {
    dropWiped();
  }
  store = restored;
  // здесь и жила единственная необратимая операция интерфейса (задача 27,
  // Д5): копия расходовалась ДО записи, а успех записи никто не проверял.
  // Пустое нынешнее состояние + отказ квоты = копия снята, на диске пустой
  // store, практика уничтожена в обоих местах. Откат возвращает и store,
  // и ключ копии — после отказа не изменилось ничего
  if (!save()) {
    store = prev;
    setWipedRaw(wasCopy);
    return false;
  }
  flushMirror();
  return true;
}

/* Восстановление из зеркала (задача 28.A, п. 2.2) — четвёртый повод
   замещения и точно такой же, как три прежних: прежнее состояние ложится
   в ТУ ЖЕ обменную копию ДО подмены, копию некуда положить — восстановление
   не выполняется вовсе, запись отказала — откат целиком. Иначе предложение
   исправить одну потерю данных само стало бы второй.
   migrate уже прогнан при разборе снапшота (mirrorParse). */
function restoreMirror() {
  if (!mirrorOffer) return false;
  const prev = store;
  const wasCopy = wipedRaw();
  if (hasData(prev) && !keepPrev(prev, 'mirror')) return false;
  store = mirrorOffer.store;
  if (!save()) {
    store = prev;
    setWipedRaw(wasCopy);
    return false;
  }
  // рабочая копия и зеркало снова сходятся — предложению нет предмета
  mirrorOffer = null;
  mirrorReady = true;
  scheduleMirror(); // снапшот и рабочий ключ приводятся к одному состоянию
  return true;
}

/* «Оставить рабочую»: предложение снято, зеркало ведётся дальше и на
   ближайшем flush примет рабочее состояние. Снапшот с этого момента
   заменяем — потому и второй тап, и «Скачать» рядом. */
function keepWorking() {
  mirrorOffer = null;
  mirrorReady = true;
  scheduleMirror();
}

/* ── Экспорт / импорт ──────────────────────────────────────── */

/* Отдать текст файлом. Приложение знает только то, что скачивание
   ЗАПУЩЕНО: сохранил ли владелец файл, в вебе узнать нечем (задача 25,
   п. 9) — отсюда осторожность формулировок вокруг exportedAt. */
function download(name, text) {
  const blob = new Blob([text], { type: 'application/json' });
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = name;
  document.body.appendChild(a);
  a.click();
  a.remove();
  setTimeout(() => URL.revokeObjectURL(a.href), 10000);
}

function exportJSON() {
  store.settings.exportedAt = Date.now(); // дата попадает и в сам файл
  save();
  download('minimum-' + todayKey() + '.json', JSON.stringify(store, null, 1));
}

/* Единицы владельца в store — считаются одинаково по сырому файлу и по
   прошедшему migrate. Расхождение и есть то, что не доехало (задача 25,
   п. 3): сводка импорта прежде говорила только про уцелевшее, и потери
   были бесшумны. Считает по-хорошему defensive: сырой файл валидность
   не гарантирует ничем. */
function dataCounts(s) {
  const len = v => (Array.isArray(v) ? v.length : 0);
  const days = (s && s.days && typeof s.days === 'object' && !Array.isArray(s.days)) ? s.days : {};
  let marks = 0, skips = 0;
  for (const k of Object.keys(days)) {
    const d = days[k];
    if (d && typeof d === 'object' && !Array.isArray(d)) {
      for (const id of Object.keys(d)) {
        if (d[id] === true) marks++;
        else if (d[id] === false) skips++; // пропуск «Не сегодня» (задача Р2) — своя категория
      }
    }
  }
  // Категории пересчитаны по тому, что migrate ДЕЙСТВИТЕЛЬНО роняет
  // (задача 28.B, п. 5.1), а не по тому, что удобно считать. Прежде счёт
  // шёл по семи верхнеуровневым спискам, и файл, у которого миграция
  // выбрасывала три блока, две записи weekLog, две записи истории, два
  // значения сессии и решение по параметру, проходил с ПУСТОЙ строкой
  // потерь: ни одно из этих чисел в счёт не входило (замер разведки 0.5).
  const arr = v => (Array.isArray(v) ? v : []);
  const sum = (list, get) => arr(list).reduce((n, x) => n + (x && typeof x === 'object' ? arr(get(x)).length : 0), 0);
  const params = (s && s.paramDecided && typeof s.paramDecided === 'object' && !Array.isArray(s.paramDecided))
    ? Object.keys(s.paramDecided).length : 0;
  return {
    items: len(s && s.items), days: Object.keys(days).length, marks, skips,
    notes: len(s && s.notes), reviews: len(s && s.reviews),
    exercises: len(s && s.exercises), sessions: len(s && s.sessions),
    groups: len(s && s.groups),
    weekLog: len(s && s.weekLog),
    // история планки и история нагрузки — одна сущность (обе пишет recordBar,
    // инвариант 5), поэтому и категория потерь одна
    history: sum(s && s.items, x => x.history) + sum(s && s.exercises, x => x.history),
    // отрезки расписания (задача 29/B) — вложенная коллекция, из которой
    // normSchedule роняет элементы: битую дату, невалидную маску, пустую
    // маску, дубль одного дня. Инвариант 6 требует категорию на ВСЁ, что
    // migrate способен отбросить, а поштучно выразить это может только счёт.
    // Подпись времени категории не получает и не должна: это скаляр, как
    // unit и note, — их migrate тоже нормализует без счёта.
    schedule: sum(s && s.items, x => x.schedule),
    // «Расписание 1/3»: ещё две вложенные коллекции, из которых migrate
    // роняет элементы, — отрезки дней блока (normBlockDays) и записи
    // журнала принадлежности (normGroupLog). Подпись и день ухода блока —
    // скаляры, категорий не получают, как note и at у пункта.
    blockDays: sum(s && s.groups, x => x.days),
    groupLog: sum(s && s.items, x => x.groupLog),
    // задача Р2: режимы и журнал активного режима — список, из которого
    // normModes роняет мусор и дубли, и отрезки, из которых normModeLog
    // роняет битые, неизвестные и схлопнутые
    modes: len(s && s.modes),
    modeLog: len(s && s.modeLog),
    entries: sum(s && s.sessions, x => x.entries),
    params
  };
}

const COUNT_WORDS = [
  ['items', 'пункт', 'пункта', 'пунктов'],
  ['days', 'день', 'дня', 'дней'],
  ['marks', 'отметка', 'отметки', 'отметок'],
  ['skips', 'пропуск', 'пропуска', 'пропусков'],
  ['notes', 'заметка', 'заметки', 'заметок'],
  ['reviews', 'разбор', 'разбора', 'разборов'],
  ['exercises', 'упражнение', 'упражнения', 'упражнений'],
  ['sessions', 'тренировка', 'тренировки', 'тренировок'],
  ['groups', 'блок', 'блока', 'блоков'],
  ['weekLog', 'запись счётчика', 'записи счётчика', 'записей счётчика'],
  ['history', 'запись истории', 'записи истории', 'записей истории'],
  ['schedule', 'отрезок расписания', 'отрезка расписания', 'отрезков расписания'],
  ['blockDays', 'отрезок дней блока', 'отрезка дней блока', 'отрезков дней блока'],
  ['groupLog', 'запись о блоке', 'записи о блоке', 'записей о блоке'],
  ['modes', 'режим', 'режима', 'режимов'],
  ['modeLog', 'отрезок режима', 'отрезка режима', 'отрезков режима'],
  ['entries', 'значение тренировки', 'значения тренировки', 'значений тренировки'],
  ['params', 'решение по параметру', 'решения по параметру', 'решений по параметру']
];

/* «2 дня, 8 отметок» — перечисление того, чего в файле больше, чем доедет.
   Пустая строка, когда числа сошлись: лишней строки в подтверждении нет. */
function droppedLine(was, got) {
  const parts = [];
  for (const [key, one, few, many] of COUNT_WORDS) {
    const n = was[key] - got[key];
    if (n > 0) parts.push(`${n} ${plural(n, one, few, many)}`);
  }
  return parts.join(', ');
}

function importJSON(file) {
  const r = new FileReader();
  r.onload = () => {
    let data;
    try { data = JSON.parse(r.result); } catch (e) { alert('Файл не читается как JSON.'); return; }
    if (!data || !Array.isArray(data.items) || typeof data.days !== 'object') {
      alert('Файл не похож на экспорт «Минимума».');
      return;
    }
    // «было» — по СЫРОМУ файлу и обязательно ДО migrate: migrate мутирует
    // переданный объект и возвращает его же, после него считать уже нечего
    const was = dataCounts(data);
    const fileVersion = numOr(data.schemaVersion, 0);
    let incoming;
    try {
      incoming = migrate(data, { external: true }); // файл извне — без посева (A.2.1)
    } catch (e) {
      alert('Импорт не выполнен: файл повреждён. Текущие данные не изменены.');
      return;
    }
    // сводка из файла — перед подтверждением (после migrate все ключи days валидны)
    const dayCount = Object.keys(incoming.days).length;
    const range = Object.keys(incoming.days).sort();
    const parts = [
      `пунктов: ${incoming.items.length}`,
      `дней с отметками: ${dayCount}`,
      `закрытых недель: ${incoming.reviews.length}`
    ];
    if (range.length) parts.push(`отметки: ${fmtShort(range[0])} — ${fmtShort(range[range.length - 1])}`);
    let msg = `Заменить текущие данные данными из файла?\n\nВ файле: ${parts.join(', ')}.`;
    // сводка уцелевшего остаётся, к ней добавляется отброшенное: владелец
    // видит и что придёт, и что потеряется (задача 25, п. 3)
    const lost = droppedLine(was, dataCounts(incoming));
    if (lost) msg += `\n\nНе будет прочитано: ${lost}.`;
    // файл новее приложения: шаги миграции для его полей ещё не написаны,
    // и незнакомое отсеется валидацией. Не блокирует — спрашивает (п. 5)
    if (fileVersion > SCHEMA_VERSION) {
      msg += `\n\nФайл снят более новой версией приложения: часть данных может не сохраниться.`;
    }
    if (!confirm(msg)) return;
    // прежние данные ложатся в копию ДО подмены и той же механикой, что при
    // чистке (инвариант 18). Копию некуда положить — импорт не выполняется:
    // необратимых операций в интерфейсе нет, импорт был последней
    const prev = store;
    const wasCopy = wipedRaw();
    if (!keepPrev(prev, 'import')) {
      alert('Импорт не выполнен: копию прежних данных некуда сохранить. Текущие данные не изменены.');
      return;
    }
    store = incoming;
    // тот же откат, что у чистки и возврата (задача 27.1, п. 2.3): копия
    // израсходована ДО записи, и если рабочий ключ не записался, импорт
    // остался бы совершённым наполовину — в памяти новое, на диске старое,
    // а копия уже подменена. Один класс, одно лечение
    if (!save()) {
      store = prev;
      setWipedRaw(wasCopy);
      alert('Импорт не выполнен: данные не удалось записать. Текущие данные не изменены.');
      return;
    }
    // импорт заменил состояние целиком: черновики форм и дневное ui-состояние
    // не переносятся, граница дня могла смениться — таймер и день заново.
    // Формы «Настроек» закрывает общий список (settingsFormsClosed): прежде
    // здесь стоял его неполный дубль, и новые формы («Расписание 1/3» —
    // быстрое добавление, дни блока) остались бы открытыми над чужими данными
    resetSettingsView();
    ui.missOpen = {};
    ui.raiseEdit = {};
    // Взведённые подтверждения относились к ПРЕЖНИМ данным и после подмены
    // указывают уже на чужие. Опаснее всех «Подтвердить: стереть»: оно
    // переживало импорт, и один тап по нему стирал импортированное, подменяя
    // ТОЛЬКО ЧТО записанную копию прежних данных — та самая обратимость,
    // ради которой копия и заводится, уничтожалась одним касанием (задача 25).
    // Список подтверждений один — в resetConfirms(); дублировать его здесь
    // значит забыть очередное, что и случилось.
    ui.wipeOpen = false;
    resetConfirms();
    ui.renderedDayKey = todayKey();
    armDayTimer();
    const n = store.items.length;
    ui.importNote = `Импортировано: ${n} ${plural(n, 'пункт', 'пункта', 'пунктов')}, ` +
      `${dayCount} ${plural(dayCount, 'день', 'дня', 'дней')}`;
    renderAll();
  };
  r.readAsText(file);
}

/* ── Интерфейс ─────────────────────────────────────────────── */

const ui = {
  tab: 'today',
  editingId: null,
  addOpen: false,
  addType: 'daily',
  addHint: false,
  raiseEdit: {},   // itemId -> true, когда открыт ввод своего значения
  missOpen: {},    // itemId -> true, когда показана подпись «вчера — пропуск»
  // Развёрнутые владельцем выполненные блоки «Сегодня» (задача Р2, п. 4):
  // { day, names: { имя блока: true } } | null. Состояние ЭКРАНА на день, а
  // не хранилище: в store не пишется, смена логического дня его снимает
  // (day сверяется при чтении, syncDay обнуляет), смена режима и замещение
  // данных — тоже (resetSettingsView): одноимённый блок другого режима или
  // других данных — другой блок. Ключ — имя блока активного режима.
  todayUnfold: null,
  justClosed: false,
  // Поле addArea ('min' | 'habit') снято («Расписание 1/3», п. 2.4): форма
  // добавления осталась у одних привычек и параметров, действия заводятся
  // быстрым добавлением в своём блоке. Область формы теперь одна.
  addPkind: 'time',     // вид параметра в форме добавления; после создания вид не меняется
  editNorm: null,       // черновик нормы недели в открытой форме привычки (null — как у пункта)
  // черновик маски дней в открытой форме (задача 29/B). Кнопки, а не поля:
  // snapshotOpenForm читает только `.value`, у кнопки его нет вовсе — так же,
  // как у степпера нормы, и механизм тот же
  editDays: null,
  // режим дней ДЕЙСТВИЯ в живом блоке («Расписание 1/3», п. 2.5): null —
  // владелец режим не трогал, и он выводится из данных (своя маска «все
  // семь» — «как блок»); true — «Свои дни» нажаты; false — «Как блок» нажат
  editOwn: null,
  // черновик типа действия в форме правки (null — как у пункта). Живёт только
  // в день заведения пункта: позже тип не меняется, и черновик игнорируется
  editType: null,
  // разовое тихое подтверждение сохранения формы (движение, задача 12).
  // С задачи 26 это не флаг, а якорь: { key, text } — ключ той строки, у
  // которой узел должен родиться, чтобы стоять у пальца, а не в шапке (п. 2.1)
  savedAt: null,
  importNote: null,     // строка «Импортировано: …», исчезает при следующем действии
  renderedDayKey: null, // логический день, для которого отрисован интерфейс (инвариант 8)
  renderedTab: null,    // последний отрисованный вид (вкладка или лист) — скролл сбрасывается только при его смене
  // Черновики форм — по одному ХРАНИЛИЩУ на экран, внутри по ключу формы:
  // { 'edit:i1': { fields, focus } }. Прежде слот держал ровно один черновик,
  // и открытие соседней формы того же экрана его затирало (задача 28.B, п. 4).
  formDraft: {},        // черновик открытой формы: значения, фокус, каретка
  weekCloseConfirm: false, // «Закрыть неделю» — вторым тапом (задача 28.B, п. 6)
  // «Убрать» — тоже вторым тапом (задача 28.E/A). Одна форма на «Настройках»
  // открыта за раз, поэтому и поле одно на пункты и упражнения: в нём ключ
  // взведённой кнопки ('item:<id>' | 'ex:<id>'), а не голый флаг
  removeConfirm: null,
  // Только что убранное: на месте строки стоит «{имя} · убран — Вернуть».
  // Это НЕ .flash: тот гаснет через FLASH_MS, а отмена, исчезающая через
  // секунду, отменой не является (A.3.4). Живёт до следующего действия
  // владельца — как ui.importNote, и снимается там же
  goneNote: null,
  // Только что убранный БЛОК — тот же короткий путь назад, что у пункта:
  // на месте карточки «{имя} · убран — Вернуть» до следующего действия
  // («Расписание 1/3», п. 4.4). Поле своё: у блока нет id, ключ — имя.
  goneGroup: null,
  groupRename: null,    // имя блока с раскрытой формой
  // groupDelete снято вместе с «Удалить блок»: блок несёт дни, от которых
  // зависят прошлые числа, и больше не удаляется, а убирается («Расписание
  // 1/3», п. 1.4). Взводка ухода — общее ui.removeConfirm ('group:<имя>').
  groupAdd: false,      // открыта форма «Добавить блок»
  groupPick: null,      // выбор в поле «Блок» открытой формы (null — как у пункта)
  groupNew: false,      // в поле «Блок» выбран «+ Новый блок…»: раскрыто имя
  // черновик маски дней в форме блока или добавления блока (null — как у
  // блока). Механизм тот же, что у ui.editDays: чипы — кнопки, не поля
  blockDays: null,
  // свёрнутые карточки «Расписания»: имя блока → true. По умолчанию
  // развёрнуты все; переименование переносит ключ (п. 2.4)
  blockFold: {},
  // быстрое добавление открыто в блоке с этим именем ('' — «Без блока»)
  quickFor: null,
  // Режимы (задача Р2): раскрыт ли список под строкой «Режим: ‹имя›» — не
  // форма, а раскрытие, как свёртка карточки; и две формы в нём —
  // переименование режима (id) и «Новый режим». Обе в механизме «одна
  // форма за раз» (openSettingsForm) и черновиков по ключу (currentFormKey)
  modesOpen: false,
  modeRename: null,
  modeAdd: false,
  // свёртка «Показать неделю» в разборе (задача 16C): null — владелец её
  // в этом разборе не трогал, состояние берётся по умолчанию (задача 24)
  weekOpen: null,
  reviewOpen: false,    // разбор открыт поверх вкладки (с таб-бара он ушёл, задача 16B)
  reviewFrom: null,     // вкладка, на которую вернёт «Готово»
  reviewScroll: 0,      // её скролл — возвращается вместе с ней
  trainOpen: false,     // лист «Тренировка» поверх вкладки (задача 16D)
  trainId: null,        // недельный пункт, чей счётчик растёт записью
  trainFrom: null,      // вкладка возврата и её скролл
  trainScroll: 0,
  // черновик листа «Тренировка» — нагрузки и заметка (задача 26, п. 3).
  // Слотов механизма осталось два — этот и «Настроек»: заметочный ушёл с
  // экраном (задача 28.C), слот листа детали — с самим листом (28.D).
  // Снимается в начале renderTrain, возвращается в конце.
  // Прежде здесь лежала строка ui.trainNote с комментарием «переживает
  // перерисовку» — ввод в неё не попадал вовсе, и обещание было ложным.
  trainDraft: {},
  // кнопка, открывшая лист: закрытие любым путём вернёт ей фокус (п. 4.2)
  sheetSrc: null,       // { act, id } | null
  exEditingId: null,    // упражнение с раскрытой правкой
  exAddOpen: false,     // открыта форма «Добавить упражнение»
  wipeOpen: false,      // раскрыто предупреждение «Начать с чистого листа» (16.1)
  wipeFailed: false,    // чистка не выполнена: копию некуда положить или запись отказала (19, C.6.4)
  restoreFailed: false, // то же у возврата — молчать о нём нельзя (задача 27, Д7)
  wipeConfirm: false,   // «Стереть» ждёт подтверждения вторым тапом
  wipeDropConfirm: false, // «Убрать копию» — тоже вторым тапом
  // «Убрать» нечитаемые данные — вторым тапом (задача 25). Источников два
  // (рабочий ключ и снапшот зеркала), поэтому здесь имя источника, а не флаг
  corruptDropConfirm: null,
  mirrorRestoreConfirm: false, // «Восстановить из копии» — вторым тапом (28.A)
  mirrorKeepConfirm: false,    // «Оставить рабочую» — тоже: снапшот станет заменяемым
  mirrorFailed: false,         // восстановление не выполнено — молчать нельзя
  // Обновление приложения (задача Р3). updateArmed — владелец нажал
  // «Обновить»: только тогда controllerchange перезагружает страницу, иначе
  // смена контроллера (чужой claim) не делает ничего. updateCheck — строка
  // результата «Проверить обновления» в «Системе»: null | 'busy' | 'done' |
  // 'lost' | 'failed' ('lost' — установка сорвалась, задача Р4); живёт, пока
  // владелец на «Настройках». Оба — память открытой страницы: в store не
  // пишутся и перезапуск не переживают
  updateArmed: false,
  updateCheck: null,
  // свёрнутые секции «Настроек»: по умолчанию раскрыто только «Расписание».
  // «Блоки» и «Пункты» ушли — блоки стали карточками расписания, а
  // привычки получили свою секцию («Расписание 1/3», п. 2.3)
  settingsOpen: { schedule: true, habits: false, exercises: false, data: false, system: false }
};

let dayTimer = null; // таймер на ближайшую границу дня

/* Инвариант 8: экран мог устареть (смена логического дня в открытом
   приложении). При расхождении — чистка дневного ui-состояния и полная
   перерисовка текущей вкладки; true = действие применять нельзя. */
function syncDay() {
  if (ui.renderedDayKey === null || todayKey() === ui.renderedDayKey) return false;
  ui.missOpen = {};
  ui.raiseEdit = {};
  ui.todayUnfold = null; // развёртка блоков — состояние экрана на ДЕНЬ (Р2, п. 4)
  // Сменой дня мог смениться и режим (задача Р2): отрезок журнала, начатый
  // «завтра», приносит только импорт. «Настройки» держат о прежнем режиме
  // формы, черновики и свёртки по ИМЕНИ блока, а одноимённый блок нового
  // режима — другой блок. Сброс — тот же, что у «Выбрать»
  if (modeOn(ui.renderedDayKey) !== activeMode()) resetSettingsView();
  ui.renderedDayKey = todayKey(); // фиксируем новый день и для не-«Сегодня» вкладок
  renderAll();
  return true;
}

// +1 c запаса: таймеры iOS могут срабатывать на самой границе
const DAY_TIMER_SLACK_MS = timing('DAY_TIMER_SLACK_MS', 1000);

function armDayTimer() {
  clearTimeout(dayTimer);
  dayTimer = setTimeout(() => { syncDay(); armDayTimer(); }, msToNextBoundary() + DAY_TIMER_SLACK_MS);
}

const esc = s => String(s ?? '').replace(/[&<>"']/g,
  c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));

function valUnit(it) {
  const parts = [];
  if (typeof it.value === 'number' && isFinite(it.value)) parts.push(String(it.value));
  if (it.unit) parts.push(it.unit);
  return parts.join(' ');
}

function el(id) { return document.getElementById(id); }

/* Баннер отказа хранилища — ПОСТОЯННЫЙ узел вне экранов (index.html),
   обновляется точечно из save(). Прежде он жил в разметке «Сегодня» и
   «Привычек», а renderAll рисует одну текущую вкладку — и все отказы,
   случившиеся на «Настройках» и листах (правка пункта,
   экспорт, «Вернуть»), проходили без единого следа на том экране, где
   владелец стоял (задача 27, Д6). Тон прежний: тихая констатация, ни
   красного, ни слова «ошибка».
   Домен без DOM (доменные тесты) выходит сразу — save() зовётся и там. */
function storageNote() {
  if (typeof document === 'undefined') return;
  const p = el('storage-note');
  if (!p) return;
  p.textContent = saveFailed ? 'Хранилище недоступно — отметки сейчас не сохраняются' : '';
  p.hidden = !saveFailed;
}

/* Постоянная область объявлений (index.html): узел role="status",
   рождённый ВМЕСТЕ с текстом, скринридером не объявляется — объявляется
   только изменение текста в уже существующем узле. Так устроен анонс
   планки дня; тем же путём идут отказы формы (задача 27.1, п. 9.2).
   Чистится в начале onClick, чтобы повторный отказ после другого
   действия снова читался как изменение. */
function announce(text) {
  if (typeof document === 'undefined') return;
  const p = el('live');
  if (p) p.textContent = text || '';
}

/* ── Движение (задача 12): короткая функциональная обратная связь ──
   Заполнение круга и ячейки полосы, fade экрана и flash сохранения —
   на CSS (transition/@keyframes). Уходу карточки разбора нужен JS:
   класс-триггер, затем удаление узла перерисовкой. */

const MOTION_MS = timing('MOTION_MS', 240); // потолок движения (12.1); fallback ухода карточки — сверх него
const MOTION_TAIL_MS = timing('MOTION_TAIL_MS', 60); // запас fallback'а сверх самого перехода

function prefersReducedMotion() {
  try { return !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches); }
  catch (e) { return false; }
}

/* Короткий scale-отклик на тап (класс-триггер .pop с рестартом анимации).
   Только в горячих путях отметки — при первичном рендере класса нет.
   При reduced-motion не навешивается (и CSS animation:none подстрахует). */
function tapPop(node) {
  if (!node || prefersReducedMotion()) return;
  node.classList.remove('pop');
  void node.offsetWidth; // рефлоу — чтобы повторный тап перезапустил анимацию
  node.classList.add('pop');
}

/* Пометить узел уходящим, затем выполнить done() — перерисовку, реально
   убирающую узел. Триггер — transitionend, но он ненадёжен (jsdom его не
   шлёт, reduced-motion отключает переход), поэтому done гарантирован
   fallback-таймаутом и вызывается ровно один раз. При reduced-motion —
   сразу, без ожидания: конечное состояние достижимо мгновенно. */
/* Тихое подтверждение «Сохранено» обычно гаснет ключевыми кадрами. При
   reduced-motion анимация выключена глобальным блоком, и узел остался бы
   невидимым (opacity: 0) — CSS показывает его статично, а убрать его после
   паузы может только JS: конечное состояние должно быть достижимо и здесь
   (задача 19, C.6.3). */
const FLASH_MS = timing('FLASH_MS', 1200);

/* Узел подтверждения — ТОЛЬКО на видимом экране. Выборка по всему
   документу брала первый узел в порядке секций index.html, а renderAll
   рисует одну текущую вкладку: узлы .flash с прежних экранов остаются в
   DOM (CSS лишь возвращает их к opacity:0), и первым находился чужой,
   скрытый. armFlash взводил таймер не на тот узел, keepInPlace считал по
   нулевому rect скрытой секции и прыгал вверх на всю высоту кнопки
   (задача 27, Д4). Экран всегда ровно один — hidden снимает renderAll.

   Видимость проверяется и ВНУТРИ экрана («Расписание 1/3», п. 4.2): у
   свёрнутой карточки блока тело скрыто атрибутом hidden, и узел в нём имеет
   нулевой rect — тот же прыжок, что давал чужой экран. */
const shownIn = sel => [...document.querySelectorAll('main .screen:not([hidden]) ' + sel)]
  .find(n => !n.closest('[hidden]')) || null;
const visibleFlash = () => shownIn('.flash:not(.keep)');

/* Строка «убран — Вернуть» — тем же правилом: только на видимом экране.
   Нужна keepInPlace, который держит точку нажатия на месте (A.3.4). */
const goneNoteEl = () => shownIn('.gone-note');

function armFlash() {
  if (!prefersReducedMotion()) return;
  // строку отказа (.flash.keep) не трогаем: она обязана дождаться правки
  const n = visibleFlash();
  if (!n) return;
  setTimeout(() => { if (n.isConnected) n.remove(); }, FLASH_MS);
}

/* ── Тихое подтверждение и тихий отказ (задача 26, п. 2) ───────
   Один и тот же узел .flash и один и тот же тон. Разводит их жизненный
   путь, и он же диктует механику.

   ПОДТВЕРЖДЕНИЕ: форма закрылась, экран перерисован — узел рождается
   разметкой у ЯКОРЯ той строки, которой форма принадлежала. Прежде оно
   печаталось в шапке экрана: у правки упражнения — на 1939 px выше
   нажатой кнопки (замер, 375×812). Ключ отдаётся ровно один раз.

   ОТКАЗ: форма ОСТАЛАСЬ открытой, и перерисовывать её нельзя — она
   вернула бы в поля сохранённые значения, а введённое владельцем
   пропало бы (п. 2.4). Поэтому строка вставляется точечно рядом с
   нажатой кнопкой; тем же путём это делал лист тренировки до задачи 26. */
const flashOk = (key, text) => { ui.savedAt = { key, text: text || 'Сохранено' }; };

/* Подтверждение ПО ФАКТУ ЗАПИСИ. Прежде все вызовы были безусловны, и
   «Сохранено» печаталось даже когда localStorage отказал: в памяти новое,
   на диске прежнее, а приложение утверждает обратное (задача 27, Д6).
   Узел, якорь и тон те же — меняется только текст; постоянный баннер о
   недоступности хранилища ставит save() сам.

   Вызовов ВОСЕМЬ: правка пункта, добавление привычки, правка и добавление
   упражнения, правка и добавление блока, возврат пункта или упражнения,
   возврат блока («Расписание 1/3»). Быстрое добавление и копия блока
   печатают свой текст через flashOk — у них запись проверена самой
   операцией. Прежде комментарий называл шесть, не считая возврата пункта
   (задача 28.E/A); ещё раньше вызовов было восемь — два ушли с формами листа
   детали (задача 28.D); а до того комментарий называл одиннадцать, хотя их
   было восемь. Второй параметр (свой
   текст подтверждения) снят там же: его не передавал ни один вызывающий,
   и okText всегда приходил undefined — «Сохранено» ставил flashOk. */
function flashWrite(key) {
  flashOk(key, lastSaveOk() ? undefined : 'Не сохранено: хранилище недоступно');
}

function flashAt(key) {
  if (!ui.savedAt || ui.savedAt.key !== key) return '';
  const text = ui.savedAt.text;
  ui.savedAt = null; // разовое: отдав узел, якорь гаснет
  return `<p class="flash" role="status">${esc(text)}</p>`;
}

/* Подтверждение обязано появиться ТАМ, ГДЕ ПАЛЕЦ, а форма при сохранении
   закрывается и разметка схлопывается на её высоту. У высокой формы
   (правка параметра — 530 px на 375×812) строка-якорь уезжала выше края
   экрана, и узел рождался за пределами видимого — замер дал −73 px.
   Держим точку нажатия на месте: запоминаем, на какой высоте ЭКРАНА стояла
   кнопка, и после перерисовки подгоняем скролл так, чтобы узел встал туда
   же. Скролл мгновенный — это не движение, а та же бухгалтерия положения,
   что у листов (п. 4.1). */
/* Куда встанет скролл, чтобы узел подтверждения оказался там, где стояла
   кнопка. Чистая функция: до задачи 27.1 арифметика жила внутри
   keepInPlace и проверялась только замером в браузере — мутант «keepInPlace
   снят» выжил бы (п. 9.3). Ниже нуля не уходим: отрицательного скролла нет.
   Смещения нет — null, и трогать скролл незачем. */
function holdScrollTarget(anchorTop, nodeTop, scrollY) {
  const dy = nodeTop - anchorTop;
  if (!dy) return null;
  return Math.max(0, (scrollY || 0) + dy);
}

/* find — чем искать узел, который должен встать на место кнопки. По
   умолчанию это подтверждение .flash; у ухода пункта якорь другой —
   строка «убран» со своим классом (A.3.4), и подменяется он здесь. */
function keepInPlace(btn, render, find) {
  const y = btn ? btn.getBoundingClientRect().top : null;
  render();
  if (y === null) return;
  const n = (find || visibleFlash)();
  if (!n) return;
  const to = holdScrollTarget(y, n.getBoundingClientRect().top, window.scrollY);
  if (to !== null) window.scrollTo(0, to);
}

function refuse(btn, text) {
  if (!btn) return;
  const box = btn.closest('.btns') || btn;
  const prev = box.previousElementSibling;
  // повторный отказ заменяет прежний, а не копится строками
  if (prev && prev.classList && prev.classList.contains('flash')) prev.remove();
  // role="status" со вставленного узла снят намеренно: узел рождается ВМЕСТЕ
  // с текстом, и такой скринридером не объявляется — обещание было ложным.
  // Объявляет постоянная область (задача 27.1, п. 9.2), у неё меняется текст
  box.insertAdjacentHTML('beforebegin', `<p class="flash keep">${esc(text)}</p>`);
  announce(text);
}

function motionLeave(node, done) {
  if (!node || prefersReducedMotion()) { done(); return; }
  // зафиксировать текущую высоту как старт схлопывания (в конце — max-height:0)
  node.style.maxHeight = node.scrollHeight + 'px';
  void node.offsetHeight; // рефлоу, чтобы стартовое значение применилось до перехода
  node.classList.add('leaving');
  node.style.maxHeight = '0px';
  let fired = false;
  // done ровно один раз; если узел уже убран (напр. соседним решением,
  // перерисовавшим весь разбор) — повторная перерисовка не нужна.
  // transitionend фильтруется по самому узлу (задача Р3, п. 0, риск 6 Р2),
  // как у motionFold: переход ребёнка карточки (фокусная рамка, отклик
  // кнопки) всплывает сюда же и оборвал бы уход раньше срока. Слушатель
  // поэтому не { once: true } — чужое событие не должно его снимать
  const fin = e => {
    if (e && e.target !== node) return; // переход ребёнка всплыл — не наш
    if (fired) return;
    fired = true;
    node.removeEventListener('transitionend', fin);
    if (node.isConnected) done();
  };
  node.addEventListener('transitionend', fin);
  setTimeout(fin, MOTION_MS + MOTION_TAIL_MS);
}

/* ── Сцена закрытия дня (задача 28.E/C) ───────────────────────
   Волна НЕ ПУТЕШЕСТВУЕТ от круга к планке, а приходит: разведка задачи 28
   измерила расстояние от круга до планки — от 103 до 475 px в зависимости
   от того, какой пункт нажат последним. Разброс 4,6× при окне движения
   180–260 мс, которое допускает 1,44×; при 260 мс это 58°/с, глаз не
   проследит. Связь несут время и цвет, а не движущийся объект.

   Три фазы, каждая ≤ 240 мс, вся сцена ≤ 360 мс (это легализовано решением
   архитектора: сцена, а не движение). Первая фаза сцены — не здесь: отклик
   круга на касание (.pop) уже стоит в updateTodayMark и играет на t = 0,
   опаздывать ему нельзя.
     кольцо от круга   60 → 260 мс  (масштаб и прозрачность)
     блик вдоль планки 100 → 340 мс (постоянный узел, клип заполнением)
     фраза «День закрыт» 120 → 360 мс (цвет и масштаб; font-weight НЕ
       анимируется — другой вес даёт другие глифы и повторный шейпинг
       каждый кадр, это layout; вес ставит статический класс .ok)

   Классы-триггеры навешивает только этот хук. Ни одна функция рендера их
   не печатает — сцена не может проиграться при загрузке экрана. Оба узла
   пересоздаются перерисовкой, поэтому перерисовка сцену просто отменяет.

   Ранний выход при reduced-motion: конечное состояние (полная планка,
   «День закрыт», акцентный тон) даёт разметка, а не анимация. */
const DAY_CLOSE_MS = timing('DAY_CLOSE_MS', 360);
let dayCloseTimer = null;

function playDayClose(dayline, label) {
  if (prefersReducedMotion()) return;
  const nodes = [dayline, label].filter(Boolean);
  if (!nodes.length) return;
  for (const n of nodes) n.classList.remove('closing');
  void nodes[0].offsetWidth; // рефлоу: повторное закрытие дня должно проигрываться заново
  for (const n of nodes) n.classList.add('closing');
  clearTimeout(dayCloseTimer);
  dayCloseTimer = setTimeout(() => {
    for (const n of nodes) n.classList.remove('closing');
  }, DAY_CLOSE_MS + MOTION_TAIL_MS);
}

/* ── Свёртка выполненного блока «Сегодня» (задача Р2, п. 4) ─────
   Свёрнутость — СОСТОЯНИЕ, и печатает его рендер (groupSections): при
   загрузке экрана и любой полной перерисовке выполненный блок уже свёрнут,
   и ничего не движется. Движение — только ОТКЛИК на отметку или пропуск,
   сделавшие блок выполненным: строки блока схлопываются по высоте, затем
   экран перерисовывается, и на месте заголовка встаёт свёрнутая строка.
   Схлопываются строки, а не заголовок: имя блока остаётся на месте, и блок
   читается как «сложился в свою строку», а не «исчез и появился другим».

   Классы-триггеры (.folding) навешивает только motionFold — по образцу
   motionLeave. Отличий два. transitionend фильтруется по узлу: переходы
   детей (заливка круга, возврат сдвинутой свайпом строки) всплывают сюда
   же и обрывали бы схлопывание раньше срока. И перерисовка не делается,
   пока играет сцена закрытия дня: сцена главнее, и свернёт её конец.

   Сцена закрытия дня и свёртка одним тапом: играет ТОЛЬКО сцена, а блок
   сворачивается перерисовкой после её окончания (DAY_CLOSE_MS), без своей
   анимации — двух одновременных откликов на одно действие не бывает.
   Сцена — единственный заметный отклик приложения (CLAUDE.md, «Движение»),
   и схлопывание рядом с ней спорило бы с ней за глаз.

   Задержек нет: схлопывание начинается на t = 0 (задержка законна только
   внутри сцены), а отложенная перерисовка — не движение, а смена
   состояния после конца сцены. Reduced-motion — ранний выход: перерисовка
   мгновенно, конечное состояние то же. */
let foldTimer = null;

function unfoldedToday(name) {
  const u = ui.todayUnfold;
  return !!(u && u.day === todayKey() && u.names[name] === true);
}

function unfoldToday(name) {
  const t = todayKey();
  if (!ui.todayUnfold || ui.todayUnfold.day !== t) ui.todayUnfold = { day: t, names: {} };
  ui.todayUnfold.names[name] = true;
}

function unfoldForget(name) {
  if (ui.todayUnfold) delete ui.todayUnfold.names[name];
}

/* Строки блока на экране: узел сразу за заголовком — .chain у блока из
   двух и более пунктов, сама строка у блока из одного (groupSections).
   Ищется по заголовку, а не по разметке строки: пометок блока в строке нет,
   и заводить их ради поиска значило бы менять разметку обоих экранов. */
function blockRowsEl(scr, name) {
  const head = [...scr.querySelectorAll('.list > .g-label')]
    .find(l => l.firstElementChild && l.firstElementChild.textContent === name);
  const n = head && head.nextElementSibling;
  return n && (n.classList.contains('chain') || n.classList.contains('rowwrap')) ? n : null;
}

/* Имя блока, чьей строке принадлежит узел; вне блока — null. Пункт без
   блока стоит в секции без заголовка — перед ним строка или .chain, а не
   .g-label */
function blockNameOfNode(node) {
  const wrap = node && node.closest ? node.closest('.list .rowwrap') : null;
  if (!wrap) return null;
  const body = wrap.parentElement && wrap.parentElement.classList.contains('chain') ? wrap.parentElement : wrap;
  const head = body.previousElementSibling;
  return head && head.classList.contains('g-label') && head.firstElementChild ? head.firstElementChild.textContent : null;
}

function motionFold(node, done) {
  if (!node || prefersReducedMotion()) { done(); return; }
  node.style.maxHeight = node.scrollHeight + 'px';
  void node.offsetHeight; // рефлоу: стартовая высота применяется до перехода
  node.classList.add('folding');
  node.style.maxHeight = '0px';
  let fired = false;
  const fin = e => {
    if (e && e.target !== node) return; // переход ребёнка всплыл — не наш
    if (fired) return;
    fired = true;
    node.removeEventListener('transitionend', fin);
    if (node.isConnected) done(); // перерисованный уже без узла экран свёрнут сам
  };
  node.addEventListener('transitionend', fin);
  setTimeout(fin, MOTION_MS + MOTION_TAIL_MS);
}

/* Перерисовка, сворачивающая блок. Фокус не теряется: был на узле, который
   пережил перерисовку (тот же data-act и data-id), — возвращается ему; был в
   строках свернувшегося блока — встаёт на его свёрнутую строку, то есть
   туда, где эти строки были. Скрытый экран не рисуется: показ вкладки или
   закрытие листа перерисует «Сегодня» сам. Сменившийся логический день —
   полная сверка (инвариант 8), а не одна вкладка. */
function foldRender() {
  clearTimeout(foldTimer);
  foldTimer = null;
  if (syncDay()) return;
  const scr = el('scr-today');
  if (scr.hidden) return;
  const a = document.activeElement;
  const src = a && a !== document.body && scr.contains(a)
    ? { act: a.dataset ? a.dataset.act : '', id: a.dataset ? a.dataset.id || '' : '', block: blockNameOfNode(a) }
    : null;
  renderToday();
  if (!src) return;
  const same = src.act
    ? [...scr.querySelectorAll(`[data-act="${src.act}"]`)].find(x => (x.dataset.id || '') === src.id)
    : null;
  const fold = src.block !== null
    ? [...scr.querySelectorAll('[data-act="block-unfold"]')].find(x => x.dataset.name === src.block)
    : null;
  const target = same || fold;
  if (target) target.focus({ preventScroll: true });
}

function foldAfterScene() {
  clearTimeout(foldTimer);
  if (prefersReducedMotion()) { foldRender(); return; }
  foldTimer = setTimeout(foldRender, DAY_CLOSE_MS + MOTION_TAIL_MS);
}

/* Отклик горячего пути: отметка, пропуск или «Вернуть» у пункта id уже
   записаны и показаны точечно. scene — этим же действием закрылся день.

   Блок сворачивается, когда действие сделало его выполненным. Флаг
   развёртки при этом снимается: владелец развернул выполненный блок, снял
   в нём отметку (блок остался развёрнутым — флаг жив) и отметил снова —
   блок снова выполнен и сворачивается. Любое действие внутри выполненного
   блока делает его невыполненным (отмечать нечего, пропускать нечего), так
   что «стал выполненным» и «выполнен после действия» здесь одно и то же. */
function todayFoldAfter(id, scene) {
  const scr = el('scr-today');
  const t = todayKey();
  const secs = groupedItems(dueDaily('min', t)).filter(s => s.group);
  const sec = secs.find(s => s.items.some(i => i.id === id));
  let node = null;
  if (sec && blockTally(sec.items, t).full) {
    unfoldForget(sec.group.name);
    node = blockRowsEl(scr, sec.group.name);
  }
  if (scene) {
    // Сцена главнее: своей анимации у свёртки нет. Перерисовка после сцены
    // сворачивает ВСЕ блоки, чьи строки ещё на экране, — и этот, и тот, чьё
    // схлопывание сцена застала на полпути (оно перерисовку уступает сцене)
    const owed = secs.some(s => blockTally(s.items, t).full && !unfoldedToday(s.group.name) && blockRowsEl(scr, s.group.name));
    if (owed) foldAfterScene();
    return;
  }
  if (!node) return; // пункт вне блоков, или блок не выполнен — не сворачивается
  motionFold(node, () => {
    if (scr.querySelector('.dayline.closing')) return; // сцена свернёт сама по окончании
    foldRender();
  });
}

/* Тап по свёрнутой строке: блок разворачивается до конца дня. Фокус — на
   первый круг блока: строка-кнопка исчезла, и её место заняли строки, с
   которыми и пришли что-то делать. У блока, где всё пропущено, круги
   неактивны — фокус на первое «Вернуть». */
function focusUnfolded(name) {
  const rows = blockRowsEl(el('scr-today'), name);
  if (!rows) return;
  const target = rows.querySelector('input[data-act="mark"]:not(:disabled)') ||
    rows.querySelector('.skipbtn:not([hidden])');
  if (target) target.focus({ preventScroll: true });
}

/* Четыре вкладки плюс ДВА листа поверх них: разбор недели и тренировка.
   Третьим был лист детали пункта; он существовал ради формулы и лестницы
   и ушёл вместе с ними (задача 28.D). Разбор с таб-бара ушёл (задача 16B) —
   открывается баннером «Сегодня» и строкой «Прогресса», закрывается
   «Готово» на ту вкладку, с которой открыт.

   Приоритет при одновременных флагах пересчитан: главнее разбор. Прежде
   первым стоял лист детали, и разбор был вторым; теперь порядок «разбор,
   затем тренировка» — тот же, что читает currentFormKey. */
function renderAll() {
  const map = {
    today: 'scr-today', habits: 'scr-habits', progress: 'scr-progress',
    settings: 'scr-settings'
  };
  // исчезнувший недельный пункт (импорт) закрывает лист тренировки
  if (ui.trainOpen && !store.items.some(i => i.id === ui.trainId)) closeTrain();
  const sheet = ui.reviewOpen ? 'review' : (ui.trainOpen ? 'train' : null);
  for (const [tab, id] of Object.entries(map)) el(id).hidden = sheet ? true : tab !== ui.tab;
  el('scr-review').hidden = sheet !== 'review';
  el('scr-train').hidden = sheet !== 'train';
  document.querySelectorAll('#tabs button').forEach(b => {
    // вкладка возврата остаётся текущей и при открытом листе
    if (b.dataset.tab === ui.tab) b.setAttribute('aria-current', 'page');
    else b.removeAttribute('aria-current');
  });
  // узлы подтверждения и отказа со СКРЫТЫХ экранов снимаются (задача 27.1,
  // п. 4.2): они принадлежали действию на другом экране и своё уже сказали,
  // а оставаясь в DOM, копились там до следующей перерисовки той вкладки.
  // Выборка по видимому экрану их и так не берёт — но держать мусор незачем
  document.querySelectorAll('main .screen[hidden] .flash').forEach(n => n.remove());
  storageNote(); // постоянный узел баннера — вне экранов, состояние сверяется здесь
  if (ui.reviewOpen) renderReview();
  else if (ui.trainOpen) renderTrain();
  else {
    if (ui.tab === 'today') renderToday();
    if (ui.tab === 'habits') renderHabits();
    if (ui.tab === 'progress') renderProgress();
    if (ui.tab === 'settings') renderSettings();
  }
  const view = sheet || ui.tab;
  if (ui.renderedTab !== view) window.scrollTo(0, 0); // скролл — только при фактической смене вида
  ui.renderedTab = view;
}

/* Экран 1 — «Сегодня»: только область min (инвариант 10) */
function renderToday() {
  const t = todayKey();
  ui.renderedDayKey = t;
  // применимость ДНЯ, а не «есть сейчас» (задача 29/B): пункт вне
  // маски сегодняшнего дня не идёт ни в список, ни в знаменатель планки
  const items = dueDaily('min', t);
  const { pct, closed, note } = todayCounts(items, t);

  // строка дня — ТРЕТЬЕЙ в шапке: над планкой и над списком. Ниже она
  // физически читалась бы как комментарий к сделанному (задача 28.E/B, п. 2.1).
  // Ни кавычек, ни курсива, ни акцента, ни aria-live, ни своей анимации:
  // это тихая справка о дате, а не объявление и не оценка
  // Имя активного режима (задача Р2) — рядом с датой, приглушённо и только
  // когда выбирать есть из чего: при одном живом режиме разметки нет вовсе,
  // и шапка та же, что до режимов. Дата и имя — одна строка шапки (.hdate),
  // поэтому строка дня остаётся третьей. Имя — слово владельца: esc, без
  // кавычек и капители; «режим» перед ним — только для чтения с экрана
  const modes = liveModes();
  const am = modes.length > 1 ? findMode(activeMode()) : null;
  const date = am
    ? `<div class="hdate"><h1>${esc(fmtDay(t))}</h1><p class="hmode"><span class="sr-only">режим </span>${esc(am.name)}</p></div>`
    : `<h1>${esc(fmtDay(t))}</h1>`;
  let h = `
    <header class="page">
      <p class="overline">${esc(fmtWeekday(t))}</p>
      ${date}
      <p class="dline">${esc(dayLine(t))}</p>
    </header>`;

  if (reviewDue()) {
    h += `<button class="banner" data-act="goto-review"><span>Доступен разбор недели</span><span class="chev" aria-hidden="true">&rsaquo;</span></button>`;
  }

  // пустой список — как на «Привычках»: планка дня без пунктов ничего не
  // измеряет, поэтому её нет вовсе (задача 16.1, состояние после чистки).
  // Список и планка стоят по ЗАПЛАНИРОВАННЫМ (items), а не по знаменателю:
  // всё пропущено — строки остаются на месте (вернуть можно только из них),
  // планка пуста и честно говорит «0 из 0 · пропусков K» (задача Р2; хвост —
  // задача Р3, п. 0.5). Строки блока, где
  // пропущено всё, свёрнуты в его строку («0 из 0 · пропусков K», п. 4) —
  // и возвращаются её развёрткой, а не пропадают
  if (items.length) {
    h += `
    <div class="dayline">
      <div class="bar"><i style="width:${pct}%"><b class="sheen" aria-hidden="true"></b></i></div>
      <p class="bar-note${closed ? ' ok' : ''}" aria-live="polite">${note}</p>
    </div>`;
    h += `<div class="list">` + groupSections(items, t, false) + `</div>`;
  } else if (liveDaily().some(i => i.area === 'min' && itemMode(i) === modeOn(t))) {
    // Действия есть, но ни одно не стоит в этом дне («Расписание 1/3», п. 2.1):
    // считаются действия активного режима (Р2) — «Настройки» показывают его,
    // и строка «пока нет» звала бы туда, где действий действительно нет
    // будний блок в субботу. «Пунктов пока нет» здесь было бы неправдой и
    // звало бы заводить то, что уже заведено. Строка называет факт дня —
    // ни оценки, ни призыва, и планки тоже нет: измерять нечего.
    h += `<p class="muted">На сегодня в расписании ничего нет.</p>`;
  } else {
    h += `<p class="muted">Пунктов пока нет — добавить можно в Настройках → Расписание.</p>`;
  }

  // area у недельного счётчика не проверяется: migrate принудительно ставит
  // ему min (инвариант 10), другого значения в данных не бывает (C.5.4)
  for (const w of liveWeekly()) {
    const n = trainCount(w.id);
    h += `
      <div class="weekcount">
        <span class="txt">
          <span class="tname">${esc(w.name)}</span>
          ${rowNote(w) ? `<span class="note">${esc(rowNote(w))}</span>` : ''}
        </span>
        <span class="wctl">
          <span class="wnum"><b>${n}</b>&thinsp;/&thinsp;${w.goal || 0}</span>
          <button class="btn icon plus" data-act="train-inc" data-id="${esc(w.id)}" aria-label="записать тренировку: «${esc(w.name)}»">+</button>
        </span>
      </div>
      ${n ? `<button class="undo" data-act="train-undo" data-id="${esc(w.id)}" aria-label="отменить последний: «${esc(w.name)}»">отменить последний</button>` : ''}`;
  }

  // Здесь стояла кредо-строка «Минимум выполняется даже в худший день.».
  // Замер на 375×812: она начиналась на 905 px при сгибе (верх таб-бара) на
  // 753 — за сгибом, и без прокрутки её не видел никто. Текст никуда не
  // делся: он первый элемент набора строк дня и теперь бывает в шапке, где
  // его читают. Кредо «Привычек» остаётся: там оно на 468 px, то есть видно
  // всегда, и снимать видимое задача не просила.

  el('scr-today').innerHTML = h;
}

/* Секции дневного экрана: заголовок блока плюс его строки. Блок из двух и
   более активных пунктов заворачивается в .chain и рисует линию; блок из
   одного пункта и пункты без блока — нет (инвариант 13). Крайние половинки
   линии гасит CSS по :first-child/:last-child — считать соседей не нужно.

   Заголовок — имя слева, подпись блока справа приглушённо («Расписание
   1/3», п. 2.1). Подпись — тоже слово владельца («7:00», «с 15:20»), а не
   надпись приложения, поэтому печатается как есть: без капители и
   трекинга, но и не тише надстрочника (З26/5.3). Функция общая, и подпись
   видна на обоих дневных экранах: блок один и тот же, и на «Привычках» он
   не должен читаться другим. Блок без сегодняшних пунктов сюда не доходит
   вовсе — groupedItems строит секции только из переданных пунктов, а
   переданы применимые сегодня (dueDaily). */
function groupSections(items, t, habit) {
  let h = '';
  // «Привычки» раскладываются тем же правилом, что секция «Привычки»
  // «Настроек» (habitSections, задача Р3, п. 0.2): блоки активного режима с
  // подписью, затем прочие имена без подписи, «Без блока» — последним и с
  // заголовком, только когда выше стоят блоки. «Сегодня» — блоками своего
  // режима (groupedItems): действия принадлежат режиму, пункты без блока
  // стоят в секции без заголовка, как прежде
  const secs = habit
    ? habitSections(items)
    : groupedItems(items).map(s => ({ name: s.group ? s.group.name : null, group: s.group, items: s.items }));
  for (const sec of secs) {
    // Выполненный блок «Сегодня» — одной строкой (задача Р2, п. 4). Только
    // блок и только на «Сегодня»: пункты без блока не сворачиваются, а на
    // «Привычках» «все отмечены» нормой не является (та же причина, по
    // которой там нет сцены). Рендер печатает свёрнутое СОСТОЯНИЕ — без
    // единого класса-триггера: при загрузке экрана и любой перерисовке
    // выполненный блок уже свёрнут и ничего не проигрывает
    if (sec.group && !habit) {
      const b = blockTally(sec.items, t);
      if (b.full && !unfoldedToday(sec.group.name)) {
        // точка «вчера — пропуск» не прячется свёрткой (задача Р3, п. 0.1)
        h += foldRow(sec.group, b, sec.items.some(it => missedYesterday(it, t)));
        continue;
      }
    }
    if (sec.name !== null) {
      const cap = sec.group ? (sec.group.caption || '').trim() : '';
      h += `<p class="g-label"><span>${esc(sec.name)}</span>${cap ? `<span class="g-cap">${esc(cap)}</span>` : ''}</p>`;
    } else if (habit && h) {
      // без блока — последними; заголовок нужен, только если выше стоят
      // блоки: иначе строки читались бы продолжением последнего из них
      h += `<p class="g-label"><span>Без блока</span></p>`;
    }
    const chained = sec.name !== null && sec.items.length > 1;
    if (chained) h += `<div class="chain">`;
    for (const it of sec.items) h += dailyRow(it, t, habit, chained);
    if (chained) h += `</div>`;
  }
  return h;
}

/* Свёрнутый блок «Сегодня» (задача Р2, п. 4): имя и подпись блока, справа
   счёт — «✓ N из N» без пропусков, «N из N · пропусков K» с ними. N —
   отмеченные: у выполненного блока это и есть знаменатель (blockTally).
   ✓ — текстовый знак U+2713, не эмодзи и не награда: он говорит о блоке
   «решён», а не о человеке, и скрыт от AT — там то же говорит «отмечено».

   Строка — кнопка, одна цель — одно действие: тап разворачивает блок до
   конца дня. aria-expanded="false" объявляет, что за ней свёрнуто; а
   aria-controls нет намеренно — строк блока в DOM нет, пока он свёрнут, и
   указывать было бы не на что (задача 26, п. 8.1). Имя для AT — словами,
   без знака: «Утро, 7:00: отмечено 5 из 5, пропусков 1». */
function foldRow(g, b, miss) {
  const cap = (g.caption || '').trim();
  const n = b.done;
  const k = b.skipped;
  const count = k
    ? `${n}&nbsp;из&nbsp;${n} · пропусков&nbsp;${k}`
    : `<span aria-hidden="true">&#10003;</span>&nbsp;${n}&nbsp;из&nbsp;${n}`;
  // Точка «вчера — пропуск» (задача Р3, п. 0.1, решение архитектора): блок с
  // ней сворачивается, как любой выполненный, но признак не прячется — тот
  // же кружок, что у точки строки, справа от счёта. Это признак, а не вторая
  // цель: подписи и «отметить» у него нет, тап по строке разворачивает блок,
  // и ретро-отметка делается у самой строки пункта. Для AT — теми же словами,
  // что у точки
  const label = `${g.name}${cap ? ', ' + cap : ''}: отмечено ${n} из ${n}${k ? ', пропусков ' + k : ''}${miss ? ', вчера — пропуск' : ''}`;
  return `<button type="button" class="bfold" data-act="block-unfold" data-name="${esc(g.name)}" aria-expanded="false" aria-label="${esc(label)}">` +
    `<span class="bf-name">${esc(g.name)}</span>${cap ? `<span class="g-cap">${esc(cap)}</span>` : ''}` +
    `<span class="bf-count">${count}</span>${miss ? `<span class="bf-miss" aria-hidden="true"><i></i></span>` : ''}</button>`;
}

/* Строка ежедневного пункта: чекбокс, точка-маркер, ретро-отметка —
   общая для «Сегодня» (area min) и «Привычек» (habit: true добавляет
   серию у названия и полосу текущей недели под строкой).
   Название внутри label: тап по всей строке переключает отметку.

   Подписью идёт СЛОВО ВЛАДЕЛЬЦА — item.note. Прежде у пункта с лестницей
   его вытесняла текущая ступень (а у вставшей привычки — строка о том, что
   работа здесь закончена); лестница снята задачей 28.D, и подпись вернулась
   к единственному источнику. Хвостовой кнопки в строке тоже больше нет: она
   вела в лист детали и существовала только у пунктов с лестницей или
   формулой — теперь строка одинакова у всех.

   Имя чекбоксу даёт содержимое label — aria-label его только затёр бы.
   Подпись «вчера — пропуск» стоит в разметке всегда, когда есть точка,
   и прячется атрибутом hidden: aria-controls обязан указывать на
   существующий узел, иначе disclosure для AT неполон (задача 26, п. 8.1). */
/* Подпись строки пункта: время (если вписано) и слова владельца, через
   домашний разделитель « · ». Время — ЯКОРЬ дня, поэтому идёт первым;
   подпись уточняет. Ни сортировки, ни просрочки, ни уведомлений: на расчёт
   оно не влияет нигде (задача 29/B, решение архитектора). */
function rowNote(it) {
  return [it.at, it.note].map(x => (x || '').trim()).filter(Boolean).join(' · ');
}

/* Кнопка под строкой действия «Сегодня» (задача Р2): «Не сегодня» у
   неотмеченного, «Вернуть» у пропущенного, у отмеченного — скрыта. Стоит в
   разметке ВСЕГДА, а не рождается жестом: свайп её только выдвигает, а
   клавиатура доходит до неё обычным Tab — фокус выдвигает её сам (CSS). Узел
   один на все три состояния, поэтому точечный путь меняет атрибуты, а не
   пересоздаёт его, и фокус на ней переживает тап. */
const SKIP_WORD = { skip: ['Не сегодня', 'не сегодня'], unskip: ['Вернуть', 'вернуть'] };

function skipBtnAttrs(it, on, skip) {
  const act = skip ? 'unskip' : 'skip';
  return { act, text: SKIP_WORD[act][0], label: `${SKIP_WORD[act][1]}: «${it.name}»`, hidden: on };
}

function skipBtn(it, on, skip) {
  const a = skipBtnAttrs(it, on, skip);
  return `<button type="button" class="btn skipbtn" data-act="${a.act}" data-id="${esc(it.id)}" aria-label="${esc(a.label)}"${a.hidden ? ' hidden' : ''}>${a.text}</button>`;
}

function dailyRow(it, t, habit, chain) {
  const on = isMarked(t, it.id);
  // пропуск — только у действия «Сегодня»; у привычки строка его не знает
  const skip = !habit && isSkipped(t, it.id);
  const miss = missedYesterday(it, t);
  const vu = valUnit(it);
  const streak = habit ? habitStreak(it) : 0; // при нуле справка скрыта
  const sub = rowNote(it);
  // половинки линии блока: верхняя идёт к предыдущему пункту, нижняя — к
  // следующему; у крайних строк лишнюю гасит CSS. Идут первыми в разметке —
  // круг отметки закрывает линию собой.
  const segs = chain
    ? `<span class="cseg up" aria-hidden="true"></span><span class="cseg down" aria-hidden="true"></span>`
    : '';
  // Строка действия «Сегодня» сдвигается свайпом (.swipe): жест выдвигает
  // кнопку «Не сегодня» / «Вернуть» из-под строки. Пропущенная строка (.skip)
  // стоит на месте — зачёркнута, приглушена, круг неактивен (disabled):
  // снять пропуск — «Вернуть», а не тап по кругу
  return `
      <div class="rowwrap${habit ? ' hrow' : ' swipe'}${skip ? ' skip' : ''}">
        ${segs}
        <label class="row check${on ? ' on' : ''}">
          <input type="checkbox" data-act="mark" data-id="${esc(it.id)}"${on ? ' checked' : ''}${skip ? ' disabled' : ''}>
          <span class="box" aria-hidden="true"></span>
          <span class="txt">
            <span class="tname">${esc(it.name)}${vu ? ` <span class="val">${esc(vu)}</span>` : ''}${streak ? ` <span class="streak">серия ${streak} нед</span>` : ''}</span>
            ${sub ? `<span class="note">${esc(sub)}</span>` : ''}
          </span>
        </label>
        ${miss ? `<button type="button" class="dot" data-act="miss-note" data-id="${esc(it.id)}" aria-expanded="${ui.missOpen[it.id] ? 'true' : 'false'}" aria-controls="miss-${esc(it.id)}" aria-label="вчера — пропуск"><i></i></button>` : ''}
        ${miss ? `<p class="miss-note" id="miss-${esc(it.id)}"${ui.missOpen[it.id] ? '' : ' hidden'}>вчера — пропуск<button type="button" class="undo" data-act="mark-yesterday" data-id="${esc(it.id)}" aria-label="отметить вчера: «${esc(it.name)}»">отметить</button></p>` : ''}
        ${habit ? habitWeekRow(it, t) : skipBtn(it, on, skip)}
      </div>`;
}

/* Полоса текущей недели привычки: визуальный язык сетки разбора (подписи
   дней, кружки), пассивна — тапов не принимает; рядом «X из N». Сама
   полоса скрыта от AT — счёт недели даёт видимый текст «X из N». */
function habitWeekRow(it, t) {
  const mon = weekStartOf(t);
  const names = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'];
  let cells = '';
  for (let i = 0; i < 7; i++) {
    const k = addDays(mon, i);
    const today = k === t;
    // день вне расписания ИНЕРТЕН: кружка нет вовсе, как у будущего дня
    // (задача 29/B, B.2.3). Пустой кружок читался бы пропуском, а дела в
    // этот день не стояло. Подпись дня остаётся — полоса не съезжает.
    const off = scheduleOn(it, k)[weekdayOf(k)] !== '1';
    cells += `<span class="hday${today ? ' today' : ''}"><span class="hd">${names[i]}</span>` +
      `<i class="c${isMarked(k, it.id) ? ' on' : ''}${today ? ' today' : ''}${k > t ? ' fut' : ''}${off ? ' off' : ''}"></i></span>`;
  }
  return `
        <div class="hweek">
          <span class="hstrip" aria-hidden="true">${cells}</span>
          <span class="hcount">${habitWeekCount(it, mon)} из ${it.normPerWeek || 7}</span>
        </div>`;
}

/* Экран 2 — «Привычки»: только сегодняшний день программы роста */
function renderHabits() {
  const t = todayKey();
  ui.renderedDayKey = t;
  // применимость ДНЯ, а не «есть сейчас» (задача 29/B): пункт вне
  // маски сегодняшнего дня не идёт ни в список, ни в знаменатель планки
  const habits = dueDaily('habit', t);
  const done = habits.filter(i => isMarked(t, i.id)).length;
  const total = habits.length;
  const pct = total ? Math.round(done / total * 100) : 0;
  const allDone = total > 0 && done === total;

  let h = `
    <header class="page">
      <p class="overline">Программа роста</p>
      <h1>Привычки</h1>
    </header>`;

  if (total) {
    h += `
    <div class="dayline">
      <div class="bar"><i style="width:${pct}%"></i></div>
      <p class="bar-note${allDone ? ' ok' : ''}" aria-live="polite">${allDone ? 'Все отмечены' : `сегодня <b>${done}</b>&nbsp;из&nbsp;${total}`}</p>
    </div>
    <div class="list">` + groupSections(habits, t, true) + `</div>`;
  } else if (liveDaily().some(i => i.area === 'habit')) {
    // Привычки есть, но ни одна не стоит в этом дне (задача Р2, п. 5) — тем же
    // правилом, что «Сегодня»: «пока нет» было бы неправдой и звало бы заводить
    // заведённое. Без планки — измерять нечего.
    h += `<p class="muted">На сегодня привычек в расписании нет.</p>`;
  } else {
    h += `<p class="muted">Привычек пока нет — добавить можно в Настройках → Привычки.</p>`;
  }

  const params = store.items.filter(i => i.type === 'param' && live(i));
  if (params.length) {
    h += `<p class="g-label">Порог недели</p>`;
    for (const p of params) {
      h += `<p class="line muted">${esc(p.name)} · ${esc(fmtParam(p))}</p>`;
    }
  }

  h += `<p class="creed">Не спеши — доверься накопительному эффекту.</p>`;

  el('scr-habits').innerHTML = h;
}

/* ── Экран 3 — «Прогресс» (инвариант 14) ───────────────────────
   Единственное место, где приложение показывает прошлое: экран
   открывают намеренно, дневные экраны остаются без истории. */

const RISE_W = 100, RISE_H = 44, RISE_PAD = 2; // система координат ступеньки

/* Ступенчатый путь: ось X — даты от первой записи до сегодня, ось Y —
   значения. Последнее значение держится до правого края, поэтому
   сегментов ровно 2N−1: N−1 горизонталей, N−1 вертикалей и финальная
   горизонталь. Ни осей, ни сетки, ни точек — только линия. */
function risePath(points) {
  const r = n => Math.round(n * 100) / 100;
  const first = points[0].date;
  const last = points[points.length - 1].date;
  const t = todayKey();
  const end = last > t ? last : t;
  const span = diffDays(end, first);
  // все записи одним днём (стартовая запись лестницы и шаг того же дня) —
  // раскладываем по индексу: дат для пропорции не хватает
  const x = (p, i) => span > 0
    ? r(RISE_W * diffDays(p.date, first) / span)
    : r(points.length > 1 ? RISE_W * i / (points.length - 1) : 0);
  const vals = points.map(p => p.value);
  const lo = Math.min(...vals), hi = Math.max(...vals);
  const y = v => hi === lo
    ? RISE_H / 2 // ряд без размаха — ровная линия посередине
    : r(RISE_H - RISE_PAD - (v - lo) / (hi - lo) * (RISE_H - RISE_PAD * 2));
  let d = `M${x(points[0], 0)} ${y(vals[0])}`;
  for (let i = 1; i < points.length; i++) d += `H${x(points[i], i)}V${y(vals[i])}`;
  return d + `H${RISE_W}`;
}

/* Значение ряда словами владельца: порог-время читается как 23:30, всё
   остальное — числом. Единицы здесь нет ни у одного источника: формат
   подписи один на все ряды — «старт → текущее», и ничего после (задача
   29/A). Прежде fmtParam вклеивал единицу параметра-числа в КАЖДОЕ
   значение, и подпись читалась «4000 шаг. → 5000 шаг.» — единица дважды
   в одной строке (замер разведки). */
function riseValue(it, v) {
  return (it.type === 'param' && it.pkind === 'time') ? fmtParam(it, v) : String(v);
}

/* Минуты суток цикличны: 00:00 и 23:45 — соседи, между ними 15 минут, а не
   1425. На линейной шкале «Подъёма» переход через полночь уходил на всю
   высоту холста, и все настоящие шаги по 15 минут становились неразличимы
   навсегда — а у посевного «Отбоя» (pvalue 0, pstep −15) ровно такой шаг
   первый же (задача 27, п. 8). Разворачиваем ряд по КРАТЧАЙШЕЙ дуге: из
   эквивалентных представлений (±1440) берётся ближайшее к предыдущему.
   Выбрана она, а не сглаживание и не отказ рисовать: движение порога и
   есть предмет визуала, а по кратчайшей дуге оно читается тем, чем
   является, — пятнадцатью минутами. Механика pvalue и pstep не тронута
   (п. 8.3), подпись «00:00 → 23:45» печатается по сырым значениям. */
const DAY_MIN = 1440;

function unwrapDayMinutes(points) {
  const out = [];
  let prev = null;
  for (const p of points) {
    let v = p.value;
    if (prev !== null) {
      while (v - prev > DAY_MIN / 2) v -= DAY_MIN;
      while (prev - v > DAY_MIN / 2) v += DAY_MIN;
    }
    out.push({ date: p.date, value: v });
    prev = v;
  }
  return out;
}

function riseBlocks() {
  let h = '';
  // упражнения идут после пунктов и по тем же правилам: у них та же
  // история нагрузки, что и планка у пункта (задача 16D)
  for (const it of store.items.concat(store.exercises)) {
    const s = riseSeries(it);
    if (!s) continue;
    const a = s.points[0].value, b = s.points[s.points.length - 1].value;
    // Подпись одна на все ряды: «старт → текущее», и ничего после (задача
    // 29/A, решение владельца). Хвост `it.unit` печатался дословно, и у
    // упражнения с числом в поле «Единица» подпись читалась «7 → 8 7» —
    // третье число, которого владелец не писал. Единица ушла со всех рядов
    // разом: формат обязан быть один, иначе «хвост» не выразить мутантом.
    const label = `${riseValue(it, a)} → ${riseValue(it, b)}`;
    // геометрия у порога-времени своя (кратчайшая дуга), подпись — по сырым
    const geo = (it.type === 'param' && it.pkind === 'time') ? unwrapDayMinutes(s.points) : s.points;
    h += `
      <div class="rise-b">
        <p class="rise-h"><span class="rise-n">${esc(it.name)}</span><span class="rise-v">${esc(label)}</span></p>
        <svg class="rise" viewBox="0 0 ${RISE_W} ${RISE_H}" preserveAspectRatio="none" aria-hidden="true" focusable="false"><path d="${risePath(geo)}"/></svg>
      </div>`;
  }
  return h;
}

/* Цепь дней: 8 календарных недель, строка — неделя, ячейка — день.
   Три состояния (задача 17): доля ≥ порога — заливка --accent, доля
   между нулём и порогом — контур --accent, ноль — пустая ячейка.
   --chain здесь не участвует: тёплый тон закреплён за связкой блока,
   сетка дней — про выполнение. Будущие дни текущей недели и дни до
   начала эпохи не рисуются. */
function chainGrid() {
  const t = todayKey();
  const since = store.settings.calendarSince;
  const weeks = chainWeeks(8);
  const names = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'];
  let cells = '';
  let drawn = 0; // ячеек с отметками: пустая цепь показывает свою строку
  let shown = 0; // видимых ячеек вообще: ни одной — сетки нет (задача 22, п. 3.2)
  const sr = [];
  for (const mon of weeks) {
    let hit = 0;
    let vis = 0;  // прожитых дней недели в эпохе: по ним решается, рисовать ли сетку
    for (let i = 0; i < 7; i++) {
      const k = addDays(mon, i);
      if (k > t) { cells += `<i class="cd fut"></i>`; continue; }
      if (isDayKey(since) && k < since) { cells += `<i class="cd pre"></i>`; continue; }
      const s = dayScore(k);
      // применимых пунктов в дне нет — делать было нечего: день СКВОЗНОЙ.
      // Серия его и так пропускает (streakBack: continue), а цепь до задачи
      // 29/B рисовала его пустой ячейкой, то есть ПРОПУСКОМ: null и 0 давали
      // один и тот же класс. Гасится тем же приёмом, что дни до эпохи и
      // будущие (.pre/.fut) — место в сетке остаётся, ряд не съезжает.
      vis++; // день прожит и лежит в эпохе — сетке он принадлежит, даже если делать было нечего
      if (s === null) { cells += `<i class="cd off"></i>`; continue; }
      const full = s >= dayThreshold() - EPS;
      if (full) hit++;
      if (s) drawn++;
      cells += `<i class="cd${full ? ' full' : (s ? ' part' : '')}"></i>`;
    }
    shown += vis;
    // неделя целиком до начала отсчёта не существует — скринридер не должен
    // слышать про неё «зачтено 0 из 7» (задача 22, п. 3.4). Знаменатель —
    // семь: это дни НЕДЕЛИ, а не дни расписания, и он совпадает с лидом
    // разбора «Минимум закрыт N из 7 дней» (задача 29/B, B.2.7).
    if (vis) sr.push(`Неделя с ${fmtShort(mon)}: зачтено ${hit} из 7`);
  }
  // все восемь недель до начала отсчёта (эпоха ещё не наступила или только
  // что задана вперёд) — рисовать нечего: одна строка вместо пустой сетки
  if (!shown) return `<p class="muted">Цепь начнётся с первого дня отсчёта.</p>`;
  return `<p class="sr-only">${esc(sr.join('. '))}</p>` +
    `<div class="cdays" aria-hidden="true">` +
    names.map(d => `<span class="cd-head">${d}</span>`).join('') + cells + `</div>` +
    // своя строка, не «Отметок» (задача 23, п. 9.1): обе карточки «Прогресса»
    // говорили одно слово в слово, и пустой экран читался как повтор
    (drawn ? '' : `<p class="muted">Цепь заполнится с первой отметки.</p>`);
}

/* Блок «Прогресса» — карточка на --surface: заголовок и содержимое */
function pcard(title, body) {
  return `<section class="pcard"><h2>${title}</h2>${body}</section>`;
}

/* Полоса сегодняшнего дня под числом серии: заполнение — доля дня.
   Форма общая с планкой дня «Сегодня» и «Привычек» — та же высота,
   радиус и тот же единственный в приложении градиент (задача 26, п. 5.1). */
function dayBar() {
  const t = todayKey();
  const m = minDayMarks(t);
  // нечего измерять — полосы нет, как и на «Сегодня»: решает ПЛАН, а не
  // знаменатель. Всё пропущено (задача Р2) — полоса пуста, «0 из 0», и
  // «День закрыт» не печатается: закрывать было что, закрыто не было
  if (!m.planned) return '';
  const pct = m.total ? Math.round(m.done / m.total * 100) : 0;
  const closed = m.total > 0 && m.done === m.total;
  return `
    <div class="dbar" aria-hidden="true"><i style="width:${pct}%"></i></div>
    <p class="muted dbar-note">${closed ? 'День закрыт' : `${m.done} из ${m.total} сегодня`}</p>`;
}

function renderProgress() {
  const t = todayKey();
  ui.renderedDayKey = t;
  const since = store.settings.calendarSince;
  const days = daysInSystem();
  const streak = dayStreak();
  const best = bestStreak();

  let h = `
    <header class="page">
      <p class="overline">Накопленное</p>
      <h1>Прогресс</h1>
    </header>`;

  // 1. В системе — счёт, который не прерывается никогда
  h += pcard('В системе',
    `<p class="stat">${days} ${plural(days, 'день', 'дня', 'дней')}</p>` +
    (isDayKey(since)
      ? `<p class="muted">${days ? 'с ' + esc(fmtDay(since)) : `Отсчёт идёт с ${esc(fmtDay(since))}.`}</p>`
      : ''));

  // 2. Серия — число, рекорд справа, полоса сегодняшнего дня под ним
  h += pcard('Серия',
    `<p class="statrow">
      <span class="stat">${streak} ${plural(streak, 'день', 'дня', 'дней')}</span>
      ${best ? `<span class="muted rec">рекорд ${best} ${plural(best, 'день', 'дня', 'дней')}</span>` : ''}
    </p>` + dayBar() +
    (streak || best
      // правда о правиле, а не приблизительная (задача 19, C.7.1): прощается
      // пропуск, если предыдущий прощённый был БОЛЬШЕ недели назад; ровно
      // через неделю — уже обрыв. «Раз в неделю» звучало мягче, чем работает.
      ? `<p class="muted">Пропуск прощается, если прошлый был больше недели назад. Иначе счёт начинается заново.</p>`
      : `<p class="muted">Серия начнётся с первого зачтённого дня.</p>`));

  h += pcard('Цепь дней', chainGrid());

  const rise = riseBlocks();
  h += pcard('Подъём', rise || `<p class="muted">Появится, когда планка изменится во второй раз.</p>`);

  const inOrder = area => groupedItems(liveDaily().filter(i => i.area === area))
    .reduce((acc, sec) => acc.concat(sec.items), []);
  const listed = inOrder('min').concat(inOrder('habit'));
  // ряд нулей — не картина, а её отсутствие (задача 22, п. 3.1): пока ни у
  // одного пункта нет ни одной отметки в окне, блок показывает одну строку,
  // как того и требует инвариант 14 от пустого блока
  let marks = '';
  if (listed.some(it => marksInSystem(it) > 0)) {
    for (const it of listed) {
      marks += `<p class="line muted">${esc(it.name)} · ${marksInSystem(it)} из ${marksWindow(it)}</p>`;
    }
  }
  h += pcard('Отметки', marks ||
    `<p class="muted">${listed.length ? 'Первые отметки появятся здесь.' : 'Пунктов пока нет.'}</p>`);

  h += reviewDue()
    ? `<button class="banner rev" data-act="goto-review"><span>Разбор недели</span><span class="chev" aria-hidden="true">&rsaquo;</span></button>`
    : `<p class="muted rev">Следующий разбор — в понедельник</p>`;

  // Ниже строки разбора не стоит ничего: выписка дня ушла с экраном
  // «Заметки» (задача 28.C), и на её место не встало ничего.
  el('scr-progress').innerHTML = h;
}

/* ── Лист «Тренировка» (задача 16D) ────────────────────────────
   Открывается кнопкой «+» недельного счётчика вместо немедленного
   инкремента: сначала записывается, что сделано, потом растёт счёт.
   «Отмена» не пишет ничего. */
function renderTrain() {
  snapshotOpenForm(); // черновик листа — тот же механизм, что у форм (задача 26, п. 3)
  const w = store.items.find(i => i.id === ui.trainId);
  // имя недельного пункта — заголовок, надстрочник — день записи: прежде
  // оба говорили «Тренировка» (задача 22, п. 7.3)
  let h = `
    <header class="page">
      <p class="overline">${esc(fmtDay(todayKey()))}</p>
      <h1 tabindex="-1">${esc(w ? w.name : 'Тренировка')}</h1>
    </header>`;

  // Поля листа — те же черновиковые поля, что в формах: обёртка несёт
  // data-form, и snapshotOpenForm находит их по общему правилу.
  h += `<div data-form="train" data-id="${esc(ui.trainId || '')}">`;
  const list = liveExercises();
  if (!list.length) {
    h += `<p class="muted">Упражнений пока нет — добавить можно в Настройках → Упражнения.</p>`;
  }
  for (const ex of list) {
    const v = (typeof ex.value === 'number' && isFinite(ex.value)) ? String(ex.value) : '';
    h += `
      <div class="exline">
        <span class="txt">
          <span class="tname">${esc(ex.name)}</span>
          ${ex.unit ? `<span class="note">${esc(ex.unit)}</span>` : ''}
        </span>
        <span class="exctl">
          <button class="btn icon quiet" data-act="ex-step" data-id="${esc(ex.id)}" data-dir="down" aria-label="меньше: «${esc(ex.name)}»">&minus;</button>
          <input class="num" id="ex-${esc(ex.id)}" type="text" inputmode="decimal" value="${esc(v)}" aria-label="${esc(ex.name)}">
          <button class="btn icon quiet" data-act="ex-step" data-id="${esc(ex.id)}" data-dir="up" aria-label="больше: «${esc(ex.name)}»">+</button>
        </span>
      </div>`;
  }

  h += `
    <label class="field">
      <span>Заметка</span>
      <input type="text" id="tr-note" value="" placeholder="необязательно">
    </label>
    </div>
    <button class="btn primary wide" data-act="train-save">Записать</button>
    <button class="btn wide" data-act="train-cancel">Отмена</button>`;

  el('scr-train').innerHTML = h;
  restoreOpenForm();
}

/* ── Точечные обновления «Сегодня» и «Привычек» (горячие пути) ──
   Существующие узлы не пересоздаются — CSS-переходы чекбокса и
   планки дня реально проигрываются. Структурные изменения идут
   через полную перерисовку экрана. */

/* Здесь стояла updateThresholdNote — точечное обновление подписи зачёта
   дня. Её единственным вызывающим был тумблер пункта, который менял число
   применимых пунктов, не перерисовывая экран. Тумблер упразднён (задача
   28.E/A, п. 2), а «Убрать» всегда идёт полной перерисовкой «Настроек»:
   подпись пересчитывает сам renderSettings. Узел #thr-note остаётся —
   его печатает разметка секции. */

/* Числа планки «Сегодня» — одной функцией для рендера и точечного пути
   (сторож сравнивает их вывод). Пропущенные (задача Р2) выпадают из
   знаменателя тем же правилом, что в minDayMarks: «N из M» и «День закрыт»
   считают только непропущенные. */
/* Счёт дня (задача Р3, п. 0.5, решение архитектора): «N из M», при
   пропусках — «N из M · пропусков K» с приглушённым хвостом (.bar-skip),
   при сплошных пропусках — «0 из 0 · пропусков K». Отдельной фразы нет:
   «пропущено» запрещено, а «День закрыт» было бы неправдой. Закрытый день
   — «День закрыт» без хвоста: акцентное слово экрана одно, и сцена та же.
   Разметка счёта — тоже здесь (note): рендер и точечный путь печатают одну
   строку, и расходиться им нечем. */
function todayCounts(items, t) {
  let done = 0, skipped = 0;
  for (const i of items) {
    if (isMarked(t, i.id)) done++;
    else if (isSkipped(t, i.id)) skipped++;
  }
  const total = items.length - skipped;
  const closed = total > 0 && done === total;
  const tail = skipped ? `<span class="bar-skip"> · пропусков&nbsp;${skipped}</span>` : '';
  const note = closed ? 'День закрыт' : `<b>${done}</b>&nbsp;из&nbsp;${total}${tail}`;
  return { done, total, skipped, pct: total ? Math.round(done / total * 100) : 0, closed, note };
}

function updateDayline() {
  const t = todayKey();
  const items = dueDaily('min', t); // то же правило, что в renderToday: сторож сравнивает их вывод
  const { pct, closed, note: text } = todayCounts(items, t);
  const bar = document.querySelector('#scr-today .bar i');
  if (bar) bar.style.width = pct + '%';
  const note = document.querySelector('#scr-today .bar-note');
  if (note) {
    note.classList.toggle('ok', closed);
    note.innerHTML = text;
  }
}

function updateHabitsDayline() {
  const t = todayKey();
  const habits = dueDaily('habit', t); // то же правило, что в renderHabits
  const done = habits.filter(i => isMarked(t, i.id)).length;
  const total = habits.length;
  const pct = total ? Math.round(done / total * 100) : 0;
  const allDone = total > 0 && done === total;
  const bar = document.querySelector('#scr-habits .bar i');
  if (bar) bar.style.width = pct + '%';
  const note = document.querySelector('#scr-habits .bar-note');
  if (note) {
    note.classList.toggle('ok', allDone);
    note.innerHTML = allDone ? 'Все отмечены' : `сегодня <b>${done}</b>&nbsp;из&nbsp;${total}`;
  }
}

/* Точечное обновление полосы недели привычки: сегодняшняя ячейка и «X из N».
   Серия не пересчитывается — текущая неделя в неё не входит (инвариант 11). */
function updateHabitWeekRow(input) {
  const wrap = input.closest('.rowwrap');
  if (!wrap) return;
  const t = todayKey();
  const cell = wrap.querySelector('.hstrip i.today');
  if (cell) { cell.classList.toggle('on', isMarked(t, input.dataset.id)); tapPop(cell); } // scale-отклик ячейки (12.1)
  const hc = wrap.querySelector('.hcount');
  const it = store.items.find(x => x.id === input.dataset.id);
  if (hc && it) hc.textContent = `${habitWeekCount(it, weekStartOf(t))} из ${it.normPerWeek || 7}`;
}

/* Точечная отметка: обновляется планка того экрана, где стоит чекбокс.
   Линия блока постоянна и от отметок не зависит — пересчитывать нечего. */
function updateTodayMark(input) {
  const on = isMarked(todayKey(), input.dataset.id);
  input.checked = on;
  const label = input.closest('label.check');
  if (label) { label.classList.toggle('on', on); tapPop(label.querySelector('.box')); } // scale-отклик круга (12.1)
  const scr = input.closest('section.screen');
  if (scr && scr.id === 'scr-habits') { updateHabitsDayline(); updateHabitWeekRow(input); }
  else {
    // у отмеченного «Не сегодня» не предлагается (задача Р2): кнопка остаётся
    // в разметке и прячется — рендер печатает её так же
    const wrap = input.closest('.rowwrap');
    const btn = wrap && wrap.querySelector('.skipbtn');
    if (btn) btn.hidden = on;
    if (wrap) swipeClose(wrap);
    updateDayline();
    // Сцена — только на «Сегодня» и только когда день закрылся ИМЕННО этим
    // тапом (задача 28.E/C, п. 2.2). На «Привычках» её нет: при норме
    // меньше семи «все отмечены» нормой не является, и подсветка давила бы
    // в сторону ежедневности там, где конституция специально разрешила
    // пропуски. Недельному счётчику — тоже нет: это завело бы вторую валюту
    // на том же экране.
    const scene = on && minDayClosed(todayKey());
    if (scene) playDayClose(scr && scr.querySelector('.dayline'), label);
    // свёртка выполненного блока (задача Р2, п. 4) — последней: её
    // перерисовка при reduced-motion пересоздаёт узлы, которые трогали выше
    todayFoldAfter(input.dataset.id, scene);
  }
}

/* Точечный путь «Не сегодня» / «Вернуть» (задача Р2) — как у отметки:
   строка, кнопка и планка дня меняются на месте, и разметка обязана совпасть
   с перерисовкой (сторож). Узлы не пересоздаются: фокус, пришедший на кнопку
   с клавиатуры, остаётся на ней, и она читается уже как «Вернуть».

   Сцена закрытия дня — если день закрылся ЭТИМ пропуском: последний
   неотмеченный пункт ушёл из знаменателя, а все прочие отмечены. Сцена —
   отклик на действие, закрывшее день, и какое действие это было, ей всё
   равно. Кольца от круга при этом нет: круг пропущенного неактивен, и
   кольцо от него читалось бы отметкой, которой не было, — играют планка и
   фраза. Все пропущены — знаменатель пуст, день не закрыт, сцены нет.
   «Вернуть» закрыть день не может: знаменатель растёт, отметок не прибавилось. */
function updateTodaySkip(id) {
  const scr = el('scr-today');
  const input = [...scr.querySelectorAll('input[data-act="mark"]')].find(i => i.dataset.id === id);
  const wrap = input && input.closest('.rowwrap');
  const btn = wrap && wrap.querySelector('.skipbtn');
  const it = store.items.find(i => i.id === id);
  if (!btn || !it) { renderToday(); return; }
  const t = todayKey();
  const skip = isSkipped(t, id);
  const on = isMarked(t, id);
  swipeClose(wrap);
  wrap.classList.toggle('skip', skip);
  input.disabled = skip;
  const a = skipBtnAttrs(it, on, skip);
  btn.dataset.act = a.act;
  btn.textContent = a.text;
  btn.setAttribute('aria-label', a.label);
  btn.hidden = a.hidden;
  updateDayline();
  const scene = skip && minDayClosed(t);
  if (scene) playDayClose(scr.querySelector('.dayline'), null);
  todayFoldAfter(id, scene); // пропуск последнего в блоке сворачивает его, как отметка (Р2, п. 4)
}

/* Дневные экраны: перерисовка и контейнер по активной вкладке */
function renderDayScreen() {
  if (ui.tab === 'habits') renderHabits();
  else renderToday();
}

function dayScreenEl() {
  return el(ui.tab === 'habits' ? 'scr-habits' : 'scr-today');
}

function updateWeekCount(id) {
  const scr = el('scr-today');
  const plus = [...scr.querySelectorAll('[data-act="train-inc"]')].find(x => x.dataset.id === id);
  if (!plus) { renderToday(); return; }
  const n = trainCount(id);
  const wc = plus.closest('.weekcount');
  const num = wc.querySelector('.wnum b');
  if (num) num.textContent = n;
  // Ветка СОЗДАНИЯ кнопки «отменить последний» снята как недостижимая
  // (задача 28.B, п. 2). Единственный вызывающий — обработчик 'train-undo',
  // то есть сама эта кнопка: чтобы её нажать, она обязана существовать,
  // значит hasUndo при входе всегда true, а счёт после undoTrain только
  // убывает. Доказано не рассуждением: ветка была заминирована throw'ом
  // в изолированной копии и не выстрелила ни разу на 452 тестах, тогда как
  // ветка удаления на том же прогоне выстрелила сразу.
  // hasUndo остаётся: он проверяет, что снимается ИМЕННО кнопка этого
  // пункта, а не случайный сосед, — это валидация узла, а не мёртвый код.
  const next = wc.nextElementSibling;
  const hasUndo = !!(next && next.classList.contains('undo') && next.dataset.id === id);
  if (!n && hasUndo) next.remove();
}

/* Лист «Разбор недели»: открывается баннером «Сегодня» и строкой
   «Прогресса», возвращает на прежнюю вкладку (задача 16B) */
const REVIEW_DONE = `<button class="btn wide" data-act="review-done">Готово</button>`;

/* Есть ли в разборе хоть одно ДЕЙСТВЕННОЕ решение: карточка повышения,
   понижения или нерешённый параметр. Все три — одна и та же планка.

   Лестничная часть условия (доступный шаг и предложение закрыть вставшую
   привычку) снята вместе с механикой (задача 28.D); с ней ушёл и единственный
   повод читать ui — флаг «Остаться». Функция снова чистая: считает по days{},
   items[] и календарю.

   Нужна свёртке недели (задача 24, п. 9.2): когда решений нет, картина
   недели остаётся единственным содержательным на экране, и прятать её
   не за чем. Когда есть — свёртка закрыта: решения важнее таблиц. */
function reviewActionable() {
  if (!reviewDue()) return false;
  if (raiseOffer()) return true;
  const keys = windowKeys();
  const inWeek = it => live(it) || keys.some(k => isMarked(k, it.id));
  if (store.items.some(it => it.type === 'daily' && it.area === 'min' && inWeek(it) && lowerEligible(it))) return true;
  return pendingParams().length > 0;
}

/* Свёртка недели: ui.weekOpen === null — владелец её в этом разборе не
   трогал, тогда действует состояние по умолчанию. Тап пишет явное
   true/false, закрытие разбора возвращает null (задача 24, п. 9.1):
   чужая память о прошлом разборе следующему не принадлежит. */
const weekFoldOpen = () => {
  // умолчание снимается ОДИН РАЗ — при первом рендере разбора, а не при
  // каждом (задача 27.1, п. 10.3). Прежде reviewActionable() пересчитывался
  // на каждой перерисовке, и последнее принятое решение само раскрывало
  // картину недели под пальцем: тап по тихой второстепенной кнопке
  // выкатывал сетки и счётчики. Снимок делает умолчание умолчанием, а не
  // реакцией; тап владельца по свёртке перезаписывает его обычным путём
  if (ui.weekOpen === null) ui.weekOpen = !reviewActionable();
  return ui.weekOpen;
};

function renderReview() {
  let h = `<header class="page"><p class="overline">Раз в неделю</p><h1 tabindex="-1">Разбор недели</h1></header>`;

  if (!reviewDue()) {
    if (ui.justClosed) h += `<p class="lead" role="status">Неделя закрыта.</p>`;
    const cur = currentWeekStart();
    if (!cur) {
      // Эпоха ещё не наступила: переходные дни скользящей эпохи — и чистый
      // лист (задача 16.1: чистка ставит calendarSince в ближайший
      // понедельник, то есть в будущее во все дни, кроме понедельника).
      // fmtDay (полный месяц) — дата умещается целиком, без «сент..» (отделка, задача 13)
      h += `<p class="muted">Разбор откроется в понедельник, ${esc(fmtDay(addDays(store.settings.calendarSince, 7)))}.</p>`;
    } else {
      h += `<p class="muted">Идёт ${diffDays(todayKey(), cur) + 1}-й день недели. Разбор откроется в понедельник, ${esc(fmtDay(addDays(cur, 7)))}.</p>`;
    }
    const ocWait = currentOneChange();
    if (ocWait) h += `<p class="muted">Изменение этой недели: „${esc(ocWait)}“</p>`;
    h += REVIEW_DONE;
    el('scr-review').innerHTML = h;
    ui.justClosed = false; // «Неделя закрыта.» показывается ровно один раз
    return;
  }

  const keys = windowKeys();
  // убранный пункт стоит в сетке, если в окне у него есть факт — отметка или
  // пропуск (задача Р2: тем же правилом, что срез closeWeek)
  const inWeek = it => live(it) || keys.some(k => isMarked(k, it.id) || isSkipped(k, it.id));
  // действия — живые активного режима и живые, стоявшие в плане разбираемой
  // недели (её режимом мог быть другой, Р2); убранные — по отметкам, как прежде
  const am = activeMode();
  const minItems = store.items.filter(it => it.type === 'daily' && it.area === 'min' &&
    (live(it) ? (itemMode(it) === am || keys.some(k => dueOn(it, k))) : inWeek(it)));
  const habitItems = store.items.filter(it => it.type === 'daily' && it.area === 'habit' && inWeek(it));

  h += `<p class="muted">Неделя ${esc(fmtShort(keys[0]))} — ${esc(fmtShort(keys[6]))}</p>`;
  // одна строка итога вместо чтения сетки: закрытых дней минимума за неделю
  h += `<p class="lead">Минимум закрыт ${keys.filter(k => minDayClosed(k)).length} из 7 дней</p>`;

  // Сетка 7 дней × пункты: кружки и числа скрыты от AT (aria-hidden-обёртки
  // с display:contents), итог строки — визуально скрытым счётчиком в имени
  //
  // У ДЕЙСТВИЯ (area min) знаменатель — дни плана, а не семь («Расписание
  // 1/3», п. 2.2): будничное действие, сделанное все пять дней, читалось бы
  // «5 из 7», то есть недобором, которого не было. Числитель считается ТЕМ
  // ЖЕ правилом, что знаменатель (weekPlan, как «Отметки», инвариант 12):
  // отметка в дне вне плана в счёт не идёт, и «6 из 5» не бывает.
  // Подпись «запланировано D дней» печатается только при 0 < D < 7: при
  // семи она ничего не сообщает, при нуле «из 0» было бы числом без смысла —
  // скринридер слышит «не запланировано». Сама подпись скрыта от AT: то же
  // число D уже звучит в sr-only («из D»), дважды его не читают. Привычкам
  // семь остаётся — у них своя недельная логика (норма, инвариант 11).
  // Круги не меняются: сетка показывает отметки, а не план.
  // Число и слово связаны неразрывным пробелом: на 375 px колонка имени —
  // около 120 px, подпись в неё не помещается и переносится, и без связки
  // «дней» оставалось одно на второй строке, оторванное от числа (замер).
  const weekGrid = (items) => {
    let g = `<div class="grid" style="--cols:${keys.length}">`;
    g += `<span class="g-vis" aria-hidden="true"><span class="g-head"></span>` +
      ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'].map(d => `<span class="g-head">${d}</span>`).join('') + `</span>`;
    for (const it of items) {
      let plan = '', sr;
      if (it.area === 'min') {
        const p = weekPlan(it, keys);
        if (!p.planned) sr = ', не запланировано';
        else {
          sr = `, отмечено ${p.done} из ${p.planned}`;
          // «пропусков K» (задача Р2) — рядом с «запланировано D дней», той
          // же приглушённой подписью и тем же правилом: число — в sr-only,
          // подпись от AT скрыта. Пропуск в знаменатель не входит и
          // недобором не зовётся: план остаётся планом, пропуск — решением
          const k = weekSkips(it, keys);
          const parts = [];
          if (p.planned < 7) parts.push(`запланировано ${p.planned}&nbsp;${plural(p.planned, 'день', 'дня', 'дней')}`);
          if (k) {
            parts.push(`пропусков&nbsp;${k}`);
            // и КАКИЕ дни (Р2/рецензия, п. 3 постановки: у ячейки пропуска —
            // «пропуск» для AT). Ячейки сетки скрыты от AT целиком, как и
            // отметки, поэтому пропуск называется в том же sr-only строки:
            // «пропусков 1: пятница». Дни — те же, что посчитаны в K
            sr += `, пропусков ${k}: ${keys.filter(d => isSkipped(d, it.id) && dueOn(it, d)).map(d => DAY_NAME[weekdayOf(d)]).join(', ')}`;
          }
          if (parts.length) plan = `<span class="g-plan" aria-hidden="true">${parts.join(' · ')}</span>`;
        }
      } else {
        sr = `, отмечено ${keys.filter(k => isMarked(k, it.id)).length} из 7`;
      }
      g += `<span class="g-name">${esc(it.name)}${plan}<span class="sr-only">${sr}</span></span>`;
      // ячейка пропуска — круг с чертой (.skip): только у действий, у привычки
      // пропуска не бывает. Как и отметки, круги показывают факт, а не план
      g += `<span class="g-vis" aria-hidden="true">` +
        keys.map(k => `<i class="c${isMarked(k, it.id) ? ' on' : ''}${it.area === 'min' && isSkipped(k, it.id) ? ' skip' : ''}"></i>`).join('') + `</span>`;
    }
    return g + `</div>`;
  };

  // Консистентность за 3 последние ЗАВЕРШЁННЫЕ календарные недели.
  // Заголовок «Три закрытые недели» стоял над тремя последними РАЗБОРАМИ,
  // которые после пропусков относились к несмежным и не последним неделям
  // (аудит, находка 13). Теперь счёт по days{} — заголовок стал правдой.
  const consist = (items) => {
    const W = closedWeeks(3);
    if (!W.length) return `<p class="muted">Закрытых недель пока нет.</p>`;
    let c = `<div class="consist">`;
    for (const it of items) {
      const counts = W.map(w => itemWeekCount(it, w));
      c += `<span class="c-name">${esc(it.name)}</span><span class="c-val">${counts.map(x => esc(x)).join(' · ')} из 7</span>`;
    }
    return c + `</div>`;
  };

  // ── Решение 1: планка. Повышение, понижение и порог параметра — одна
  // и та же механика: recordBar, история пункта, одно решение за разбор
  // (инварианты 5 и 10). Поэтому карточка параметра стоит здесь, а не
  // под свёрткой с read-only сетками (задача 24, п. 2). Порядок внутри
  // решения — вверх, вниз, параметры. Предложений нет — одна строка.
  h += `<h2>Решение 1 · Планка</h2>`;
  let bar = '';
  const offer = raiseOffer(); // одно предложение повышения за разбор (п. 6)
  if (offer) {
    const it = offer;
    const sug = raiseSuggest(it.value);
    const editing = ui.raiseEdit[it.id];
    bar += `
      <div class="card raise" data-raise="${esc(it.id)}">
        <p>${esc(it.name)}: три недели подряд не меньше ${raiseNeed(weekMaskDays(it, closedWeeks(1)[0] || currentWeekStart() || todayKey()))} из ${weekMaskDays(it, closedWeeks(1)[0] || currentWeekStart() || todayKey())}.</p>
        <p class="raise-line">Повысить ${esc(String(it.value))} →
          ${editing
            ? `<input class="num" id="raise-${esc(it.id)}" type="text" inputmode="decimal" value="${esc(String(sug))}" aria-label="новая планка: «${esc(it.name)}»${it.unit ? ', ' + esc(it.unit) : ''}">`
            : `<b>${esc(String(sug))}</b>`}
          ${it.unit ? esc(it.unit) : ''}?</p>
        <p class="muted">Только если стало легко</p>
        <div class="btns">
          <button class="btn" data-act="raise-ok" data-id="${esc(it.id)}">Принять</button>
          ${editing ? '' : `<button class="btn quiet" data-act="raise-edit" data-id="${esc(it.id)}">Изменить</button>`}
          <button class="btn quiet" data-act="raise-later" data-id="${esc(it.id)}">Не сейчас</button>
        </div>
      </div>`;
  }
  // остальным готовым — тихая строка без имён и без счётчика-акцента:
  // владелец знает, что предложение не потеряно, и не выбирает из четырёх
  const restReady = raiseReady().length - (offer ? 1 : 0);
  if (restReady > 0) {
    bar += `<p class="muted">Ещё ${restReady} ${plural(restReady, 'пункт готов', 'пункта готовы', 'пунктов готовы')} к повышению — предложение вернётся на следующей неделе</p>`;
  }
  const lowW = closedWeeks(2);
  for (const it of minItems) {
    if (!lowerEligible(it)) continue;
    const to = lowerSuggest(it.value);
    const counts = lowW.map(w => planWeekCount(it, w)).join(' и ');
    bar += `
      <div class="card lower" data-lower="${esc(it.id)}">
        <p>Сделать легче</p>
        <p class="muted">${esc(it.name)} — ${counts} из ${lowDen(it, lowW)} за две недели</p>
        <div class="btns">
          ${to === null ? '' : `<button class="btn" data-act="lower-ok" data-id="${esc(it.id)}">Сделать легче ${esc(String(it.value))} → ${esc(String(to))}${it.unit ? ' ' + esc(it.unit) : ''}</button>`}
          <button class="btn quiet" data-act="lower-keep" data-id="${esc(it.id)}">Оставить</button>
        </div>
      </div>`;
  }
  // порог параметра — решение того же рода; одно на параметр за разбор
  for (const p of pendingParams()) {
    bar += `
      <div class="card param">
        <p>«${esc(p.name)} · ${esc(fmtParam(p))}» — как прошла неделя?</p>
        <div class="btns">
          <button class="btn" data-act="param-step" data-id="${esc(p.id)}">Шаг: → ${esc(fmtParam(p, paramStepTarget(p)))}</button>
          <button class="btn quiet" data-act="param-keep" data-id="${esc(p.id)}">Оставить</button>
        </div>
      </div>`;
  }
  h += bar || `<p class="muted">Планка держится, менять нечего</p>`;

  // ── Решение 2: одно изменение на следующую неделю.
  // Прежде вторым решением стояла «Ступень» — шаг лестницы; она снята
  // задачей 28.D, и решений осталось ДВА. Номер сдвинут, а не оставлен
  // пустым: дыры в нумерации не бывает (задача 24, п. 4) — «1, 3»
  // читалось владельцем как потеря.
  // Placeholder с примером — подсказка в ФОРМЕ, а не в списке пунктов
  // (анти-требование задачи 14 разрешает подсказки формам). В первых двух
  // разборах это единственное живое поле, и «необязательно» не объясняло,
  // чего от владельца ждут. Пример операционный и мелкий: не лозунг.
  h += `<h2>Решение 2 · Одно изменение</h2>`;
  const oc = currentOneChange();
  if (oc) h += `<p class="muted">Изменение этой недели: „${esc(oc)}“</p>`;
  h += `
    <label class="field">
      <span>Одно изменение на следующую неделю</span>
      <input type="text" data-bind="one-change" value="${esc(store.draftOneChange)}" placeholder="например: перенести зарядку на утро">
    </label>`;

  // ── Неделя целиком: сетки, счётчики и готовность — под свёрткой.
  // Здесь остаётся только read-only (задача 24, п. 2.3): решения уехали
  // в видимую часть, а картина недели — справка, а не действие.
  let wk = `<h2>Минимум</h2>`;
  wk += weekGrid(minItems);
  for (const w of store.items.filter(i => i.type === 'weekly' && live(i))) {
    // счёт разбираемой недели — тот же интервал, что уйдёт в срез closeWeek
    const n = store.weekLog.filter(e => e.itemId === w.id && e.date >= keys[0] && e.date <= keys[6]).length;
    wk += `<p class="line">${esc(w.name)}: ${n} из ${w.goal || 0}</p>`;
  }
  wk += `<h2>Три закрытые недели</h2>`;
  wk += consist(minItems);

  wk += `<h2>Привычки</h2>`;
  if (habitItems.length) {
    wk += weekGrid(habitItems);
    // норма и серия разбираемой недели: read-only справка, закрытие данных не меняет
    for (const hb of habitItems) {
      const x = habitWeekCount(hb, keys[0]);
      const norm = hb.normPerWeek || 7;
      // строка прерывания серии упразднена (задача 15): невыполненная неделя
      // сообщается счётом «x из normPerWeek» и молчанием, без тона и оценки
      const tail = x >= norm ? ` · серия ${habitStreakFrom(hb, keys[0])} нед` : '';
      wk += `<p class="muted">${esc(hb.name)}: ${x} из ${norm}${tail}</p>`;
    }
    wk += consist(habitItems);
  } else {
    wk += `<p class="muted">Привычек пока нет — добавить можно в Настройках → Привычки.</p>`;
  }

  // итог принятого решения по параметру — read-only строка; нерешённые
  // стоят карточками выше, в «Решении 1» (решение чужой недели карточку
  // не гасит: paramDecision привязан к разбираемой неделе)
  for (const p of store.items.filter(i => i.type === 'param' && live(i))) {
    const decided = paramDecision(p.id);
    if (!decided) continue;
    wk += `<p class="muted">${esc(p.name)}: ${decided.to === null
      ? `${esc(fmtParam(p, decided.from))}, без шага`
      : `${esc(fmtParam(p, decided.from))} → ${esc(fmtParam(p, decided.to))}`}</p>`;
  }

  if (habitsSteady()) {
    wk += `<p class="muted">Привычки устойчивы 2 недели — можно добавить новую</p>`;
  }

  h += `
    <details class="sect week"${weekFoldOpen() ? ' open' : ''}>
      <summary data-act="week-fold">Показать неделю<span class="chev" aria-hidden="true">&rsaquo;</span></summary>
      <div class="sect-b">${wk}</div>
    </details>`;

  // Закрытие недели — самая тяжёлая необратимость приложения: срез уходит в
  // архив, а принятые решения, поле «одного изменения» и решения по
  // параметрам чистятся. Один тап для этого мало (задача 28.B, п. 6):
  // «Стереть» и «Удалить блок» давно просят второго, а эта кнопка стояла
  // вплотную над «Готово» и срабатывала с первого.
  // Последствие названо между тапами и названо нейтрально: что произойдёт,
  // без тревоги и без уговоров.
  //
  // Строка стоит ПОД кнопкой, а не над ней (задача 28.D, п. 9.3). Сверху
  // она сдвигала кнопку вниз ровно в тот момент, когда владелец готовился
  // ко второму тапу: замер на 375×812 дал 957 → 1026 px, то есть 69 px —
  // палец попадал в пустоту или в соседний узел. Порядок «кнопка, затем
  // объяснение» держит точку нажатия на месте, а текст остаётся на экране
  // и прочитывается до тапа: он ниже кнопки, но выше «Готово».
  h += `<button class="btn primary wide" data-act="close-week">${ui.weekCloseConfirm ? 'Подтвердить: закрыть неделю' : 'Закрыть неделю'}</button>`;
  if (ui.weekCloseConfirm) {
    h += `<p class="muted">Неделя уйдёт в архив: принятые решения, «одно изменение» и решения по параметрам очистятся. Отметки останутся.</p>`;
  }
  h += REVIEW_DONE;

  el('scr-review').innerHTML = h;
}

/* Экран «Настройки» */
/* Варианты поля «Блок» — из списка блоков (инвариант 13): он же задаёт
   порядок на экранах, поэтому другого источника имён у формы нет. Только
   ЖИВЫЕ блоки («Расписание 1/3», п. 1.4): войти в убранный блок пункт не
   может — интерфейс показал бы его «без блока», а маска невидимого блока
   резала бы его дни. */
function groupList() {
  return liveGroups().map(g => g.name);
}

function barHistory(it, word) {
  if (typeof it.value !== 'number' || !isFinite(it.value)) return ''; // после очистки значения строка не показывается
  if (!Array.isArray(it.history) || it.history.length < 2) return '';
  const vals = it.history.map(x => String(x.value));
  const shown = vals.length > 6 ? ['…'].concat(vals.slice(-6)) : vals;
  const last = it.history[it.history.length - 1];
  return `<span class="hist">${word || 'Планка'}: ${shown.map(esc).join(' → ')}${it.unit ? ' ' + esc(it.unit) : ''} · с ${esc(fmtShort(last.date))}</span>`;
}

/* История порога параметра: «Порог: 00:00 → 23:45 · с <дата>». Прежде
   здесь печаталось ИМЯ пункта, а строка списка начинается с него же —
   имя выходило дважды подряд (задача 29/A). */
function paramHistory(it) {
  if (!Array.isArray(it.history) || it.history.length < 2) return '';
  const last = it.history[it.history.length - 1];
  const vals = it.history.map(x => fmtParam(it, x.value));
  const shown = vals.length > 6 ? ['…'].concat(vals.slice(-6)) : vals;
  return `<span class="hist">Порог: ${shown.map(esc).join(' → ')} · с ${esc(fmtShort(last.date))}</span>`;
}

/* Черновик открытой формы «Настроек»: значения всех полей (сливаются
   с прежним черновиком — цель переживает смену типа), сфокусированное
   поле и позиция каретки. Восстанавливается при каждом renderSettings,
   пока открыта та же форма. */
function currentFormKey() {
  // лист тренировки перекрывает вкладку целиком — его поля и есть открытая
  // форма, пока он открыт (задача 26, п. 3). Порядок проверок — как в
  // renderAll: разбор главнее листа тренировки. Прежде первым стоял лист
  // детали, а разбор в проверке не участвовал вовсе; лист детали снят
  // (задача 28.D), и на его место встал тот, кто и рисуется первым.
  if (!ui.reviewOpen && ui.trainOpen) return 'train:' + ui.trainId;
  if (ui.addOpen) return 'add';
  if (ui.editingId !== null) return 'edit:' + ui.editingId;
  // быстрое добавление («Расписание 1/3», п. 2.4): ключ — имя блока, у
  // «Без блока» пустое. Порядок — после форм пункта, перед формами блока
  if (ui.quickFor !== null) return 'quick:' + ui.quickFor;
  // формы блока и упражнения черновиком не считались вовсе — ключ был null,
  // и введённое имя пропадало при перерисовке по чужому поводу (задача 27,
  // п. 7.1). Слот у них общий с формами пункта: он один на экран, а пятого
  // заводить незачем — форму снимок теперь ищет по СОВПАДЕНИЮ КЛЮЧА, а не
  // «первую по DOM», и потому чужую больше не подхватывает. Приоритет у
  // форм пункта: они самые большие, и терять в них ввод дороже всего.
  // У форм добавления разделитель ДРУГОЙ — «+», а не «:» (Р1/ревью): имя
  // блока свободное, и блок «new» получал ключ формы добавления — её
  // черновик с днями накатывался на правку чужого блока и записывался в его
  // дни. «group:» + имя и «ex:» + id с «group+new» / «ex+new» не совпадут ни
  // при каком имени и id
  if (ui.groupAdd) return 'group+new';
  if (ui.groupRename !== null) return 'group:' + ui.groupRename;
  if (ui.exAddOpen) return 'ex+new';
  if (ui.exEditingId !== null) return 'ex:' + ui.exEditingId;
  // формы режима (задача Р2): переименование — по id режима, добавление — тем
  // же разделителем «+», что у прочих форм добавления. Приставка «mode»
  // своя: ни с «group:» + имя, ни с «ex:» + id, ни с «quick:» + имя ключи не
  // совпадут ни при каком имени и id
  if (ui.modeAdd) return 'mode+new';
  if (ui.modeRename !== null) return 'mode:' + ui.modeRename;
  return null;
}

/* Ключ формы по её разметке — та же строка, что даёт currentFormKey.
   Пары «data-form → ключ» неоднозначны только у форм добавления: у них
   нет data-id, и суффикс «+new» проставляется здесь, а не в разметке —
   через «+», чтобы не совпасть с правкой блока или упражнения, чьё имя
   или id — «new» (Р1/ревью).
   Быстрое добавление (data-form="quick") своего вида не требует: общая
   ветка даёт 'quick:' + имя блока, у «Без блока» — 'quick:'. */
const FORM_KIND = { 'group-edit': 'group', 'ex-edit': 'ex', 'mode-rename': 'mode' };

function domFormKey(form) {
  const f = form.dataset.form;
  if (f === 'add') return 'add';
  if (f === 'group-add') return 'group+new';
  if (f === 'ex-add') return 'ex+new';
  if (f === 'mode-add') return 'mode+new';
  return (FORM_KIND[f] || f) + ':' + (form.dataset.id || '');
}

/* ДВА слота черновика (задача 15, п. 6; лист тренировки — задача 26,
   п. 3): формы листа тренировки и «Настроек» не делят один. Иначе уход в
   лист стирал бы начатую правку названия, а возврат — черновик формы
   листа. Слотов было четыре: третьим был заметочный, ушедший с экраном
   (задача 28.C), четвёртым — слот форм листа детали, ушедший с самим
   листом (задача 28.D). Механизм при этом не изменился ни разу. */
const isTrainKey = key => key.startsWith('train:');
const draftSlot = key => (isTrainKey(key) ? 'trainDraft' : 'formDraft');
/* Экран, на котором живёт форма ключа: снимок ищет её именно там */
const formScope = key => (isTrainKey(key) ? '#scr-train' : '#scr-settings');

/* Черновик формы по ключу. Хранилище — по одному на экран (`draftSlot`),
   внутри — запись на каждую форму этого экрана (задача 28.B, п. 4.3):
   прежде запись была ОДНА, и открытие соседней формы того же экрана
   затирало набранное в первой. Ключ хранится ключом объекта, а не полем
   записи, — сверять его на восстановлении больше не нужно. */
const draftOf = key => ui[draftSlot(key)][key] || null;

/* Производные состояния открытой формы — не поля DOM, а ui: черновики
   масок дней и нормы, режим дней действия («как блок» / «свои»), тип
   действия в день заведения, выбор в поле «Блок». Снимок полей их не видит
   (у кнопки нет .value, а селект типа несёт data-act и снимком пропускается),
   и без записи здесь переход в соседнюю форму и обратно возвращал бы поля,
   но терял выбранные дни и нажатые «Свои дни» («Расписание 1/3», п. 4.5).
   Лежат в той же записи черновика, по ключу формы. */
const FORM_UI = ['editDays', 'editNorm', 'editOwn', 'editType', 'blockDays', 'groupPick', 'groupNew'];

function snapshotOpenForm() {
  const key = currentFormKey();
  if (!key) return; // ничего не открыто — черновики живут до своей формы
  const slot = draftSlot(key);
  // Форма ищется на том экране, которому принадлежит ключ, и именно ТА,
  // чей ключ совпал. Прежде бралась ПЕРВАЯ форма экрана — а на «Настройках»
  // секция «Блоки» стояла выше «Пунктов», и раскрытая правка блока крала
  // черновик формы пункта: домашний ключ не совпадал с чужой разметкой, и
  // снимок молча не снимался (задача 27, п. 7.1). Форм на экране с тех пор
  // только прибавилось — карточки блоков, быстрое добавление, — и правило
  // «по совпадению ключа» держит их все.
  // Признак формы — data-form, а не класс .card.form: поля листа тренировки
  // карточкой не обёрнуты, но черновик им нужен тот же (задача 26, п. 3.2)
  let form = null;
  for (const f of document.querySelectorAll(formScope(key) + ' [data-form]')) {
    if (domFormKey(f) === key) { form = f; break; }
  }
  // Формы ещё нет в разметке — её открывают прямо сейчас, текущий рендер её
  // только напечатает. Снимать нечего, и чистить тоже: черновики соседних
  // форм лежат по своим ключам и этой не мешают. Прежде здесь стояла чистка
  // «открыли другую форму», и набранное в первой пропадало (28.B, п. 4.3).
  if (!form) return;
  const fields = {};
  for (const inp of form.querySelectorAll('input[id], select[id], textarea[id]')) {
    if (inp.dataset.act) continue; // управляемые ui-состоянием контролы (select типа) — не черновик
    fields[inp.id] = inp.value;
  }
  const ae = document.activeElement;
  const focus = (ae && form.contains(ae) && ae.id)
    ? { id: ae.id, start: ae.selectionStart ?? null, end: ae.selectionEnd ?? null }
    : null;
  const base = draftOf(key);
  const draft = { fields: Object.assign({}, base && base.fields, fields), focus };
  // лист тренировки ui-производных не имеет: запись только у форм «Настроек»
  if (slot === 'formDraft') {
    draft.ui = {};
    for (const k of FORM_UI) draft.ui[k] = ui[k];
  }
  ui[slot][key] = draft;
}

/* ── Формы «Настроек»: одна за раз (задача 28.B, п. 4) ─────────
   На экране было четыре семейства форм — пункт, добавление пункта, блок и
   упражнение, — и слот черновика у них ОДИН (`formDraft`, он же один на
   экран по инварианту). С «Расписанием 1/3» пятым стало быстрое добавление
   действий, а добавление пункта осталось у одних привычек; правило ниже
   держит их все без правки. Гасили друг друга они несимметрично: `edit-open`
   снимал только `addOpen`, `group-add-open` — только правку блока, а
   обратно не гасило ничто. Замер: в 20 сочетаниях из 30 на экране
   оставалось ДВЕ открытые формы, и в 18 набранное в первой пропадало.

   Правило теперь одно на всех: сначала снимок того, что открыто, потом
   закрытие ВСЕХ форм экрана, потом открытие нужной. Порядок обязателен —
   снимок читает `currentFormKey()`, и если ui уже переписан, ключ укажет
   на новую форму, а набранное в прежней не будет снято вовсе (именно так
   оно и терялось: черновик снимается только при перерисовке). */
function settingsFormsClosed() {
  ui.editingId = null;
  ui.addOpen = false;
  ui.groupRename = null;
  ui.groupAdd = false;
  ui.quickFor = null;
  ui.exEditingId = null;
  ui.exAddOpen = false;
  ui.modeRename = null; // формы режима (задача Р2) — те же правила
  ui.modeAdd = false;
  // производные состояния открытой формы принадлежат ей, не экрану. Маска
  // дней пункта прежде здесь не гасла: выбранные в одной форме дни
  // переезжали в форму соседнего пункта. Теперь они лежат в черновике своей
  // формы (FORM_UI) и возвращаются только ей («Расписание 1/3», п. 4.5) —
  // и вместе с ними режим «Свои дни» и тип действия: нажатые в одной форме,
  // они иначе открыли бы соседнее действие в чужом режиме
  for (const k of FORM_UI) ui[k] = null;
  ui.groupNew = false;
  ui.removeConfirm = null; // взведённое «Убрать» принадлежало закрытой форме
}

function openSettingsForm(apply) {
  snapshotOpenForm();     // набранное в прежней форме — в слот, ДО смены ui
  settingsFormsClosed();
  apply();
  // производные состояния новой формы — из её черновика, ПОСЛЕ закрытия
  // прочих и ДО рендера: рендер читает их, печатая чипы и поле «Блок»
  const key = currentFormKey();
  const d = key ? draftOf(key) : null;
  if (d && d.ui) Object.assign(ui, d.ui);
  renderSettings();
}

/* Замещение данных целиком (импорт, чистка, возврат копии, восстановление
   из зеркала): всё, что «Настройки» держали о ПРЕЖНИХ данных, снимается —
   формы, их черновики, свёртка карточек и короткие пути назад
   («Расписание 1/3», п. 4.5). Иначе раскрытая форма блока или быстрое
   добавление остались бы открытыми над чужим блоком, а «Вернуть» указывал
   бы на запись, которой больше нет. */
function resetSettingsView() {
  settingsFormsClosed();
  ui.formDraft = {};
  ui.goneNote = null;
  ui.goneGroup = null;
  ui.blockFold = {};
  // и раскрытый список режимов (Р2/рецензия): он показывал режимы ПРЕЖНИХ
  // данных, и после импорта или чистки оставался раскрытым над чужими.
  // Формы списка гасит settingsFormsClosed выше
  ui.modesOpen = false;
  // Развёртка блоков «Сегодня» (задача Р2, п. 4) — не «Настройки», но повод
  // тот же: все вызывающие меняют, КАКИЕ блоки стоят за именами (замещение
  // данных, выбор режима, день с другим режимом). Развёрнутое «Утро» прежних
  // данных или прежнего режима не должно разворачивать чужое «Утро»
  ui.todayUnfold = null;
}

/* Переименование блока уносит с собой черновики, привязанные к его ИМЕНИ
   (Р1/рецензия). Их два рода. Быстрое добавление лежит под ключом
   'quick:'+имя: без переноса набранные строки пропадали из интерфейса, а
   запись под старым именем ждала будущего блока с тем же именем. Выбор в
   поле «Блок» (FORM_UI.groupPick) лежит в черновике любой формы пункта:
   без переноса форма открывалась с «Старое (нет в списке)», и «Сохранить»
   уводило пункт в осиротевшее имя — в «Без блока», заняв это имя для
   nameTaken. Черновик самой формы блока снят сохранением раньше. */
function renameDrafts(from, to) {
  const d = ui.formDraft;
  if (d['quick:' + from]) { d['quick:' + to] = d['quick:' + from]; delete d['quick:' + from]; }
  for (const rec of Object.values(d)) {
    if (rec && rec.ui && rec.ui.groupPick === from) rec.ui.groupPick = to;
  }
}

/* «Отмена» — осознанный отказ от набранного, а не переключение: черновик
   снимается явно. Иначе повторное открытие той же формы вернуло бы то,
   от чего владелец только что отказался (снимок теперь делается при
   открытии соседней формы, а прежде не делался вовсе). */
/* Успешное сохранение черновик тоже снимает — зовёт эту же функцию ДО
   закрытия формы («Расписание 1/3», п. 4.5). Черновик снимается при каждой
   перерисовке с открытой формой (тап по дню, выбор блока, переход в
   соседнюю форму), а сохранение его прежде не трогало: в слоте оставалось
   состояние на момент ПОСЛЕДНЕЙ перерисовки, и повторное открытие формы
   накатывало его поверх сохранённого — набранное после той перерисовки
   пропадало. С производными состояниями в черновике это стало бы видно
   сразу: дни блока возвращались бы прежними. */
function dropOpenDraft() {
  const key = currentFormKey();
  if (key) delete ui[draftSlot(key)][key];
}

function restoreOpenForm() {
  const key = currentFormKey();
  if (!key) return;
  const draft = draftOf(key);
  if (!draft) return;
  for (const [fid, v] of Object.entries(draft.fields)) {
    const inp = el(fid);
    if (inp) inp.value = v;
  }
  const f = draft.focus;
  if (f) {
    const inp = el(f.id);
    if (inp) {
      inp.focus();
      if (f.start !== null && typeof inp.setSelectionRange === 'function') {
        try { inp.setSelectionRange(f.start, f.end); } catch (e) { /* select и др. */ }
      }
    }
  }
}

/* ── Уход из виду: две дороги назад (задача 28.E/A, п. 3) ──────
   КОРОТКАЯ — строка на месте только что убранного: «{имя} · убран —
   Вернуть». Своим классом, а не .flash: тот гаснет через FLASH_MS, а
   отмена, исчезающая через секунду, отменой не является (A.3.4). Живёт
   до следующего действия владельца, как ui.importNote.
   ДЛИННАЯ — блок «Убранные» внизу своей области. Пустым не рисуется.

   Род слова разный: пункт и блок убраны, упражнение убрано. */
const GONE_WORD = { item: 'убран', ex: 'убрано', group: 'убран', mode: 'убран' };

/* Что произойдёт — названо МЕЖДУ тапами и названо нейтрально: последствие,
   без тревоги и без уговоров. Числа «во что превратится серия» здесь нет и
   быть не может — это была бы инструкция по накрутке (A.3.3). */
/* Здесь стояло последствие «Удалить блок» (GROUP_DELETE_WHAT: «пункты
   останутся — они просто выйдут из блока»). Удаление блока снято
   («Расписание 1/3», п. 1.4): блок несёт дни и теперь убирается, а
   последствие его ухода — третья строка словаря ниже. */
const REMOVE_WHAT = {
  item: 'Пункт уйдёт из списков. Отметки и прошлые дни останутся как есть.',
  ex: 'Упражнение уйдёт из списков. Записанные тренировки и история нагрузки останутся как есть.',
  group: 'Блок уйдёт из списков вместе с действиями и привычками. Отметки и прошлые дни останутся как есть.',
  // одноимённый блок жив в другом режиме — привычки, параметры и недельные
  // счётчики остаются при нём (removeGroup, Р2), и прежние слова обещали бы
  // уход, которого второй тап не сделает (Р2/рецензия)
  groupNamesake: 'Блок уйдёт из списков вместе со своими действиями. Привычки и недельные счётчики останутся: блок с этим именем есть в другом режиме. Отметки и прошлые дни останутся как есть.',
  // режим (задача Р2): блоки и действия режима никуда не уходят — уходит
  // только строка выбора; прошлые дни читаются режимом того дня
  mode: 'Режим уйдёт из выбора. Прошлые дни и отметки останутся как есть.'
};

/* Отказы формы блока — по причине, которую назвала доменная операция
   (updateGroup, «Расписание 1/3», п. 1.4). Одна таблица на правку и на
   добавление: одно и то же занятое имя не должно звучать двумя фразами. */
const GROUP_REFUSAL = {
  missing: 'Не сохранено: блока больше нет',
  empty: 'Название не заполнено',
  taken: 'Это имя уже занято',
  nodays: 'Нужен хотя бы один день недели',
  storage: 'Не сохранено: хранилище недоступно'
};

/* Отказы форм режима (задача Р2) — по причине, названной операцией
   (addMode, renameMode). Пустое имя звучит той же фразой, что у блока */
const MODE_REFUSAL = {
  empty: GROUP_REFUSAL.empty,
  taken: 'Режим с таким именем уже есть',
  missing: 'Не сохранено: режима больше нет',
  storage: 'Не сохранено: хранилище недоступно'
};

function groupRefusal(r, typed) {
  if (r.reason === 'removed') return groupJoinRefusal(String(typed ?? '').trim()) || GROUP_REFUSAL.taken;
  if (r.reason === 'zero') return `Не останется ни одного дня: ${(r.names || []).join(', ')}`;
  return GROUP_REFUSAL[r.reason] || GROUP_REFUSAL.storage;
}

/* Хвост формы правки: свои кнопки, «Убрать» и под ними — последствие.

   Строка последствия стоит ПОД кнопкой, а не над ней (правило задачи 28.D,
   п. 9.3): сверху она сдвигала бы «Убрать» вниз ровно в тот момент, когда
   владелец готовится ко второму тапу.

   «Убрать» живёт в СВОЁМ ряду, отдельно от «Сохранить» и «Отмены». Замер в
   браузере (375×812): в общем ряду надпись «Подтвердить: убрать» переносила
   кнопку на следующую строку flex-обёртки и уводила её вниз на 54 px — тот
   же дефект, от которого лечили «Закрыть неделю». В собственном ряду ширина
   надписи ни на что не влияет: ряд один, переносить нечего. Заодно ряд
   разводит сохранение и уход визуально. */
function removeFoot(kind, id, buttons) {
  const armed = ui.removeConfirm === kind + ':' + id;
  return `
      <div class="btns">
        ${buttons}
      </div>
      <div class="btns">
        <button class="btn quiet" data-act="${kind}-remove" data-id="${esc(id)}">${armed ? 'Подтвердить: убрать' : 'Убрать'}</button>
      </div>
      ${armed ? `<p class="muted">${REMOVE_WHAT[kind]}</p>` : ''}`;
}

function goneNote(x, kind) {
  return `<p class="gone-note">${esc(x.name)} · ${GONE_WORD[kind]}` +
    `<button type="button" class="undo" data-act="${kind}-restore" data-id="${esc(x.id)}"` +
    ` aria-label="вернуть «${esc(x.name)}»">Вернуть</button></p>`;
}

/* Короткий путь назад у блока — та же разметка, ключ другой: у блока нет
   id, он ищется по имени (п. 4.4) */
function groupGoneNote(g) {
  return `<p class="gone-note">${esc(g.name)} · ${GONE_WORD.group}` +
    `<button type="button" class="undo" data-act="group-restore" data-name="${esc(g.name)}"` +
    ` aria-label="вернуть «${esc(g.name)}»">Вернуть</button></p>`;
}

/* Строка «Убранных»: имя, день ухода, «Вернуть». attr — чем её ищет
   обработчик: data-id у пункта и упражнения, data-name у блока. */
function goneRow(x, kind, attr) {
  return `
      <div class="rowwrap gone">
        <div class="row item">
          <span class="gtxt">
            <span class="tname">${esc(x.name)}</span>
            <span class="meta">${GONE_WORD[kind]} ${esc(fmtShort(x.removedAt))}</span>
          </span>
          <span class="ictl">
            <button class="btn quiet" data-act="${kind}-restore" ${attr} aria-label="вернуть «${esc(x.name)}»">Вернуть</button>
          </span>
        </div>
      </div>`;
}

/* Только что убранное здесь не повторяется: оно стоит строкой на своём
   прежнем месте, и двух «Вернуть» на одну запись быть не должно.
   lead — строки, которые идут в «Убранные» первыми: убранные блоки
   «Расписания» стоят над убранными действиями.
   Прежний отрезок пункта, у которого уже есть преемник (laterSegmentOf),
   здесь тоже не стоит: его дело уже вернулось и живёт строкой выше, а
   «Вернуть» завело бы дубль (Р1/рецензия). У упражнения — то же правило
   (laterExerciseOf, задача Р2, п. 5). */
function goneBlock(list, kind, lead) {
  const succ = kind === 'item' ? laterSegmentOf : laterExerciseOf;
  const gone = list.filter(x => !live(x) && ui.goneNote !== x.id && !succ(x));
  const rows = (lead || '') + gone.map(x => goneRow(x, kind, `data-id="${esc(x.id)}"`)).join('');
  if (!rows) return '';
  return `<h2>Убранные</h2><div class="list">${rows}</div>`;
}

/* ── «Расписание» («Расписание 1/3», п. 2.4) ───────────────────
   Блоки — карточки: шапка (имя, подпись, дни), под ней действия блока и
   быстрое добавление. Вторым экраном «Блоки» больше не нужны — порядок,
   дни и состав блока правятся там же, где видны его действия.

   Сводка дней — словами владельца, а не маской: «будни», «пн, ср, пт». */
const DAY_WORD = ['пн', 'вт', 'ср', 'чт', 'пт', 'сб', 'вс'];
const MASK_WORKDAYS = '1111100';
const MASK_WEEKEND = '0000011';

function daysSummary(mask) {
  if (mask === WEEK_ALL) return 'ежедневно';
  if (mask === MASK_WORKDAYS) return 'будни';
  if (mask === MASK_WEEKEND) return 'выходные';
  const days = DAY_WORD.filter((_, i) => mask[i] === '1');
  // пустая маска у действия достижима только импортом (п. 1.3): пустой
  // строкой она читалась бы как «ничего не сказано», а сказать надо
  return days.length ? days.join(', ') : 'ни одного дня';
}

/* Пресеты дней — те же три слова, что у сводки: выбор одним тапом того,
   что владелец назвал бы словом. Кнопка-действие, а не переключатель:
   состояние дней показывают чипы над ней. */
const DAY_PRESETS = [[WEEK_ALL, 'Ежедневно'], [MASK_WORKDAYS, 'Будни'], [MASK_WEEKEND, 'Выходные']];

function presetsRow() {
  return `
      <div class="btns presets">${DAY_PRESETS.map(([m, label]) =>
    `<button type="button" class="btn" data-act="days-preset" data-mask="${m}">${label}</button>`).join('')}</div>`;
}

/* Строка действия в карточке блока — компактная: имя и через « · » всё,
   что уточняет его в этом блоке. Дни печатаются, только если они ОТЛИЧАЮТСЯ
   от дней блока: «как блок» — норма, и повторять «будни» у каждой строки
   будничного блока значило бы засорить список тем, что сказано в шапке.
   История планки из строки ушла в форму правки: строка — про расписание,
   справка о прошлом — про пункт. */
function actionRow(it, blockMask) {
  const parts = [rowNote(it), valUnit(it)];
  if (it.type === 'weekly') parts.push(`цель ${it.goal || 0} / нед.`);
  if (it.type === 'daily') {
    const eff = effectiveMaskOn(it, todayKey());
    if (eff !== blockMask) parts.push(daysSummary(eff));
  }
  const sub = parts.filter(Boolean).join(' · ');
  return `
      <div class="rowwrap drag-row" data-drag="item" data-drag-id="${esc(it.id)}" data-dgroup="${esc(scheduleSectionName(it))}">
        <div class="row item">
          <button class="itxt" data-act="edit-open" data-id="${esc(it.id)}" aria-label="изменить «${esc(it.name)}»">
            <span class="tname">${esc(it.name)}${sub ? `<span class="csub"> · ${esc(sub)}</span>` : ''}</span>
          </button>
          <span class="ictl">
            <button class="btn icon quiet" data-act="move-up" data-id="${esc(it.id)}"${canMoveItem(it.id, 'up') ? '' : ' disabled'} aria-label="выше">&uarr;</button>
            <button class="btn icon quiet" data-act="move-down" data-id="${esc(it.id)}"${canMoveItem(it.id, 'down') ? '' : ' disabled'} aria-label="ниже">&darr;</button>
          </span>
        </div>
        ${ui.editingId === it.id ? editForm(it) : ''}
        ${flashAt('item:' + it.id)}
      </div>`;
}

/* Список действий карточки: живые строки и короткий путь назад только что
   убранного — на его прежнем месте */
function actionList(items, blockMask) {
  let h = '';
  for (const it of items) {
    if (live(it)) h += actionRow(it, blockMask);
    else if (ui.goneNote === it.id) h += goneNote(it, 'item');
  }
  return `<div class="list">${h}</div>`;
}

/* Хвост карточки: подтверждение быстрого добавления стоит НА МЕСТЕ кнопки
   «Добавить действия» — у пальца, а не в шапке блока (задача 26, п. 2.1) */
function quickTail(name) {
  return flashAt('quick:' + name) + (ui.quickFor === name
    ? quickForm(name)
    : `<button class="btn wide" data-act="quick-open" data-name="${esc(name)}">Добавить действия</button>`);
}

/* Быстрое добавление: по действию в строке, подпись — после « · » или
   тире с пробелами (« — », « – », « - »; подсказка называет два самых
   набираемых, парсер принимает все четыре). Поле —
   textarea с кеглем 16px (iOS не масштабирует при фокусе, общее правило
   .field textarea). Разбор строк — parseQuickLines, запись — addActions. */
function quickForm(name) {
  return `
        <div class="card form" data-form="quick" data-id="${esc(name)}">
          <label class="field"><span>Действия — по одному в строке</span><textarea id="q-lines" rows="5" placeholder="Кровать&#10;Развитие · 10 мин"></textarea></label>
          <p class="muted">Подпись — после « · » или « - ».</p>
          <div class="btns">
            <button class="btn primary" data-act="quick-save" data-name="${esc(name)}">Добавить</button>
            <button class="btn quiet" data-act="quick-cancel">Отмена</button>
          </div>
        </div>`;
}

/* Форма блока: имя, подпись, дни с пресетами; сохранение и отмена; копия;
   уход — вторым тапом, последствие ПОД кнопкой (правило 28.D, п. 9.3).
   Каждое действие в своём ряду — как «Убрать» у пункта: надпись
   «Подтвердить: убрать блок» не переносит соседей и не сдвигает кнопку. */
/* «Дублировать блок» получает вариант «в режим …» (задача Р2) — по кнопке
   на каждый ЖИВОЙ режим, кроме режима самого блока, и только когда такие
   есть: при одном режиме ряд прежний. Кнопки, а не выбор с отдельным
   «Дублировать»: копия ничего не стирает и делается одним тапом, как
   «Дублировать блок». Ряд тот же — это варианты одного действия. */
function dupTargets(g) {
  return liveModes().filter(m => m.id !== blockMode(g)).map(m =>
    `<button class="btn quiet" data-act="group-dup-to" data-name="${esc(g.name)}" data-mode="${esc(m.id)}"` +
    ` aria-label="дублировать блок «${esc(g.name)}» в режим «${esc(m.name)}»">в режим «${esc(m.name)}»</button>`).join('');
}

function blockForm(g) {
  const nm = g.name;
  const mask = ui.blockDays !== null ? ui.blockDays : blockMaskNow(g);
  const armed = ui.removeConfirm === 'group:' + nm;
  return `
      <div class="card form" data-form="group-edit" data-id="${esc(nm)}">
        <label class="field"><span>Название</span><input type="text" id="g-name" value="${esc(nm)}"></label>
        <label class="field"><span>Подпись</span><input type="text" id="g-cap" value="${esc(g.caption || '')}" placeholder="например: 7:00"></label>
        ${daysField(mask)}${presetsRow()}
        <div class="btns">
          <button class="btn primary" data-act="group-save" data-name="${esc(nm)}">Сохранить</button>
          <button class="btn quiet" data-act="group-cancel">Отмена</button>
        </div>
        <div class="btns">
          <button class="btn quiet" data-act="group-dup" data-name="${esc(nm)}">Дублировать блок</button>${dupTargets(g)}
        </div>
        <div class="btns">
          <button class="btn quiet" data-act="group-remove" data-name="${esc(nm)}">${armed ? 'Подтвердить: убрать блок' : 'Убрать блок'}</button>
        </div>
        ${armed ? `<p class="muted">${hasLiveNamesake(g) ? REMOVE_WHAT.groupNamesake : REMOVE_WHAT.group}</p>` : ''}
      </div>`;
}

function addBlockForm() {
  const mask = ui.blockDays !== null ? ui.blockDays : WEEK_ALL;
  return `
    <div class="card form" data-form="group-add">
      <label class="field"><span>Название блока</span><input type="text" id="g-add" placeholder="Например: Вечер"></label>
      <label class="field"><span>Подпись</span><input type="text" id="g-add-cap" placeholder="например: 7:00"></label>
      ${daysField(mask)}${presetsRow()}
      <div class="btns">
        <button class="btn primary" data-act="group-add-save">Добавить</button>
        <button class="btn quiet" data-act="group-add-cancel">Отмена</button>
      </div>
    </div>`;
}

/* Карточка живого блока. Тап по шапке открывает форму блока, свёртка —
   отдельной кнопкой-шевроном: одна цель — одно действие. Свёрнутое тело
   скрыто атрибутом hidden (CSS .bbody[hidden]), разметка на месте — фокус
   и якоря ищут видимые узлы (shownIn). */
function blockCard(g, i, pos, count) {
  const nm = g.name;
  const cap = (g.caption || '').trim();
  const folded = !!ui.blockFold[nm];
  const mask = blockMaskNow(g);
  // действия режима блока и глобальные счётчики с его именем (Р2)
  const items = store.items.filter(it => it.area === 'min' && groupNameOf(it) === nm && belongsToMode(it, blockMode(g)));
  return `
    <div class="bcard drag-row" data-drag="group" data-drag-id="${esc(nm)}">
      <div class="row item bhead">
        <button class="itxt" data-act="group-open" data-name="${esc(nm)}" aria-label="изменить блок «${esc(nm)}»">
          <span class="bline"><span class="tname">${esc(nm)}</span>${cap ? `<span class="bcap">${esc(cap)}</span>` : ''}</span>
          <span class="meta">${esc(daysSummary(mask))}</span>
        </button>
        <span class="ictl">
          <button class="btn icon quiet" data-act="group-up" data-name="${esc(nm)}"${pos === 0 ? ' disabled' : ''} aria-label="выше: «${esc(nm)}»">&uarr;</button>
          <button class="btn icon quiet" data-act="group-down" data-name="${esc(nm)}"${pos === count - 1 ? ' disabled' : ''} aria-label="ниже: «${esc(nm)}»">&darr;</button>
          <button class="btn icon quiet fold" data-act="group-fold" data-name="${esc(nm)}" aria-expanded="${folded ? 'false' : 'true'}" aria-controls="bb-${i}" aria-label="${folded ? 'развернуть' : 'свернуть'} «${esc(nm)}»"><span class="chev" aria-hidden="true">&rsaquo;</span></button>
        </span>
      </div>
      ${ui.groupRename === nm ? blockForm(g) : ''}
      ${flashAt('group:' + nm)}
      <div class="bbody" id="bb-${i}"${folded ? ' hidden' : ''}>
        ${actionList(items, mask)}
        ${quickTail(nm)}
      </div>
    </div>`;
}

/* Убранный пункт стоит в «Убранных», только если его блок НЕ убран: ушедшие
   вместе с блоком возвращаются вместе с ним, двух дорог к одной записи не
   бывает. С режимами (задача Р2) «его блок» у действия и у глобального пункта
   разный. Действие ищет блок своего режима. Привычка, параметр и недельный
   счётчик ссылаются на блок только именем, и имя это может жить в нескольких
   режимах: строка стоит, если одноимённый блок жив хоть в одном (его уход не
   увёл пункт — removeGroup уводит глобальные только с последним живым), или
   если блока с таким именем нет нигде. Блок убран во ВСЕХ режимах — строки нет:
   вернуть такой пункт в одиночку значило бы оставить живую ссылку на убранный
   блок, и следующий старт молча оживил бы блок (migrate), а владелец видел бы
   блок, которого не возвращал. Дорога назад — возврат блока.
   С задачи Р3 (п. 0.3) «жив» — в смысле holdsName: живой блок ЖИВОГО
   режима. Блок убранного режима пункт не держит, и уход одноимённого блока
   активного режима уводит пункт с собой (removeGroup) — значит, и дорога
   назад у него через возврат того блока, а не строкой «Убранных». Строка
   стоит и тогда, когда убранного блока с этим именем нет нигде: вернуть
   пункт блоком было бы нечем, и прятать его значило бы оставить без дороги
   назад (например, имя живёт только в блоке убранного режима). */
function goneBesideBlock(it) {
  const name = groupNameOf(it);
  if (isAction(it)) {
    const g = findGroup(name, itemMode(it));
    return !g || live(g);
  }
  const same = store.groups.filter(g => g.name === name);
  return same.some(holdsName) || !same.some(g => !live(g));
}

/* ── Переключатель режима (задача Р2) ─────────────────────────
   Первая строка «Расписания»: «Режим: ‹имя› ⌄». Тап раскрывает список —
   не форму, а раскрытие, как свёртка карточки: тело всегда в разметке и
   скрыто атрибутом hidden, поэтому aria-controls указывает на живой узел.

   В списке живые режимы в порядке store.modes, выбранный отмечен
   (aria-current и тихая мета «выбран»). У каждого — «Переименовать»; у
   невыбранного ещё «Выбрать» (один тап) и «Убрать» (вторым тапом, своим
   рядом, последствие ПОД кнопкой — правило задачи 28.D, п. 9.3). Выбранный
   режим не убирается: сегодняшний день остался бы без режима. Ниже —
   убранные режимы строками «Убранных» с «Вернуть» и последним «Новый режим».

   Подтверждение выбора печатается под строкой режима (ключ 'modes'): список
   после выбора закрывается, и карточки ниже — уже нового режима. */
function modeSelector(am) {
  const cur = findMode(am);
  const open = !!ui.modesOpen;
  const rows = liveModes().map(m => modeRow(m, m.id === am)).join('');
  const gone = modeList().filter(m => !live(m))
    .map(m => goneRow(m, 'mode', `data-id="${esc(m.id)}"`)).join('');
  const add = ui.modeAdd ? modeAddForm(cur) : `<button class="btn wide" data-act="mode-add-open">Новый режим</button>`;
  return `
    <div class="modes">
      <button class="itxt mhead fold" data-act="mode-list" aria-expanded="${open ? 'true' : 'false'}" aria-controls="mode-list">
        <span class="tname">Режим: ${esc(cur ? cur.name : MAIN_MODE_NAME)}</span><span class="chev" aria-hidden="true">&rsaquo;</span>
      </button>
      ${flashAt('modes')}
      <div class="mlist" id="mode-list"${open ? '' : ' hidden'}>
        ${rows}${gone}
        ${add}
      </div>
    </div>`;
}

function modeRow(m, active) {
  const armed = ui.removeConfirm === 'mode:' + m.id;
  const pick = active ? ''
    : `<span class="ictl"><button class="btn" data-act="mode-pick" data-id="${esc(m.id)}" aria-label="выбрать режим «${esc(m.name)}»">Выбрать</button></span>`;
  return `
        <div class="mrow"${active ? ' aria-current="true"' : ''}>
          <div class="row item">
            <span class="gtxt"><span class="tname">${esc(m.name)}</span>${active ? '<span class="meta">выбран</span>' : ''}</span>
            ${pick}
          </div>
          ${ui.modeRename === m.id ? modeRenameForm(m) : `<div class="btns">
            <button class="btn quiet" data-act="mode-rename-open" data-id="${esc(m.id)}" aria-label="переименовать режим «${esc(m.name)}»">Переименовать</button>
          </div>`}
          ${active ? '' : `<div class="btns">
            <button class="btn quiet" data-act="mode-remove" data-id="${esc(m.id)}" aria-label="${armed ? 'подтвердить: убрать' : 'убрать'} режим «${esc(m.name)}»">${armed ? 'Подтвердить: убрать' : 'Убрать'}</button>
          </div>`}
          ${armed ? `<p class="muted">${REMOVE_WHAT.mode}</p>` : ''}
          ${flashAt('mode:' + m.id)}
        </div>`;
}

function modeRenameForm(m) {
  return `<div class="card form" data-form="mode-rename" data-id="${esc(m.id)}">
            <label class="field"><span>Название режима</span><input type="text" id="m-name" value="${esc(m.name)}"></label>
            <div class="btns">
              <button class="btn primary" data-act="mode-rename-save" data-id="${esc(m.id)}">Сохранить</button>
              <button class="btn quiet" data-act="mode-rename-cancel">Отмена</button>
            </div>
          </div>`;
}

/* «Новый режим»: имя и два способа завести — копией выбранного или пустым.
   Кнопки заводят сразу, без переключателя «копия / пустой»: производного
   состояния у формы нет, черновик — одно поле. Подсказка — в форме, где
   подсказкам и место: что берёт копия и что общее для всех режимов. */
function modeAddForm(cur) {
  return `<div class="card form" data-form="mode-add">
          <label class="field"><span>Название режима</span><input type="text" id="m-add" placeholder="Например: Каникулы"></label>
          <p class="muted">Копия берёт блоки и действия режима «${esc(cur ? cur.name : MAIN_MODE_NAME)}» с сегодняшнего дня. Привычки общие для всех режимов.</p>
          <div class="btns">
            <button class="btn primary" data-act="mode-add-copy">Копия текущего</button>
            <button class="btn" data-act="mode-add-empty">Пустой</button>
            <button class="btn quiet" data-act="mode-add-cancel">Отмена</button>
          </div>
        </div>`;
}

/* Секция «Расписание»: карточки блоков в порядке store.groups, «Без
   блока», «Добавить блок», «Убранные», затем граница дня и зачёт дня —
   обе настройки про то, как считается день действий. */
function scheduleSection() {
  // блоки и действия АКТИВНОГО режима (задача Р2): переключатель режима
  // стоит первой строкой над карточками, и карточки ниже — его
  const am = activeMode();
  const lives = liveGroups(am);
  let h = modeSelector(am) + `<div class="blocks">`;
  store.groups.forEach((g, i) => {
    if (blockMode(g) !== am) return;
    if (!live(g)) {
      if (ui.goneGroup === g.name) h += groupGoneNote(g);
      return;
    }
    h += blockCard(g, i, lives.indexOf(g), lives.length);
  });

  // «Без блока»: действия без живого блока. Карточка стоит и тогда, когда
  // блоков нет вовсе (иначе действию негде родиться), и тогда, когда в ней
  // только короткий путь назад последнего убранного (п. 4.4), и тогда,
  // когда в ней открыто быстрое добавление. Последнее — не прихоть: в
  // карточке, видимой только ради «Вернуть», стоит и «Добавить действия», а
  // тап по ней снимает короткий путь (onClick) раньше, чем форма
  // откроется. Без этого условия перерисовка карточку не рисовала, форма
  // не появлялась, а ui.quickFor оставался — «призрачная» форма всплывала
  // потом сама, при первом же поводе нарисовать карточку (Р1/рецензия).
  // Строки карточки — та же функция, что даёт им соседей (looseRows, Р4)
  const loose = looseRows(am);
  if (!lives.length || loose.some(live) || loose.some(it => ui.goneNote === it.id) || ui.quickFor === '') {
    h += `
    <div class="bcard loose">
      <p class="g-label">Без блока</p>
      ${actionList(loose, WEEK_ALL)}
      ${quickTail('')}
    </div>`;
  }

  h += ui.groupAdd
    ? addBlockForm()
    : `<button class="btn wide" data-act="group-add-open">Добавить блок</button>`;

  // «Убранные»: убранные блоки (кроме только что убранного — у него
  // короткий путь) и убранные действия, чей блок НЕ убран: ушедшие вместе
  // с блоком возвращаются вместе с ним, двух дорог к ним не бывает
  const goneGroups = store.groups
    .filter(g => blockMode(g) === am && !live(g) && ui.goneGroup !== g.name)
    .map(g => goneRow(g, 'group', `data-name="${esc(g.name)}"`)).join('');
  const goneItems = store.items.filter(it => it.area === 'min' && belongsToMode(it, am) && goneBesideBlock(it));
  h += goneBlock(goneItems, 'item', goneGroups);
  h += `</div>`;

  // граница дня — механика самих пунктов, поэтому живёт в секции их
  // расписания (прежде — в «Пунктах», «Расписание 1/3», п. 2.3)
  const hours = [];
  for (let i = 0; i <= 8; i++) hours.push(`<option value="${i}"${store.settings.dayBoundary === i ? ' selected' : ''}>${pad2(i)}:00</option>`);
  h += `
    <h2>Граница дня</h2>
    <label class="field inline">
      <span>Смена дня в</span>
      <select data-act="boundary">${hours.join('')}</select>
    </label>
    <p class="muted">Отметки до этого часа относятся к предыдущему дню.</p>`;

  // порог зачёта дня (задача 17): «день зачтён» — доля, а не всё подряд.
  // Живёт рядом с границей дня: обе настройки — про то, как считается день.
  const th = dayThreshold();
  const thrNote = thresholdNote();
  h += `
    <h2>Зачёт дня</h2>
    <div class="field inline norm">
      <span>Отмечено не меньше <b>${Math.round(th * 100)}%</b></span>
      <span class="btns">
        <button type="button" class="btn icon quiet" data-act="thr-dec"${th <= THRESHOLD_MIN + EPS ? ' disabled' : ''} aria-label="ниже порог">&minus;</button>
        <button type="button" class="btn icon quiet" data-act="thr-inc"${th >= THRESHOLD_MAX - EPS ? ' disabled' : ''} aria-label="выше порог">+</button>
      </span>
    </div>
    <p class="muted" id="thr-note"${thrNote ? '' : ' hidden'}>${thrNote}</p>
    <p class="muted">Влияет на серию и цепь дней на «Прогрессе». «День закрыт» на «Сегодня» считается отдельно — там нужны все пункты.</p>`;
  return h;
}

/* Строка привычки или параметра — прежняя строка списка «Пунктов», без
   имени блока в мете: блок теперь стоит заголовком над строкой (п. 2.6) */
function habitRow(it) {
  const vu = it.type === 'param' ? `порог ${fmtParam(it)}` : valUnit(it);
  // data-dgroup — секция раскладки, а не сырое имя блока: соседи
  // перетаскивания те же, что у стрелок (habitSectionOf, Р3/рецензия)
  return `
      <div class="rowwrap drag-row" data-drag="item" data-drag-id="${esc(it.id)}" data-dgroup="${esc(habitSectionName(it))}">
        <div class="row item">
          <button class="itxt" data-act="edit-open" data-id="${esc(it.id)}" aria-label="изменить «${esc(it.name)}»">
            <span class="tname">${esc(it.name)}</span>
            ${vu ? `<span class="meta">${esc(vu)}</span>` : ''}
            ${rowNote(it) ? `<span class="note">${esc(rowNote(it))}</span>` : ''}
            ${it.type === 'param' ? paramHistory(it) : barHistory(it)}
          </button>
          <span class="ictl">
            <button class="btn icon quiet" data-act="move-up" data-id="${esc(it.id)}"${canMoveItem(it.id, 'up') ? '' : ' disabled'} aria-label="выше">&uarr;</button>
            <button class="btn icon quiet" data-act="move-down" data-id="${esc(it.id)}"${canMoveItem(it.id, 'down') ? '' : ' disabled'} aria-label="ниже">&darr;</button>
          </span>
        </div>
        ${ui.editingId === it.id ? editForm(it) : ''}
        ${flashAt('item:' + it.id)}
      </div>`;
}

/* Секция «Привычки»: привычки и параметры по ЖИВЫМ блокам в порядке
   store.groups, затем без блока. Заголовок — только имя: дни блока
   привычку не ограничивают (п. 1.2), и сводка дней здесь солгала бы. */
/* С режимами (задача Р2) привычки и параметры глобальны, а блок у них —
   только имя. Заголовки — сначала живые блоки АКТИВНОГО режима в их порядке,
   затем прочие имена живых блоков других режимов, встречающиеся у привычек, в
   порядке store.groups. Смена режима меняет порядок заголовков, но привычка не
   пропадает и не переезжает в «Без блока»: её блок жив, пусть и в другом
   режиме. «Без блока» — пустое имя и имя, у которого живого блока нет нигде
   (импорт), как было. Убранные — правилом goneBesideBlock, по всем режимам.
   С задачи Р3 раскладка одна с дневным экраном (habitSections), и «живой
   блок» в ней — живой блок ЖИВОГО режима (holdsName): имя, которое носит
   только блок убранного режима, стоит в «Без блока». */
function habitsSection() {
  const habits = store.items.filter(i => i.area === 'habit');
  const rowsOf = list => list.map(it => (live(it) ? habitRow(it) : (ui.goneNote === it.id ? goneNote(it, 'item') : ''))).join('');
  let h = '';
  // раскладка — общая с дневным экраном «Привычки» (habitSections, задача
  // Р3, п. 0.2); здесь заголовок — только имя, без подписи и без дней
  for (const sec of habitSections(habits)) {
    const rows = rowsOf(sec.items);
    if (!rows) continue; // секция из одних убранных без короткого пути — не рисуется
    if (sec.name !== null) h += `<p class="g-label">${esc(sec.name)}</p><div class="list">${rows}</div>`;
    // без блока — последними. Заголовок «Без блока» нужен, только если выше
    // стоят блоки: иначе строки читались бы продолжением последнего из них
    else h += (h ? `<p class="g-label">Без блока</p>` : '') + `<div class="list">${rows}</div>`;
  }
  h += ui.addOpen
    ? addForm()
    : `<button class="btn wide" data-act="add-open" data-area="habit">Добавить привычку</button>`;
  // убранные привычки, чей блок не убран: ушедшие с блоком вернутся с ним
  h += goneBlock(habits.filter(goneBesideBlock), 'item');
  return h;
}

/* Редактор упражнений (задача 16D): та же механика, что у пунктов —
   строка с именем раскрывает правку, стрелки задают порядок, «Убрать» в
   форме уводит из виду. Нагрузку правит запись тренировки, не форма. */
function exerciseEditor() {
  let h = '';
  if (!store.exercises.length) {
    h += `<p class="muted">Упражнений пока нет.</p>`;
  }
  // границы стрелок считаются по ВИДИМОМУ списку: убранное в порядке не
  // участвует, но своё место в store.exercises сохраняет (exerciseIndexes)
  const shown = liveExercises();
  store.exercises.forEach((ex) => {
    if (!live(ex)) {
      if (ui.goneNote === ex.id) h += goneNote(ex, 'ex');
      return;
    }
    const i = shown.indexOf(ex);
    const meta = [valUnit(ex)].filter(Boolean).join(' · ');
    h += `
      <div class="rowwrap drag-row" data-drag="ex" data-drag-id="${esc(ex.id)}">
        <div class="row item">
          <button class="itxt" data-act="ex-open" data-id="${esc(ex.id)}" aria-label="изменить «${esc(ex.name)}»">
            <span class="tname">${esc(ex.name)}</span>
            ${meta ? `<span class="meta">${esc(meta)}</span>` : ''}
            ${barHistory(ex, 'Нагрузка')}
          </button>
          <span class="ictl">
            <button class="btn icon quiet" data-act="ex-up" data-id="${esc(ex.id)}"${i === 0 ? ' disabled' : ''} aria-label="выше">&uarr;</button>
            <button class="btn icon quiet" data-act="ex-down" data-id="${esc(ex.id)}"${i === shown.length - 1 ? ' disabled' : ''} aria-label="ниже">&darr;</button>
          </span>
        </div>
        ${ui.exEditingId === ex.id ? `
        <div class="card form" data-form="ex-edit" data-id="${esc(ex.id)}">
          <label class="field"><span>Название</span><input type="text" id="x-name" value="${esc(ex.name)}"></label>
          <label class="field"><span>Единица</span><input type="text" id="x-unit" value="${esc(ex.unit)}" placeholder="повт., мин, кг"></label>
          ${removeFoot('ex', ex.id, `<button class="btn primary" data-act="ex-save" data-id="${esc(ex.id)}">Сохранить</button>
            <button class="btn quiet" data-act="ex-cancel">Отмена</button>`)}
        </div>` : ''}
        ${flashAt('ex:' + ex.id)}
      </div>`;
  });
  h += ui.exAddOpen
    ? `<div class="card form" data-form="ex-add">
        <label class="field"><span>Название</span><input type="text" id="x-add-name" placeholder="Например: Жим лёжа"></label>
        <label class="field"><span>Единица</span><input type="text" id="x-add-unit" placeholder="повт., мин, кг"></label>
        <label class="field"><span>Рабочая нагрузка</span><input type="text" id="x-add-value" inputmode="decimal" placeholder="необязательно"></label>
        <div class="btns">
          <button class="btn primary" data-act="ex-add-save">Добавить</button>
          <button class="btn quiet" data-act="ex-add-cancel">Отмена</button>
        </div>
      </div>`
    : `<button class="btn wide" data-act="ex-add-open">Добавить упражнение</button>`;
  h += goneBlock(store.exercises, 'ex');
  return h;
}

/* Сворачиваемая секция «Настроек» на нативном details: раскрытие —
   дело браузера, ui хранит только состояние, чтобы перерисовка после
   действия внутри секции её не захлопнула. */
function sect(key, title, body) {
  return `
    <details class="sect"${ui.settingsOpen[key] ? ' open' : ''}>
      <summary data-act="sect" data-sect="${key}">${title}<span class="chev" aria-hidden="true">&rsaquo;</span></summary>
      <div class="sect-b">${body}</div>
    </details>`;
}

/* Строка возврата — первой в «Данных», пока копия существует. Поводов к её
   жизни три: чистка, импорт (задача 25, п. 2.2) и сам возврат (задача 26,
   п. 1.1) — все замещают прежнее состояние и все обратимы одной кнопкой.
   Копия без kind — снятая до задачи 25, то есть чисткой.

   Строка говорит, ЧТО лежит в копии и ОТКУДА взялось (п. 1.2), а не что
   было сделано: копия обменная, и «Стёрто» после второго тапа по «Вернуть»
   называло бы стёртым то, что как раз вернули. Тон нейтральный: ни
   тревоги, ни оценки. */
const PREV_WHENCE = { import: 'до импорта', restore: 'до возврата', mirror: 'до восстановления из резервной копии' };

function restoreLine() {
  const c = wipedCopy();
  if (!c) return '';
  const st = c.stats || {};
  const when = (typeof c.wipedAt === 'number' && isFinite(c.wipedAt))
    ? fmtDay(dateKeyShift(new Date(c.wipedAt), store.settings.dayBoundary))
    : '';
  const whence = PREV_WHENCE[c.kind] || 'до чистки';
  const n = Number(st.items) || 0;
  const d = Number(st.days) || 0;
  // копия из одних заметок читалась как пустая: считались только пункты и
  // дни (задача 27.1, п. 10.5). Заметки и выписки — такие же слова
  // владельца, и их отсутствие в строке делало содержательную копию немой.
  // Экран заметок снят (задача 28.C), новых записей не заводится, но
  // прежние живут в store и в копию идут — значит, и в счёт тоже
  const q = Number(st.notes) || 0;
  const parts = [
    `${n} ${plural(n, 'пункт', 'пункта', 'пунктов')}`,
    `${d} ${plural(d, 'день', 'дня', 'дней')} отметок`
  ];
  if (q) parts.push(`${q} ${plural(q, 'запись', 'записи', 'записей')}`);
  // и режимы — по той же причине: копия из одних режимов не должна читаться
  // пустой (Р2/рецензия). Прежние копии поля не несут — числа нет, строки нет
  const md = Number(st.modes) || 0;
  if (md) parts.push(`${md} ${plural(md, 'режим', 'режима', 'режимов')}`);
  return `
    <div class="restore">
      <p class="muted">В копии — состояние ${whence}${when ? ', ' + esc(when) : ''} · ${parts.join(', ')}</p>
      <p class="muted">«Вернуть» меняет местами: нынешнее состояние ляжет в копию.</p>
      ${ui.restoreFailed ? `<p class="muted">Возврат не выполнен — данные не изменены</p>` : ''}
      <div class="btns">
        <button class="btn" data-act="wipe-undo">Вернуть</button>
        <button class="btn quiet" data-act="wipe-drop">${ui.wipeDropConfirm ? 'Подтвердить: стереть копию' : 'Стереть копию'}</button>
      </div>
    </div>`;
}

/* Нечитаемые данные, отложенные load(), становятся видимыми (задача 25,
   п. 6): прежде они молча лежали в ключе, о котором владелец не знал, —
   ни скачать, ни убрать. Тон — как у «Резервная копия не проверена»:
   это «неизвестно», а не тревога, поэтому muted и без акцента. */
function corruptLine() {
  return Object.keys(CORRUPT_SRC).map(corruptBlock).join('');
}

function corruptBlock(src) {
  const c = corruptCopy(src);
  if (!c) return '';
  const w = CORRUPT_SRC[src];
  const when = (typeof c.at === 'number' && isFinite(c.at))
    ? ' от ' + esc(fmtDay(dateKeyShift(new Date(c.at), store.settings.dayBoundary)))
    : '';
  return `
    <div class="restore corrupt">
      <p class="muted">${w.title}${when}</p>
      <p class="muted">${w.why}</p>
      <div class="btns">
        <button class="btn" data-act="corrupt-save" data-src="${src}">Скачать нечитаемое</button>
        <button class="btn quiet" data-act="corrupt-drop" data-src="${src}">${ui.corruptDropConfirm === src ? 'Подтвердить: стереть нечитаемое' : 'Стереть нечитаемое'}</button>
      </div>
    </div>`;
}

/* Предложение восстановления (задача 28.A, п. 2.2). Появляется, когда в
   снапшоте есть практика, которой нет в рабочей копии. Автоматической
   подмены нет: приложение не знает, какое из двух состояний владелец
   считает своим, — оно знает лишь, что одно не является продолжением
   другого. Пока строка стоит, зеркало не перезаписывается.
   Три пути наружу, и ни один ничего не теряет: восстановить (обменной
   копией, как импорт и возврат), скачать файлом, оставить рабочую. */
function mirrorOfferLine() {
  if (!mirrorOffer) return '';
  const st = mirrorOffer.stats || {};
  const when = (typeof mirrorOffer.savedAt === 'number' && isFinite(mirrorOffer.savedAt))
    ? ' от ' + esc(fmtStamp(mirrorOffer.savedAt))
    : '';
  const n = Number(st.items) || 0;
  const d = Number(st.days) || 0;
  return `
    <div class="restore corrupt">
      <p class="muted">Резервная копия${when} отличается от рабочей: ${n} ${plural(n, 'пункт', 'пункта', 'пунктов')}, ${d} ${plural(d, 'день', 'дня', 'дней')} отметок</p>
      <p class="muted">В ней есть записи, которых в рабочей копии нет. Пока решение не принято, копия не перезаписывается.</p>
      ${ui.mirrorFailed ? `<p class="muted" role="status">Восстановление не выполнено — данные не изменены</p>` : ''}
      <div class="btns">
        <button class="btn" data-act="mirror-restore">${ui.mirrorRestoreConfirm ? 'Подтвердить: восстановить' : 'Восстановить из копии'}</button>
        <button class="btn quiet" data-act="mirror-save">Скачать копию</button>
        <button class="btn quiet" data-act="mirror-keep">${ui.mirrorKeepConfirm ? 'Подтвердить: оставить рабочую' : 'Оставить рабочую'}</button>
      </div>
    </div>`;
}

/* «Начать с чистого листа» — внизу «Данных», отделено линией.
   Предупреждение перечисляет стираемое числами: решение принимается
   с открытыми глазами, а не по памяти. */
function wipeBlock() {
  if (!ui.wipeOpen) {
    return `<div class="danger"><button class="btn quiet" data-act="wipe-open">Начать с чистого листа</button></div>`;
  }
  const s = wipeStats(store);
  // Список стираемого называет и лестницы, и заметки — механик и экрана для
  // них больше нет (задачи 28.C и 28.D), но данные владельца в store лежат и
  // чисткой действительно стираются. Умолчать о том, что уходит, нельзя:
  // решение принимается с открытыми глазами, а не по памяти.
  const line = [
    `${s.items} ${plural(s.items, 'пункт', 'пункта', 'пунктов')}`,
    `${s.groups} ${plural(s.groups, 'блок', 'блока', 'блоков')}`,
    `${s.days} ${plural(s.days, 'день', 'дня', 'дней')} отметок`,
    `${s.reviews} ${plural(s.reviews, 'разбор', 'разбора', 'разборов')}`,
    `${s.ladders} ${plural(s.ladders, 'лестница', 'лестницы', 'лестниц')}`,
    `${s.exercises} ${plural(s.exercises, 'упражнение', 'упражнения', 'упражнений')}`,
    `${s.sessions} ${plural(s.sessions, 'тренировка', 'тренировки', 'тренировок')}`,
    `${s.notes} ${plural(s.notes, 'заметка', 'заметки', 'заметок')}`
  ];
  // режимы — только когда они есть (Р2/рецензия): «0 режимов» у владельца,
  // который режимов не заводил, называло бы то, чего нет даже в интерфейсе.
  // Слова — те же, что в строке потерь импорта (dataCounts): словарь один
  if (s.modes) line.push(`${s.modes} ${plural(s.modes, 'режим', 'режима', 'режимов')}`);
  if (s.modeLog) line.push(`${s.modeLog} ${plural(s.modeLog, 'отрезок режима', 'отрезка режима', 'отрезков режима')}`);
  return `
    <div class="danger">
      <p class="lead">Начать с чистого листа</p>
      ${ui.wipeFailed ? `<p class="muted" role="status">Чистка не выполнена — данные не изменены</p>` : ''}
      <p class="muted">Будут стёрты: ${line.join(', ')}.</p>
      ${mirrorReady ? '' : (mirrorOffer
        ? `<p class="muted">Резервная копия ждёт решения выше — чистка её не сбросит и не тронет.</p>`
        : `<p class="muted">Резервная копия сейчас недоступна, и стереть её нечем. Она останется от прежнего состояния, и приложение предложит её при следующем запуске — подменить данные само оно не станет.</p>`)}
      <p class="muted">Копия останется в приложении — вернуть можно, пока она не убрана.${wipedCopy() && hasData(store) ? ' Прежняя копия будет заменена новой: хранится одна, последняя.' : ''}</p>
      <div class="btns">
        <button class="btn" data-act="export">Сначала скачать мои данные</button>
        <button class="btn quiet" data-act="wipe-do">${ui.wipeConfirm ? 'Подтвердить: стереть' : 'Стереть'}</button>
        <button class="btn quiet" data-act="wipe-cancel">Отмена</button>
      </div>
    </div>`;
}

/* Экран 5 — «Настройки»: секции по порядку (задачи 16B, 16D) */
function renderSettings() {
  snapshotOpenForm();
  // подтверждение сохранения печатается не здесь, а у якоря той строки,
  // которой принадлежала форма (задача 26, п. 2.1) — см. flashAt()
  let h = `<header class="page"><p class="overline">Устройство приложения</p><h1>Настройки</h1></header>`;

  // Порядок секций «Расписания 1/3» (п. 2.3). «Блоки» и «Пункты» ушли:
  // блоки стали карточками расписания вместе со своими действиями, а
  // привычки — своей секцией. Секций по-прежнему пять.
  h += sect('schedule', 'Расписание', scheduleSection());
  h += sect('habits', 'Привычки', habitsSection());
  h += sect('exercises', 'Упражнения', exerciseEditor());

  // «запускался», а не «был»: приложение знает лишь то, что отдало файл
  // браузеру. Сохранил ли его владелец — в вебе узнать нечем, и строка
  // не должна утверждать больше, чем приложение знает (задача 25, п. 9)
  const exp = (typeof store.settings.exportedAt === 'number' && isFinite(store.settings.exportedAt))
    // логический день — как в имени файла экспорта (инвариант 1)
    ? `Данные скачивались: ${esc(fmtShort(dateKeyShift(new Date(store.settings.exportedAt), store.settings.dayBoundary)))}`
    : 'Данные ещё не скачивались';
  h += sect('data', 'Данные', mirrorOfferLine() + restoreLine() + `
    <div class="btns">
      <button class="btn" data-act="export">Скачать мои данные</button>
      <button class="btn" data-act="import">Загрузить из файла</button>
    </div>
    ${ui.importNote ? `<p class="muted" role="status">${esc(ui.importNote)}</p>` : ''}
    <p class="muted">${exp}</p>
    <p class="muted" id="mirror-note" hidden></p>
    ${corruptLine()}
    <input type="file" id="import-file" accept="application/json,.json" hidden>
    <p class="muted">Все данные — на этом устройстве: рабочая копия и автоматическая резервная. Скачать — способ сохранить их вне приложения.</p>
    <h2>Начало отсчёта</h2>
    <label class="field">
      <span>Первый день в системе</span>
      <input type="date" id="since" data-act="since" value="${esc(isDayKey(store.settings.calendarSince) ? store.settings.calendarSince : '')}">
    </label>
    <p class="muted">Дата сдвигается на понедельник своей недели. Меняет счёт дней в системе, серию и доступность разбора. Отметки не затрагивает.</p>` + wipeBlock());

  h += sect('system', 'Система', systemSection());

  el('scr-settings').innerHTML = h;
  restoreOpenForm();
  armFlash();
  updateMirrorNote();
}

/* Строка «Резервная копия: …» — асинхронно и точечно после рендера
   «Настроек»; при недоступном зеркале не показывается вовсе.
   Стартовое чтение не завершилось — так и сказано: не «ошибка» и не
   тревога, а честное «неизвестно» тем же muted (A.1.4). */
function updateMirrorNote() {
  const p = el('mirror-note');
  if (!p) return;
  // о зеркале уже говорит блок предложения, и говорит подробнее — второй
  // строки про ту же копию не нужно
  if (mirrorOffer) { p.hidden = true; return; }
  if (mirrorUnverified) {
    p.textContent = 'Резервная копия не проверена';
    p.hidden = false;
    return;
  }
  if (typeof indexedDB === 'undefined') return;
  mirrorRead().then(snap => {
    if (!snap || typeof snap.savedAt !== 'number') return;
    p.textContent = 'Резервная копия: ' + fmtStamp(snap.savedAt);
    p.hidden = false;
  });
}

/* Поле «Блок» — выбор из заведённых (задача 17). Свободного ввода нет:
   опечатка заводила блок-двойник. «+ Новый блок…» всегда последний
   вариант — значение-маркер могло бы совпасть с именем блока, индекс
   не может; отсюда isNewGroupPick. */
const isNewGroupPick = sel => !!sel && sel.selectedIndex === sel.options.length - 1;

function groupField(idPrefix, value) {
  const cur = ui.groupPick !== null ? ui.groupPick : (value || '').trim();
  const names = groupList();
  let opts = `<option value=""${cur ? '' : ' selected'}>— без блока</option>`;
  for (const g of names) {
    opts += `<option value="${esc(g)}"${g === cur ? ' selected' : ''}>${esc(g)}</option>`;
  }
  // имя из импорта, которого нет в списке блоков: вариант виден и выбран,
  // чтобы правка формы не сбрасывала принадлежность молча (инвариант 13)
  if (cur && !names.includes(cur)) {
    opts += `<option value="${esc(cur)}" selected>${esc(cur)} (нет в списке)</option>`;
  }
  opts += `<option value=""${ui.groupNew ? ' selected' : ''}>+ Новый блок…</option>`;
  return `
    <label class="field"><span>Блок</span>
      <select id="${idPrefix}-group" data-act="group-pick">${opts}</select>
    </label>
    ${ui.groupNew ? `<label class="field"><span>Имя блока</span>
      <input type="text" id="${idPrefix}-gnew" placeholder="Например: Утро"></label>` : ''}`;
}

/* Значение поля «Блок» при сохранении формы: имя из списка либо новое
   имя из раскрытого поля. Пустое имя блока не заводит и принадлежность
   пункта не меняет — прежняя возвращается как fallback.

   Чтение без записи → { name, isNew, refusal } («Расписание 1/3», п. 1.4).
   Прежде функция заводила новый блок прямо при чтении — то есть в фазе
   записи формы, когда отказывать уже поздно. А отказ появился: имя из
   «+ Новый блок…», которое носит УБРАННЫЙ блок, молча привязало бы пункт к
   невидимому блоку (groupJoinRefusal). Проверка — вместе с прочими, до
   записи; блок заводит commitGroupField, уже в фазе записи. Отказа нет,
   если имя не меняется: пункт, уже стоящий в блоке, никуда не входит. */
function readGroupField(idPrefix, fallback) {
  const was = String(fallback ?? '').trim();
  const sel = el(idPrefix + '-group');
  let name = was, isNew = false;
  if (sel && !isNewGroupPick(sel)) {
    name = sel.value;
  } else if (sel) {
    const inp = el(idPrefix + '-gnew');
    const typed = inp ? inp.value.trim() : '';
    if (typed) { name = typed; isNew = true; }
  }
  return { name, isNew, refusal: name !== was ? groupJoinRefusal(name) : null };
}

/* Новое имя из «+ Новый блок…» заводит блок в конце списка (инвариант 13);
   имя живого блока просто в него входит */
function commitGroupField(pick) {
  if (pick.isNew && !findGroup(pick.name)) addGroup(pick.name);
  return pick.name;
}

/* Выбор дней недели: семь целей Пн…Вс. Кнопки, а не чекбоксы, по двум
   причинам: снимок черновика читает только `.value`, а у чекбокса он равен
   'on' независимо от галочки (замер разведки), и `.field input` перебил бы
   вид скрытого чекбокса из `.check`. Состояние живёт в `ui.editDays` по
   образцу `ui.editNorm` и переживает перерисовку формы.

   Состояние выбранности читается НЕ ТОЛЬКО ЦВЕТОМ (B.4.1): выбранный день
   несёт `aria-pressed="true"`, заливку И жирное начертание, невыбранный —
   пустую обводку и обычное. Тач-отклик даровой: класс `.btn` уже в списке
   TAPPABLE, и сторож живого DOM это проверяет.

   Ряд вынесен на padding карточки (`.days` в styles.css): внутренняя
   ширина формы на 375 px — 301 px, а семь целей по 44 требуют 308.

   Поле общее для формы пункта и формы блока («Расписание 1/3», п. 2.4):
   открыта всегда одна форма, поэтому id подписи не повторяется, а чей
   черновик правит чип — решает обработчик day-toggle.

   Своими днями действие в живом блоке дни блока только СУЖАЕТ (п. 2.5):
   чип дня ВНЕ дней блока (within) недоступен и не «on» — вне дней блока
   действие не появится, что бы ни стояло в своей маске. Недоступность
   названа словами, а не одной бледностью: «среда — не в днях блока». lead —
   строка между подписью поля и чипами («Дни блока: будни»). */
const DAY_SHORT = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'];
const DAY_FULL = ['понедельник', 'вторник', 'среду', 'четверг', 'пятницу', 'субботу', 'воскресенье'];
const DAY_NAME = ['понедельник', 'вторник', 'среда', 'четверг', 'пятница', 'суббота', 'воскресенье'];

function daysField(mask, within, lead) {
  let b = '';
  for (let i = 0; i < 7; i++) {
    const out = !!within && within[i] !== '1';
    const on = !out && mask[i] === '1';
    const label = out ? `${DAY_NAME[i]} — не в днях блока` : `${on ? 'убрать' : 'добавить'} ${DAY_FULL[i]}`;
    b += `<button type="button" class="btn day${on ? ' on' : ''}" data-act="day-toggle" data-day="${i}"` +
      ` aria-pressed="${on ? 'true' : 'false'}" aria-label="${label}"${out ? ' disabled' : ''}>${DAY_SHORT[i]}</button>`;
  }
  return `
      <div class="field">
        <span id="days-lbl">Дни недели</span>${lead || ''}
        <div class="days" role="group" aria-labelledby="days-lbl">${b}</div>
      </div>`;
}

/* ── Дни и тип ДЕЙСТВИЯ в форме правки («Расписание 1/3», п. 2.5) ──
   Одно состояние на разметку и на сохранение: иначе форма показала бы
   одно, а записала другое. Блок-кандидат — тот, что СЕЙЧАС выбран в поле
   «Блок» (оттого «Блок» и стоит над «Днями»: дни зависят от блока).
   «+ Новый блок…» — блока ещё нет, и дни его — «все семь», как у действия
   без блока; убранный и неизвестный блок дней не несут тоже.

   Режим «как блок» — своя маска «все семь» (нейтральный элемент ∧), и
   второго способа сказать то же форма данных не заводит (п. 1.2). Режим
   выводится из данных, пока владелец его не трогал (ui.editOwn === null). */
function editDaysState(it) {
  const cand = ui.groupNew ? '' : String(ui.groupPick !== null ? ui.groupPick : groupNameOf(it)).trim();
  const g = cand ? findGroup(cand) : null;
  const bm = g && live(g) ? blockMaskNow(g) : null;
  const now = scheduleNow(it);
  return {
    bm,  // маска живого блока-кандидата или null — «без блока»
    now, // своя маска как есть
    inherit: bm !== null && (ui.editOwn === false || (ui.editOwn === null && now === WEEK_ALL)),
    // выбор в чипах: черновик либо своя маска в пределах дней блока
    pick: ui.editDays !== null ? ui.editDays : (bm !== null ? andMask(now, bm) : now),
    // дни трогали — только тогда расписание пишется (п. 2.5)
    touched: ui.editDays !== null || ui.editOwn !== null,
    same: !ui.groupNew && cand === groupNameOf(it)
  };
}

/* Итог дней при сохранении: { mask, refusal }. mask — новая своя маска
   или null («не трогали, не пишется»). Отказ — по ИТОГОВОМУ блоку и
   итоговой своей маске (п. 1.3): пустая эффективная маска неотличима от
   убранного пункта, а убирать умеет «Убрать».
   Две фразы, и разводит их то, что владелец видит: выбор, который форма
   показала пустым, — «Нужен хотя бы один день недели» (как в 29/B); выбор
   непустой, но в днях итогового блока его нет — «Не останется ни одного
   дня: {имя}». Нуль, пришедший импортом, в форме, где не трогали ни дни, ни
   блок, отказом не становится: владелец правил не их. */
function editDaysOutcome(it, st, finalGroup, shownName) {
  let mask = null;
  if (st.touched) {
    const shown = st.bm === null ? st.pick : (st.inherit ? st.bm : andMask(st.pick, st.bm));
    if (maskDays(shown) === 0) return { mask: null, refusal: 'Нужен хотя бы один день недели' };
    mask = st.bm === null ? st.pick : (st.inherit ? WEEK_ALL : mergeOwnMask(st.now, st.pick, st.bm));
  }
  const fg = findGroup(finalGroup);
  const fm = fg && live(fg) ? blockMaskNow(fg) : WEEK_ALL;
  const moved = String(finalGroup ?? '').trim() !== groupNameOf(it);
  if ((st.touched || moved) && maskDays(andMask(mask !== null ? mask : st.now, fm)) === 0) {
    return { mask: null, refusal: `Не останется ни одного дня: ${shownName}` };
  }
  return { mask, refusal: null };
}

/* Поле «Дни» действия. Без живого блока — прежние чипы своей маски (29/B).
   В живом блоке — два режима: «как блок» одной строкой с кнопкой «Свои
   дни», либо чипы в пределах дней блока с кнопкой «Как блок». */
function actionDaysField(it) {
  const st = editDaysState(it);
  if (st.bm === null) return daysField(st.pick);
  if (st.inherit) {
    return `
      <div class="field">
        <span>Дни</span>
        <p class="dnow">как блок · ${esc(daysSummary(st.bm))}<button type="button" class="btn quiet" data-act="days-own">Свои дни</button></p>
      </div>`;
  }
  // нуль пришёл импортом, а дни и блок не трогали: сказано тихо, без отказа
  const zero = !st.touched && st.same && maskDays(andMask(st.now, st.bm)) === 0;
  return daysField(st.pick, st.bm, `<p class="muted">Дни блока: ${esc(daysSummary(st.bm))}</p>`) +
    (zero ? `
      <p class="muted">Сейчас не попадает ни в один день блока</p>` : '') + `
      <div class="btns">
        <button type="button" class="btn quiet" data-act="days-inherit">Как блок</button>
      </div>`;
}

/* Тип формы: черновик — только пока тип вообще можно менять (день
   заведения). Иначе черновик не значит ничего: день мог смениться при
   открытой форме, и рисуется тип пункта. */
const editTypeOf = it => (ui.editType !== null && canChangeType(it) ? ui.editType : it.type);

/* Поле времени — подпись для памяти. type="time" уже применён в проекте
   к порогу параметра: значение всегда каноническое «ЧЧ:ММ», на iOS
   открывается системный барабан, валидация даровая. */
function timeField(idPrefix, value) {
  return `<label class="field"><span>Время</span>` +
    `<input type="time" id="${idPrefix}-at" value="${esc(value || '')}" placeholder="23:30"></label>`;
}

function editForm(it) {
  const head = `
    <div class="card form" data-form="edit" data-id="${esc(it.id)}">
      <label class="field"><span>Название</span><input type="text" id="e-name" value="${esc(it.name)}"></label>
      <label class="field"><span>Подпись</span><input type="text" id="e-note" value="${esc(it.note || '')}" placeholder="необязательная строка под названием"></label>
      ${timeField('e', it.at)}`;
  // маска — только у ежедневных пунктов обеих областей; у недельного
  // счётчика и параметра дневных отметок нет вовсе (инвариант 10). Здесь —
  // чипы привычки: её дни блок не ограничивает (п. 1.2). У действия поле
  // своё, с режимами «как блок» и «свои» (actionDaysField ниже)
  const days = it.type === 'daily'
    ? daysField(ui.editDays !== null ? ui.editDays : scheduleNow(it)) : '';
  // Здесь стояла ссылка «Формула и лестница ›» — вход в лист детали, и
  // предупреждение о том, что у пункта с лестницей подпись на дневных
  // экранах вытесняется ступенью. Обе механики сняты задачей 28.D: ссылке
  // некуда вести, а подпись снова показывается всегда и как есть.
  // «Убрать» живёт В ФОРМЕ, а не в строке: на 375 px строка занята именем,
  // стрелками и метой — места под ещё одну цель в ней нет (A.3.1)
  const foot = removeFoot('item', it.id,
    `<button class="btn primary" data-act="edit-save" data-id="${esc(it.id)}">Сохранить</button>
        <button class="btn quiet" data-act="edit-cancel">Отмена</button>`) + `
    </div>`;
  if (it.type === 'param') {
    // вид фиксируется при создании (инвариант 10) — не селект, а тихая строка
    return head + `
      <p class="muted">Вид: ${it.pkind === 'number' ? 'число' : 'время'}</p>
      ${it.pkind === 'number'
        ? `<div class="pair">
            <label class="field"><span>Порог</span><input class="num" type="text" inputmode="decimal" id="e-pvalue" value="${esc(it.pvalue)}"></label>
            <label class="field"><span>Единица</span><input type="text" id="e-punit" value="${esc(it.unit || '')}"></label>
          </div>`
        : `<label class="field"><span>Порог</span><input id="e-ptime" type="time" value="${esc(fmtParam(it))}"></label>`}
      <label class="field"><span>Шаг (со знаком)</span><input class="num" type="text" inputmode="decimal" id="e-pstep" value="${esc(it.pstep)}"></label>
      ${groupField('e', it.group)}` + foot;
  }
  if (it.area === 'habit') {
    // привычка: название, подпись, время, дни и норма недели степпером.
    // Потолок нормы — число дней ВЫБРАННОЙ маски (инвариант 11): норма 5
    // при двух днях в маске невыполнима, и форма её не даёт (B.2.4).
    const mask = ui.editDays !== null ? ui.editDays : scheduleNow(it);
    const cap = maskDays(mask);
    const n = Math.min(cap, ui.editNorm !== null ? ui.editNorm : (it.normPerWeek || 7));
    return head + days + `
      <div class="field inline norm">
        <span>Норма в неделю: <b>${n}</b></span>
        <span class="btns">
          <button type="button" class="btn icon quiet" data-act="norm-dec" data-id="${esc(it.id)}"${n <= 1 ? ' disabled' : ''} aria-label="уменьшить норму">&minus;</button>
          <button type="button" class="btn icon quiet" data-act="norm-inc" data-id="${esc(it.id)}"${n >= cap ? ' disabled' : ''} aria-label="увеличить норму">+</button>
        </span>
      </div>
      ${cap < 7 ? `<p class="muted">Не больше ${cap} — столько дней в расписании.</p>` : ''}
      ${groupField('e', it.group)}` + foot;
  }
  // ── действие: ежедневное или недельный счётчик минимума (п. 2.5) ──
  // Порядок: название, подпись, время, [тип], блок, дни, значение, [цель],
  // история, хвост. «Блок» стоит НАД «Днями»: дни действия зависят от блока,
  // и чипы рисуются в пределах дней того блока, что выбран сейчас.
  // Тип правится только в день заведения; позже он называется тихой строкой
  // (как «Вид» у параметра) — прошлого у пункта уже есть, и оно типа не меняет
  const type = editTypeOf(it);
  const typeRow = canChangeType(it)
    ? `
      <label class="field"><span>Тип</span>
        <select id="e-type" data-act="edit-type">
          <option value="daily"${type === 'weekly' ? '' : ' selected'}>ежедневный чекбокс</option>
          <option value="weekly"${type === 'weekly' ? ' selected' : ''}>недельный счётчик с целью</option>
        </select>
      </label>`
    : `
      <p class="muted">Тип: ${it.type === 'weekly' ? 'недельный счётчик' : 'ежедневный'}</p>`;
  // история планки — в форме, а не в строке: строка «Расписания» стала
  // компактной («Расписание 1/3», п. 2.4), справка о прошлом живёт у пункта
  const hist = barHistory(it);
  return head + typeRow + `
      ${groupField('e', it.group)}${type === 'daily' ? actionDaysField(it) : ''}
      <div class="pair">
        <label class="field"><span>Значение</span><input class="num" type="text" inputmode="decimal" id="e-value" value="${esc(it.value)}"></label>
        <label class="field"><span>Единица</span><input type="text" id="e-unit" value="${esc(it.unit || '')}"></label>
      </div>
      ${type === 'weekly' ? `<label class="field"><span>Цель за неделю</span><input class="num" type="text" inputmode="numeric" id="e-goal" value="${esc(it.goal)}"></label>` : ''}
      ${hist ? `<p class="muted">${hist}</p>` : ''}` + foot;
}

/* Форма добавления — только привычка и параметр («Расписание 1/3», п. 2.4).
   Ветка минимума снята вместе с кнопкой «Добавить пункт» и подсказкой «Одно
   новое дело за раз»: действия заводятся быстрым добавлением в карточке
   своего блока, по несколько строк сразу, а тип и цель — правкой. */
function addForm() {
  const hint = ui.addHint
    ? `<p class="hint">Одна новая привычка за раз: последнее добавлено меньше 14 дней назад.</p>`
    : '';
  const head = `
    <div class="card form" data-form="add">
      ${hint}
      <label class="field"><span>Название</span><input type="text" id="f-name" placeholder="Например: чтение"></label>
      <label class="field"><span>Подпись</span><input type="text" id="f-note" placeholder="необязательная строка под названием"></label>`;
  const foot = `
      <div class="btns">
        <button class="btn primary" data-act="add-save">Добавить</button>
        <button class="btn quiet" data-act="add-cancel">Отмена</button>
      </div>
    </div>`;
  const isParam = ui.addType === 'param';
  return head + `
      <label class="field"><span>Тип</span>
        <select id="f-type" data-act="add-type">
          <option value="daily"${isParam ? '' : ' selected'}>привычка (ежедневная)</option>
          <option value="param"${isParam ? ' selected' : ''}>параметр (порог недели)</option>
        </select>
      </label>
      ${isParam ? `
      <label class="field"><span>Вид</span>
        <select id="f-pkind" data-act="add-pkind">
          <option value="time"${ui.addPkind === 'number' ? '' : ' selected'}>время</option>
          <option value="number"${ui.addPkind === 'number' ? ' selected' : ''}>число</option>
        </select>
      </label>
      ${ui.addPkind === 'number'
        ? `<div class="pair">
            <label class="field"><span>Порог</span><input class="num" type="text" inputmode="decimal" id="f-pvalue" placeholder="4000"></label>
            <label class="field"><span>Единица</span><input type="text" id="f-punit" placeholder="шаг."></label>
          </div>`
        : `<label class="field"><span>Порог</span><input id="f-ptime" type="time" value="00:00"></label>`}
      <label class="field"><span>Шаг (со знаком)</span><input class="num" type="text" inputmode="decimal" id="f-pstep" placeholder="-15"></label>` : ''}
      ${groupField('f', '')}` + foot;
}

/* Экран 4 — «Система» */
/* Тексты «Системы» — секция «Настроек» (задача 16B), не отдельный экран */
function systemSection() {
  let h = '';
  for (const s of SYSTEM_TEXTS) {
    h += `<section class="sys"><h2>${esc(s.title)}</h2>`;
    if (s.kind === 'rules') {
      h += `<ol class="rules">` + s.items.map(r => `<li>${esc(r)}</li>`).join('') + `</ol>`;
    } else if (s.kind === 'leads') {
      h += s.items.map(x => `<p class="lead-p"><strong>${esc(x.lead)}</strong> ${esc(x.text)}</p>`).join('');
    } else if (s.kind === 'note') {
      h += `<p class="note-block">${esc(s.text)}</p>`;
    }
    h += `</section>`;
  }
  return h + versionBlock();
}

/* Последняя строка «Системы» (задача Р3): «Минимум · v48» и «Проверить
   обновления», под ними — результат проверки строкой на месте. Номер —
   ответ активного воркера, а не константа app.js: второго источника
   версии в коде нет (sw.js, VERSION). Узел печатает рендер, число
   приходит точечно (updateVersionLine) — по образцу строки резервной
   копии. Service worker недоступен — кнопки нет: проверять нечем. */
function versionBlock() {
  const sw = swApi();
  return `
    <div class="appver">
      <p class="muted" id="version-line">${esc(versionLineText())}</p>
      ${sw ? `<button class="btn" data-act="update-check"${ui.updateCheck === 'busy' ? ' disabled' : ''}>Проверить обновления</button>` : ''}
    </div>
    ${sw ? `<p class="muted upd" id="update-check" role="status"${ui.updateCheck ? '' : ' hidden'}>${updateCheckHtml()}</p>` : ''}`;
}

/* ── Обработчики ───────────────────────────────────────────── */

function parseNum(v) {
  const s = String(v ?? '').trim().replace(',', '.');
  if (!s) return null;
  const n = Number(s);
  return isFinite(n) ? n : null;
}

/* Единая валидация пользовательского ввода чисел: только > 0, иначе null */
function parsePositive(v) {
  const n = parseNum(v);
  return n !== null && n > 0 ? n : null;
}

/* ── Листы поверх вкладок (задача 26, п. 4) ────────────────────
   ДВА листа: разбор недели и тренировка. Третьим был лист детали пункта,
   снятый вместе с формулой и лестницей (задача 28.D). Оба возвращают
   прежнюю вкладку с прежним скроллом и фокусом — каким бы путём ни
   закрывались: кнопкой «Готово» или таб-баром. Прежде скролл и фокус
   возвращали только кнопки, а таб-бар ронял экран наверх. */

/* Куда вернёт закрытие открытого сейчас листа: вкладка, её скролл и
   кнопка, которой лист открыт. Зовётся ДО close*(), пока состояние цело.
   Порядок проверок — как в renderAll: разбор главнее тренировки. */
function sheetReturn() {
  const src = ui.sheetSrc;
  if (ui.reviewOpen) return { tab: ui.reviewFrom || ui.tab, y: ui.reviewScroll, src };
  if (ui.trainOpen) return { tab: ui.trainFrom || ui.tab, y: ui.trainScroll, src };
  return null;
}

/* Фокус — в открытый лист: заголовок несёт tabindex="-1", чтобы клавиатура
   и чтение с экрана начинались с листа, а не с начала документа. Ловушки
   фокуса нет: лист закрывается таб-баром, запирать в нём нельзя (п. 4.3). */
function focusSheet(id) {
  const h = document.querySelector('#' + id + ' h1');
  // preventScroll: скроллом распоряжается renderAll и sheetReturn, а не фокус
  if (h && typeof h.focus === 'function') h.focus({ preventScroll: true });
}

/* Фокус обратно на кнопку-источник. Узел к этому моменту пересоздан
   перерисовкой, поэтому помним не ссылку, а действие и пункт. Зовётся
   ПОСЛЕ возврата скролла: кнопка тогда уже на экране и фокус его не двинет. */
function focusSrc(src) {
  if (!src) return;
  // Кнопка ищется в пределах ВИДИМОГО экрана — тот же приём, что у
  // visibleFlash (задача 27.1, Д4), и тот же класс дефекта: renderAll рисует
  // одну текущую вкладку, а прочие экраны сохраняют прежнюю разметку. Баннер
  // разбора (`goto-review`) стоит и на «Сегодня», и на «Прогрессе», и data-id
  // у него нет ни там, ни там — выборка по всему документу отдавала list[0],
  // то есть узел «Сегодня». Замер: разбор, открытый с «Прогресса», возвращал
  // фокус на кнопку внутри scr-today при hidden = true (задача 28.B, п. 3).
  const list = [...document.querySelectorAll(`main .screen:not([hidden]) [data-act="${src.act}"]`)];
  const btn = src.id ? list.find(x => x.dataset.id === src.id) : list[0];
  if (btn && typeof btn.focus === 'function') btn.focus({ preventScroll: true });
}

/* Лист разбора закрывается и таб-баром — как лист тренировки */
function closeReview() {
  ui.reviewOpen = false;
  ui.reviewFrom = null;
  // свёртка недели принадлежит открытому разбору: следующий открывается
  // в состоянии по умолчанию, а не с чужой памятью (задача 24, п. 9.1)
  ui.weekOpen = null;
  ui.sheetSrc = null;
}

function closeTrain() {
  ui.trainOpen = false;
  ui.trainId = null;
  ui.trainFrom = null;
  ui.trainDraft = {}; // черновик принадлежал закрытому листу
  ui.sheetSrc = null;
}

/* Взведённое подтверждение вторым тапом — временное состояние одного
   экрана, а не решение владельца: уход с экрана его снимает (задача 22,
   п. 4). Иначе «Подтвердить: стереть» переживало смену вкладки, и один
   тап по возвращении стирал всё. Список полный и живёт здесь одним
   местом: новое подтверждение добавляется сюда, а не в обработчики. */
function resetConfirms() {
  ui.wipeConfirm = false;
  ui.wipeDropConfirm = false;
  ui.corruptDropConfirm = null;
  ui.mirrorRestoreConfirm = false;
  ui.mirrorKeepConfirm = false;
  // здесь гасло «Удалить блок» (ui.groupDelete) — снято вместе с удалением
  // блока; уход блока взводит общее removeConfirm ниже
  ui.weekCloseConfirm = false;
  ui.removeConfirm = null;
}

/* ── Перетаскивание (задача 16F) ───────────────────────────────
   Pointer Events без библиотек. Захват — долгим нажатием (250 мс):
   до него любое движение считается скроллом. Соседи раздвигаются
   CSS-переходом (при reduced-motion — мгновенно, глобальный блок).
   Стрелки ↑↓ остаются: перетаскивание их не заменяет, а дополняет.
   Пункт двигается только среди соседей по блоку — как и стрелками. */

const DRAG_HOLD = timing('DRAG_HOLD', 250);   // мс удержания до захвата
const DRAG_SLOP = 8;     // px до захвата: это скролл, а не перетаскивание (не время — не в TIMING)
const DRAG_CLICK_MS = timing('DRAG_CLICK_MS', 300); // сколько глушить клик после перетаскивания
const DRAG_EDGE = 64;    // px от края экрана, где включается автоскролл
const DRAG_SPEED = 12;   // px за кадр автоскролла
const DRAG_OUT = 60;     // px вбок от списка — отмена

let drag = null;
let dragSuppressClick = false; // тап после перетаскивания не открывает форму
let dragClickTimer = null;     // страховка: флаг снимается, если клика не последовало

function dragSiblings(row) {
  const kind = row.dataset.drag;
  const g = row.dataset.dgroup;
  return [...row.parentElement.children].filter(n =>
    n.dataset && n.dataset.drag === kind && (kind !== 'item' || n.dataset.dgroup === g));
}

function dragDown(e) {
  if (drag || (typeof e.button === 'number' && e.button > 0)) return;
  const row = e.target.closest ? e.target.closest('[data-drag]') : null;
  if (!row) return;
  // стрелки, открытая форма, короткий путь назад и подтверждение живут
  // своей жизнью. Карточка блока крупная — внутри неё строки, формы и
  // кнопка «Добавить действия», поэтому список исключений шире прежнего
  // («Расписание 1/3», п. 4.3): долгое нажатие на кнопку или поле не
  // должно поднимать весь блок
  if (e.target.closest('.ictl, .card.form, [data-form], .gone-note, .flash, input, textarea, select')) return;
  // и сам блок берут только за шапку: тело карточки — чужая территория
  if (row.dataset.drag === 'group' && !e.target.closest('.bhead')) return;
  drag = {
    row, kind: row.dataset.drag, id: row.dataset.dragId || '',
    x: e.clientX, y: e.clientY, startY: e.clientY, startX: e.clientX,
    active: false, from: -1, to: -1, sibs: [], mids: [], h: 0, listRect: null,
    raf: null,
    timer: setTimeout(dragActivate, DRAG_HOLD)
  };
  document.addEventListener('pointermove', dragMove, { passive: false });
  document.addEventListener('pointerup', dragUp);
  document.addEventListener('pointercancel', dragStop);
  document.addEventListener('keydown', dragKey);
}

/* Захват: замер соседей в координатах документа — автоскролл во время
   перетаскивания не должен сбивать расчёт позиции */
function dragActivate() {
  if (!drag) return;
  const sc = window.scrollY || 0;
  drag.sibs = dragSiblings(drag.row);
  drag.from = drag.sibs.indexOf(drag.row);
  if (drag.sibs.length < 2 || drag.from < 0) { dragStop(); return; }
  drag.mids = drag.sibs.map(n => {
    const r = n.getBoundingClientRect();
    return r.top + r.height / 2 + sc;
  });
  const own = drag.row.getBoundingClientRect();
  drag.h = own.height || 56;
  // смещение центра строки относительно пальца в момент захвата
  drag.offset = drag.mids[drag.from] - (drag.y + sc);
  drag.listRect = drag.row.parentElement.getBoundingClientRect();
  drag.active = true;
  drag.to = drag.from;
  drag.row.classList.add('drag-live');
  document.body.classList.add('dragging');
  // iOS: скролл страницы во время перетаскивания глушится непассивным touchmove
  document.addEventListener('touchmove', dragTouch, { passive: false });
  dragFrame();
}

function dragTouch(e) { if (drag && drag.active) e.preventDefault(); }

/* Автоскролл у краёв экрана — отдельным кадром, чтобы работал и когда
   палец стоит на месте у края */
function dragFrame() {
  if (!drag || !drag.active) return;
  const vh = window.innerHeight || 812;
  let d = 0;
  if (drag.y < DRAG_EDGE) d = -DRAG_SPEED;
  else if (drag.y > vh - DRAG_EDGE) d = DRAG_SPEED;
  if (d) { window.scrollBy(0, d); dragApply(); }
  drag.raf = (window.requestAnimationFrame || setTimeout)(dragFrame, 16);
}

/* Положение перетаскиваемой строки и раздвижение соседей */
function dragApply() {
  if (!drag || !drag.active) return;
  const center = drag.y + (window.scrollY || 0) + drag.offset;
  let to = 0;
  drag.mids.forEach((m, k) => { if (k !== drag.from && m < center) to++; });
  drag.to = to;
  drag.row.style.transform = `translateY(${center - drag.mids[drag.from]}px) scale(1.03)`;
  drag.sibs.forEach((n, k) => {
    if (k === drag.from) return;
    let dy = 0;
    if (to > drag.from && k > drag.from && k <= to) dy = -drag.h;
    else if (to < drag.from && k >= to && k < drag.from) dy = drag.h;
    n.style.transform = dy ? `translateY(${dy}px)` : '';
  });
}

function dragMove(e) {
  if (!drag) return;
  drag.x = e.clientX;
  drag.y = e.clientY;
  if (!drag.active) {
    // движение до захвата — это скролл: удержание отменяется
    if (Math.abs(e.clientY - drag.startY) > DRAG_SLOP || Math.abs(e.clientX - drag.startX) > DRAG_SLOP) dragStop();
    return;
  }
  e.preventDefault();
  // уход пальца за пределы списка вбок — отмена без записи
  if (drag.listRect && (e.clientX < drag.listRect.left - DRAG_OUT || e.clientX > drag.listRect.right + DRAG_OUT)) {
    dragStop();
    return;
  }
  dragApply();
}

function dragKey(e) { if (e.key === 'Escape') dragStop(); } // отмена с клавиатуры

function dragUp() {
  if (!drag) return;
  const { active, kind, id, from, to } = drag;
  dragStop();
  if (!active || to === from || to < 0) return;
  const ok = kind === 'item' ? reorderItem(id, to)
    : kind === 'group' ? reorderGroup(id, to)
      : reorderExercise(id, to);
  if (ok) renderSettings(); // перестановка — структурное изменение
}

/* Снятие захвата: стили сняты, слушатели убраны, модель не тронута */
function dragStop() {
  if (!drag) return;
  clearTimeout(drag.timer);
  if (drag.raf !== null) (window.cancelAnimationFrame || clearTimeout)(drag.raf);
  if (drag.active) {
    dragSuppressClick = true;
    // клик приходит сразу за отпусканием пальца; если его не будет вовсе
    // (pointercancel, отмена с клавиатуры), флаг снимается сам и не глотает
    // следующий, уже не связанный с перетаскиванием тап (аудит, находка 29)
    clearTimeout(dragClickTimer);
    dragClickTimer = setTimeout(() => { dragSuppressClick = false; }, DRAG_CLICK_MS);
    drag.row.classList.remove('drag-live');
    drag.row.style.transform = '';
    drag.sibs.forEach(n => { n.style.transform = ''; });
    document.body.classList.remove('dragging');
    document.removeEventListener('touchmove', dragTouch);
  }
  document.removeEventListener('pointermove', dragMove);
  document.removeEventListener('pointerup', dragUp);
  document.removeEventListener('pointercancel', dragStop);
  document.removeEventListener('keydown', dragKey);
  drag = null;
}

/* ── Свайп «Не сегодня» (задача Р2) ────────────────────────────
   Pointer Events без библиотек, только строки действий «Сегодня» (.swipe).
   Жест — сдвиг строки влево, открывающий кнопку под ней; сама запись
   делается ТАПОМ по кнопке, а не жестом: промахнувшийся палец ничего не
   пишет, и путь с клавиатуры тот же самый.

   Блокировка направления: пока палец не ушёл дальше SWIPE_SLOP, жест не
   решён; ушёл вбок сильнее, чем вниз, — строка захвачена, иначе это скролл,
   и строка отпускается навсегда до следующего касания. Скролл страницы
   остаётся браузеру: `touch-action: pan-y` отдаёт ему вертикаль, а горизонталь
   приходит сюда pointermove'ами.

   Порог — SWIPE_SHARE ширины строки: дальше — строка остаётся открытой, ближе
   — возвращается. Во время жеста строка идёт за пальцем без перехода
   (.swiping), отпускание доезжает переходом в окне движения; при
   reduced-motion переход гасит глобальный блок, и конечное положение
   достигается мгновенно. Двигается только transform — layout не трогается.

   Открытое состояние (.open) — не данные и не ui: рендер его не печатает,
   перерисовка закрывает строку, любое касание вне её кнопки — тоже. */
const SWIPE_SLOP = 8;       // px до решения, жест это или скролл (как DRAG_SLOP)
const SWIPE_SHARE = 0.4;    // доля ширины строки, после которой строка остаётся открытой

let swipe = null;
let swipeSuppressRow = null; // строка, клик по которой после жеста не переключает отметку
let swipeClickTimer = null;

/* Закрыть строку: снять сдвиг и открытое состояние. style снимается целиком,
   а не свойством: пустой атрибут style="" разошёлся бы с перерисовкой */
function swipeClose(row) {
  if (!row) return;
  row.classList.remove('open', 'swiping');
  row.removeAttribute('style');
}

function swipeDown(e) {
  if (swipe || (typeof e.button === 'number' && e.button > 0)) return;
  const tgt = e.target && e.target.closest ? e.target : null;
  const row = tgt ? tgt.closest('#scr-today .rowwrap.swipe') : null;
  // касание вне открытой строки (или по её содержимому, но не по кнопке)
  // закрывает её; клик по содержимому при этом глушится — тап по открытой
  // строке значит «закрыть», а не «отметить»
  const open = [...document.querySelectorAll('#scr-today .rowwrap.swipe.open')];
  const onBtn = tgt && tgt.closest('.skipbtn');
  let closed = false;
  for (const r of open) {
    if (onBtn && r.contains(onBtn)) continue;
    swipeClose(r);
    if (r === row) closed = true;
  }
  if (closed) { suppressUntilRelease(row); return; }
  if (!row || onBtn || tgt.closest('.dot, .miss-note')) return;
  const btn = row.querySelector('.skipbtn');
  if (!btn || btn.hidden) return; // у отмеченной строки жеста нет
  swipe = { row, x: e.clientX, y: e.clientY, active: false, dx: 0, w: row.getBoundingClientRect().width || 0 };
  document.addEventListener('pointermove', swipeMove, { passive: false });
  document.addEventListener('pointerup', swipeUp);
  document.addEventListener('pointercancel', swipeCancel);
}

function swipeMove(e) {
  if (!swipe) return;
  const dx = e.clientX - swipe.x;
  const dy = e.clientY - swipe.y;
  if (!swipe.active) {
    if (Math.abs(dx) <= SWIPE_SLOP && Math.abs(dy) <= SWIPE_SLOP) return; // жест ещё не решён
    if (Math.abs(dx) > Math.abs(dy)) {
      swipe.active = true;
      swipe.row.classList.add('swiping');
    } else { swipeEnd(); return; } // вертикаль — скролл: строка отпущена
  }
  e.preventDefault();
  // только влево: вправо строка дальше покоя не едет, влево — не дальше своей ширины
  swipe.dx = Math.min(0, Math.max(-(swipe.w || Infinity), dx));
  swipe.row.style.setProperty('--sx', swipe.dx + 'px');
}

function swipeUp() {
  if (!swipe) return;
  const { row, active, dx, w } = swipe;
  swipeEnd();
  if (!active) return; // тап без жеста — обычный клик, его не трогаем
  suppressSwipeClick(row);
  const keep = w > 0 && -dx >= w * SWIPE_SHARE;
  swipeClose(row);
  if (keep) row.classList.add('open');
}

/* pointercancel: браузер забрал жест себе (скролл) — строка возвращается */
function swipeCancel() {
  if (!swipe) return;
  const { row } = swipe;
  swipeEnd();
  swipeClose(row);
}

function swipeEnd() {
  document.removeEventListener('pointermove', swipeMove);
  document.removeEventListener('pointerup', swipeUp);
  document.removeEventListener('pointercancel', swipeCancel);
  swipe = null;
}

/* Клик приходит сразу за отпусканием мыши; после жеста пальцем его не бывает
   вовсе. Поэтому глушится не «следующий клик», а клик ПО СОДЕРЖИМОМУ ЭТОЙ
   строки: тап по выдвинутой кнопке сразу после жеста — законное нажатие, и
   чужой тап не глотается. Страховочный таймер снимает память сам. */
function suppressSwipeClick(row) {
  swipeSuppressRow = row;
  clearTimeout(swipeClickTimer);
  swipeClickTimer = setTimeout(() => { swipeSuppressRow = null; }, DRAG_CLICK_MS);
}

/* Тап по содержимому открытой строки: клик придёт после ОТПУСКАНИЯ, а
   держать палец или кнопку мыши владелец может сколько угодно. Память
   ставится сразу, а страховочный таймер взводится только отпусканием
   (Р2/рецензия): прежде он шёл от касания, и нажатие дольше DRAG_CLICK_MS
   закрывало строку И отмечало пункт — тап по открытой строке значит
   «закрыть». До отпускания таймера нет вовсе: память снимет сам клик. */
let swipeRelease = null;
function suppressUntilRelease(row) {
  swipeSuppressRow = row;
  clearTimeout(swipeClickTimer);
  if (swipeRelease) swipeRelease(false);
  const done = arm => {
    document.removeEventListener('pointerup', up);
    document.removeEventListener('pointercancel', up);
    swipeRelease = null;
    if (arm) suppressSwipeClick(row);
  };
  const up = () => done(true);
  swipeRelease = done;
  document.addEventListener('pointerup', up);
  document.addEventListener('pointercancel', up);
}

function onClick(e) {
  // клик, родившийся из перетаскивания, форму не открывает
  if (dragSuppressClick) { dragSuppressClick = false; return; }
  // клик, родившийся из свайпа, не переключает отметку: label переключил бы
  // чекбокс своим действием по умолчанию, поэтому оно отменяется явно
  if (swipeSuppressRow) {
    const r = swipeSuppressRow;
    swipeSuppressRow = null;
    if (r.contains(e.target) && !e.target.closest('.skipbtn')) { e.preventDefault(); return; }
  }
  const b = e.target.closest('[data-act]');
  if (!b) return;
  if (syncDay()) return; // stale-экран: действие не применяется (инвариант 8)
  const hadImportNote = ui.importNote !== null;
  ui.importNote = null; // строка «Импортировано…» живёт до следующего действия
  // и строка «убран — Вернуть» тоже: короткий путь назад открыт до
  // следующего действия владельца, а не до таймера (A.3.4). У блока — так же
  ui.goneNote = null;
  ui.goneGroup = null;
  // и якорь подтверждения тоже: не отданный прошлым рендером, он не должен
  // всплыть позже у чужого действия (задача 26, п. 2.1)
  ui.savedAt = null;
  // область объявлений чистится здесь же: скринридер объявляет ИЗМЕНЕНИЕ
  // текста, и повторный тот же отказ после другого действия обязан снова
  // читаться как изменение (задача 27.1, п. 9.2)
  announce('');
  const act = b.dataset.act;
  const id = b.dataset.id;
  const item = id ? store.items.find(i => i.id === id) : null;

  switch (act) {
    // разбор — лист поверх вкладки: помним, откуда открыт и с каким скроллом
    case 'goto-review':
      ui.missOpen = {}; // уход с дневного экрана — как в таб-баре
      ui.raiseEdit = {};
      ui.reviewScroll = window.scrollY || 0;
      ui.reviewFrom = ui.tab;
      ui.reviewOpen = true;
      ui.sheetSrc = { act, id: id || '' };
      renderAll();
      focusSheet('scr-review');
      break;

    case 'review-done': {
      const back = sheetReturn();
      closeReview();
      resetConfirms(); // «Готово» уводит с листа — как таб-бар
      if (back && back.tab) ui.tab = back.tab;
      renderAll();
      if (back) { window.scrollTo(0, back.y); focusSrc(back.src); } // прежняя вкладка, скролл и фокус
      break;
    }

    // сворачивание секции «Настроек»: раскрытие делает сам details,
    // здесь только запоминается состояние — перерисовка его не теряет
    case 'sect':
      if (b.dataset.sect in ui.settingsOpen) ui.settingsOpen[b.dataset.sect] = !ui.settingsOpen[b.dataset.sect];
      break;

    case 'miss-note': {
      ui.missOpen[id] = !ui.missOpen[id];
      renderDayScreen();
      // вернуть фокус пересозданной кнопке — disclosure-паттерн остаётся рабочим для AT
      const dot = [...dayScreenEl().querySelectorAll('[data-act="miss-note"]')].find(d => d.dataset.id === id);
      if (dot) dot.focus();
      break;
    }

    case 'mark-yesterday': {
      markYesterday(id); // guard'ы внутри; «вчера» актуален — stale-guard уже отработал
      delete ui.missOpen[id];
      renderDayScreen(); // структурный путь: точка исчезает
      const cb = [...dayScreenEl().querySelectorAll('input[data-act="mark"]')].find(i => i.dataset.id === id);
      if (cb) cb.focus();
      break;
    }

    // «Не сегодня» и «Вернуть» (задача Р2): горячий путь, как у отметки.
    // Отказ домена (день сменился под открытой строкой, пункт уже отмечен,
    // хранилище не записало) — перерисовка: экран сверяется с данными
    case 'skip':
    case 'unskip': {
      const ok = act === 'skip' ? skipToday(id) : unskipToday(id);
      if (ok) updateTodaySkip(id);
      else renderToday();
      break;
    }

    // Свёрнутый выполненный блок разворачивается до конца дня (задача Р2,
    // п. 4): состояние экрана, в хранилище не пишется. Структурное
    // изменение — перерисовка, строки блока рождаются заново, фокус — на них
    case 'block-unfold': {
      const name = b.dataset.name || '';
      unfoldToday(name);
      renderToday();
      focusUnfolded(name);
      break;
    }

    // «+» открывает лист тренировки: сначала записывается, что сделано
    case 'train-inc':
      ui.trainScroll = window.scrollY || 0;
      ui.trainFrom = ui.tab;
      ui.trainId = id;
      ui.trainDraft = {}; // новый лист — чистые поля
      // прежний лист остаётся в DOM скрытым; чистим разметку, иначе снимок
      // черновика подхватит его поля как «текущие» (у листа тот же ключ)
      el('scr-train').innerHTML = '';
      ui.trainOpen = true;
      ui.sheetSrc = { act, id };
      renderAll();
      focusSheet('scr-train');
      break;

    case 'train-save': {
      const list = liveExercises();
      const entries = list.map(ex => {
        const inp = el('ex-' + ex.id);
        return { exId: ex.id, value: inp ? parsePositive(inp.value) : null };
      });
      // заполнять было что, но не заполнено ничего — записывать нечего
      // (задача 22, п. 7.4): ни сессии, ни счётчика. Упражнений нет вовсе —
      // поведение прежнее: лист вырождается в подтверждение «тренировка была»,
      // и это единственный способ засчитать её (решение архитектора).
      if (list.length && !entries.some(e => e.value !== null)) {
        // отказ молчаливым не бывает: лист остаётся и говорит — той же
        // строкой у кнопки, что и все прочие формы (задача 26, п. 2.5)
        refuse(b, 'Нечего записать: ни одно упражнение не заполнено');
        break;
      }
      const note = el('tr-note') ? el('tr-note').value : '';
      const back = sheetReturn(), weekly = ui.trainId;
      recordSession(weekly, entries, note);
      closeTrain();
      resetConfirms();
      if (back && back.tab) ui.tab = back.tab;
      renderAll();
      if (back) { window.scrollTo(0, back.y); focusSrc(back.src); }
      break;
    }

    case 'train-cancel': { // ничего не пишет
      const back = sheetReturn();
      closeTrain();
      resetConfirms();
      if (back && back.tab) ui.tab = back.tab;
      renderAll();
      if (back) { window.scrollTo(0, back.y); focusSrc(back.src); }
      break;
    }

    // шаг ±1 правит поле на месте: перерисовка листа сбросила бы соседние
    case 'ex-step': {
      const inp = el('ex-' + id);
      if (!inp) break;
      const cur = parseNum(inp.value);
      const next = Math.round(((cur === null ? 0 : cur) + (b.dataset.dir === 'up' ? 1 : -1)) * 100) / 100;
      // вниз до нуля не доводим (задача 22, п. 7.5): сессия берёт только
      // значение > 0, а ноль в поле «Записать» молча выбросил бы — поле
      // показывало бы одно, а записалось бы другое
      if (!(next > 0)) break;
      inp.value = String(next);
      break;
    }

    case 'ex-add-open': openSettingsForm(() => { ui.exAddOpen = true; }); break;
    case 'ex-add-cancel': dropOpenDraft(); ui.exAddOpen = false; renderSettings(); break;
    case 'ex-add-save': {
      const name = el('x-add-name') ? el('x-add-name').value.trim() : '';
      if (!name) { refuse(b, 'Название не заполнено'); break; } // безымянное упражнение не заводится
      const raw = el('x-add-value') ? el('x-add-value').value : '';
      // нагрузка необязательна, но написанная и непринятая — отказ, а не тишина
      if (String(raw).trim() && parsePositive(raw) === null) {
        refuse(b, 'Нагрузка не принята: нужно число больше нуля');
        break;
      }
      const ex = addExercise(name, el('x-add-unit') ? el('x-add-unit').value : '', parsePositive(raw));
      dropOpenDraft(); // сохранённое не возвращается черновиком (п. 4.5)
      ui.exAddOpen = false;
      if (ex) flashWrite('ex:' + ex.id); // подтверждение — у только что заведённой строки
      keepInPlace(b, renderSettings);
      break;
    }
    case 'ex-open': openSettingsForm(() => { ui.exEditingId = id; }); break;
    case 'ex-cancel': dropOpenDraft(); ui.exEditingId = null; renderSettings(); break;
    case 'ex-save': {
      const name = el('x-name') ? el('x-name').value.trim() : '';
      if (!name) { refuse(b, 'Название не заполнено'); break; }
      updateExercise(id, name, el('x-unit') ? el('x-unit').value : '');
      dropOpenDraft();
      ui.exEditingId = null;
      flashWrite('ex:' + id);
      keepInPlace(b, renderSettings);
      break;
    }
    case 'ex-up':
    case 'ex-down': {
      if (!moveExercise(id, act === 'ex-up' ? 'up' : 'down')) break;
      renderSettings();
      // фокус — той же кнопке пересозданной строки, как у пунктов
      const find = a => [...el('scr-settings').querySelectorAll(`[data-act="${a}"]`)].find(x => x.dataset.id === id);
      const same = find(act);
      const pair = find(act === 'ex-up' ? 'ex-down' : 'ex-up');
      if (same && !same.disabled) same.focus();
      else if (pair) pair.focus();
      break;
    }
    case 'train-undo': {
      // баннер хранилища ушёл из разметки экрана в постоянный узел
      // (задача 27.1, п. 5.2) — его появление больше не структурный путь
      // и перерисовки не требует: storageNote() обновляет его сам
      undoTrain(id);
      updateWeekCount(id);
      break;
    }

    case 'raise-edit': ui.raiseEdit[id] = true; renderReview(); break;
    case 'raise-later':
      if (item) { resetRaiseCount(item); delete ui.raiseEdit[id]; motionLeave(b.closest('.card'), renderReview); }
      break;
    // понижение планки: шаг применяется немедленно и уходит в срез недели,
    // «Оставить» просто фиксирует решение — обе кнопки ставят якорь недели
    case 'lower-ok': {
      if (!item) break;
      const to = lowerSuggest(item.value);
      if (to === null) break;
      acceptLower(item, to);
      motionLeave(b.closest('.card'), renderReview);
      break;
    }

    case 'lower-keep':
      if (item) { keepBar(item); motionLeave(b.closest('.card'), renderReview); }
      break;

    case 'week-fold': // раскрытие свёртки делает сам details, здесь — только память
      ui.weekOpen = !weekFoldOpen(); // от состояния по умолчанию, а не от null
      break;

    case 'raise-ok': {
      if (!item) break;
      const input = el('raise-' + id);
      const v = input ? parsePositive(input.value) : raiseSuggest(item.value);
      // карточка остаётся — и говорит, почему (задача 26, п. 2.3): прежде
      // это был тихий no-op, и тап по «Принять» выглядел как промах
      if (v === null) { refuse(b, 'Планка не принята: нужно число больше нуля'); break; }
      acceptRaise(item, v);
      delete ui.raiseEdit[id];
      motionLeave(b.closest('.card'), renderReview); // карточка уходит, затем перерисовка
      break;
    }

    case 'param-step': if (applyParamStep(id)) motionLeave(b.closest('.card'), renderReview); break;
    case 'param-keep': if (keepParam(id)) motionLeave(b.closest('.card'), renderReview); break;

    case 'close-week':
      if (!ui.weekCloseConfirm) { ui.weekCloseConfirm = true; renderReview(); break; }
      ui.weekCloseConfirm = false;
      if (closeWeek()) ui.justClosed = true;
      renderReview();
      window.scrollTo(0, 0); // длинный экран разбора схлопывается — наверх
      break;

    case 'move-up':
    case 'move-down': {
      if (!moveItem(id, act === 'move-up' ? 'up' : 'down')) break;
      renderSettings();
      // вернуть фокус кнопке того же действия и пункта; на краю списка — парной
      const find = a => [...el('scr-settings').querySelectorAll(`[data-act="${a}"]`)].find(x => x.dataset.id === id);
      let btn = find(act);
      if (!btn || btn.disabled) btn = find(act === 'move-up' ? 'move-down' : 'move-up');
      if (btn && !btn.disabled) btn.focus();
      break;
    }

    case 'group-up':
    case 'group-down': {
      if (!moveGroup(b.dataset.name, act === 'group-up' ? 'up' : 'down')) break;
      renderSettings();
      // фокус на кнопке того же действия и той же группы; на краю — парной
      const find = a => [...el('scr-settings').querySelectorAll(`[data-act="${a}"]`)].find(x => x.dataset.name === b.dataset.name);
      let btn = find(act);
      if (!btn || btn.disabled) btn = find(act === 'group-up' ? 'group-down' : 'group-up');
      if (btn && !btn.disabled) btn.focus();
      break;
    }
    case 'group-open': {
      // тап по раскрытой строке её сворачивает — цель считается ДО того,
      // как settingsFormsClosed обнулит ui.groupRename
      const next = ui.groupRename === b.dataset.name ? null : b.dataset.name;
      openSettingsForm(() => { ui.groupRename = next; });
      break;
    }
    case 'group-cancel':
      dropOpenDraft();
      ui.groupRename = null; ui.blockDays = null; ui.removeConfirm = null;
      renderSettings();
      break;

    // Свёртка карточки — не форма и не точечный путь: на «Настройках»
    // точечных путей нет (п. 4.2). Фокус — той же кнопке шеврона
    case 'group-fold': {
      const nm = b.dataset.name;
      if (ui.blockFold[nm]) delete ui.blockFold[nm];
      else ui.blockFold[nm] = true;
      renderSettings();
      const back = [...el('scr-settings').querySelectorAll('[data-act="group-fold"]')].find(x => x.dataset.name === nm);
      if (back) back.focus();
      break;
    }

    // Отказ больше не молчит и не закрывает форму: пустое или занятое имя
    // прежде уносило правку целиком — форма закрывалась, введённое пропадало
    // (задача 26, пп. 2.3–2.4). Проверки и запись — одной доменной операцией
    // updateGroup («Расписание 1/3», п. 1.4): имя, подпись и дни пишутся
    // вместе или не пишутся вовсе, и отказ хранилища откатывает всё.
    case 'group-save': {
      const from = b.dataset.name;
      const r = updateGroup(from, {
        name: el('g-name') ? el('g-name').value : from,
        caption: el('g-cap') ? el('g-cap').value : undefined,
        mask: ui.blockDays !== null ? ui.blockDays : undefined
      });
      if (!r.ok) { refuse(b, groupRefusal(r, el('g-name') ? el('g-name').value : from)); break; }
      dropOpenDraft(); // сохранённое не должно вернуться черновиком
      // свёртка — по имени, и переименование уносит её с собой
      if (r.name !== from && ui.blockFold[from]) { ui.blockFold[r.name] = true; delete ui.blockFold[from]; }
      // и развёртка блока на «Сегодня» (задача Р2, п. 4) — тем же ключом
      if (r.name !== from && unfoldedToday(from)) { unfoldForget(from); unfoldToday(r.name); }
      // и черновики, привязанные к имени, — тоже (Р1/рецензия)
      if (r.name !== from) renameDrafts(from, r.name);
      ui.groupRename = null; ui.blockDays = null; ui.removeConfirm = null;
      flashWrite('group:' + r.name);
      keepInPlace(b, renderSettings);
      break;
    }
    // раскрыт один: «Добавить блок» закрывает открытую правку блока, как
    // «Добавить упражнение» закрывает правку упражнения (задача 27, п. 7.4)
    case 'group-add-open': openSettingsForm(() => { ui.groupAdd = true; }); break;
    case 'group-add-cancel': dropOpenDraft(); ui.groupAdd = false; ui.blockDays = null; renderSettings(); break;
    // Те же проверки, что у правки, включая «не останется ни одного дня».
    // Осиротевшее имя (есть в пунктах, блока нет) addGroup разрешает — дни
    // нового блока начинаются сегодня, прошлое не двигается, — но действия с
    // этим именем в новый блок ВОЙДУТ, и его дни лягут на их свои дни.
    // Прежде здесь стоял довод «действий у нового блока нет», верный только
    // для нового имени: «Добавить блок» в осиротевшее молча оставляло
    // действие без единого дня и писало «Сохранено» (Р1/рецензия)
    case 'group-add-save': {
      const inp = el('g-add');
      const nm = inp ? inp.value.trim() : '';
      const mask = ui.blockDays !== null ? ui.blockDays : WEEK_ALL;
      const byName = !nm ? GROUP_REFUSAL.empty
        : groupJoinRefusal(nm) || (findGroup(nm) ? GROUP_REFUSAL.taken : null);
      const zero = byName || maskDays(mask) === 0 ? [] : zeroDaysIn(nm, mask);
      const no = byName || (maskDays(mask) === 0 ? GROUP_REFUSAL.nodays : null)
        || (zero.length ? groupRefusal({ reason: 'zero', names: zero }) : null);
      // фокус — в название, только если отказ про имя: про дни правят дни
      if (no) { refuse(b, no); if (inp && byName) inp.focus(); break; }
      addGroup(nm, el('g-add-cap') ? el('g-add-cap').value : '', mask);
      dropOpenDraft();
      ui.groupAdd = false; ui.blockDays = null;
      flashWrite('group:' + nm); // подтверждение — у только что заведённой карточки
      keepInPlace(b, renderSettings);
      break;
    }

    // Копия блока — одним тапом: она ничего не стирает и убирается так же,
    // как любой блок. Отказ записи — строкой, форма цела (duplicateGroup уже
    // откатил массивы). Подтверждение — у шапки ИСТОЧНИКА, где стояла форма:
    // палец там, а копия встаёт сразу за ним и видна тут же
    case 'group-dup': {
      const src = b.dataset.name;
      const nm = duplicateGroup(src);
      if (nm === null) { refuse(b, 'Не скопировано: хранилище недоступно'); break; }
      // Копия берёт СОХРАНЁННЫЙ блок, а форма источника закрывается. Набранное
      // в ней — не отказ (это «Отмена») и не запись (это «Сохранить»), а
      // переход, как в соседнюю форму: снимок целиком, ДО закрытия
      // (openSettingsForm, п. 4.5). Прежде снимка не было: пропадало всё,
      // набранное после последней перерисовки, а черновик той перерисовки
      // оставался — и повторное открытие накатывало смесь (Р1/рецензия)
      snapshotOpenForm();
      settingsFormsClosed();
      flashOk('group:' + src, `Копия создана: «${nm}»`);
      keepInPlace(b, renderSettings);
      break;
    }

    // Копия блока в ДРУГОЙ режим (задача Р2) — те же правила формы, что у
    // «Дублировать блок»: одним тапом, форма источника закрывается снимком,
    // подтверждение — у шапки источника. Копии на экране не видно (карточки —
    // выбранного режима), поэтому строка называет режим, куда она легла.
    // null — не всегда отказ хранилища: режим мог уйти (кнопка устарела)
    case 'group-dup-to': {
      const src = b.dataset.name;
      const target = findMode(b.dataset.mode);
      const nm = duplicateGroupTo(src, b.dataset.mode);
      if (nm === null) {
        if (!lastSaveOk()) refuse(b, 'Не скопировано: хранилище недоступно');
        else renderSettings();
        break;
      }
      snapshotOpenForm();
      settingsFormsClosed();
      flashOk('group:' + src, `Копия создана в режиме «${target.name}»: «${nm}»`);
      keepInPlace(b, renderSettings);
      break;
    }

    /* ── Режимы (задача Р2) ───────────────────────────────────────
       Раскрытие списка — не форма: формы экрана оно не трогает. Закрытие
       списка закрывает формы, которые в нём стоят (снимком, как переход в
       соседнюю форму): скрытая открытая форма была бы призраком — ключ
       черновика указывал бы на то, чего владелец не видит. */
    case 'mode-list': {
      const open = !ui.modesOpen;
      if (!open && (ui.modeRename !== null || ui.modeAdd)) { snapshotOpenForm(); settingsFormsClosed(); }
      if (!open && String(ui.removeConfirm).startsWith('mode:')) ui.removeConfirm = null;
      ui.modesOpen = open;
      renderSettings();
      const head = el('scr-settings').querySelector('[data-act="mode-list"]');
      if (head) head.focus();
      break;
    }

    // Выбор — один тап, отрезок журнала с сегодняшнего дня (setActiveMode).
    // Карточки ниже становятся карточками другого режима, а «Настройки»
    // держали о прежнем формы, черновики, свёртки и короткие пути назад по
    // ИМЕНИ блока: одноимённый блок нового режима — другой блок, и всё это
    // снимается (resetSettingsView). Список закрывается, подтверждение —
    // под строкой режима, у которой он и открывался
    case 'mode-pick': {
      const r = setActiveMode(id);
      if (!r.ok) {
        if (r.reason === 'storage') refuse(b, 'Не выбрано: хранилище недоступно');
        else renderSettings(); // режим ушёл или исчез — кнопка устарела
        break;
      }
      resetSettingsView();
      ui.modesOpen = false;
      flashOk('modes', `Режим: ${findMode(id).name} — с сегодняшнего дня`);
      keepInPlace(b, renderSettings);
      break;
    }

    // «Переименовать» уступает место форме в строке режима — закрывает её
    // «Отмена», «Сохранить» или переход в соседнюю форму
    case 'mode-rename-open': openSettingsForm(() => { ui.modeRename = id; }); break;
    case 'mode-rename-cancel': dropOpenDraft(); ui.modeRename = null; renderSettings(); break;
    case 'mode-rename-save': {
      const inp = el('m-name');
      const r = renameMode(id, inp ? inp.value : '');
      if (!r.ok) {
        refuse(b, MODE_REFUSAL[r.reason] || MODE_REFUSAL.storage);
        if (inp && (r.reason === 'empty' || r.reason === 'taken')) inp.focus();
        break;
      }
      dropOpenDraft();
      ui.modeRename = null;
      flashWrite('mode:' + id); // запись проверена renameMode: здесь всегда «Сохранено»
      keepInPlace(b, renderSettings);
      break;
    }

    case 'mode-add-open': openSettingsForm(() => { ui.modeAdd = true; }); break;
    case 'mode-add-cancel': dropOpenDraft(); ui.modeAdd = false; renderSettings(); break;
    // Новый режим — не выбранным: выбор — отдельное решение. Подтверждение
    // у его строки в списке, то есть у пальца: строка встаёт над формой
    case 'mode-add-copy':
    case 'mode-add-empty': {
      const inp = el('m-add');
      const copy = act === 'mode-add-copy';
      const r = addMode(inp ? inp.value : '', copy ? { copyOf: activeMode() } : undefined);
      if (!r.ok) {
        refuse(b, MODE_REFUSAL[r.reason] || MODE_REFUSAL.storage);
        if (inp && (r.reason === 'empty' || r.reason === 'taken')) inp.focus();
        break;
      }
      dropOpenDraft();
      ui.modeAdd = false;
      const made = findMode(r.id).name;
      flashOk('mode:' + r.id, copy ? `Копия создана: «${made}»` : `Режим создан: «${made}»`);
      keepInPlace(b, renderSettings);
      break;
    }

    // Уход режима — вторым тапом, как уход блока; последствие ПОД кнопкой.
    // Строка уходит в конец списка к убранным, её «Вернуть» и есть короткий
    // путь назад — туда и фокус
    case 'mode-remove': {
      const key = 'mode:' + id;
      if (ui.removeConfirm !== key) { ui.removeConfirm = key; renderSettings(); break; }
      ui.removeConfirm = null;
      const r = removeMode(id);
      if (!r.ok) {
        if (r.reason === 'storage') refuse(b, 'Не убрано: хранилище недоступно');
        else renderSettings(); // выбран или уже убран — кнопка устарела
        break;
      }
      if (ui.modeRename === id) { dropOpenDraft(); ui.modeRename = null; } // форма ушла вместе со строкой
      const back = () => [...el('scr-settings').querySelectorAll('[data-act="mode-restore"]')].find(x => x.dataset.id === id) || null;
      keepInPlace(b, renderSettings, back);
      const u = back();
      if (u) u.focus();
      break;
    }
    case 'mode-restore': {
      const r = restoreMode(id);
      if (!r.ok) {
        if (r.reason === 'storage') refuse(b, 'Не возвращено: хранилище недоступно');
        else renderSettings();
        break;
      }
      flashWrite('mode:' + id);
      keepInPlace(b, renderSettings);
      break;
    }

    /* Уход блока — вторым тапом, как уход пункта (A.3.2): первый взводит и
       печатает последствие ПОД кнопкой, второй уводит блок вместе с его
       действиями и привычками и оставляет на месте карточки короткий путь
       назад. Отказ записи — строкой у той же кнопки, форма цела. */
    case 'group-remove': {
      const nm = b.dataset.name;
      const key = 'group:' + nm;
      if (ui.removeConfirm !== key) { ui.removeConfirm = key; renderSettings(); break; }
      ui.removeConfirm = null;
      if (!removeGroup(nm)) { refuse(b, 'Не убрано: хранилище недоступно'); break; }
      dropOpenDraft();               // форма ушла вместе с карточкой
      settingsFormsClosed();
      ui.goneGroup = nm;
      keepInPlace(b, renderSettings, goneNoteEl);
      const back = goneNoteEl();
      if (back) { const u = back.querySelector('.undo'); if (u) u.focus(); }
      break;
    }

    /* Возврат блока — обе дороги назад, короткая и из «Убранных». Якорь
       подтверждения ложится в карточку, поэтому она разворачивается ДО
       перерисовки (п. 4.2). Действие без единого дня возвращается «как
       блок» (restoreItemCore) — это решение за владельца, и оно
       называется строкой, как у возврата пункта. Признак считается ДО
       возврата, по записям, уходящим из «Убранных»: прежний отрезок, у
       которого уже есть пара-преемник, не вернётся, и называть его нельзя
       (Р1/ревью). */
    case 'group-restore': {
      const nm = b.dataset.name;
      const g = findGroup(nm);
      const asBlock = g ? restoreSetOf(g)
        .filter(it => !laterSegmentOf(it) && returnsWithoutDays(it))
        .map(it => it.name) : [];
      // false — не всегда отказ хранилища: блок мог уже вернуться (кнопка
      // устарела). Отказ называется только по факту записи — тем же
      // правилом, что у item-restore (задача Р2, п. 5); иначе перерисовка
      // снимает устаревшую строку
      if (!restoreGroup(nm)) {
        if (!lastSaveOk()) refuse(b, 'Не возвращено: хранилище недоступно');
        else renderSettings();
        break;
      }
      delete ui.blockFold[nm];
      if (asBlock.length) flashOk('group:' + nm, `${plural(asBlock.length, 'Вернулось', 'Вернулись', 'Вернулись')} с днями блока: ${asBlock.join(', ')} — свои дни в них не попадали`);
      else flashWrite('group:' + nm);
      keepInPlace(b, renderSettings);
      break;
    }

    // пресет дней в форме блока: правит только черновик маски, запись — по
    // «Сохранить», как у чипов. Фокус — тому же пресету (п. 4.2)
    case 'days-preset': {
      const mask = b.dataset.mask;
      if (!isMask(mask) || (ui.groupRename === null && !ui.groupAdd)) break;
      ui.blockDays = mask;
      renderSettings();
      const back = document.querySelector(`#scr-settings [data-act="days-preset"][data-mask="${mask}"]`);
      if (back) back.focus();
      break;
    }

    // Быстрое добавление. Карточка разворачивается ДО перерисовки: форма
    // и подтверждение лягут в её тело (п. 4.2)
    case 'quick-open': {
      const nm = b.dataset.name || '';
      delete ui.blockFold[nm];
      openSettingsForm(() => { ui.quickFor = nm; });
      break;
    }
    case 'quick-cancel': dropOpenDraft(); ui.quickFor = null; renderSettings(); break;
    // Отказы — строкой, форма открыта, текст в поле цел: перерисовки нет
    // (задача 26, п. 2.4). Успех — «Добавлено: N» на месте кнопки
    // «Добавить действия», то есть у пальца (п. 2.4)
    case 'quick-save': {
      const nm = b.dataset.name || '';
      const ta = el('q-lines');
      const lines = parseQuickLines(ta ? ta.value : '');
      if (!lines.length) { refuse(b, 'Ни одной строки'); break; }
      const made = addActions(nm, lines);
      if (!made.length) { refuse(b, 'Не добавлено: хранилище недоступно'); break; }
      dropOpenDraft();
      ui.quickFor = null;
      flashOk('quick:' + nm, `Добавлено: ${made.length}`);
      keepInPlace(b, renderSettings);
      break;
    }

    case 'edit-open':
      // поле «Блок» открывается на значении пункта: groupPick/groupNew
      // гасит settingsFormsClosed вместе с прочими формами экрана
      openSettingsForm(() => { ui.editingId = id; });
      break;
    case 'edit-cancel':
      dropOpenDraft();
      ui.editingId = null; ui.editNorm = null; ui.editDays = null; ui.groupPick = null; ui.groupNew = false;
      ui.editOwn = null; ui.editType = null;
      renderSettings();
      break;

    /* Режим дней действия в живом блоке (п. 2.5). «Свои дни» открывают чипы,
       предзаполненные днями блока: своя маска ∧ дни блока — ровно то, что
       действие делает сейчас. «Как блок» возвращает режим без чипов. Обе —
       только черновик; запись — по «Сохранить». Фокус после перерисовки —
       туда, где продолжится работа (п. 4.2): на первый доступный чип либо
       на кнопку, которой режим открывается снова. */
    case 'days-own':
    case 'days-inherit': {
      const it = store.items.find(x => x.id === ui.editingId);
      if (!it) break;
      const st = editDaysState(it);
      if (st.bm === null) break; // без живого блока режимов нет
      if (act === 'days-own') { ui.editOwn = true; ui.editDays = andMask(st.now, st.bm); }
      else { ui.editOwn = false; ui.editDays = null; }
      renderSettings();
      const back = act === 'days-own'
        ? document.querySelector('#scr-settings [data-form="edit"] [data-act="day-toggle"]:not([disabled])')
        : document.querySelector('#scr-settings [data-form="edit"] [data-act="days-own"]');
      if (back) back.focus();
      break;
    }

    /* Уход пункта — вторым тапом (A.3.2). Первый взводит и печатает
       последствие ПОД кнопкой, поэтому сама кнопка с места не двигается.
       Второй убирает и оставляет на месте строки короткий путь назад.

       Отказ записи говорит строкой у той же кнопки и формы не закрывает:
       removeItem уже откатил поле, и в памяти ровно то же, что на диске. */
    case 'item-remove':
    case 'ex-remove': {
      const kind = act === 'item-remove' ? 'item' : 'ex';
      const key = kind + ':' + id;
      if (ui.removeConfirm !== key) { ui.removeConfirm = key; renderSettings(); break; }
      ui.removeConfirm = null;
      if (!(kind === 'item' ? removeItem(id) : removeExercise(id))) {
        refuse(b, 'Не убрано: хранилище недоступно');
        break;
      }
      dropOpenDraft();               // форма ушла вместе со строкой
      settingsFormsClosed();
      ui.goneNote = id;
      // строка «убран» встаёт туда, где стояла кнопка: форма схлопывается,
      // и без этого точка нажатия уехала бы вверх (механика keepInPlace)
      keepInPlace(b, renderSettings, goneNoteEl);
      const back = goneNoteEl();
      if (back) { const u = back.querySelector('.undo'); if (u) u.focus(); }
      break;
    }

    /* Возврат. В тот же день — полная отмена, позже — новая запись
       (restoreItem/restoreExercise, инвариант 12). Обе дороги назад,
       короткая и длинная, ведут сюда. */
    case 'item-restore':
    case 'ex-restore': {
      const kind = act === 'item-restore' ? 'item' : 'ex';
      // действие без единого дня возвращается «как блок» (restoreItemCore):
      // смена маски — решение за владельца, и она называется строкой, как
      // зажатие нормы. Признак считается ДО возврата — по той записи, что
      // уходит из «Убранных»
      const asBlock = kind === 'item' && returnsWithoutDays(store.items.find(x => x.id === id));
      const made = kind === 'item' ? restoreItem(id) : restoreExercise(id);
      // null — не всегда отказ хранилища: запись могла уже вернуться или
      // получить пару-преемника (laterSegmentOf / laterExerciseOf), и тогда кнопка просто
      // устарела. Отказ хранилища называется только по факту записи
      // (Р1/ревью), иначе экран перерисовывается и устаревшая строка уходит
      if (!made && !lastSaveOk()) { refuse(b, 'Не возвращено: хранилище недоступно'); break; }
      if (!made) { renderSettings(); break; }
      // вернувшаяся строка встаёт в карточку своего блока — свёрнутой та
      // быть не должна, иначе подтверждение родится невидимым (п. 4.2).
      // Привычка встаёт в секцию «Привычки», не в карточку: её возврат
      // чужую свёртку не снимает (Р1/рецензия)
      if (kind === 'item' && made.area === 'min') delete ui.blockFold[groupNameOf(made)];
      if (asBlock) flashOk(kind + ':' + made.id, 'Вернулся с днями блока: свои дни в них не попадали');
      else flashWrite(kind + ':' + made.id); // подтверждение — у вернувшейся строки
      keepInPlace(b, renderSettings);
      break;
    }

    // выбор дня недели в открытой форме: правит только ЧЕРНОВИК маски,
    // запись — по «Сохранить», как у нормы. Перерисовка нужна: от маски
    // зависит потолок степпера нормы и его подпись.
    // Чип один на две формы («Расписание 1/3», п. 2.5): открыта форма блока
    // или добавления блока — правится черновик дней блока, иначе — пункта.
    // Одна форма за раз, поэтому спутать их нечем.
    // У действия чип правит выбор В ПРЕДЕЛАХ дней блока-кандидата: день вне
    // них недоступен и в разметке, и здесь — тап по нему (или по чипу режима
    // «как блок», которого на экране нет) ничего не меняет
    case 'day-toggle': {
      const i = +b.dataset.day;
      const flip = m => { const x = m.split(''); x[i] = x[i] === '1' ? '0' : '1'; return x.join(''); };
      if (ui.groupRename !== null || ui.groupAdd) {
        const g = ui.groupAdd ? null : findGroup(ui.groupRename);
        ui.blockDays = flip(ui.blockDays !== null ? ui.blockDays : (g ? blockMaskNow(g) : WEEK_ALL));
      } else {
        const item2 = store.items.find(x => x.id === ui.editingId);
        if (!item2) break;
        if (item2.area === 'min') {
          const st = editDaysState(item2);
          if (st.bm !== null && (st.inherit || st.bm[i] !== '1')) break;
          ui.editDays = flip(st.pick);
        } else {
          ui.editDays = flip(ui.editDays !== null ? ui.editDays : scheduleNow(item2));
        }
      }
      renderSettings();
      // фокус остаётся на той же кнопке: перерисовка её пересоздала
      const back = document.querySelector(`#scr-settings [data-act="day-toggle"][data-day="${i}"]`);
      if (back) back.focus();
      break;
    }

    case 'norm-dec':
    case 'norm-inc': {
      if (!item) break;
      const capNorm = maskDays(ui.editDays !== null ? ui.editDays : scheduleNow(item));
      const cur = Math.min(capNorm, ui.editNorm !== null ? ui.editNorm : (item.normPerWeek || 7));
      const next = Math.max(1, Math.min(capNorm, cur + (act === 'norm-inc' ? 1 : -1)));
      if (next === cur) break;
      ui.editNorm = next;
      renderSettings();
      // вернуть фокус кнопке того же действия; на границе — парной (как у «выше/ниже»)
      const find = a => [...el('scr-settings').querySelectorAll(`[data-act="${a}"]`)].find(x => x.dataset.id === id);
      let btn = find(act);
      if (!btn || btn.disabled) btn = find(act === 'norm-inc' ? 'norm-dec' : 'norm-inc');
      if (btn && !btn.disabled) btn.focus();
      break;
    }
    case 'thr-dec':
    case 'thr-inc': {
      const cur = dayThreshold();
      const next = clampThreshold(cur + (act === 'thr-inc' ? THRESHOLD_STEP : -THRESHOLD_STEP));
      if (next === cur) break;
      store.settings.dayThreshold = next;
      save();
      renderSettings();
      // фокус — той же кнопке; на границе диапазона парной (как у нормы недели)
      let btn = el('scr-settings').querySelector(`[data-act="${act}"]`);
      if (!btn || btn.disabled) {
        btn = el('scr-settings').querySelector(`[data-act="${act === 'thr-inc' ? 'thr-dec' : 'thr-inc'}"]`);
      }
      if (btn && !btn.disabled) btn.focus();
      break;
    }

    /* Форма правки пункта. Порядок жёсткий: сначала ВСЕ проверки, потом
       запись. Прежде поля писались по ходу, невалидное число молча
       оставляло старое значение, а форма всё равно закрывалась с
       «Сохранено» — приложение говорило «сохранено» о том, что выбросило
       (задача 26, п. 2.2). Теперь первый же непринятый ввод — отказ:
       форма остаётся открытой, введённое не переписано, в store не
       записано ничего (п. 2.4). Сообщается первый отказ: строка короткая,
       и владелец правит по одному полю. */
    case 'edit-save': {
      if (!item) break;
      // Тип — только в день заведения, и условие ПЕРЕПРОВЕРЯЕТСЯ здесь, первым
      // делом: день мог смениться при открытой форме (п. 2.5). Не выполнено —
      // черновик типа не значит ничего и снимается, чем бы ни кончилось
      // сохранение; форма к этому моменту уже перерисована по типу пункта
      // (syncDay), и поля сходятся с ним
      if (ui.editType !== null && !canChangeType(item)) ui.editType = null;
      const name = el('e-name').value.trim();
      if (!name) { refuse(b, 'Название не заполнено'); break; }
      const type = editTypeOf(item);
      if (type !== item.type) {
        const no = typeChangeRefusal(item, type);
        if (no) { refuse(b, no); break; }
      }
      let pv = null, pstep = null, value, goal = null;
      if (item.type === 'param') {
        // pkind фиксирован при создании — правятся только порог, единица и шаг
        if (item.pkind === 'number') {
          const n = parseNum(el('e-pvalue') ? el('e-pvalue').value : '');
          if (n === null) { refuse(b, 'Порог не принят: нужно число'); break; }
          pv = n;
        } else {
          const m = /^(\d{1,2}):(\d{2})$/.exec((el('e-ptime') ? el('e-ptime').value : '') || '');
          if (!m) { refuse(b, 'Порог не принят: нужно время вида 23:30'); break; }
          pv = Math.min(23, +m[1]) * 60 + Math.min(59, +m[2]);
        }
        const st = parseNum(el('e-pstep') ? el('e-pstep').value : '');
        if (st === null) { refuse(b, 'Шаг не принят: нужно число со знаком'); break; }
        pstep = Math.round(st);
      } else if (item.area !== 'habit') {
        const rawValue = el('e-value').value;
        if (!String(rawValue).trim()) {
          value = null; // осознанная очистка: пункт остаётся чекбоксом без числа, история не трогается
        } else {
          value = parsePositive(rawValue);
          if (value === null) { refuse(b, 'Значение не принято: нужно число больше нуля'); break; }
        }
        if (type === 'weekly') {
          const g = parsePositive(el('e-goal') ? el('e-goal').value : null);
          if (g === null || Math.round(g) < 1) { refuse(b, 'Цель не принята: нужно целое число от 1'); break; }
          goal = Math.round(g);
        }
      }
      // время — подпись, но невалидное не сохраняется молча (B.3.1)
      const rawAt = el('e-at') ? el('e-at').value.trim() : '';
      if (rawAt && !TIME_RE.test(rawAt)) { refuse(b, 'Время не принято: нужно вида 23:30'); break; }
      // в убранный блок пункт не входит: отказ — до записи (п. 1.4). Раньше
      // проверки дней: итоговый блок решает, останутся ли у действия дни
      const pick = readGroupField('e', item.group);
      if (pick.refusal) { refuse(b, pick.refusal); break; }
      // пустая маска — не расписание: пункт, не существующий ни в один день
      // недели, неотличим от убранного, а убирать умеет «Убрать». У привычки
      // маска своя (29/B); у действия дни считаются по итоговому блоку
      let mask = null; // новая своя маска — только если дни трогали
      if (item.type === 'daily' && item.area === 'habit') {
        if (ui.editDays !== null && maskDays(ui.editDays) === 0) { refuse(b, 'Нужен хотя бы один день недели'); break; }
        mask = ui.editDays;
      } else if (type === 'daily' && item.area === 'min') {
        const plan = editDaysOutcome(item, editDaysState(item), pick.name, name);
        if (plan.refusal) { refuse(b, plan.refusal); break; }
        mask = plan.mask;
      }

      // проверки пройдены — запись
      item.name = name;
      item.note = el('e-note').value.trim();
      item.at = rawAt;
      // тип — первым: от него зависят цель, расписание и журнал блока ниже
      if (type !== item.type) setItemType(item, type, goal);
      if (item.type === 'param') {
        if (item.pkind === 'number' && el('e-punit')) item.unit = el('e-punit').value.trim();
        if (pv !== item.pvalue) { item.pvalue = pv; recordBar(item, pv); } // история — по общим правилам
        item.pstep = pstep;
      } else if (item.area !== 'habit') {
        if (value === null) item.value = null;
        else if (value !== item.value) { item.value = value; recordBar(item, value); }
        item.unit = el('e-unit').value.trim();
        if (item.type === 'weekly') item.goal = goal;
      } else if (ui.editNorm !== null) {
        item.normPerWeek = Math.max(1, Math.min(7, ui.editNorm)); // ежедневная привычка: норма недели
      }
      // расписание пишется ОТРЕЗКОМ с сегодняшнего дня; прежние не трогаются.
      // Зажатие нормы — это смена нормы со всеми её последствиями (серия
      // недель пересчитывается ретроактивно), поэтому оно НАЗЫВАЕТСЯ вслух,
      // а не происходит молча (B.2.4).
      // Своя маска действия хранится КАК ЕСТЬ (п. 2.5): форма без касания
      // дней отрезка не пишет, а касание, вернувшее прежнюю маску, — тоже
      let clamped = null;
      if (item.type === 'daily' && mask !== null && mask !== scheduleNow(item)) {
        const wasNorm = item.normPerWeek || 7;
        setSchedule(item, mask); // внутри — clampNorm и save
        if (item.area === 'habit' && item.normPerWeek !== wasNorm) clamped = wasNorm;
      }
      // блок — поле всех форм, а не только минимума (задача 19, B.3): блоки
      // работали на «Привычках», но назначить их привычке и параметру было
      // нечем. Без поля в форме readGroupField возвращает прежнее значение.
      // Смена блока у действия пишет журнал принадлежности («Расписание
      // 1/3»): дни блока ограничивают действие, и перенос в будний блок
      // не должен задним числом выбросить пункт из прошлых выходных.
      if (setItemGroup(item, commitGroupField(pick)) && item.area === 'min') {
        // строка переехала в карточку другого блока: подтверждение ляжет
        // туда, и свёрнутой карточка быть не должна (п. 4.2). Только у
        // действия: привычка стоит в секции «Привычки», её подтверждение в
        // карточку «Расписания» не ложится, а свёртка — выбор владельца,
        // и снимать её без повода нельзя (Р1/рецензия)
        delete ui.blockFold[groupNameOf(item)];
      }
      save();
      dropOpenDraft(); // сохранённое не должно вернуться черновиком (п. 4.5)
      ui.editingId = null;
      ui.editNorm = null;
      ui.groupPick = null;
      ui.groupNew = false;
      ui.editDays = null;
      ui.editOwn = null;
      ui.editType = null;
      flashWrite('item:' + id); // тихое подтверждение у той же строки (задача 26)
      if (clamped !== null) {
        // не тихое «Сохранено», а сказанное последствие: норма изменилась
        flashOk('item:' + id, `Норма ${clamped} → ${item.normPerWeek}: столько дней в расписании`);
      }
      keepInPlace(b, renderSettings);
      break;
    }

    // Форма добавления осталась только у привычек и параметров («Расписание
    // 1/3», п. 2.4): действия заводятся быстрым добавлением в своём блоке
    case 'add-open': {
      // Подсказка «одна новая привычка за раз» видима все 14 дней после
      // добавления пункта — но считается только по добавленному владельцем
      const newest = ownerNewestItem();
      const hint = !!(newest && diffDays(todayKey(), newest.addedAt) < 14);
      openSettingsForm(() => {
        ui.addOpen = true; ui.addType = 'daily'; ui.addPkind = 'time';
        ui.addHint = hint;
      });
      break;
    }
    case 'add-cancel':
      dropOpenDraft();
      ui.addOpen = false; ui.addHint = false; ui.groupPick = null; ui.groupNew = false;
      renderSettings();
      break;
    /* Форма добавления — тот же порядок, что у правки: сначала проверки,
       потом создание (задача 26, пп. 2.2–2.5). Прежде пустое название
       молча ставило фокус в поле, а непринятое число подставляло
       умолчание — и пункт заводился не таким, каким его набрали. */
    case 'add-save': {
      const name = el('f-name').value.trim();
      if (!name) { refuse(b, 'Название не заполнено'); el('f-name').focus(); break; }
      const note = el('f-note').value.trim();
      let item;
      if (ui.addType === 'param') {
        const pkind = el('f-pkind') && el('f-pkind').value === 'number' ? 'number' : 'time';
        let pvalue;
        if (pkind === 'time') {
          const m = /^(\d{1,2}):(\d{2})$/.exec((el('f-ptime') ? el('f-ptime').value : '') || '');
          if (!m) { refuse(b, 'Порог не принят: нужно время вида 23:30'); break; }
          pvalue = Math.min(23, +m[1]) * 60 + Math.min(59, +m[2]);
        } else {
          const n = parseNum(el('f-pvalue') ? el('f-pvalue').value : '');
          if (n === null) { refuse(b, 'Порог не принят: нужно число'); break; }
          pvalue = n;
        }
        const st = parseNum(el('f-pstep') ? el('f-pstep').value : '');
        if (st === null) { refuse(b, 'Шаг не принят: нужно число со знаком'); break; }
        item = {
          id: uid(), name, value: null,
          unit: pkind === 'number' && el('f-punit') ? el('f-punit').value.trim() : '',
          note, group: '',
          type: 'param', area: 'habit', pkind, pvalue,
          pstep: Math.round(st),
          goal: null, removedAt: null, addedAt: todayKey(), at: '', raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null,
          history: [{ date: todayKey(), value: pvalue }]
        };
      } else {
        item = {
          id: uid(), name, value: null, unit: '', note, group: '',
          type: 'daily', area: 'habit', normPerWeek: 7, // каноническая форма привычки (инвариант 11)
          schedule: [{ from: todayKey(), mask: WEEK_ALL }], at: '',
          goal: null, removedAt: null, addedAt: todayKey(), raiseAfter: 0, raiseAfterWeek: null, lowerAfterWeek: null, history: []
        };
      }
      // войти в убранный блок новая привычка не может: отказ — до записи,
      // вместе с прочими (groupJoinRefusal, «Расписание 1/3», п. 1.4)
      const pick = readGroupField('f', '');
      if (pick.refusal) { refuse(b, pick.refusal); break; }
      item.group = commitGroupField(pick);
      store.items.push(item);
      save();
      dropOpenDraft();
      ui.addOpen = false;
      ui.groupPick = null;
      ui.groupNew = false;
      flashWrite('item:' + item.id); // подтверждение у только что появившейся строки
      keepInPlace(b, renderSettings);
      break;
    }

    case 'export':
      exportJSON();
      renderSettings(); // обновить строку «Последний экспорт» (и погасить строку импорта)
      break;

    // ── обновление приложения (задача Р3) ─────────────────────
    // Обе — точечно: результат приходит асинхронно, и узел role="status"
    // обязан пережить его, чтобы скринридер объявил изменение текста
    case 'update-check': manualUpdateCheck(); break;
    case 'update-apply': applyUpdate(); break;

    // ── чистка и её отмена (задача 16.1) ──────────────────────
    case 'wipe-open': ui.wipeOpen = true; ui.wipeConfirm = false; renderSettings(); break;
    case 'wipe-cancel': ui.wipeOpen = false; ui.wipeConfirm = false; renderSettings(); break;

    case 'wipe-do': {
      if (!ui.wipeConfirm) { ui.wipeConfirm = true; renderSettings(); break; } // второй тап
      // копию некуда положить — чистка не выполнена; молчать об этом нельзя:
      // владелец видел бы, что «Стереть» ничего не сделало (аудит, находка 20)
      if (!wipeAll()) { ui.wipeConfirm = false; ui.wipeFailed = true; renderSettings(); break; }
      ui.wipeFailed = false;
      // экран после чистки — «Сегодня» с пустым списком; ui-состояние,
      // указывавшее на исчезнувшие записи, снимается целиком
      ui.wipeOpen = false;
      ui.wipeConfirm = false;
      ui.wipeDropConfirm = false;
      resetSettingsView(); // формы, черновики, свёртка и короткие пути — к прежним данным
      ui.missOpen = {};
      ui.raiseEdit = {};
      closeReview();
      closeTrain();
      ui.tab = 'today';
      renderAll();
      break;
    }

    case 'wipe-undo':
      // возврат отказывает СТРОКОЙ, как чистка (задача 27, Д7): прежде
      // ветки else не было вовсе, и владелец видел кнопку, которая ничего
      // не сделала, — при том что в самой чистке про это написано прямо
      if (!restoreWiped()) { ui.restoreFailed = true; renderSettings(); break; }
      ui.restoreFailed = false;
      // взведённые подтверждения относились к ПРЕЖНИМ данным: «Подтвердить:
      // стереть» переживало возврат, и один тап уничтожал обмен, ради
      // которого копия и заводится (задача 27, Д7). Список один — resetConfirms
      ui.wipeOpen = false;
      resetConfirms();
      resetSettingsView(); // и открытые формы — тоже к прежним данным (п. 4.5)
      // копия могла нести другую границу дня — день и таймер заново,
      // тем же путём, что импорт (задача 27.1, п. 5.5)
      ui.renderedDayKey = todayKey();
      armDayTimer();
      ui.tab = 'settings';
      renderAll(); // вернулось всё сразу, включая дневные экраны
      break;

    case 'wipe-drop':
      if (!ui.wipeDropConfirm) { ui.wipeDropConfirm = true; renderSettings(); break; }
      dropWiped();
      ui.wipeDropConfirm = false;
      renderSettings();
      break;

    // ── нечитаемые данные (задача 25, п. 6; источников два — 28.A) ──
    case 'corrupt-save': {
      const src = b.dataset.src || 'data';
      const c = corruptCopy(src);
      if (c) download(CORRUPT_SRC[src].file + todayKey() + '.json', c.raw);
      break;
    }

    case 'corrupt-drop': {
      const src = b.dataset.src || 'data';
      if (ui.corruptDropConfirm !== src) { ui.corruptDropConfirm = src; renderSettings(); break; }
      dropCorrupt(src);
      // у зеркала «Убрать» снимает и сам нечитаемый снапшот: иначе каждый
      // следующий старт упирался бы в него и копия не велась бы никогда
      if (src === 'mirror') mirrorClear();
      ui.corruptDropConfirm = null;
      renderSettings();
      break;
    }

    // ── предложение восстановления из зеркала (задача 28.A, п. 2.2) ──
    case 'mirror-restore':
      if (!ui.mirrorRestoreConfirm) { ui.mirrorRestoreConfirm = true; renderSettings(); break; }
      ui.mirrorRestoreConfirm = false;
      if (!restoreMirror()) { ui.mirrorFailed = true; renderSettings(); break; }
      ui.mirrorFailed = false;
      // дальше — тот же хвост, что у «Вернуть» (задача 27, Д7; 27.1, п. 5.5):
      // состояние подменено целиком, взведённое и открытое к нему не относится,
      // а копия могла нести другую границу дня
      ui.wipeOpen = false;
      resetConfirms();
      resetSettingsView();
      ui.renderedDayKey = todayKey();
      armDayTimer();
      renderAll();
      break;

    case 'mirror-save':
      if (mirrorOffer) download('minimum-копия-' + todayKey() + '.json', JSON.stringify(mirrorOffer.store, null, 1));
      break;

    case 'mirror-keep':
      if (!ui.mirrorKeepConfirm) { ui.mirrorKeepConfirm = true; renderSettings(); break; }
      ui.mirrorKeepConfirm = false;
      keepWorking();
      renderSettings();
      break;

    case 'import':
      if (hadImportNote) renderSettings(); // до открытия диалога: file-input должен остаться в живом DOM
      el('import-file').click();
      break;
  }
}

function onChange(e) {
  const t = e.target;
  if (syncDay()) return; // stale-экран: действие не применяется (инвариант 8)
  ui.importNote = null;
  const act = t.dataset.act;
  if (act === 'mark') {
    // отказ хранилища больше не меняет разметку экрана: баннер — постоянный
    // узел вне экранов (задача 27.1, п. 5.2), и горячий путь остаётся горячим
    toggleMark(todayKey(), t.dataset.id);
    updateTodayMark(t);
  // Здесь стояли тумблеры пункта и упражнения (data-act toggle-active и
  // ex-active) с точечным обновлением класса .off и подписи зачёта дня.
  // Поле active упразднено вместе с ними (задача 28.E/A, п. 2): уход из
  // виду — теперь «Убрать» в форме правки, и он всегда полная перерисовка.
  } else if (act === 'boundary') {
    store.settings.dayBoundary = Number(t.value) || 0;
    save();
    armDayTimer(); // граница сместилась — таймер на новую; экран «Настроек» от неё не зависит, select держит фокус
    if (todayKey() !== ui.renderedDayKey) {
      // новая граница сдвинула логический день прямо сейчас — фиксируем без
      // перерисовки, иначе guard молча проглотит следующее действие
      ui.missOpen = {};
      ui.raiseEdit = {};
      ui.renderedDayKey = todayKey();
    }
  } else if (act === 'since') {
    // начало отсчёта: приводится к понедельнику своей недели. Дата вперёд
    // законна (задача 22, п. 8): пустая эпоха — обычное состояние приложения,
    // в неё же кладёт чистка, и владелец вправе назначить старт сам.
    const v = t.value;
    if (isDayKey(v)) {
      store.settings.calendarSince = weekStartOf(v);
      save();
    }
    t.value = store.settings.calendarSince; // поле показывает принятое значение
  } else if (act === 'group-pick') {
    // «+ Новый блок…» — последний вариант: раскрывает поле имени в форме
    ui.groupNew = isNewGroupPick(t);
    if (!ui.groupNew) ui.groupPick = t.value;
    renderSettings();
  } else if (act === 'edit-type') {
    // тип действия в день заведения (п. 2.5): только черновик, запись — по
    // «Сохранить». Цель, набранная у счётчика, переживает смену туда и
    // обратно — её держит черновик полей, как держал в прежней форме добавления
    ui.editType = t.value === 'weekly' ? 'weekly' : 'daily';
    renderSettings();
  } else if (act === 'add-type') {
    ui.addType = t.value;
    renderSettings(); // снимок/восстановление формы — внутри renderSettings, цель не сбрасывается
  } else if (act === 'add-pkind') {
    ui.addPkind = t.value === 'number' ? 'number' : 'time';
    renderSettings();
  } else if (t.id === 'import-file') {
    if (t.files && t.files[0]) importJSON(t.files[0]);
    t.value = '';
  }
}

function onInput(e) {
  if (e.target.dataset.bind === 'one-change') {
    store.draftOneChange = e.target.value;
    save();
  }
}

/* ── Обновление приложения (задача Р3) ───────────────────────
   Новая версия ПРЕДЛАГАЕТСЯ, а не применяется. До Р3 обновление было
   тихим: воркер вытеснял старый сразу после установки (skipWaiting в
   install), и новая версия доезжала при следующем запуске — или не
   доезжала, пока iOS держал приложение в памяти. Теперь установленный
   воркер ждёт, над таб-баром встаёт полоса «Доступна версия vN —
   Обновить», и перезагружается страница только по тапу владельца.

   Инвариант: приложение НЕ ПЕРЕЗАГРУЖАЕТ СЕБЯ САМО. Перезагрузку зовёт
   ровно одно место — controllerchange при взведённом ui.updateArmed (и
   тап по «Обновить», когда воркер уже активирован другой вкладкой).
   Без взвода смена контроллера не делает ничего: чужой claim — другой
   вкладки, первой установки — не повод рвать владельцу форму под
   пальцем, а перезагрузка по любому controllerchange при двух вкладках
   давала бы петлю.

   Проверка — registration.update(): при запуске, при возвращении из фона
   (не чаще UPDATE_CHECK_MS — метка в памяти, не в store: это не данные
   владельца) и по «Проверить обновления» в «Системе» (без троттлинга —
   владелец спросил сам). Отказ сети у автопроверки молчит: офлайн —
   обычное состояние приложения, а не событие. У ручной — говорит
   строкой: владелец ждёт ответа.

   Номер версии знает только воркер (sw.js, VERSION): страница спрашивает
   его сообщением {type: 'version'} через MessageChannel. Не ответил за
   VERSION_ASK_MS — номера нет, и строки говорят без него («Доступна
   новая версия», «версия не определена»). Воркер v48 протокола не знает
   вовсе, поэтому на переходе v48 → v49 ответа не будет — это ожидаемо. */

const UPDATE_CHECK_MS = timing('UPDATE_CHECK_MS', 600000); // автопроверка — не чаще раза в 10 минут
const VERSION_ASK_MS = timing('VERSION_ASK_MS', 1000);     // сколько ждать номер у воркера

/* Перезагрузка — через хук, подменяемый тестом ДО загрузки app.js
   объектом globalThis.MINIMUM_RELOAD (по образцу timing()): проверять
   нужно именно то, КОГДА она зовётся, а jsdom location.reload не умеет.
   Читается один раз при загрузке; в рантайме хука нет. */
const RELOAD_HOOK = (typeof globalThis !== 'undefined' && typeof globalThis.MINIMUM_RELOAD === 'function')
  ? globalThis.MINIMUM_RELOAD : null;

function pageReload() {
  if (RELOAD_HOOK) RELOAD_HOOK();
  else location.reload();
}

/* «minimum-v48» → «v48». Формат — тот же, что у замка версии
   (tools/deploy-hash.mjs, versionNumber); иное — null, и строка говорит
   без номера, а не печатает владельцу мусор из чужого ответа. */
function versionLabel(v) {
  const m = /^minimum-(v\d+)$/.exec(typeof v === 'string' ? v : '');
  return m ? m[1] : null;
}

/* Пора ли автопроверке: первой ещё не было — пора; прошло меньше
   UPDATE_CHECK_MS — нет. Часы, ушедшие НАЗАД (now < last), и нечисловое
   время проверку разрешают: метка из «будущего» иначе запирала бы
   автопроверки на весь сдвиг часов, а лишний registration.update()
   ничего не стоит. Чистая функция: время передаётся снаружи. */
function updateCheckDue(last, now) {
  if (typeof last !== 'number' || !isFinite(last)) return true;
  const d = now - last;
  return !(d >= 0 && d < UPDATE_CHECK_MS);
}

let swRegReady = null;    // Promise регистрации (null — service worker недоступен)
let updateWaiting = null; // ожидающий воркер, которому сделано предложение
let updateOffer = null;   // Promise показа предложения этому воркеру (номер спрошен)
let updateVersion = null; // его номер ('v49') или null — не ответил
let updateShown = false;  // номер получен или истёк таймаут — строка на экране
let activeVersion;        // номер активного воркера: undefined — ждём, null — не определён
let lastAutoCheck = null; // метка последней АВТОпроверки — память страницы, не store
let reloading = false;    // перезагрузка уже позвана: второй controllerchange её не повторит
let updateReg = null;     // регистрация: у неё dropOffer ищет преемника вытесненного воркера
let updateHold = null;    // удержанная полоса {worker: преемник, version: прежний номер} (Р4/рецензия)

function swApi() {
  return (typeof navigator !== 'undefined' && navigator.serviceWorker) || null;
}

/* Номер у воркера: {type: 'version'} и порт MessageChannel для ответа.
   Разрешается всегда — номером или null (таймаут, нет MessageChannel,
   воркер недоступен). Порт закрывается в любом исходе: открытый порт
   держал бы страницу, а в тестах — процесс. */
function askVersion(target) {
  return new Promise(resolve => {
    let done = false, port = null;
    const finish = v => {
      if (done) return;
      done = true;
      clearTimeout(timer);
      try { if (port) port.close(); } catch {}
      resolve(versionLabel(v));
    };
    const timer = setTimeout(() => finish(null), VERSION_ASK_MS);
    try {
      const ch = new MessageChannel();
      port = ch.port1;
      port.onmessage = ev => finish(ev && ev.data && ev.data.version);
      target.postMessage({ type: 'version' }, [ch.port2]);
    } catch { finish(null); }
  });
}

/* Предложение ожидающему воркеру — только странице под живым
   контроллером. Без него это первая установка: активировавшись, воркер
   и так станет контроллером, обновлять нечего. Строка встаёт ОДИН раз —
   когда номер получен или таймаут истёк: «новая версия», через секунду
   переписанная в «v49», объявлялась бы скринридером дважды. */
function offerUpdate(worker) {
  const sw = swApi();
  if (!worker || !sw || !sw.controller) return Promise.resolve();
  if (updateWaiting === worker) return updateOffer;
  updateWaiting = worker;
  updateVersion = null;
  updateShown = false;
  // воркер, вытесненный более новым, становится лишним — предлагать его нельзя
  if (typeof worker.addEventListener === 'function') {
    worker.addEventListener('statechange', () => { if (worker.state === 'redundant') dropOffer(worker); });
  }
  updateOffer = askVersion(worker).then(v => {
    if (updateWaiting !== worker) return;
    updateVersion = v;
    updateShown = true;
    updateHold = null; // предложение преемника сменяет удержанную полосу на месте
    updateNote();
  });
  return updateOffer;
}

/* Преемник вытесненного ожидающего (Р4/рецензия): воркер регистрации —
   устанавливаемый или уже ожидающий, — не сам вытесненный и не лишний.
   По спецификации ожидающий становится redundant, когда более новый
   УЖЕ установился: statechange вытесненного приходит раньше, чем
   регистрация переписала installing/waiting и новый стал installed.
   Чистая функция: регистрация передаётся снаружи. */
function offerSuccessor(reg, dropped) {
  if (!reg) return null;
  for (const w of [reg.installing, reg.waiting]) {
    if (w && w !== dropped && w.state !== 'redundant') return w;
  }
  return null;
}

/* Предложение снято: воркер вытеснен или postMessage к нему бросил.

   Полоса на экране, и у регистрации есть преемник, — полоса УДЕРЖИВАЕТСЯ
   (Р4/рецензия): тот же узел, тот же текст, тот же отступ has-update, пока
   предложение преемника не перепишет текст на месте. Прежде вытеснение
   прятало полосу и снимало отступ, а через задачу и обмен сообщениями с
   воркером предложение преемника возвращало их: у нижнего края прокрутки
   документ на это время укорачивался на 56px, прокрутка прижималась, и
   экран уезжал вниз (замер рецензии в Chromium: 585 → 641px), а
   вернувшаяся полоса проигрывала появление заново и закрывала последнюю
   строку. Снимается полоса только окончательным исходом: установка
   преемника сорвалась, предложить его некому (нет контроллера) или
   преемника нет вовсе. Удержанная «Обновить» не делает ничего, пока
   предложение преемника не встало: взвод принадлежал вытесненному. */
function dropOffer(worker) {
  if (updateWaiting !== worker) return;
  const onScreen = updateShown ? { version: updateVersion } : updateHold;
  const next = onScreen ? offerSuccessor(updateReg, worker) : null;
  updateWaiting = null;
  updateOffer = null;
  updateVersion = null;
  updateShown = false;
  ui.updateArmed = false; // взвод принадлежал этому воркеру
  const hold = updateHold = next ? { worker: next, version: onScreen.version } : null;
  if (hold) {
    installSettled(next)
      .then(state => (state === 'installed' ? offerUpdate(next) : null))
      .then(() => { if (updateHold === hold) { updateHold = null; updateNote(); } });
  }
  updateNote();
}

/* Что стоит на полосе: предложение ожидающему с полученным номером — или
   удержанная полоса вытесненного (dropOffer). Одна функция на #update-note
   и строку результата «Системы»: рендер и точечный путь печатают одно. */
function offerOnScreen() {
  if (updateWaiting && updateShown) return { version: updateVersion };
  return updateHold;
}

/* Установка завершилась так или иначе: installed, activating, activated
   или redundant. registration.update() разрешается, когда установка лишь
   НАЧАТА, поэтому ручной проверке и updatefound нужен этот исход.
   Разрешается состоянием воркера в миг завершения (задача Р4, п. 4):
   'redundant' — установка сорвалась, не дойдя до installed. */
function installSettled(w) {
  return new Promise(resolve => {
    const settled = () => w.state !== 'installing';
    if (settled()) { resolve(w.state); return; }
    const on = () => {
      if (!settled()) return;
      w.removeEventListener('statechange', on);
      resolve(w.state);
    };
    w.addEventListener('statechange', on);
  });
}

function watchInstalling(w) {
  if (!w) return;
  installSettled(w).then(() => { if (w.state === 'installed') offerUpdate(w); });
}

/* Проверка: update(), затем установка, если она началась, затем
   ожидающий — предложение. Отказ (нет сети, нет регистрации) — reject.

   Исход (задача Р4, п. 4): 'lost' — новая версия найдена, но её воркер
   стал redundant, не дойдя до installed (оборвалась закачка файлов в
   установке); иначе 'done'. До Р4 такой исход читался «Это последняя
   версия» — неправдой: версия есть, не загрузилась. Воркер берётся и из
   updatefound, пришедшего ВО ВРЕМЯ update(): сорвись установка раньше, чем
   update() разрешится, reg.installing был бы уже пуст, и найденное снова
   читалось бы последней версией. Исход читает только ручная проверка;
   автопроверка его отбрасывает — молчит (checkForUpdate). */
async function runUpdateCheck() {
  const reg = await swRegReady;
  if (!reg || typeof reg.update !== 'function') throw new Error('нет регистрации');
  let found = null;
  const onFound = () => { if (reg.installing) found = reg.installing; };
  const listen = typeof reg.addEventListener === 'function';
  if (listen) reg.addEventListener('updatefound', onFound);
  try {
    await reg.update();
  } finally {
    if (listen && typeof reg.removeEventListener === 'function') reg.removeEventListener('updatefound', onFound);
  }
  const w = reg.installing || found;
  const state = w ? await installSettled(w) : null;
  if (reg.waiting) await offerUpdate(reg.waiting);
  return state === 'redundant' ? 'lost' : 'done';
}

/* Автопроверка: троттлинг и молчание при отказе */
function checkForUpdate(manual) {
  if (!swRegReady) return Promise.resolve();
  if (manual) return manualUpdateCheck();
  const now = Date.now();
  if (!updateCheckDue(lastAutoCheck, now)) return Promise.resolve();
  lastAutoCheck = now;
  return runUpdateCheck().catch(() => {});
}

/* «Проверить обновления»: кнопка неактивна, строка «Проверяю…», затем
   результат на месте. Ушёл владелец с «Настроек», пока шла проверка, —
   результат не сохраняется: говорить его некому, а увиденный позже, он
   был бы устаревшим. Найденное предложение при этом остаётся — оно
   живёт в #update-note, на любом экране. */
function manualUpdateCheck() {
  if (!swRegReady || ui.updateCheck === 'busy') return Promise.resolve();
  ui.updateCheck = 'busy';
  updateCheckLine();
  return runUpdateCheck().then(res => res, () => 'failed').then(res => {
    const here = ui.tab === 'settings' && !ui.reviewOpen && !ui.trainOpen;
    ui.updateCheck = here ? res : null;
    updateCheckLine();
  });
}

/* «Обновить»: взвод, затем skipWaiting ожидающему. Перезагрузит страницу
   controllerchange, когда новый воркер активируется и заберёт её (claim).
   Воркер уже контроллер — его активировала другая вкладка, и
   controllerchange прошёл без взвода: перезагрузка — по этому же тапу. */
function applyUpdate() {
  const w = updateWaiting;
  if (!w) { updateNote(); return; }
  ui.updateArmed = true;
  updateNote(); // «Обновляю…»
  const sw = swApi();
  if (sw && sw.controller === w) {
    if (!reloading) { reloading = true; pageReload(); }
    return;
  }
  try { w.postMessage({ type: 'skipWaiting' }); }
  catch { dropOffer(w); }
}

/* Текст и кнопка предложения — одна функция на #update-note и на строку
   результата в «Системе»: «Обновить» там и там — одна операция. После
   тапа кнопки нет: второе нажатие ничего бы не добавило. */
function offerHtml(text) {
  return ui.updateArmed
    ? `<span>Обновляю…</span>`
    : `<span>${esc(text)}</span><button class="btn primary" data-act="update-apply">Обновить</button>`;
}

/* Постоянный узел вне экранов, полоса над таб-баром, — точечно, по образцу
   storageNote().
   Домен без DOM выходит сразу. Строку «Системы» ведёт тот же вызов:
   предложение, найденное автопроверкой, меняет и её результат.

   С задачи Р4 (п. 3) узел — фиксированная полоса над таб-баром, вне потока
   документа: в потоке он вставал над экраном и сдвигал его вниз посреди
   чтения. Пока полоса показана, body несёт .has-update — нижний отступ
   прокрутки на высоту полосы, чтобы последняя строка экрана (недельный
   счётчик «Сегодня») не пряталась под ней. Класс ставит и снимает ТОЛЬКО
   эта функция, тем же вызовом, что показывает и прячет узел: отступ и
   полоса разойтись не могут, а рендер экранов о полосе не знает.
   Вытесненное более новым предложение полосу не снимает, а удерживает до
   предложения преемника (dropOffer, Р4/рецензия). */
function updateNote() {
  if (typeof document === 'undefined') return;
  const p = el('update-note');
  if (p) {
    const offer = offerOnScreen();
    const on = !!offer;
    const html = on ? offerHtml('Доступна ' + (offer.version ? 'версия ' + offer.version : 'новая версия')) : '';
    // та же разметка не переписывается: удержанная полоса остаётся теми же
    // узлами — скринридер не слышит её заново, фокус на «Обновить» не теряется
    if (p.innerHTML !== html) p.innerHTML = html;
    p.hidden = !on;
    if (document.body) document.body.classList.toggle('has-update', on);
  }
  updateCheckLine();
}

function updateCheckHtml() {
  switch (ui.updateCheck) {
    case 'busy': return '<span>Проверяю…</span>';
    case 'failed': return '<span>Не удалось проверить — нет сети?</span>';
    // «не загрузилась» (задача Р4, п. 4) уступает предложению так же, как
    // «последняя версия»: ожидающий есть — обновиться владельцу есть чем,
    // и строка говорит об этом, а не о сорвавшейся следующей установке
    case 'done':
    case 'lost': {
      const offer = offerOnScreen();
      if (offer) return offerHtml('Доступна ' + (offer.version || 'новая версия'));
      return ui.updateCheck === 'lost'
        ? '<span>Новая версия найдена, но не загрузилась — попробуйте позже</span>'
        : '<span>Это последняя версия</span>';
    }
    default: return '';
  }
}

/* Результат проверки и доступность кнопки — точечно: рендер печатает
   ту же разметку той же функцией (versionBlock). Узлов нет (экран не
   «Настройки» ещё не рисовался) — выходит молча. */
function updateCheckLine() {
  if (typeof document === 'undefined') return;
  const p = el('update-check');
  if (p) {
    const html = updateCheckHtml();
    if (p.innerHTML !== html) p.innerHTML = html; // та же разметка — те же узлы, как у полосы
    p.hidden = !ui.updateCheck;
  }
  const b = document.querySelector('#scr-settings [data-act="update-check"]');
  if (b) b.disabled = ui.updateCheck === 'busy';
}

function versionLineText() {
  const sw = swApi();
  if (!sw || !sw.controller || activeVersion === null) return 'Минимум · версия не определена';
  return activeVersion ? 'Минимум · ' + activeVersion : 'Минимум';
}

function updateVersionLine() {
  if (typeof document === 'undefined') return;
  const p = el('version-line');
  if (p) p.textContent = versionLineText();
}

/* Запуск: слушатель controllerchange, номер активного воркера,
   регистрация с updateViaCache: 'none' (скрипт воркера мимо HTTP-кэша —
   иначе GitHub Pages с max-age отдавал бы прежний sw.js, и новая версия
   не находилась бы до истечения кэша), ожидающий воркер и начатая
   установка, первая автопроверка. */
function startUpdates() {
  const sw = swApi();
  if (!sw) return;
  sw.addEventListener('controllerchange', () => {
    if (!ui.updateArmed || reloading) return; // без взвода — ничего
    reloading = true;
    pageReload();
  });
  if (sw.controller) {
    askVersion(sw.controller).then(v => { activeVersion = v; updateVersionLine(); });
  } else {
    activeVersion = null;
  }
  swRegReady = Promise.resolve()
    .then(() => sw.register('./sw.js', { updateViaCache: 'none' }))
    .then(reg => {
      updateReg = reg || null;
      if (reg && typeof reg.addEventListener === 'function') {
        reg.addEventListener('updatefound', () => watchInstalling(reg.installing));
      }
      if (reg && reg.installing) watchInstalling(reg.installing);
      if (reg && reg.waiting) offerUpdate(reg.waiting);
      return reg || null;
    }, () => null);
  checkForUpdate(false);
}

/* ── Запуск ────────────────────────────────────────────────── */

async function init() {
  // Стартовая проверка (инвариант 9): зеркало не пишется, пока она не завершена
  store = load();
  const fromLocal = !!store;
  if (fromLocal) {
    // localStorage валиден, но безоговорочным источником истины он больше
    // не признаётся (задача 28.A, п. 2.1): зеркало сверяется тоже — после
    // первого рендера, чтобы старт не удлинялся. До конца сверки
    // mirrorReady остаётся false, и save() зеркала не трогает.
    save(); // результат migrate на диск; в зеркало — после verifyMirror()
  } else {
    // localStorage пуст или бит (corrupt-ключ уже записан) — пробуем зеркало;
    // зависший IndexedDB (WebKit) не должен блокировать первый рендер
    const probe = await mirrorProbe(MIRROR_PROBE_MS);
    // непарсящийся снапшот — такой же отказ, как недочитанный (п. 1.1):
    // сырая строка откладывается на виду, а сам снапшот не трогается
    let readable = probe.status !== 'failed';
    if (probe.status === 'read') {
      store = mirrorParse(probe.snap);
      if (!store) { keepMirrorCorrupt(probe.snap); readable = false; }
    }
    // 'failed' — под недочитанным зеркалом может лежать живой снапшот.
    // Писать туда в этой сессии нельзя вовсе: дефолтный store затёр бы
    // его безвозвратно (A.1.2). Приложение при этом работает как обычно.
    mirrorReady = readable;
    mirrorUnverified = !readable;
    if (!store) store = defaultStore();
    // и в localStorage дефолт тоже не пишется: непустой localStorage на
    // следующем старте стал бы источником истины, зеркало перестали бы
    // читать — и всё равно затёрли бы. Пустой localStorage оставляет
    // перезапуску шанс дочитать зеркало и восстановить данные (A.1.5).
    // Первое же действие владельца сохранится обычным save().
    if (readable) save();
  }
  navigator.storage?.persist?.()?.catch?.(() => {}); // fire-and-forget: просим не вычищать localStorage
  document.addEventListener('click', onClick);
  document.addEventListener('change', onChange);
  document.addEventListener('input', onInput);
  document.addEventListener('pointerdown', dragDown); // перетаскивание (задача 16F)
  document.addEventListener('pointerdown', swipeDown); // свайп «Не сегодня» (задача Р2)
  document.querySelectorAll('#tabs button').forEach(b =>
    b.addEventListener('click', () => {
      ui.importNote = null;
      ui.goneNote = null; // короткий путь назад не переживает уход с экрана
      ui.goneGroup = null; // ни у пункта, ни у блока (п. 4.4)
      // лист закрывается и таб-баром: если владелец вернулся на ту же
      // вкладку, с которой лист открыт, вернуть её скролл и фокус — как
      // это делает «Готово» (задача 26, п. 4.1). Замер снимается ДО close*.
      const back = sheetReturn();
      if (b.dataset.tab !== ui.tab) {
        ui.missOpen = {};
        ui.raiseEdit = {};
        // результат «Проверить обновления» — строка на месте, а не справка:
        // «Это последняя версия», прочитанная через день, была бы неправдой
        if (ui.updateCheck !== 'busy') ui.updateCheck = null;
      }
      resetConfirms(); // взведённое подтверждение не переживает уход с экрана
      closeReview();
      closeTrain();
      ui.tab = b.dataset.tab;
      if (!syncDay()) renderAll(); // при смене дня syncDay уже перерисовал новую вкладку
      if (back && back.tab === ui.tab) { window.scrollTo(0, back.y); focusSrc(back.src); }
    }));
  document.addEventListener('visibilitychange', () => {
    // возвращение из фона — и автопроверка обновления (не чаще UPDATE_CHECK_MS)
    if (document.visibilityState === 'visible') { syncDay(); armDayTimer(); checkForUpdate(false); }
    else flushMirror(); // уход в фон — немедленный сброс незаписанного зеркала
  });
  window.addEventListener('focus', syncDay);
  window.addEventListener('pagehide', flushMirror);
  renderAll();
  armDayTimer();
  startUpdates(); // регистрация воркера, предложение обновления, номер версии (задача Р3)
  // Сверка с зеркалом — последней строкой и только при валидном localStorage
  // (в пустой ветке зеркало уже прочитано выше). Await стоит ПОСЛЕ рендера:
  // кадр отдан браузеру, старт не удлиняется (п. 2.4), а тесты получают
  // детерминированный момент, когда сверка завершена.
  if (fromLocal) await verifyMirror();
}

/* Тестовый хук для Node; в браузере — обычный запуск. */
if (typeof module !== 'undefined' && module.exports) {
  module.exports = {
    get store() { return store; }, set store(v) { store = v; },
    SCHEMA_VERSION,
    defaultStore, migrate, dateKeyShift, dateKeyFromDate, addDays, diffDays,
    todayKey, msToNextBoundary, weekStartOf, currentWeekStart, previousWeekStart,
    toggleMark, isMarked, incTrain, undoTrain, trainCount,
    reviewDue, windowKeys, currentOneChange, raiseEligible, raiseSuggest, resetRaiseCount,
    acceptRaise, closeWeek, missedYesterday, markYesterday, plural, parseNum,
    // разбор: одно предложение повышения, действенность решений (задача 24)
    raiseReady, raiseOffer, pendingParams, reviewActionable,
    // планка вниз (задача 16C)
    lowerEligible, lowerSuggest, acceptLower, keepBar,
    // упражнения и тренировки (задача 16D)
    liveExercises, findExercise, addExercise, updateExercise, moveExercise, recordSession,
    // чистка и её отмена (задача 16.1)
    WIPE_KEY, emptyStore, wipeStats, wipedCopy, wipeAll, restoreWiped, dropWiped,
    // копия перед замещением, нечитаемые данные и счёт потерь (задача 25)
    CORRUPT_KEY, hasData, keepPrev, corruptCopy, dropCorrupt, dataCounts, droppedLine,
    // страховка зеркала (задача 28.A). keepMirrorCorrupt и verifyMirror
    // в хук не идут: localStorage и IndexedDB в Node нет, доменного теста
    // им не написать, а непокрытых имён в хуке и без того долг (28, п. 8Г).
    // Оба закреплены интерфейсным уровнем через init()
    MIRROR_CORRUPT_KEY, mirrorParse, mirrorHasMore,
    fmtParam, paramDecision, applyParamStep, keepParam, habitsSteady,
    habitWeekCount, habitStreakFrom, habitStreak,
    moveItem, canMoveItem, reorderItem, reorderGroup, reorderExercise,
    // уход и возврат (инвариант 12, задача 28.E/A)
    live, livedOn, removeItem, restoreItem, removeExercise, restoreExercise, laterExerciseOf, successorAmong,
    // расписание пункта (задача 29/B, инвариант 12): отрезки, действовавшая
    // маска, применимость дня, пороги планки как доли от дней маски
    weekdayOf, WEEK_ALL, isMask, maskDays, normSchedule, normTime,
    scheduleOn, scheduleNow, dueOn, dueNow, weekMaskDays, setSchedule, clampNorm,
    raiseNeed, lowerNeed,
    recordBar, parsePositive, isDayKey, load,
    mirrorRead, mirrorWrite, flushMirror,
    closedWeeks, itemWeekCount,
    // Механики формулы и лестницы сняты задачей 28.D; их доменные функции
    // ушли из файла целиком. Остались нормализаторы: поля formula, ladder и
    // ladderLog живут в store владельца, и migrate обязан приводить их к
    // канонической форме, как прежде. Экспортируются они здесь, чтобы
    // сохранность данных проверялась прямо, а не через один лишь migrate.
    normFormula, normLadder, normLadderLog,
    // блоки (инвариант 13)
    // deleteGroup снят вместе с «Удалить блок» («Расписание 1/3», п. 1.4)
    findGroup, addGroup, moveGroup, renameGroup,
    groupedItems, groupList,
    // сводка дней словами — чистая функция интерфейса «Расписания»
    daysSummary,
    // «Расписание 1/3»: дни блока, журнал принадлежности, эффективные дни,
    // уход/возврат и копия блока, быстрое добавление действий
    normBlockDays, blockMaskOn, blockMaskNow, setBlockDays, liveGroups, nameTaken,
    groupOn, setItemGroup, normGroupLog, andMask, effectiveMaskOn,
    updateGroup, removeGroup, restoreGroup, duplicateGroup, groupJoinRefusal, zeroDaysIn,
    parseQuickLines, addActions, weekPlan, restoreItemCore, inEffectiveDays, liveGroupIndexes, renameGroupCore,
    scheduleCore, returnsWithoutDays, planWeekCount, laterSegmentOf,
    // форма правки действия: своя маска внутри дней блока, тип в день заведения
    mergeOwnMask, canChangeType, typeChangeRefusal, setItemType,
    // режимы (задача Р2): журнал активного режима отрезками, режим записи,
    // операции над режимами одной записью, копия блока в другой режим
    MAIN_MODE, MAIN_MODE_NAME, WEEK_NONE, normModes, normModeLog, modeOn, activeMode,
    isAction, itemMode, blockMode, findMode, liveModes, belongsToMode, modeNameTaken,
    addMode, renameMode, removeMode, restoreMode, setActiveMode, duplicateGroupTo,
    restoreSetOf, actionCopy, programModes,
    // «Убранные» по блоку: действие — блоком своего режима, глобальный пункт — всех (Р2, этап 3)
    goneBesideBlock,
    // живой одноимённый блок другого режима — уход блока и слова его последствия (Р2/рецензия)
    hasLiveNamesake,
    // держит имя только живой блок живого режима; раскладка привычек — одна на
    // дневной экран и секцию «Настроек» (задача Р3, п. 0.2–0.3)
    holdsName, habitSections, habitSectionOf, habitSectionName,
    // соседи строк «Расписания» — строки их карточки; в «Без блока» вместе с
    // недельным счётчиком (задача Р4, п. 1)
    looseRows, scheduleSectionOf, scheduleSectionName,
    // «Не сегодня» (задача Р2, этап 4): пропуск — false в days{}, только
    // сегодня, только у действия минимума; число пропусков сетки разбора
    isSkipped, skipToday, unskipToday, weekSkips,
    // свёртка выполненного блока «Сегодня» (задача Р2, этап 5): счёт блока
    blockTally,
    // прогресс (инвариант 14)
    minDayItems, minDayMarks, minDayClosed, daysInSystem, dayStreak,
    chainWeeks, marksInSystem, riseSeries, risePath,
    // доля дня, порог и рекорд (задача 17)
    dayScore, dayNeed, dayThreshold, clampThreshold, bestStreak,
    marksWindow, seedProgram,
    // задача 22
    everMarked, thresholdNote, seedDayKey, ownerNewestItem,
    // строка дня (задача 28.E/B): набор и выбор
    DAY_LINES, DAY_LINE_EPOCH, dayLine,
    // задача 27.1: ремонт по приёмке
    lastSaveOk, wipedRaw, setWipedRaw, pendingThisWeek,
    holdScrollTarget, unwrapDayMinutes, domFormKey, currentFormKey,
    // обновление приложения (задача Р3): номер из ответа воркера и троттлинг
    // автопроверки — чистые функции; остальное живёт на service worker API
    // и закреплено интерфейсным уровнем с подменой navigator.serviceWorker
    versionLabel, updateCheckDue,
    // преемник вытесненного ожидающего — удержание полосы (Р4/рецензия)
    offerSuccessor,
    // константы времени (задача 23): TIMING — значения этой загрузки,
    // TIMING_DEFAULTS — рантайм приложения, подмене не подверженный
    TIMING, TIMING_DEFAULTS
  };
} else if (typeof document !== 'undefined') {
  document.addEventListener('DOMContentLoaded', init);
}
