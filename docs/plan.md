# Минимум — план

Источник задач — этот файл; память чатов не используется.

## Выполнено

- Задача 12 «Движение» — тихие функциональные анимации на CSS: заполнение круга и сегодняшней ячейки полосы при тапе, opacity-fade содержимого при смене вкладок, сворачивание карточки разбора после решения (`motionLeave`), тихое «Сохранено» при сохранении формы. Глобальный `@media (prefers-reduced-motion: reduce)` отключает всё; конечные состояния достижимы мгновенно.

## План

Задача 13 «Отделка» — визуальный слой (типографика, семантические акценты, цвета статусов); список ведёт архитектор.

## Бэклог

Рекордная серия привычки (лучший стрик). FakeDate для jsdom в DOM-тестах.
