# Минимум — план (исторический документ)

**Этот файл больше не источник задач.** Он остановился на задаче 13 и к
задаче 26 отстал на тринадцать задач: перечисленное ниже верно на момент
задачи 13 и с тех пор не поддерживалось. Помечен историческим, а не
дописан задним числом — восстанавливать по памяти нечего, а живая копия
плана есть в двух местах, которые обновляются по-настоящему:

- **CLAUDE.md** — конституция: принципы, инварианты, правила изменений.
  Единственный документ, которому обязан следовать код.
- **Отчёты по задачам** — что сделано, что решено и почему. Задачи
  приходят промптом от архитектора, не из этого файла.

Бэклог ниже тоже исторический: часть его сделана (перетаскивание, серия
привычки, рекорд серии), часть отменена. Новые идеи попадают в раздел
«Риски и вопросы архитектору» отчёта, а не сюда.

## Что было в плане на задачу 13

- Задача 12 «Движение» — тихие функциональные анимации на CSS: заполнение круга и сегодняшней ячейки полосы при тапе, opacity-fade при смене вкладок, сворачивание карточки разбора после решения (`motionLeave`), тихое «Сохранено» при сохранении формы. Глобальный `@media (prefers-reduced-motion: reduce)` отключает всё; конечные состояния достижимы мгновенно.
- Задача 12.1 «Движение: усиление» — амплитуда движения поднята до отчётливо заметной (диапазон 180–260 мс): scale-«просадка» круга и ячейки на тап, fade экрана с вертикальным подъёмом, отчётливое схлопывание карточки разбора по высоте.
- Задача 13 «Отделка» — визуальный слой: полная дата в тексте ожидания разбора (без «сент..»), единый вес стрелок ↑↓ и тумблеров с заметным гашением граничных, гармонизация типографики через токены (типо-шкала, радиусы, ритм секций), семантический тон `--warn` для «серия прервана» (приглушённый тёплый, не алармовый).

Исходный план проекта был исчерпан на задаче 13.

## Исторический бэклог задачи 13

Рекордная серия привычки (лучший стрик). FakeDate для jsdom в DOM-тестах.
Положительный семантический акцент на выполненной норме недели.

Из этого списка к задаче 26 сделан рекорд серии дней (`bestStreak`,
задача 17), тон `--warn` упразднён решением архитектора (задача 15),
остальное не начиналось.
